# 採購 / 會計模組 開發規格建議（交付執行 AI 用）

> 撰寫：2026-07-29。用途：EGsystem 網頁 ERP 要完全取代電腦 ERP，本文件是「採購」與「會計（應收/應付）」兩個新模組的設計約束與接點清單。
> **執行前必讀**：`CLAUDE.md`（鐵律全部）、`ai-rules/07-附件路徑儲存規範.md`、`ai-rules/08-UI互動規範.md`、`ai-rules/10-配色與文字可讀性.md`。本文件只補「這兩個模組特有」的部分，不重複上述內容。

---

## 零、先決定的三件事（沒定案不要動工）

1. **會計要做到哪一層？** 建議**只做到應收/應付＋對帳單＋收付款沖帳**，不做總帳傳票與財務報表——傳票留給會計軟體，網頁提供「銷項/進項明細匯出」即可。理由：總帳一旦自製，就要負責科目、期末結轉、財報格式與稅務申報，工作量是應收應付的 3 倍以上，而且做錯的代價是報稅出錯。
2. **委外加工單價的權威來源要換手。** 現在單價來自 ERP 移轉紀錄匯入（`bom_ing_transfer_log.price`）。採購模組上線後同一個欄位會有兩個來源，**必須先定案**：建議改成「委外加工單（PO）的單價為準，移轉回廠時自動帶入」，ERP 匯入降級為每日差異比對、不再寫入。沒定案就開發＝保證做出兩套帳。
3. **電子發票不要自己串。** 台灣 B2B 電子發票需透過加值中心（turnkey）。建議第一期只做「發票號碼登錄 + 銷項/進項媒體申報檔匯出」，不直接對接加值中心 API。

---

## 一、專案特有地雷（違反就是重工）

| 地雷 | 規則 |
|---|---|
| 巨檔 | `master_data_management.php` 1.5MB、`stock.php` 721KB。**新功能一律開獨立 view + 獨立 API 檔**，禁止往巨檔裡加。 |
| 新頁面 JS | 頁面底部必須依序載入 `jquery.min.js`→`bootstrap.min.js`→`fastclick.js`→`nprogress.js`→`custom.min.js`。**缺 `custom.min.js` 左側欄選單會死**（已重複踩坑兩次）。 |
| 選單登記 | 新頁面要 `INSERT` 一列到 `system_module_pages`（`page_url` 格式 `/EGsystem/views/...`、`sort_order=MAX+1`），並綁到 `system_module_groups.group_name='測試功能'`。帶參數才能開的子頁不登記。 |
| 欄位命名 | 本專案慣例是 `Created_By` / `Created_At` / `Modified_By` / `Modified_At`（**不是** `updated_at`）。使用者 id 型別 `char(11)`。 |
| 登入者 id | `$_SESSION['id']`（**不是** `userid`）。 |
| 料號歸戶 | 一律用 `d_setting.d_id`（整數 PK）關聯，**禁止用料號字串 join**——系統有 159 個重複料號字串，字串 join 會灌水。出貨統計已因此踩過坑。 |
| 資產快取 | 獨立 JS/CSS 的 `?v=` 版號要用該檔自身 `filemtime()`，不可共用頁面的 `$_av`。 |
| 資料表路徑 | 任何路徑相關欄位（附件）遵守 ai-rules/07：DB 只存檔名/相對值，完整路徑讀取當下現組。 |
| 附件 | 新增單據當下就要能上傳附件，用 `temp`/`active` 狀態機（見 ai-rules/07，可抄 `quotation_attachments` 前例）。 |
| 收尾 | 每個檔案改完立刻 `php -l` → `git add` → `git commit` → **`git pushall`**（雙 remote）→ 寫 `page_change_log`。 |

---

## 二、可以直接抄的既有機制（不要重造輪子）

| 需求 | 抄哪個既有實作 |
|---|---|
| 單據編號並發配號 | `car_seq`（異常矯正單）與 `stamp_serial` 已解決並發配號問題，直接沿用同一手法 |
| 簽核流程 | 共用表 `approval_record`（欄位 `module` / `entity_id` / `level` / `status` / `approver_id` / `decided_at` / `live_event_id`）。新模組只要帶 `module='purchase_order'` 等值即可，**禁止另建簽核表** |
| 簽核圖章 | `eg_stamp.js`（已圖章共用化，`asset_map` 會自動替換真章） |
| 通知推播 | `live_event` + `eg_push_send_to_users`（共用帳號展開已收斂在此，勿繞過） |
| 代理人（簽核者不在） | 一律走 `delegate_lib`，見 ai-rules/11。**禁止各頁自寫代理 SQL** |
| 附件狀態機 | `quotation_attachments`（temp/active/pending/trash） |
| 變更管控 | `order_change_log` / `quotation_change_log` 的「已生效單據不可直接改，走變更單」模式 |
| 庫存異動 | `stock_transactions`（已有 `txn_type` in/out/adjust、批號、單位換算）。進貨入庫、領料出庫**一律寫這張表**，禁止另建庫存表 |

---

## 三、與既有主檔的接點（欄位已存在，不要新增重複欄位）

### 3.1 廠商 `maker_list`（應付用）
已有付款條件欄位，**直接用，不要再開一組**：
- `maker_id_no`(PK, varchar11)、`maker_id`(簡稱)、`maker_id_all`(全名)、`tax_id`(統編)
- `settlement_mode`、`settlement_day`、`payment_method`、`net_days`、`confirmed_settlement`、`confirmed_payment`
- `invoice_address` / `billing_address` / `factory_address` 三種地址已分開
- `internal`（是否廠內）、`is_qualified`（合格廠商）→ **採購單應擋下 `is_qualified=0` 的廠商**，這是 AS9100 要求

### 3.2 客戶 `customer_list`（應收用）
- `customer_id`(PK, char11)、`customer`(簡稱)、`customer_full`、`tax_id`
- `settlement_mode` enum('FIXED','EOM','VARIABLE')、`settlement_day`、`net_days`、`payment_method`、`payment_term`
- `credit_limit`（信用額度）→ 訂單/出貨時應檢查
- `invoice_email`、`billing_contact`、`billing_note`、`allow_deduct`（可否扣款）
- `bank_name` / `bank_branch` / `bank_account`
- **結帳日的歷史變更**走 `customer_account_terms`（`close_type` DAY/EOM、`close_day`、`effective_from`、`effective_to`）——算對帳期間時必須查這張表取當期有效值，不可直接讀 `customer_list.settlement_day`

### 3.3 出貨 `is_list`（應收/開立發票的來源）
`IS_id`、`Order_date`、`IS_number`(出貨單號)、`Client_id`、`d_setting_id`、`Qty`、`Unit_price`、`Order_id`、`sale_type`、`billing_month_override`、`anomaly_confirmed`
- **`billing_month_override` 已存在**（跨月請款調整），產對帳單時必須優先採用此欄，沒有才用 `Order_date` 推月份
- 歸戶一律用 `d_setting_id`

### 3.4 委外加工 `bom_ing_transfer_log`（應付明細的天然來源）
`transfer_id`、`bom`、`bom_sn`、`maker_from`、`maker_to`、`transfer_qty`、`price`、`modified_unit_price`、**`process_amount`**、**`tax_amount`**、**`paid_qty`**、**`invoice_date`**、**`invoice_ym`**、`order_no`
- 粗體欄位顯示**這張表本來就是為了對帳設計的**。應付明細請以此表為來源，**不要另建一張加工應付明細表**，否則兩邊數字會打架。
- 既有鐵則：`price` 為 0/空/負數的列整筆不寫入（0 價列會把畫面單價蓋成 0）。歷史 5.2 萬筆 0 價列定案保留不動。
- 取單價的既有邏輯是「每 `bom`+`bom_sn` 取 `transfer_id` 最新一筆」，改動前先確認不影響 `OreadyReply` 與 `ERP_Cost_Analysis`。

### 3.5 訂單 `order_list`（多幣別已存在）
`Order_id`、`d_id`、`Qty`、`Open_Qty`、`unit_price`、**`currency`**、**`exchange_rate`**、`Delivery_date`
- 訂單已支援多幣別 → **採購與應付從第一天就要支援多幣別**，不要先做單一幣別再補。

### 3.6 成本口徑
`opa_process_cost_setting` 與 `views/Sales/Order_Profit_Analysis.php` 已定案「實際發包單價回推成本、廠內不計」的口徑。會計端的成本數字必須與此對齊，不一致時以既有口徑為準或明確標示兩者差異，**不可各算各的**。

---

## 四、建議的新資料表（欄位為最低要求，命名沿用專案慣例）

### 4.1 採購
```
purchase_order              po_id(PK), po_no(唯一), po_type(OUTSOURCE委外加工/MATERIAL物料/EXPENSE費用),
                            maker_id_no, po_date, expect_date, currency, exchange_rate,
                            tax_type(TAXABLE/ZERO/FREE), tax_rate, amount_untaxed, tax_amount, amount_total,
                            status(draft/pending/approved/partial/closed/void), req_dept_id, req_user_id,
                            note, Created_By, Created_At, Modified_By, Modified_At
purchase_order_item         poi_id(PK), po_id, seq, d_id(→d_setting.d_id) 或 stock_item_id(→stock_items),
                            bom, bom_sn(委外加工時對應製令製程), qty, unit_price, amount,
                            received_qty, invoiced_qty, expect_date, note
goods_receipt               gr_id(PK), gr_no, po_id, maker_id_no, receipt_date, status, Created_*
goods_receipt_item          gri_id(PK), gr_id, poi_id, qty, qc_batch_id(→qc_incoming_batch), stock_txn_id(→stock_transactions)
```
- 收料要串既有的進料檢驗 `qc_incoming_batch`，並在檢驗合格後才寫 `stock_transactions` 入庫（或先入待檢區庫位，用既有 `stock_locations`）。

### 4.2 應付
```
ap_invoice        ap_id(PK), ap_no, maker_id_no, invoice_no(廠商發票號), invoice_date, invoice_ym,
                  currency, exchange_rate, amount_untaxed, tax_amount, amount_total,
                  due_date, status(draft/confirmed/partial/paid/void), Created_*
ap_invoice_item   源頭二選一：gri_id(進貨) 或 transfer_id(→bom_ing_transfer_log 委外加工)
ap_payment        pay_id(PK), pay_no, maker_id_no, pay_date, pay_method, currency, exchange_rate, amount, Created_*
ap_payment_apply  沖帳明細：pay_id, ap_id, apply_amount（多對多，禁止一對一）
```

### 4.3 應收
```
ar_invoice        ar_id(PK), ar_no, customer_id, invoice_no(統一發票號), invoice_date, billing_ym,
                  currency, exchange_rate, amount_untaxed, tax_amount, amount_total, due_date, status, Created_*
ar_invoice_item   is_id(→is_list.IS_id), qty, unit_price, amount
ar_receipt        rcv_id(PK), rcv_no, customer_id, rcv_date, rcv_method, amount, Created_*
ar_receipt_apply  沖帳明細：rcv_id, ar_id, apply_amount
ar_statement      對帳單：customer_id, period_from, period_to, 產出快照（金額凍結，不隨後續異動變動）
```

### 4.4 共通
```
doc_serial        單據編號配號（比照 car_seq 的並發安全作法）
acc_period_lock   結帳鎖月：period_ym, module(AP/AR/ALL), locked_by, locked_at
ap_ar_opening     期初開帳：未結應收/應付餘額帶入（切換上線必備，最常被漏掉）
```

---

## 五、金額與沖帳的鐵則

1. **禁止 FLOAT**。單價 `DECIMAL(15,4)`、金額 `DECIMAL(15,2)`、數量 `DECIMAL(15,4)`、匯率 `DECIMAL(12,6)`。
2. **稅額計算集中在一支共用函式**（新增 `src/common/tax_lib.php`），含四捨五入規則（單身逐項算 vs 單頭合計後算，要選一種寫死）。**禁止各頁自算**——這是最常見的「差 1 元」來源。
3. **匯率是交易當下的快照**，寫進單據欄位，報表不可即時重算。
4. **沖帳一律「明細對明細」多對多**（`*_apply` 表）。一張付款可沖多張發票、一張發票可分次收付。做成一對一三個月後就要重做。
5. **已確認/已沖帳的單據禁止直接修改**，要改一律開沖銷單（紅字單）或走變更單流程（可抄 `order_change_log`）。
6. **結帳鎖月**：`acc_period_lock` 鎖住的月份，任何寫入（含後台匯入）都要 fail-closed 擋下。沒有這個機制，上線半年後所有月報表都會對不起來。
7. **所有單據寫入用 transaction**（鐵律 4），且採購→進貨→應付、出貨→應收→收款這種跨表連動要全有全無。
8. **總計/彙總一律後端對全部符合條件的資料算**，不可用前端已載入那一頁計算（CLAUDE.md UI 規則，會計頁尤其嚴重）。

---

## 六、權限設計

- 角色權限一律走既有 RBAC，`Roles_API` **必帶 `module`**（各頁角色分開設定）；「管理者」固定全權。
- 建議獨立功能碼（各自可指派不同角色）：
  - `po_view` / `po_create` / `po_approve`（採購單開立與簽核分離）
  - `gr_create`（收料）
  - `ap_view` / `ap_confirm` / `ap_pay`
  - `ar_view` / `ar_invoice` / `ar_receipt`
  - `acc_period_lock`（結帳鎖月，僅財務主管）
  - `cost_view`（**金額可見性**：生管/現場看得到採購單但看不到單價，必須是獨立控制項，不是靠隱藏 DOM）
- 金額遮蔽要在**後端就不回傳**，不可只在前端 `display:none`。

---

## 七、UI 要求（沿用全站規範，勿另創）

- 輸入欄位：雙擊清空、聚焦全選、Enter 跳下一欄、最後一欄 Enter 送出；多列表格 ↑↓ 切換上下列同欄、末列 ↓ 自動加列。
- 數字欄：無上下增減鈕、小數尾 0 省略。
- 日期欄一律可點月曆。
- 列表 >10 筆分頁、分頁鈕在右上、每頁 5/10/20/50、必備 PDF+CSV 匯出。
- **配色一律暖色系**（橘/琥珀/砂/赭/暖棕/珊瑚紅），禁冷暖混雜、禁隨機上色；顏色不可是唯一資訊，需搭配文字標籤。詳見 ai-rules/10。
- 列印（對帳單、採購單）：**禁止 JS 量高度自算分頁**，一律單一表格交給瀏覽器原生分頁（報價單已為此踩過三次）。

---

## 八、上線切換策略

1. **平行期**：採購/會計上線後，ERP 匯入保留但改成「只比對、不寫入」，每日產差異報表。差異連續一週為零才關掉匯入。**差異比對報表本身就是要開發的功能，不是額外的。**
2. **期初開帳**：切換日的未結應收、未結應付、未交採購單餘額必須有匯入工具（`ap_ar_opening`）。沒有這個就無法真正脫離 ERP。
3. **上線順序建議**：採購單（含委外加工）→ 收料/進貨 → 應付 → 應收/發票 → 對帳單/沖帳 → 結帳鎖月。應付先於應收，因為委外加工的資料 (`bom_ing_transfer_log`) 已經最完整、驗證最快。

---

## 九、最常被做錯的五件事（請執行 AI 自我檢查）

1. 只做「單據」不做「沖帳」→ 三個月後資料無法使用。
2. 沒有結帳鎖月 → 舊月份被改，報表永遠對不起來。
3. 稅額分散在各頁計算 → 到處差 1 元。
4. 一對一沖帳 → 遇到分次付款就要重做資料表。
5. 用料號字串而非 `d_id` 關聯 → 159 個重複料號造成金額灌水。
