# 推播附件功能擴充規格（給 AI 實作用）

## 背景

現有系統：內網 PHP/MySQL ERP（MAMP），已完成 Telegram Bot 推播（私訊逐人發送、主動轉播回覆、多步驟簽核流程），並規劃 4G Web Push + 隔離回覆頁面（Cloudflare Tunnel + Token 驗證）。本文件描述要新增的「附件標籤、浮水印、格式轉換」功能。

## ⚠️ 實作前必守規則

1. **不要猜測任何現有資料表、欄位、檔案名稱**。Telegram 推播部分已由使用者修改並上線，與原始規格文件可能不同。實作前請先要求使用者提供：現有相關資料表結構（SHOW CREATE TABLE）、推播發送函式的檔案內容、附件目前的儲存方式與路徑欄位
2. 需要 join 現有表時，先列出需要哪些欄位，等使用者確認後再寫
3. 不修改現有頁面 layout，只在指定位置加功能
4. 所有新增檔案完整輸出，不省略
5. 錯誤處理用 error_log，不 die() 或 echo 錯誤到畫面

## 一、附件上傳限制與格式轉換

### 允許格式

只允許：圖片（jpg / jpeg / png）、PDF、Excel（xlsx / xls）、Word（docx / doc）。驗證 MIME type 與副檔名雙重檢查，不只看副檔名。

### Excel / Word 自動轉 PDF

- 上傳 Excel 或 Word 後，系統自動轉成 PDF 才算完成上傳
- **完整流程**：
  1. 按上傳 → 原始檔存入後端暫存目錄
  2. Excel 多工作表時：讀取工作表清單（PhpSpreadsheet），使用者勾選要轉換的工作表，或選「全部轉換」；Word 直接整份轉換
  3. 伺服器端執行轉換產生 PDF
  4. 顯示 PDF 預覽給上傳者確認，不滿意可取消重傳
  5. 確認後：PDF 移入正式附件儲存路徑，資料庫寫入附件紀錄並綁定至該公告或異常單
  6. **刪除暫存目錄中的原始檔與所有中間檔**——系統只保存 PDF，不保留原始 Excel / Word（使用者已確認此決定）
- 轉換引擎兩條路，擇一實作：
  - **主要方案**：LibreOffice headless CLI（`soffice --headless --convert-to pdf`），保真度最高。實作前先確認使用者主機環境（macOS MAMP 或 Windows）是否已安裝，未安裝提供安裝指引
  - **備援方案**（環境無法安裝 LibreOffice 時）：PhpSpreadsheet 轉 HTML → headless Chrome / wkhtmltopdf 列印成 PDF。已知限制：圖表會遺失、複雜合併儲存格還原度較差
- **不論使用哪個引擎，轉換完成後必須先顯示 PDF 預覽給上傳者確認**，確認可讀後才完成上傳；不滿意可取消重傳
- 對外與對內流通的一律是轉換後的 PDF 版本
- 轉換失敗時：記錄 error_log，介面提示使用者轉換失敗、該附件視同未完成上傳

## 二、附件說明與標籤系統

### 附件說明

- 公告附件與異常單附件都有「附件說明」欄位，純文字，**非必填**

### 標籤設計

- 標籤分兩組 scope，互不混用：`announcement`（公告用）、`abnormal`（異常單用）
- 每個標籤帶兩個開關：`allow_webpush`（可傳送 4G）、`allow_telegram`（可傳送 Telegram）
- **只有主管角色**可以：新增/修改/停用標籤、調整標籤的兩個開關
- 每組 scope 中，主管需指定一個「**預設標籤**」：上傳者未選標籤時，附件自動套用此預設標籤。預設標籤的兩個開關建議都關（不可外送），確保安全預設

### 指派流程

- 上傳附件的人在上傳時從該 scope 的標籤清單中選擇一個標籤（單選）
- 未選擇 → 自動套用主管設定的預設標籤
- 附件是否會被發送到 4G / Telegram，完全由其標籤的開關決定
- 標籤不可外送的附件：推播通知照發，但附件欄顯示「請至內網查看」
- 事後修改附件標籤：只影響未來的發送；Telegram 已發出的檔案無法收回（此為已知且接受的限制，介面上向主管提示即可）

### 異動紀錄

- 標籤開關變更、附件標籤指派/變更，都要寫 log：誰、何時、改了什麼（舊值→新值）

## 三、輸出加註（浮水印＋角落資訊）

輸出加註分兩層，同一條處理管線（PDF 用 FPDI + TCPDF，圖片用 Imagick / GD），皆在「送出的那一刻」動態產生：

### 3-1. 溯源浮水印（受 require_watermark 控制）

- **產生時機**：檔案送出的那一刻，不是上傳時
  - 4G：download.php 輸出前即時蓋印
  - Telegram：發送前為每位收件人各自產生一份（私訊逐人發送，每人拿到印著自己代碼的版本）
- **內容**：員工代碼 + 開啟/發送時間 + 單號（異常單號或公告編號）
- **樣式**：半透明、斜向、平鋪整頁（不是只印角落），截圖外流也能溯源
- 標籤的 `require_watermark` 關閉時跳過此層

### 3-2. 角落資訊標註（一律顯示，不受浮水印開關影響）

- **標籤名稱**：固定顯示於每頁左上角，小型標貼樣式（例如淡色底、深色字），方便收件者辨識附件類別
- **附件備注**（若有填寫）：顯示備注內容 + 填寫人員名稱 + 填寫時間，固定放置於頁面角落空白處（建議右上角或頁面下緣），字體小、不遮蓋主要內容
- 位置固定即可，不需中央放大；目的為辨識而非溯源
- 圖片與 PDF 皆適用；備注過長時截斷並加「…」（建議上限 100 字）
- **檢視用快取版**：附件上傳確認完成時，預先產生一份「僅含角落標註」的檢視版（無溯源浮水印），供檢視畫面直接顯示使用；標籤或備注變更時重新產生。對外發送仍走即時加註流程

### 3-3. 共通規則

- 因 Excel/Word 已在上傳階段轉成 PDF，輸出檔案只會有 PDF 與圖片兩種
- 加註後的暫存檔用完即刪（發送/下載完成後 unlink），未加註的儲存檔永不外送（免浮水印標籤除外，該情況輸出檔僅含角落標註層）

## 四、4G 附件下載（配合既有隔離回覆頁）

- 回覆頁面列出該單附件（僅限標籤允許 4G 的）
- 每個附件產生短效簽名連結：綁定員工 ID + 附件 ID，有效 10 分鐘，**一次性**（下載後失效，頁面可重新產生新連結)
- download.php:驗證簽名 → 從資料庫查實際路徑（白名單，不接受路徑參數）→ 即時蓋浮水印 → 輸出
- 下載紀錄表：員工、時間、附件、來源 IP
- 檔案大小上限建議 20MB（浮水印處理 CPU 成本考量）；Telegram 另有 Bot 50MB 硬限制

## 四之二、Telegram 附件發送方式（兩段式，含開啟紀錄）

Telegram 不提供「對方已下載/已開啟檔案」的回報事件，因此**不直接在推播訊息中夾帶檔案**，改用兩段式：

1. 推播訊息中放一顆 Inline Keyboard 按鈕「📎 取得附件：檔名」（callback button，非 URL，維持 Bot 不放連結的既有規則）。一則通知有多個附件時，每個附件各一顆按鈕
2. 員工按下按鈕 → Bot 收到 callback → 此時寫入 `attachment_download_logs`（channel='telegram'，記錄員工、附件、時間、chat_id）
3. 寫入紀錄後，當場產生該員工的浮水印版本並 `sendDocument` 發送，發送完刪除暫存檔
4. 浮水印上的時間即為按鈕按下（實際開啟）的時間
5. callback 處理需防重複：同一員工重複按同一附件按鈕，仍可重新發送檔案，但每次都寫一筆紀錄
6. 已知限制：檔案進入對方聊天室後，後續重複開啟該檔案無法追蹤，只能記錄每次「向 Bot 索取」的行為

## 四之三、回覆時上傳附件（4G 與 Telegram）

電腦網頁版回覆已支援上傳附件（**現況請向使用者確認：現有回覆附件的資料表與儲存方式**），4G 與 Telegram 管道也要支援，規則與主流程附件一致（備注非必填、可選標籤、未選套預設標籤、輸出時同樣走加註管線）。

### 4G 回覆頁面

- 回覆表單加入：檔案上傳欄位、附件備注輸入框（非必填）、標籤下拉選單（該 scope 的有效標籤）
- **格式限制：只接受圖片與 PDF**（與 Telegram 回覆統一）。Excel/Word 請至電腦網頁上傳，介面上顯示此提示
- 上傳附件綁定至該筆回覆紀錄

### Telegram 回覆附件

- 員工直接在聊天中傳送圖片或 PDF 給 Bot（可加 caption），輪詢腳本處理：
  1. 透過 getFile 下載檔案，綁定至該員工當前處理中的異常單回覆（**如何判定「當前異常單」請依現有回覆對應邏輯，向使用者確認**）
  2. caption 文字自動存為附件備注（無 caption 則備注空白）
  3. Bot 回覆一組 Inline Keyboard 標籤選項供員工點選；員工未點選則套用預設標籤
- **格式限制：Telegram 回覆只接受圖片與 PDF**。收到 Excel/Word 時 Bot 回覆「Excel/Word 請至網頁上傳」並不儲存（因轉檔的工作表勾選與預覽確認流程不適合在聊天視窗進行）
- 檔案大小超過上限時同樣回覆提示並不儲存

## 四之四、檢視畫面（公告與異常單）

### 公告檢視功能（新增）

- 公告通知頁面目前**沒有檢視功能**，需新增：點擊公告後開啟檢視畫面，顯示公告完整內容與附件（**現有公告頁面結構請向使用者索取後再動工**）

### 直接顯示附件的勾選選項

- 新增/修改公告、新增/修改異常單的表單中，加入勾選選項：「附件 2 件（含）以下時，於檢視畫面直接顯示附件」
- **此選項僅作用於電腦版檢視畫面**（4G 與 Telegram 一律不自動展開附件）
- 勾選且附件數量符合 → 電腦版檢視畫面自動展開附件，不需另外點擊
- 未勾選或附件超過 2 件 → 附件以清單（檔名 + 標籤 + 備注摘要）呈現，點擊才開啟

### 電腦版檢視版面

- 左右分欄：**左側**為公告/異常單內容，**右側**為附件顯示區
- 直接顯示的附件使用「角落標註快取版」（見 3-2），標籤與備注已印在附件上，**不需另外顯示文字說明**
- 每個附件皆可點擊 → 以新分頁開啟，或以放大燈箱（lightbox）顯示

### 4G 檢視版面

- 上下排列：公告/異常單內容在上 → 附件清單在下方 → 回覆/回簽按鈕在最下方
- **不自動展開附件內容**：附件以清單呈現（檔名 + 標籤 + 備注摘要），點擊後透過簽名連結取得即時加註版（含下載者溯源浮水印，除非標籤為免浮水印），以新頁面開啟
- 每次點擊皆寫入下載紀錄（與 download.php 同一機制）
- 只顯示標籤允許 4G 傳送的附件；不允許者顯示「請至內網查看」

### Telegram 檢視

- Telegram 無版面概念，無法自動展開附件；檢視方式為既有的兩段式：按「取得附件」按鈕 → 收到檔案 → 點開（圖片直接看、PDF 用 Telegram 內建閱讀器）
- 維持以檔案（sendDocument）形式發送，保留畫質與加註完整性

## 五、建議新增資料表（欄位名稱可依現有命名習慣調整）

- `attachment_tags`：id, scope, name, allow_webpush, allow_telegram, require_watermark, is_default, is_active, created_by, timestamps
- `attachments`（若現有附件表已存在則改為加欄位，**先確認現有結構**）：tag_id, description, file_path（Excel/Word 上傳者此處存的是轉換後 PDF；圖片與 PDF 上傳則為原檔）, original_filename（保留原始檔名供顯示）, file_size, uploaded_by, timestamps
- `attachment_download_logs`：attachment_id, employee_id, channel(webpush/telegram), downloaded_at, ip
- `tag_change_logs`：操作者、對象（標籤或附件）、舊值、新值、時間

## 六、風險防護（必做）

### 6-1. 員工狀態檢查（HR 連動）

- 系統已有 HR 員工狀態設定（含離職、留職停薪），這些狀態的員工**不得收到任何推播與附件**
- 檢查時機有兩個，缺一不可：
  1. **每次發送推播前**：過濾掉狀態不符的員工
  2. **Telegram 附件按鈕 callback 時**：重新檢查員工當下狀態，不合格則回覆「附件已不開放」並不發檔
- ⚠️ 實作前請向使用者詢問 HR 員工狀態設定頁面的網址與對應資料表/欄位，由使用者提供，不要自行猜測

### 6-2. 綁定管理（最高管理員）

- 新增管理介面：最高管理員可查看所有員工的綁定清單（Telegram 與 4G Web Push），並可移除任一筆綁定
- 用途：員工換手機、裝置遺失、離職清理
- 移除綁定寫入異動 log

### 6-3. Telegram 舊按鈕的時效驗證

- 聊天室中的附件按鈕永久存在，因此 callback 處理時必須即時重新驗證三件事，全部通過才發檔：
  1. 員工目前狀態（見 6-1）
  2. 該員工綁定是否仍有效（未被 6-2 移除）
  3. 附件標籤目前是否仍允許 Telegram 傳送
- 任一不通過：回覆「附件已不開放」，並寫 log

### 6-4. 輪詢心跳監控

- Telegram 輪詢腳本每次執行時更新一筆心跳時間戳（設定表或獨立小表）
- ERP 管理頁（或首頁指定位置）檢查心跳：超過 5 分鐘未更新，顯著顯示「Telegram 推播服務異常」警告

### 6-5. Web Push 訂閱失效處理（iOS）

- 發送 Web Push 收到 404 / 410 回應時：自動將該筆綁定設為失效（is_active=0），記錄失效時間與原因
- 管理介面標示失效綁定，讓管理者通知該員工重新綁定

### 6-6. 免浮水印標籤

- 標籤新增 `require_watermark` 開關（預設開啟）
- 主管可將低敏感度標籤（如一般公告、員工旅遊通知）設為免浮水印，發送時跳過蓋印直接輸出，降低處理成本
- 預設標籤（未選標籤自動套用者）的 require_watermark 必須保持開啟

### 6-7. 規模說明（給 AI 的背景）

- 4G 與 Telegram 推播目前僅開放主管使用，總數 10 人以內；一般員工只使用內網網頁（電腦通知或置頂未讀提示）
- 因此不需要實作發送佇列或高併發防護，但程式仍應避免在單一請求中同步處理多個浮水印

## 七、實作順序建議

1. 先確認現有資料表與檔案現況（向使用者索取，含 HR 員工狀態表）
2. 標籤系統 + 主管管理介面（含 require_watermark 開關）
3. 上傳限制 + Excel/Word 轉 PDF（含工作表選擇與預覽確認）
4. 輸出加註函式庫（溯源浮水印 + 角落資訊標註，獨立函式庫，兩管道共用）
5. 4G 簽名下載端點（含員工狀態檢查）
6. Telegram 兩段式附件發送（按鈕 → callback 三重驗證 → 記錄 → 加註 → 發檔）
7. 回覆附件上傳（4G 回覆頁 + Telegram 聊天上傳與標籤選擇）
8. 檢視畫面（公告檢視新增、電腦版左右分欄、4G 上下排列、直接顯示勾選選項）
9. 綁定管理介面（最高管理員）+ Web Push 失效處理 + 輪詢心跳
10. 各項 log
