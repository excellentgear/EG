# NAS 路徑改造 — 修改紀錄

> 配合 `NAS路徑改造_相對路徑遷移計畫.md` 使用。每次動手前先把對應模組狀態改成「進行中」；每完成一步就更新，不要等全部做完才補寫；中途若中斷，下一個接手的人（或 AI）從這份檔案就能知道做到哪、卡在哪。

## 狀態總覽

| 順序 | 模組 | 狀態 | 開始時間 | 完成時間 | 測試結果 |
|---|---|---|---|---|---|
| 1 | CAR 矯正單附件 | 待測試 | 2026-07-13 13:39 | 2026-07-13 13:45 | |
| 2 | IR 退貨附件 | 未開始 | | | |
| 3 | QA 異常單附件 | 未開始 | | | |
| 4 | 公告附件 | 未開始 | | | |

狀態只能是：`未開始` / `進行中` / `待測試` / `測試通過` / `測試失敗-已回復備份` / `暫緩`。

---

## 模組 1：CAR 矯正單附件

**狀態**：待測試（開始 2026-07-13 13:39，完成改動 2026-07-13 13:45，等待使用者實測）

### 動手前發現的額外地雷（文件未列出，已與使用者確認處理方式）
`car_link_desc_attachments()`（開單前 temp_key 暫存附件 → 正式開單後複製綁定 car_id）只複製 DB 紀錄、**不搬動實體檔案**（註解：保留暫存以供其他拆單複製），但複製時沒有把 `temp_key` 一併存進新紀錄。若解析函式只看 `car_id` 判斷資料夾，這類 desc 附件將來會被算到錯誤（單號）資料夾，實際檔案仍在暫存資料夾。
→ 已與使用者確認，修法：`car_link_desc_attachments()` 複製時一併帶入 `temp_key`；解析函式**先判斷 `temp_key` 是否有值，有值就用暫存資料夾，沒有才用單號資料夾**（不是只看 car_id）。不影響任何既有上傳/命名規則。
（目前 `car_attachment` 資料表為空表，0 筆資料，不影響既有資料）

### 變更記錄
- 新增 `carAttResolvePath($pdo, array $row): string`（`src/store/store_CAR_API.php:1474-1494`）：判斷順序為「`temp_key` 有值 → 用暫存資料夾 `_temp/{temp_key}`；否則用 `car_id` 對應的單號（清洗後的 `car_no`，查不到單號時退回 `ID{car_id}`）資料夾」，最後接 `file_name` 組出完整路徑。根路徑一律讀取當下的 `car_attach_root_path` 設定值。
- `download_attachment`（原 :1414/:1420-1427）：不再直接 `is_file($a['file_path'])`／`readfile($a['file_path'])`，改呼叫 `carAttResolvePath()` 取得路徑後再操作。
- `delete_attachment`（原 :1439）：不再直接 `is_file($a['file_path'])`／`unlink($a['file_path'])`，改呼叫 `carAttResolvePath()`。
- `car_link_desc_attachments()`（:1454-1470）：拆單複製 desc 附件時，SELECT/INSERT 新增帶入 `temp_key` 欄位（原本被留白，導致複製後的紀錄無法反映「實體檔案仍留在暫存資料夾、未搬動」的事實）。**此為動手前發現、與使用者確認後一併修正的地雷，非文件原列任務範圍**，詳見上方「動手前發現的額外地雷」。
- 上傳邏輯（`upload_attachment`）與子資料夾命名規則（:1388-1391）**完全未改動**，DB `file_path` 欄位仍照舊寫入（僅不再被讀取端信任）。
- `page_change_log` 已寫入一筆（id=240，page_name=`src/store/store_CAR_API.php`）。

### 備份檔清單
- `src/store/store_CAR_API.php.bak-20260713-1339`

### php -l 結果
`No syntax errors detected in c:/MAMP/htdocs/EGsystem/src/store/store_CAR_API.php`（2026-07-13 13:45）

### 測試結果
（尚無，等待使用者實測）

---

## 模組 2：IR 退貨附件

**狀態**：未開始

### 變更記錄
（尚無）

### 備份檔清單
（尚無）

### 測試結果
（尚無）

---

## 模組 3：QA 異常單附件

**狀態**：未開始

### 變更記錄
（尚無）

### 備份檔清單
（尚無）

### 測試結果
（尚無）

### ⚠️ 共用函式影響追蹤
`attachment_lib.php` 的 `eg_att_make_preview()`/`eg_att_refresh_preview()` 為 QA 與公告共用，本模組動到此函式時，須同步在此記錄，並提醒模組4驗證時一併確認。

---

## 模組 4：公告附件

**狀態**：未開始（依計畫排在最後，且需完整驗證通知/Telegram 系統未受影響）

### 變更記錄
（尚無）

### 備份檔清單
（尚無）

### 測試結果
（尚無）

---

## 全站待確認事項（動手前需與使用者確認，任何一項未確認前不可視為「可以開始」）

- [ ] 異常單號（`abnormal_order_no`）建立後是否可能被修改？
- [ ] CAR 單號（`car_no`）建立後是否可能被修改？
- [ ] 公告編號（`event_no`）建立後是否可能被修改？
- [ ] 退貨單號（`IR_no`）建立後是否可能被修改？

> 以上四項若答案為「可能改名」，代表「現場用目前單號重組路徑」的做法對該模組不成立，需要另外設計（例如改存「建立當下的單號快照」而非即時查詢），此時須回頭跟使用者重新討論該模組的做法，不可直接套用本計畫預設邏輯。
