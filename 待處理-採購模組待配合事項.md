# 待處理：採購模組要配合會計的事項（交接給負責採購模組的人）

> 建立日期：2026-07-31（前身是「待處理-會計模組未完成項目.md」，該檔兩件會計工作都已交付，故改寫成本檔）
> 建立者：Claude（會計模組開發者）
> **本檔用途**：會計端已經把材料／其他採購接進應付了，但有三件事必須由**採購模組**這邊配合，
> 否則現場會出現「兩邊各說各話」。做完這三件請刪除本檔。

---

## 0. 先讀這個：會計端現在怎麼看採購單

| 事項 | 會計端現在的作法 | 程式位置 |
|---|---|---|
| 哪些採購單算應付 | **已下單之後**（`ordered_at` 有值，或 status 為 ordered/partial/received/closed），且 `grand_total<>0`、`is_active=1`、狀態非 rejected/canceled | `acc_purc_is_payable_stage()` |
| 現金 vs 月結 | 依 `pay_method` **自由文字**判關鍵字（現金／零用金／小額／代墊／petty／cash → 現金，其餘當月結）；會計可逐張改判，覆寫存在 `acc_purc_flag`（會計自己的表，不動採購資料） | `acc_purc_settle_mode()`、`acc_purc_set_mode()` |
| 帳款月份 | 廠商發票日 `invoice_date` 的月份優先，沒有才用 `ordered_at` 的月份 | `acc_purc_month()` |
| 付款狀態 | **會計的付款沖帳算出來的**：全額沖完＝`paid`、部分＝`unpaid`，並回寫 `purchase_request.pay_status`／`pay_date`／`pay_method` | `acc_purc_sync_pay_status()` |
| 金額 | 會計端**不可**改採購單金額（`acc_editable_fields('PURC')` 回空陣列），因為總額是 `purchase_item` 明細算出來的 | `acc_editable_fields()` |

會計端頁面：`views/ACC/ap_statement.php`（應付對帳單，含來源切換與現金單改判）、
`views/ACC/payment.php`（付款與沖帳）。資料表：`acc_payment`／`acc_payment_alloc`／`acc_purc_flag`。

---

## 1. 【必做】採購頁的「付款狀態」要改成唯讀顯示

`views/pages/purchase_request.php` 的「記帳」跳窗目前讓人手動選 `付款狀態(未付/已付)`＋`付款日`
（`#aPayStatus`、`#aPayDate`，存檔走 `Purchase_API.php` 的 `save_account`）。

**問題**：使用者已拍板「月結採購走會計」，會計那邊沖完帳會回寫 `pay_status`／`pay_date`。
兩邊都能寫同一個欄位＝誰後寫誰贏，現場會看到付款狀態自己跳來跳去。

**要做的**（擇一，建議前者）：
1. **月結單**：付款狀態與付款日改成**唯讀顯示**，旁邊註明「由會計付款沖帳決定」，並附連結到
   `views/ACC/payment.php`；**現金單**維持可手填（現金本來就不經會計）。
2. 若堅持保留手動，至少在 `save_account` 擋下「該單已有會計沖帳紀錄」的情況並提示。
   判斷方式：`SELECT COUNT(*) FROM acc_payment_alloc WHERE src_type='PURC' AND src_id=?`。

發票號碼／發票日期兩欄不受影響，維持採購自己填（會計是讀這兩欄來歸帳款月份的，請務必保留）。

---

## 2. 【建議】加一個結構化的「結帳方式」欄位，取代關鍵字猜測

現在會計是用 `pay_method` 這個**自由文字**欄位猜現金還是月結（提示字是「匯款／月結／現金」），
一定會有猜錯的——猜錯的單子會整張跑進或跑出應付。目前的補救是會計可以逐張改判，
但那是補破網，不是正解。

**正解**：在 `purchase_request` 加一個結構化欄位，例如
`settle_type VARCHAR(10)`（`CASH`／`CREDIT`），詢價／下單那一關由採購用**下拉**選（不要自由文字），
預設可依廠商主檔 `maker_list.settlement_mode` 帶值。

加好之後請通知會計端（或直接改）：把 `src/common/acc_lib.php` 的 `acc_purc_settle_mode()`
改成**優先讀 `settle_type`**，讀不到才退回現在的關鍵字判定，`acc_purc_flag` 的手動覆寫仍然最優先。
改這一個函式即可，其餘（彙總／對帳／付款沖帳）都是吃它的結果，不必動。

---

## 3. 【待確認】用途歸屬（purpose_*）的 UI 是否已完成

DB 欄位已經存在（2026-07-30 覆查 `SHOW COLUMNS FROM purchase_request LIKE 'purpose%'`）：

| 欄位 | 實際型別 | 說明 |
|---|---|---|
| `purpose_type` | VARCHAR(10)（不是當初規格寫的 ENUM，實測有 `STOCK` 值） | 用途類別 |
| `purpose_order_id` | INT NULL（有索引） | → `order_track.Order_id` |
| `purpose_bom` | VARCHAR(30) NULL（有索引） | → `bom.bom` |
| `purpose_d_id` | INT NULL（有索引） | → `d_setting.d_id`（料號主鍵） |
| `purpose_note` | VARCHAR(200) NULL | 其他原因自由文字 |
| `purpose_label` | VARCHAR(150) NULL（規格外新增） | 顯示用文字 |

**關鍵：綁訂單一定要存 `Order_id`，不能存訂單號字串。** `order_track` 有 8,641 列但只有 4,768 個
不同 `Order_oo`，一個訂單號最多對到 25 列（多料號訂單），存字串會歸錯成本；料號同理走
`d_setting.d_id`（料號字串有 159 個重複）。`reason` 保留當補充說明選填，不要刪。

請確認採購頁的填寫 UI 是否已經照這個規格做完（下拉選類別→換對應選擇器，存 id 顯示名稱）。
會計端的採購應付清單與匯出已經在讀 `purpose_label`，做成本歸屬統計時直接吃這些欄位即可，不要另建一套。

---

## 4. 已知的資料現況（不是 bug，會影響驗收判斷）

- `purchase_request` 到 2026-07-31 為止只有 **1 筆**測試單（`PR-20260730-001`，金額 0、無廠商），
  所以會計端的採購應付是用臨時測試資料驗收的（建 5 張測試單跑 34 項檢查，測完已精準刪除、殘留 0 筆）。
  等現場開始開真實採購單後，請再實際走一次「下單 → 記發票 → 會計付款沖帳 → 回頭看採購單付款狀態」。
- 廠商主檔 `maker_list` 目前 904 家全部是 `settlement_mode='FIXED'`，其中只有 37 家填了 `payment_method='匯款'`，
  其餘 867 家 `payment_method` 是 NULL——所以**沒辦法**單靠廠商主檔判斷現金／月結，這也是第 2 節建議加欄位的原因。

---

## 5. 完成後要做的事

1. 每個改過的檔跑 `php -l`
2. 兩支健檢（`ai-rules/tools/check_sidebar.php`、`ai-rules/tools/check_input_rules.php`）全部通過
3. 寫 `page_change_log`（範本見 CLAUDE.md）
4. `git add` → `git commit` → **`git pushall`**（雙 remote，只 `git push` 會漏備份庫）
5. **刪除本檔**：
   ```
   git rm 待處理-採購模組待配合事項.md
   git commit -m "採購模組待配合事項已完成，移除交接文件"
   git pushall
   ```
   若只完成一部分，請把完成的那節整節刪掉、保留未完成的，不要在檔內留「已完成」字樣。
