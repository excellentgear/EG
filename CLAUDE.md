# EGsystem — AI 工作規範（每次 session 必讀）

> 最後修改：2026-09-24（八次）— **報工紀錄查詢列印：欄位版面調整＋補開異常單新增「開立日期」選擇＋異常單欄顯示狀態；品質異常處理單：自動開立責任單位自動帶廠商、簡化異常現象文字、幾個自動帶入的欄位鎖定不給改**（使用者連續交辦一整批，接續 09-24 六次報工NG累積開單那一批）。**①版面**：機台/日期/備註篩選框縮短寬度＋工單欄正名「製令」並改左對齊（含機台/料號同步左對齊），讓列印/匯出CSV回到第一列不再跑版；雙擊「製令」或「料號」儲存格內容可直接帶入上方篩選框並立即查詢；篩選框輸入即改為即時查詢（350ms 防抖），取消「查詢」按鈕。**②補開/批次補開新增「開立日期」選擇**（使用者原話：要能以NG那天開立，多天NG或期間內開立則可選「最後一次NG報工日期～期間結束」之間的工作日，開立日期不可早於NG日期）：`qab_auto_open_from_bom_ing()` 新增 `$fillDate` 參數，驗證**不可早於這批最後一次NG報工日期、不可晚於今天、必須是工作日**（借用 `leave_lib.php` 既有的 `eg_leave_is_workday()`，不重寫一套假日判斷）；新增 `qab_backfill_preview_stations()` 唯一實作，補開跳窗按下後先預覽各站累積的期間/數量再選日期，跨站取日期較晚的站別當下限；驗證失敗只擋該站（收進 `failed[]`），不會讓整批補開因一站日期不合法就全部失敗。**③異常單欄顯示狀態**：只分「處理中」與「已結案（結果，如報廢/重工/特採，總經理裁示優先）」兩種（沿用既有 `qab_status_map()`，不再另組一套），**扣款是獨立欄位不在 `qa_option` 選項清單裡，所以自然不會印在結果括號內**（使用者要求「若總經理裁示扣款，不需顯示在上面」，查證後這條本來就自動成立）；**異常單被刪除時這一欄要移除**——`order_delete` 新增：刪除當下把該單累積歸入的 `pm_process_daily_report.abnormal_order_id` 一併清空（report_id 清單寫進 `qa_abnormal_del_log.snapshot`），這批NG變回「尚未歸入」可重新補開；`order_restore` 對稱地把 snapshot 記錄的 report_id 接回來，但只接「目前仍未被其他單認領」的（不搶已被新單累積走的），`process_report_query.php` 的 JOIN 也加上 `qao.deleted_at IS NULL` 當第二道防護；**順手修復一筆既有懸空參照**——使用者截圖操作時把測試建立的 Q1150924001（id=78）刪除，但當時還沒有這支清空邏輯，導致真實案例 `report_id=4391` 仍連著一張已刪除的單，已用同一條 UPDATE 復原。**④截圖回報「已選定製程但廠商欄空白」**：`qab_create_order()` 新增——`resp_process_no` 有值時自動查 `bom_ing` 該站登記的 `maker_id_no` 一併帶出 `responsible_vendor_id`／`resp_is_internal`／`responsible_unit`（與 `save_head` 裡既有的 `$autoVendor` 邏輯同一個判斷依據，取站序最早那筆），不必開單後再進表單選一次。**⑤自動開立的異常現象文字改成固定「報工發現不良」**（原本「報工累積發現不良，自動開立（期間，共N筆...）」落落長，使用者：改成顯示這樣就好），期間等細節仍完整留在 `auto_open_note` 供追溯。**⑥自動開立的單，幾個「本來就是系統算出來的」欄位改成鎖定**（使用者拍板）：**製令編號任何人都不可改（連管理員也不行）**；**責任單位（製程/廠商/部門人員）只有異常單管理員可以改**；**批量（＝累積生產數量=良品+NG）／檢驗數（＝同批量）／不良數（＝NG總數）任何人都不可改**。後端 `QaAbnormal_API.php` 的 `save_head` 同規則把關（鐵律8：忽略鎖定欄位送來的值，不報錯但也不採信），前端 `qa_abnormal_form.php` 對應反灰並加提示文字；`RESP_LOCKED` 全域旗標同時控制「新增部門人員」按鈕顯示與既有 chip 的移除鈕。**測試方法**：核心日期驗證與廠商自動帶入用 CLI 直接呼叫函式驗證（`bom_ing_fid=1`，查證過完全沒有真實報工紀錄的合成測試站別）；欄位鎖定的前後端把關改用**真 HTTP 端對端**（偽造 session：一般使用者＝把測試訂單 `created_by` 指給他讓 `qab_can_edit_form` 放行、系統管理員＝id=1）分別驗證「非管理員送出 bom_no/batch/insp/ng/resp_process_no/responsible_vendor_id 全部改不動、abnormal_phenomenon 正常改得動」與「管理員 resp 欄位改得動、bom_no/batch/insp/ng 仍然改不動」；`order_delete`／`order_restore` 也是真 HTTP 端對端驗證 FK 清空與接回。驗證：`php -l`＋**渲染後**inline JS `node --check`（process_report_query.php／qa_abnormal_form.php 皆過）全過；測試建立的合成報工、異常單、`qa_abnormal_opt`／`qa_abnormal_edit_log`／`qa_abnormal_del_log` 測試列、偽造 session 檔皆已以明確 id/report_id 清除，`bom_ing_fid=1` 最終確認報工筆數回到 0。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-24（七次）— **出貨紀錄分析／客戶季度分析：修好客戶篩選對排行沒有作用、補上出貨性質篩選、列印補上趨勢圖**（使用者實測回報三件）。**①客戶=旭陽卻還是顯示一堆其他客戶**：成長／衰退排行本來就是拿全部客戶互相比較，跟趨勢分頁共用同一個客戶篩選欄，但選了客戶之後排行畫面完全沒有跟著變——共用欄位就在正上方，使用者當然以為它也篩了排行。改成客戶篩選有選定時，排行分頁只顯示那一家跟基期/當期的比較卡片與逐季走勢（不再列全部客戶的成長/衰退兩欄），並附「看全部客戶排行」連結退回全部比較；逐季走勢直接用後端 `cq_growth` 早就一起送回來的 `series`（271 家×12 季約 58KB，量過留著比另開一支 API 划算）。**②只有上方「出貨性質」有設定的才列入計算**：客戶季度分析原本自己另訂一套「排除 `is_count=0`」的出貨性質範圍，跟頁面上方「出貨性質」篩選完全無關——上面篩掉的，這裡照樣算。改成前端一律讀上方下拉目前的即時選取值，`client_quarter_lib.php` 新增 `cqa_sale_type_sql()`（**唯一實作，與主查詢區 `$sql_sale_type_condition` 同一套規則**），只影響出貨金額（訂單／退貨沒有出貨性質欄位不受影響）；後端白名單過濾非法值（實測 SQL 注入字串被排除、合法值仍生效）。**這條在目前資料上驗不出視覺差異**——查證後發現 `is_list.sale_type` 全庫 37,971 筆**皆為 NULL**（這個篩選欄位在現場資料上還沒被實際用過），已改用直接呼叫函式＋SQL 語法測試驗證邏輯正確，等現場開始標記出貨性質後自動生效；**碰到「篩選前後結果一樣」時，先查資料本身有沒有可供區分的值，不要直接假設篩選沒作用**。**③列印缺乏圖表**：客戶趨勢的列印版原本只有表格，把 Highcharts 圖表 `getSVG()` 匯出直接嵌進列印頁（做法與既有 `printAnalysisReport()` 一致），單一客戶模式的排行列印也同步只印那一家並附逐季走勢表。**測試中一個值得記的假警報**：無頭 Chrome 對整份列印 HTML 找 `undefined` 判定失敗，查證後是 **Highcharts SVG 匯出固定會帶 `highcharts-color-undefined` 這種內部 class hook**（沒設 `colorIndex` 時的既定寫法，即使每個 series 都給了明確 hex color 也一樣），不是使用者看得到的文字；改成排除 `<svg>` 區塊再檢查可視文字才是正確判準。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 實際登入點擊 **28 項新增測項＋65 項既有回歸全過**（選定旭陽後排行只顯示旭陽、清除後排行家數與清除前完全一致、SQL 注入被擋下、列印版含 `<svg>` 且可視文字無 undefined、單一客戶排行列印不含全部客戶清單）。未寫入任何業務資料。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-24（六次）— **報工NG自動開立品質異常單：使用者更正成「累積到完工才一次開」，並新增持續性生產每兩週提醒**（緊接上一輪，使用者看完設計後更正）。上一輪做成「每筆報工各開一張」，使用者更正：**應該是完工時把整站累積的NG加總開一張單**；若一直不完工（持續性生產），改成**每兩週提醒一次是否要先開立累積單**，不是每天報工都跳。**函式改法**：`qab_auto_open_from_pm_ng($reportId)` 整支換成 `qab_auto_open_from_bom_ing($bomIngFid, $triggerReportId=null)`——新查詢入口 `qab_pm_uncovered_reports()`（該站 `abnormal_order_id IS NULL` 的所有報工，唯一實作，`qab_auto_open_from_bom_ing()`／`qab_pm_suggest_check()` 都靠它算，不各自寫一份）先撈出該站全部尚未歸入的報工，加總NG與良品數、算出期間（最早～最新報工日），開**一張**單後把**全部**涵蓋到的報工 `abnormal_order_id` 一次寫回同一個單號（不是各自對一張）；開單人／現場主管解析改用**觸發那一筆**（完工那筆或使用者按「先開立」當下）的報工人員回推，不是被涵蓋進來的每一筆各自解析一次。**兩週提醒**：新函式 `qab_pm_suggest_check()`＋新表 `qab_pm_suggest_state`（`bom_ing_fid` 主鍵，記 `last_suggested_at`）——週期**從「最早一筆尚未歸入的NG」那筆報工日期起算**（使用者拍板），到期才跳提醒且**當下就記錄這次已提醒過**（不論使用者選先開立或暫不開立都算，避免同一個 14 天窗口內每天存檔都被打斷），下一次提醒＝上一次提醒日期＋14 天；累積被清空（開單或本來就沒有）時計時器一併歸零，下一批NG從頭起算。**掛載點同步調整**：`process_schedule.php`／`process_schedule_NOW.php` 的存檔回應改依 `is_finished` 分流（完工→自動開單／未完工→問要不要跳提醒），新增 `action=qab_suggest_accept` 給提醒跳窗「先開立」按鈕呼叫（`confirm()` 陣列式跳窗，按下才送出，文字明講「不開立也沒關係，下次完工會自動累積開單或兩週後再提醒」）；`process_report_query.php` 的補開／批次補開改成**先把選到的 report_id 反查成所屬的 bom_ing_fid、同一站只呼叫一次**，結果與確認文字都改講清楚「開出來的量可能比選到的還多（該站其他未歸入的報工也會一起被加總）」，避免管理員誤解。**測試時踩到並立刻修回的坑**：第一次測 `qab_auto_open_from_bom_ing()` 圖方便沿用了真實 bom_ing_fid=144009（就是使用者原始案例 B-1150910015 那一站），函式正確地把**真實的 report_id=4391 一起累積進了測試單**（設計上完全正確——它本來就是該站尚未歸入的NG），但測試收尾只刪了自己建的合成報工與異常單，忘記 4391 的 `abnormal_order_id` 也被寫回同一個（已刪除的）單號，一度留下懸空參照；**立刻查出並補一道 `UPDATE ... SET abnormal_order_id=NULL WHERE report_id=4391` 復原**，之後測試改用查證過「完全沒有既有報工紀錄」的 bom_ing_fid（如 =1）當合成測試對象，不再共用任何有真實資料的站別。**教訓：涉及「累積/加總某個既有實體全部關聯資料」的函式，測試不能只挑一個看起來方便的既有 id 下去用，要先確認那個 id 底下沒有不屬於這次測試的真實資料，否則收尾清理會漏掉被牽連進來的既有列。**驗證：`php -l`＋**渲染後**inline JS `node --check`（process_schedule.php／process_schedule_NOW.php／process_report_query.php 全過）＋側欄與輸入規則健檢全過；CLI 直接呼叫函式完整跑過一輪（3 筆合成報工 NG 1/2/3→`qab_pm_suggest_check` 於+10天回null、+14天回累積7件建議、同日再問回null（冷卻中）、+14天後再問又回建議→`qab_auto_open_from_bom_ing` 正確加總 7 件、4 筆報工全部歸入同一張單、`qab_pm_suggest_state` 隨之清空）；真 HTTP 端對端（`process_report_query.php` 的 `qab_backfill_open` 用 2 筆合成報工驗證正確合併成 1 張累積單、金額與期間正確、DB 關聯正確寫回）。**測試建立的異常單、合成測試報工紀錄、測試 session 皆已以明確 id 清除；使用者原始案例 `report_id=4391` 已確認復原為 `abnormal_order_id=NULL` 的未開單狀態**。**尚未經使用者瀏覽器實際點擊驗收**。
> 最後修改：2026-09-24（五次）— **新增「報工NG自動開立品質異常單」＋讓報廐正確扣減待包裝／BOM總覽／快速出貨的數量**（使用者以 BOM `B-1150910015` 報工NG一顆為例提問「應該怎麼做」，逐輪確認後交辦）。**先查證推翻了「這只是顯示問題」**：`bom_ing.sqty` 是製令固定投入量，不論哪一站NG/完工從來不會被扣減；待包裝清單「BOM總數」、BOM總覽「發單日」欄的「70x」、快速出貨可出量，全部只是直接讀 `sqty`，就算異常單最終裁決報廐、配了報廐單號，畫面上的數量也完全不會變小——這是要新做的功能，不是抓 bug。**使用者逐輪拍板的設計**：①報廐在**異常單結案配發報廐單號時**才正式扣減（不是報工/檢驗當下就先扣，決策翻案不必復原）②報工完工後NG自動由**現場主管**開立異常單帶入NG數量、轉品管判定；管理員可對已開單的補完簽核；對舊資料可比照補開；並提供**期間內批次勾選補開**③扣減**只影響該站之後（含該站）所有站的sqty**④現場主管＝報工人員所屬部門的單位主管，**不像 ai-rules/24 卡課級**，逐層往上找到「當天在職」的人為止，最後保底退回全站最高決策者。**異常單能否從BOM製程選擇開單製程**：查證確認**已支援**（`qab_bom_processes()` 供「責任單位」下拉挑選、寫入 `resp_process_no`），不需修正。**新增/擴充函式（皆為唯一實作，鐵律4）**：`qa_abnormal_lib.php` 抽出 `qab_create_order()`（原本內嵌在 `QaAbnormal_API.php` 的 `case 'create'`，`created_by` 改吃參數不再讀 `$_SESSION`，自動開單與人工開單共用同一支）、新增 `qab_bom_scrap_rows()`/`qab_bom_scrap_sum_rows()`/`qab_bom_scrap_qty()`（只算 `is_closed=1 AND scrap_no IS NOT NULL` 的異常單，依 `pm_report_id`→`bom_ing_fid` 或 `resp_process_no`→同bom最早一站回推是哪一站發現的；批次版供一次列很多BOM的畫面用，避免N+1查詢）、`qab_auto_open_from_pm_ng()`（由一筆報工NG建單，整支包 try/catch，失敗絕不可讓報工存檔跟著失敗）；`unit_supervisor_lib.php` 新增 `eg_unit_supervisor_available()`＋`eg_unit_dept_candidates()`（不卡課級、逐層往上找「當天在職」的主管，請假判定呼叫 `doc_apply_lib.php` 既有的 `da_user_on_leave_asof()`，全部爬完退回 `eg_org_user($db,'top_approver')`——與 ai-rules/24 的 `eg_unit_supervisor()` 是刻意分開的不同用途變體，已登記進路由表）。**掛載點**：`process_schedule.php`／`process_schedule_NOW.php` 的 `submit_daily_report` 在 `commit()` 之後、獨立 try/catch 呼叫（不可在交易內呼叫，`qab_create_order()` 自己會 `beginTransaction()`，巢狀交易會炸「already an active transaction」）；`process_report_query.php`（今天才新增良品/NG欄位的報工查詢頁）新增「異常單」欄與單筆/批次「補開」（限品質異常單管理員，批次補開時用**該筆報工自己的 `report_date`** 回推當時的現場主管，不是今天）。**顯示改動三處**：線上檢驗頭部「尚未檢驗 / 訂單數」→「尚未檢驗 / 良品數 / BOM總數」（`inspection_combined_prototype.php` 的 `load_context` 新增 `good_qty`）；包裝排程待包裝清單與 `#f-order-qty` 改顯示良品數（**readonly**，避免現場手改蓋掉報廐扣減，旁邊小字顯示原總數）；BOM總覽「發單日」欄「70x」有報廐時顯示「良品/總數 x」——**這一項踩到本頁已知的「三份各建一次」坑**（2026-09-04 記錄過同一種），除了 `OreadyReply_ForPm_BaseOfTime.php` 首次載入，**自動更新實際餵資料的是 `src/store/_fetch_data.php` 與 `_fetch_data2.php`**，三處都要同步改否則會「首次載入正確、更新後打回原數」。**快速出貨 `sq_bom_avail_map()` 也要扣**：不扣的話待包裝/BOM總覽都已顯示良品數變少，快速出貨卻還能出到報廐前的滿額，兩邊自相矛盾（實測 70→66，扣4件確認報廐）。驗證：`php -l`＋**渲染後**inline JS `node --check`（process_schedule.php／process_schedule_NOW.php／process_report_query.php／inspection_entry_v2.php／packing_schedule.php／OreadyReply_ForPm_BaseOfTime.php 全過）＋側欄與輸入規則健檢全過；CLI 直接呼叫函式驗證 `eg_unit_supervisor_available()`（正常在職一次命中）、`qab_auto_open_from_pm_ng()`（含合成測試報工建單、`resp_process_no`/`pm_report_id` 正確寫入、重複呼叫不重複建單）、`qab_bom_scrap_qty()`（未結案/非報廐不計入、結案報廐後站別邊界正確）、`sq_bom_avail_map()`（插入假的已結案報廐單後 70→66，刪除後還原）；真 HTTP 端對端（`process_report_query.php` 的 `qab_backfill_open` 單筆/重複略過/非管理員 403）；`inspection_combined_prototype.php`／`packing_schedule.php` 的 `get_form` 皆實測 `good_qty` 隨假報廐單正確增減。**測試建立的異常單、合成測試報工紀錄、測試 session 皆已以明確 id 清除**。**尚未處理**：使用者原始提到的真實案例（`pm_process_daily_report.report_id=4391`，B-1150910015 製程12 報工NG1顆）**刻意維持未開單狀態**——是否要標為報廐是品管的業務判斷不是我能代為決定的，留給使用者用剛做好的「補開」按鈕在畫面上親自試第一筆。**尚未經使用者瀏覽器實際點擊驗收**。
> 最後修改：2026-09-24（四次）— **量測儀器校驗管理「類別設定」：其他頁面顯示欄改三欄排列＋補上量具設定新增類別的重複名稱檢查＋合併重複的「目視」類別**（使用者回報兩件：畫面太長、為什麼目視有兩個）。**①版面**：8 個顯示欄位改 CSS Grid `repeat(3, minmax(0,1fr))`，8 項→3 列，不再一行一個把「類別設定」整列撐高；欄寬 280→320px 給 3 欄留空間。**②查出「目視」變成兩筆的根因**：`qc_tool_list` 有兩個新增類別的入口——`ToolCalib_API.php` 的 `save_category`（「類別設定」用）**有**檢查同名，`inspection_combined_prototype.php` 的 `save_tool_category`（舊版「量具設定」跳窗用，`inspection_entry_v2.php` 沿用同一支）**完全沒檢查**，兩個入口寫同一張表卻只有一邊守門；查證確認 id=13「目視」是正確設定（`calib_required=0`／`has_tool_no=0`，符合本頁自己的說明「不是實體量具、只是檢驗方式者請取消勾選」），id=21 是這條沒擋下重複造出來的、底下掛著一支孤兒量具 `Tool_id=89`（逐一查過 `ss_item`／`ss_item_tpl`／`qc_form_tool`／`qc_measurement`／`qc_inspection_item_tool_type` 全部 0 筆引用，合併零風險）。**已補上同一條重複名稱檢查**並在新增時比照設定 `sort_order`（銜接 09-24 三次新加的拖曳排序）；**用既有的 `replace_tool_category`（取代並刪除）把 id=21 合併進 id=13**，Tool_id=89 保留身分只換掛的類別，合併後查詢確認全庫只剩一筆「目視」。驗證：`php -l`＋**渲染後**inline JS `node --check` 全過；側欄與輸入規則健檢全過；SQL 逐欄核對合併結果（`qc_tool_list` 剩 18 筆、`qc_tool.Tool_id=89` 的 `QC_Tool_List_id` 已改為 13）。**尚未經使用者瀏覽器實際點擊驗收**。
> 最後修改：2026-09-24（三次）— **量測儀器校驗管理（`views/QC/tool_calibration.php`）「類別設定」：可拖曳排序＋修好挑選跳窗類型順序沒依設定生效的既有 bug**（使用者交辦兩項）。**①排序**：類別列最左邊加「☰」拖曳把手（走站上既有 `resource/js/Sortable.min.js`，**把手限定最左格**，跟 SOP/SIP 同一套規則——這張表「其他頁面顯示」欄有輸入框，整列可拖會讓在那欄選字變成拖列）；順序就是 `qc_tool_list.sort_order`，按「儲存」時後端 `save_categories` 直接拿送出的陣列位置當 `sort_order` 寫回（不必再收一個額外欄位，兩邊各算一套順序遲早對不起來）；未存檔前的拖曳順序也會在改名/新增類別/新增分頁等重繪時保留（`collectCatUI()` 新增 `__order`）。這顆設定同時決定三個地方的順序：本頁上方分頁列、`qc_tool_pick_rows()`（全站唯一挑量具清單）、以及下面②修好的挑選跳窗類型排列。**②查出「先點量具類型」按鈕排列跟設定頁對不起來的真因**：`inspection_combined_prototype.php` 的 `get_tool_manage_data` 把 `qc_tool` 查詢寫成 `ORDER BY t.Tool_No ASC`，跟 `qc_tool_list` 的排序完全無關；而 `inspection_entry_v2.php` 的 `tpRenderCats()` 又是拿這份**依編號排序的量具清單**、用「哪個類型的第一支量具最先出現」去湊類型按鈕順序——兩層都繞過了管理員設定的 `sort_order`，按鈕順序其實是偶然值。**雙重修正**：SQL 改成 `JOIN qc_tool_list ORDER BY l.sort_order, l.QC_Tool_List_id, t.Tool_No`；JS 另外在 `loadToolInstances()` 保留一份正確排序的 `TOOL_CATS_ORDER`（來自 `res.categories`），`tpRenderCats()` 改用它決定按鈕順序、不再用第一次出現順序推。驗證：`php -l`＋**渲染後**inline JS `node --check`（tool_calibration.php 4 段、inspection_entry_v2.php 7 段）全過；**CLI 直接呼叫 API 驗證排序寫回與完整還原**（把最後一筆搬到最前面存檔→逐格核對新順序與送出的完全相同→還原成原始順序與全部旗標欄位，兩次比對皆 PASS，`disp_fields` 顯示設定筆數在這次操作中維持 0 未被牽動）；**直接呼叫 `get_tool_manage_data` 核對**：修正後 `tools` 陣列的類型首次出現順序與 `categories`（即 `sort_order`）完全一致（目視／游標卡尺／缸徑規／盤式分厘卡…）。測試僅暫時重排後立刻還原，**未變更任何正式類別設定值**。**尚未經使用者瀏覽器實際拖曳點擊驗收**。
> 最後修改：2026-09-24（二次）— **訂單分析：修正「新客戶」誤標——拆成真正的新客戶與回流客戶兩種標籤**（使用者上一輪回報後拍板「改」）。**問題**：客戶比較表的「新客戶」原本只是「比較基期 0、本期有」，跟客戶在系統裡的完整歷史無關；實測 2026 Q3 有 **41** 家被標新客戶，其中只有 **9** 家是系統裡真正第一次出現，另外 **31** 家（如倉佑 2020-08-17、鈞諺 2024-01-30 就下過單）只是比較基期剛好沒下單、這期又回來——**新客戶要問怎麼開發到的，回流客戶要問之前為什麼停了、現在為什麼回來，兩者對業務的意義完全不同，混在一起看只會誤導**。**修法**：新增 `oa_client_first_seen()`（比照料號的 `oa_first_seen()` 同一套做法），查出每個客戶在**出貨／訂單／退貨**三個來源裡最早出現的日期——**刻意不含製令**：製令的客戶要透過料號或訂單反推，作為判定客戶關係起點不夠直接，`bom_client_lib.php` 已經記過用料號文字辨識客戶不可靠，不重複踩那個坑。flag 判定改為：比較基期 0、本期有時，先查這家客戶最早出現的日期是否落在本期——**是**才算真正的新客戶，**落在本期之前**則改標「回流客戶」。前端新增 `.badge-return`（暖色調色盤 amber），客戶比較表／成長衰退排名／CSV 匯出／A3 列印報告的客戶排名都補上回流客戶顯示；排名區塊說明與使用說明跳窗補充兩者定義差異，避免使用者把回流客戶誤讀成新開發的客源。驗證：`php -l`＋渲染後 inline JS `node --check` 全過＋側欄與輸入規則健檢全過；**CLI 直接呼叫函式核對**（41 家精確拆成 9 家真新客戶＋31 家回流客戶，並對照 `order_track`／`is_list`／`ir_track` 逐一核對 `first` 日期）；**無頭 Chrome 實際渲染 9 項全過**（新舊兩種徽章都真的畫出來、排名說明文字正確、回流客戶樣本的 first 日期確實早於本期起日、使用說明含區分說明、零 JS 例外）；既有全套回歸（54＋10 項）與列印報告測試（10 項）全數通過不受影響。**尚未經使用者瀏覽器實際點擊驗收**。

> 最後修改：2026-09-24 — **訂單分析（`views/Sales/Order_Analysis.php`）：新增自動分析、KPI 未達標提醒、金額移動平均監控與通知、A3 列印報告、料號開圖面、回首頁**（使用者交辦六項）。**①料號可點選開啟圖面檢視**：料號排名／新料號明細表格的料號一律帶**料號主檔 PK**（`d_setting.d_id`）開 `bom_viewer.php?pk=`——同名料號常分屬不同客戶，只帶文字會開錯圖，沒綁主檔的標「未綁主檔」不給點。**②右下角回首頁**圓形按鈕，列印時隱藏。**③列印報告固定 A3 橫式**（使用者原話「報告固定用a3橫式列印」）：**刻意不做「A4 放不下自動升 A3」**——`project_mgmt_ui.js` 已踩過這個坑並拿掉：CSS 宣告的紙張尺寸是網頁單方面說的，印表機紙匣實際放什麼是設備決定的，宣告 A3、紙匣放 A4 時 Chrome 會把 A3 版面硬套到 A4 紙上、右邊被裁掉；直接宣告 420mm×297mm（等同 A3 橫式），列印當下用 toast 提醒使用者「列印對話框請把紙張選成 A3」。**圖表一律用畫面上既有 Highcharts 實例的 `getSVG()` 轉成向量圖嵌進報告**，不重新畫一次——這樣畫面看到的圖跟列印看到的圖保證是同一份數據；版面依 ai-rules/16（公司全名動態取、字型統一、表頭跨頁重複、多頁才印左下角頁碼且量寬度用列印實際寬度不是視窗寬度），**本報表不是正式 AS9100 文件（沒有綁定表單編號），只印公司全名不印 AS 編號**（與同批之前做的「客戶季度分析」列印同一個判斷）。**④自動分析**（`oa_insights()`，唯一實作放 `order_analysis_lib.php`）：整體金額/數量漲跌、**連續兩期下滑**（單期比較看不出來的趨勢型警訊）、客戶集中度（前三大佔比≥50%）、流失客戶（基期有下單本期完全沒有）、新料號貢獻佔比、全製/單製結構變化、小量訂單佔比偏高、未開價訂單提醒、KPI 未達標、移動平均警示——**每一條都附具體數字，沒有數字的結論不敢拿去做決定**。**⑤KPI 未達標→本月要衝刺**：讀 `views/news/KPI.php`「月份受訂目標達成金額」的年度設定（`calculator_key='order_target_amount'`，指標 id 不寫死、自動找），**只看最近幾個已經結束的月份**（本月還沒過完，拿半個月的數字判未達標一定是錯的），未達標時算出**本月離月目標還差多少、剩幾天**，達標與否一律呼叫 KPI 模組自己的 `kpi_as_display_value()`／`kpi_as_below_target()`，不在這裡另寫一套判定。**⑥金額移動平均監控與自動通知**（使用者交辦「每月出自動評估前N個月訂單金額移動平均，連續兩個月低於安全水平就自動送通知」）：前 N 月移動平均連續 M 個月低於安全水平（該年度訂單 KPI 月目標金額或管理員自訂金額）就自動通知指定人員，**站內通知＋Web Push＋Telegram 並行**（比照 `comm_ctrl_notify.php` 同一套做法，新增 `order_analysis_notify.php`），**每月評估一次、順路觸發不需要工作排程器**（`order_analysis_tick.php`／`order_analysis_run.php`，掛進 `_config.php`）；**資料品質保護**：當月「有填單價的訂單」佔比低於門檻（預設 60%）的月份一律視為資料不足、不納入評估——本站 2026-03 以前幾乎沒有人填單價，不擋的話每個月都會發出假警報，畫面與通知內容都要講清楚是哪幾個月被排除。**設定跳窗新增「監控設定」**：KPI 提醒看幾個月、移動平均取幾個月、連續幾個月觸發、安全水平來源（KPI月目標／自訂金額）、資料不足門檻、收通知人員（走全站共用 `people_lib`），可先按「用目前設定試算」只計算不發通知。**過程中自己抓到一個剛寫的 bug**：`oa_settings_save()` 存檔前有一行 `$cur = oa_settings($db) + $cur` 想夾上下限，但 **PHP 的 `+` 陣列運算子是左邊的鍵優先**，`oa_settings($db)` 重新去讀資料庫舊值排在左邊，等於**用舊值蓋掉剛剛要存的新值**——測試送 `ma_enabled=1、門檻500萬` 存完讀回來卻還是舊的 `ma_enabled=0` 才抓到。改成把上下限夾範圍抽成 `oa_settings_clamp()`，讀取（`oa_settings`）與存檔（`oa_settings_save`）都直接對記憶體裡的陣列正規化，不重新讀資料庫覆蓋。**測試環境的教訓**：無頭 Chrome 下用 `Runtime.evaluate` 觸發的 `.click()` 呼叫 `window.open()`，**不會被瀏覽器判定為「使用者手勢」，一律被彈出視窗封鎖擋下且靜默回傳 null**（症狀＝測試腳本量到「按了但沒有新視窗」，一度誤判為程式壞掉）——**這只是測試環境的限制，正式頁面上真人點擊觸發的 `window.open()` 不受影響**；測試腳本改用啟動參數 `--disable-popup-blocking` 解決。**又一次踩到 git 共用 index 的既有問題**：收尾時發現 `order_analysis_notify.php`／`_config.php`／`print_log_lib.php`／`OrderAnalysis_API.php` 等六支檔案已被平行 session 的「客戶提醒」commit(`3f0cd44`) 一起夾帶走，逐一比對工作目錄與該 commit 內容完全一致，依既有 SOP 只驗證不 reset；順手把遺漏的 `order_analysis_last_check.txt`（順路觸發狀態檔）補進 `.gitignore` 並取消追蹤，比照 `car_remind`／`comm_ctrl` 等既有模組同一組規則。驗證：`php -l` 全過＋**渲染後**inline JS `node --check`（4 段）＋側欄與輸入規則健檢全過；真 HTTP 端對端（`settings_save` 三種驗證擋下、`ma_preview`、`users`）；**CLI 直接呼叫函式驗證移動平均通知整條鏈路**（用不存在的假使用者 id 測試，確認不會真的推播給任何真人；驗證 `live_event`／`live_event_target` 內容正確、同一期不重複發送、測完以明確 id 刪除並還原設定）；**無頭 Chrome 實際登入點擊 79 項全過**（KPI 卡片、自動分析內容、監控表格與圖表、料號點擊真的開出 `bom_viewer.php?pk=`、列印報告內容逐項核對含 5 張 SVG 圖表與 8 張 KPI 卡與 6 張表格且列印寬度下零元件橫向溢出、監控設定跳窗四種前端驗證與試算按鈕、三種螢幕寬度零橫向捲動、零 JS 例外）。測試建立的角色/設定/live_event/session/Chrome 暫存設定檔已全部以明確 id 刪除，設定已還原為預設值，**未寫入任何業務資料**。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：使用者上一輪回報「客戶比較表的『新客戶』標籤其實是跟比較基期比、不是真的第一次下單」（實測 41 家被標新客戶裡只有 10 家是真的第一次），已提出「新客戶／回流客戶」兩個標籤的改法但使用者尚未回覆確認，本次未動。
> 最後修改：2026-09-23（二次）— **產品開發評估表（`views/TD/td_dev_eval.php`）：修好開啟全表填寫模式後，確認項目按「儲存」反而把資料洗空**（使用者回報「開啟補登，填完確認項目後，就算按下儲存也都沒儲存，應該選擇完自動儲存」）。查證發現 09-23 當天使用者自己測試留下的三張草稿裡有一張（id=98）32 項全部是 NULL，坐實了這個回報。**根因是兩條路徑互相踩線**：①「開啟全表填寫模式」套用管理員預設值時，只把值寫進 JS 變數 `CUR_ANSWERS` 並重畫畫面，**沒有觸發 `#chkBody` 的 change 事件**，所以雖然畫面上看起來已經勾好，卻從來沒被同一批次新增的逐項 autosave（`answer_save`，commit f43f96e）送出存檔——這正是「花時間填完資料又一直不見」的來源：根本沒存過，不是存了又被清掉。②表頭「儲存」鈕（`saveHeader()`）原本會把 `collectAnswers()` 讀到的畫面現狀，以「管理員整批存檔、沒送到的項次一律清成 null」的語意整批覆寫寫回 32 項——這個邏輯是舊版（autosave 出現前）唯一的存檔路徑，**autosave 上線後兩條路徑並存卻沒有互相知會**，只要按下儲存當下畫面沒能完整反映每一項最新狀態（例如尚未渲染完成、或使用者根本沒點過某些用預設值帶入的項目），就會把已經自動存好的答案一起洗成空白。**修法**：全表填寫模式套用預設值當下，比照使用者手動點選改用 `queueAnswerAutoSave()` 立即補送（不再只是畫面顯示）；**移除表頭儲存對確認項目的整批覆寫**——確認項目已經完全由逐項 autosave 專責，表頭「儲存」鈕改為只處理客戶／料號／品名／數量／填表日期／樣品時間，不再觸碰 32 項答案，兩條路徑合併成一條。同步更新全表填寫模式的提示文字（拿掉「未存檔前不會真的變更」這句已經不準確的舊說法）與「使用說明」跳窗，講明確認項目一律逐項即時自動儲存。驗證：`php -l`＋**渲染後**inline JS `node --check`（4 段）＋側欄與輸入規則健檢全過；真 HTTP 端對端重現並驗證修復（建立草稿→模擬逐項 autosave 存入 2 項→表頭儲存不帶 answers 欄位→確認資料庫裡的答案未被清空）。測試建立的草稿與 session 已刪除；**使用者自己 09-23 測試留下的 3 張草稿（id 97/98/104）維持原樣未動**，其中 id=98 那張全部是 NULL 正是本次回報的實例、id=97 與 104 因為只透過逐項 autosave 存過所以資料完整。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-23 — **異常原因分類的「設定」改成逐層挑選畫面（鉛筆改／刪除要先進修改／刪除前偵測使用中並自動移轉）；矯正單與客退單的原因分類改可複選**（使用者交辦）。**①使用者原話：「設定 請改成像附件二這種『選擇異常原因分類』這樣的方式新增/修改/刪除，比截圖1的好用多了」**——設定頁那張「一列一個輸入框＋刪」的表格，跟各表單填寫時挑分類的大方塊是兩套完全不同的操作方式，同一棵樹卻要學兩次。做法是在**共用元件 `resource/js/eg_cause_picker.js` 加「維護模式」**（`manage:{can,url,csrf}`），設定頁只留一顆「維護分類」＋唯讀總覽，**刻意不在設定頁自刻第二套 UI**（鐵律4：挑選與維護是同一棵樹，兩份遲早長出兩種操作方式）。維護模式與挑選模式共用同一份方塊與同一套逐層瀏覽，差別只有三件事：**點方塊＝往下一層看（不是選它）／右上角多一顆 ✎＝開修改面板／底部沒有「確定」，改的東西當下就存**。②**刪除刻意藏在 ✎ 裡面**（使用者指定「刪除則需要進去修改才能按刪除」）——方塊上只有 ✎，誤按不會刪到東西；修改面板裡是「名稱輸入框（直接改文字）＋啟用勾選＋↑↓ 同層排序＋刪除」，**↑↓ 取代原本的拖曳排序**（大方塊上拖曳不好操作）。③**修改與刪除前一律先查「有沒有異常單／矯正單已經選了它」並列出單號**（新 action `cause_usage`，含底下的子孫分類）。**使用中偵測的唯一實作 `qab_cause_usage()` 一定要同時算三個來源**：`qa_abnormal_cause`（異常單複選表）、`car_order_cause`（矯正單複選子表）、`car_order.cause_cat_id`（舊資料可能只有這一欄）——少算任何一個，畫面就會說「沒有人在用」然後把別張單的分類刪成空白。④**刪除有單據在用的分類時請使用者指定移轉對象，按下去就全部自動移轉再刪除**（使用者原話「刪除的部分需要詢問是否轉到別的選項，可訂設定完直接自動全部移轉」），移轉與刪除包在同一個交易；移轉目標的候選**自動排除自己與自己的子孫**（移到自己底下＝刪完一樣會不見），52 個分類所以附關鍵字篩選。**移轉一律「先插目標再刪來源」不可以用 UPDATE**：`qa_abnormal_cause` 是 `UNIQUE(order_id,cat_id)`，同一張單原本就同時選了來源與目標時直接 UPDATE 會撞鍵 1062（這條已寫進函式註解）。⑤**使用者更正「異常原因分類 要可以多選」**：品質異常處理單本來就是複選，**矯正單原本只存得下一個**（`car_order.cause_cat_id`），兩邊口徑對不起來。新增子表 `car_order_cause`（建表時把既有 2 筆自動回填），**`car_order.cause_cat_id` 降為「主要分類」快取、由 `car_cause_set()` 一處同步**，所以既有讀那一欄的程式（列印、清單摘要）一行都不必改；讀取一律走 `car_cause_ids()`（子表查不到時退回舊欄位＝即時退路）。**`save_reply` 收 `cause_cat_ids`（JSON），沒送這個欄位時不可以動原本的分類**——只存矯正措施那一次會把原因分類清空（本專案第 N 次踩「沒送＝不要動它」）。客退單 `IR_Track.php` 開異常單那一頁也一併放寬成複選（後端本來就存得下）。**⑥測試方法上的三個坑**：⑴`cause_save` 帶中文名稱時，**Git Bash 直接在指令列傳中文會以 Big5 送出**，症狀是「回傳沒有 cat_id」（中文 payload 一律 `--data-urlencode name@UTF-8檔案`）⑵**矯正單 `save_reply` 回「已儲存」卻什麼都沒寫**——那不是 bug，是該段已經簽章（`$wCause` 為 false）的既有保護；要測到寫入那一行得先暫時撤銷簽章（測完還原）⑶**點一次方塊整個 grid 就重畫**，測試裡先抓好的第二個元素會變成失效節點、點了沒反應（看起來像複選壞掉），**兩次點擊之間一定要重新挑元素**。驗證：`php -l`＋**渲染後** inline JS `node --check`（12 段）＋側欄與輸入規則健檢全過；CLI 22 項（撞鍵情境、移轉後筆數、快取同步、清空後快取變 NULL）；**真 HTTP**（使用情形、需要移轉、移轉到自己／不存在／有下層皆擋下、正常移轉並刪除、多選存取、沒送分類不可清空、不存在的分類擋下、CSRF、未登入 401）；**無頭 Chrome 實際登入點擊 40＋15＋4 項全過**（鉛筆真的在方塊右上角、點方塊是往下鑽不是被選起來、刪除鈕只在修改面板裡、從畫面新增→改名→刪除一輪、有單據在用時真的跳出移轉清單且不含自己、移轉目標打字篩選、關閉後總覽自動重畫、三種寬度零橫向捲動、零 JS 例外）。測試分類、暫時掛上去的使用列、`car_order` 15（**77 欄逐欄比對與測試前完全相同**）、簽章 `revoked`、session 與 Chrome 暫存設定檔已全部還原刪除；`qa_cause_cat` 回到 52 筆。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-22（五次）— **SOP／SIP（`views/QA/sop_sip.php`）：明細表格可拖曳排序、圖面點了跳窗放大、修好「最後一列按 ↓ 沒反應」、建立完沒存檔自動刪除**（使用者交辦四件）。**①拖曳排序**：檢驗項目、檢驗項目預設值（同一批也給了操作步驟）都可以按住最左邊那一格上下拖曳，唯一實作 `ssSortable()`，走站上既有的 `resource/js/Sortable.min.js`（**不要再引 CDN 版**，站內網路根本連不出去）。**把手刻意只放在「☰ #／項次」那一格，不讓整列可拖**——這幾張表整列都是輸入框，整列可拖等於「在欄位裡按住想反白一段文字」就變成拖列，使用者連把料號或品質特性複製去別的系統查都做不到（2026-09-18 KPI 明細那次踩過同一個坑）。**☰ 一律走 CSS `::before`**：序號那一格的文字是 `renumber()` 用 `.text()` 重寫的，寫進 HTML 的 ☰ 會在下一次重新編號時被整個洗掉。順序就是 DOM 順序，`collectItems()`／`collectSteps()`／`tplSave` 本來就照 DOM 逐列讀、後端 `seq` 也照送出的順序寫，所以拖完按原本的存檔就存得下去（已實測拖完存檔再開，DB 回來的順序完全相同）；唯讀版次不給把手也不啟用。**②圖面點了跳窗放大**（圖面／操作步驟參考圖示／各段落附件圖都可以）：滾輪縮放**以游標那一點為準**（以中心放大的話，正在看的那個尺寸標註會跑出畫面）、按住拖曳平移、符合視窗／100%／另開分頁。另做**「釘在旁邊」**——遮罩改成透明且 `pointer-events:none`，**底下的文件跳窗照樣打得了字**，這才是使用者要的「方便看圖填寫相關資料」；釘住時視窗自動靠右（留在原本置中的位置＝還是壓在表單上，實測它蓋住第一列的管理重點欄），標題列可拖著搬位置。`load`／`error` **不會冒泡**，所以那兩個事件不可以用 `$(document).on()` 委派，一律直接綁在那張 img 上（挑到的圖面可能是 PDF，載不出來要明白說明而不是留一個破圖示）。**③「最後一列按鍵盤下按鈕沒反應」的真因**：共用檔 `eg_input_rules.js` 規則 6 是**不帶參數**呼叫 `data-eg-row-add`／`del` 的，而本頁寫成 `stepAdd($tbody)`／`stepDel($tr)`，呼叫時參數是 `undefined` 當場丟例外，**又被共用檔的 `try/catch` 吃掉**，所以畫面上完全沒有反應也沒有任何錯誤——三組（step／item／tpl）一律改成「沒給參數就自己找那張表、自己取最後一列」，× 按鈕那條路照舊。**往後掛 `data-eg-row-add` 一定要照共用檔的約定寫成無參數可呼叫**（站上其他頁面如 `training_record.php` 的 `dayAdd()`、`communication_mgmt.php` 的 `cmItemAdd()` 都是無參數的，本頁是唯一的例外才會壞）。**④建立完沒存檔＝自動刪除**：按了「新增」又改變主意，清單上會永遠留一份空殼草稿，**而且它還會佔住「同一個對象＋同一個製程只能有一份」的名額，下次要建同一份反而被自己擋下來**。`FRESH` 只在建立當下設起來，**對這份文件有任何一次寫入成功（存檔／送簽／帶圖面／傳附件／改綁定）就清掉**；關掉文件跳窗時，完全沒動過的直接刪並在右下角提示，**動過的先問一句**（確定＝先存檔、取消＝刪除）——自動刪除的本意是清空殼，不是把人家打的字丟掉；直接關掉分頁時以 `sendBeacon` 補送一筆刪除。驗證：`php -l`＋**渲染後** inline JS `node --check`＋側欄與輸入規則健檢全過；**無頭 Chrome 實際登入點擊 47＋31＋28 項全過**（真的用滑鼠拖曳並確認順序存進資料庫、↓／↑ 增刪列、放大與釘選後底下欄位仍聚焦得了、唯讀版次不給拖、1280/1600/1920 三種寬度零橫向捲動、零 JS 例外）。測試建立的 23 份文件與其版次明細已全部以明確 doc_id 刪除（`ss_doc` 回到測試前的 48 筆、最大 doc_id 回到 69），既有文件與預設值的順序逐列比對確認未被動到；測試 session 與 Chrome 暫存設定檔已刪。**尚未經使用者瀏覽器實際點擊驗收**。**這一輪最該記住的測試教訓：無頭 Chrome 的埠被平行 session 佔走時，Chrome 只在 log 印一行 `bind() returned an error` 就改去聽 IPv6（`ws://[::1]:埠`），而測試腳本連的是 `127.0.0.1` ＝ 接到的是別人的瀏覽器**（實測接到了平行 session 正在測 `correction_order.php` 的那一個，還把人家的分頁導走）。**往後啟動前一定要先 `netstat` 確認那個埠沒人在聽，啟動後再驗一次 `/json/list` 裡的分頁是不是自己那個 `about:blank`**；`/json/list` 也會列出擴充功能的背景頁，挑 target 不可以只看 `type==='page'` 就拿第一個。另兩個小坑：`Runtime.evaluate` 帶 `returnByValue` 時**不可以讓運算式回傳 jQuery 物件**（回 `Object reference chain is too long`，一律結尾補 `return 1`）；**拖曳測試兩端都要先 `scrollIntoView` 進可視範圍**，列在畫面外時滑鼠事件根本打不到它（第一版就是因為第 10 列在畫面外，拖了完全沒反應）。
> 前次：2026-09-22（四次）— **新增「訂單分析」（`views/Sales/Order_Analysis.php`＋`OrderAnalysis_API.php`＋唯一實作 `src/common/order_analysis_lib.php`）**（使用者交辦）。入口＝訂單追蹤工具列多一顆按鈕，**本體刻意做成獨立頁面**——使用者明確要求「嚴禁影響現有使用者」，而 `NewOrder_Track.php` 已經 11,600 行、載入路徑上每一件事都直接影響現場每天在用的清單，所以那一頁只新增 17 行（兩個功能碼＋一個權限變數＋一顆按鈕），既有邏輯一行未動。內容：本年度可選 月／季／半年／整年，看**新訂單（料號第一次出現）**、訂單金額與筆數趨勢（疊去年同期）、**全製／單製**、**數量區間佔比（管理員可設定）**、多選客戶比較表、**客戶增減排名與受訂料號排名**，全部用 Highcharts 畫。**五個「不講出來就會被當成程式壞掉」的資料事實，全部寫進畫面說明列**：⑴**`Order_status` 6＝暫停/取消、9＝已結案**（依 `toggleOrderStatus()` 實際寫入的語意），本頁排除 6、計入 9；**注意 `kpi_as_lib` 與 `client_quarter_lib` 把 9 當成「已取消」排除掉**，兩邊口徑不同（那是既有模組，本次未動，但畫面上寫明本頁用哪一種）⑵**訂單金額只算得出「有填單價」的訂單**（2024 年 2,922 張只有 9 張、2025 年 3,628 張只有 11 張、2026 年 2,893 張有 1,970 張），所以逐期回報覆蓋率，**基期覆蓋率低於 30% 時排序自動改用「數量」並說明原因**；**成長率超過 10 倍時一律不印百分比**——拿 2026 的 3,973 萬去比 2025 的 11,740 會算出「+338368%」，那是基期沒有資料不是業績成長 ⑶**「新料號」＝在系統現有資料裡第一次出現**（比對出貨／訂單／製令／退貨四個來源），不等於公司從來沒做過，所以畫面即時印出**四個來源各自最早的資料日期**（出貨 2020-01、退貨 2018-08、製令 2022-08、訂單 2015-04）⑷**還沒過完的期間不可以直接比完整的期間**，基期預設截到相同天數 ⑸對不到客戶主檔的標「未建主檔」。**判定上的三個關鍵決定**：①**料號一律以主檔 id 歸戶**，另外把「同一個料號文字、同一家客戶、卻被建了兩筆主檔」的併起來（實測可少報 14 支假新料號；**料號文字掛在兩家以上客戶底下時刻意不併**，那本來就是不同料號）②**製令開立日用編號回推**（呼叫既有的 `dqa_bom_open_date()`，不自己再寫一次），且製令有 8,348 筆沒綁主檔 id，改用料號文字回查、對到多筆時全部都算（保守方向：寧可少報新料號）③**全製／單製只能用關鍵字規則判定**——`Processing_items` 是**手打的自由文字**（2026 年 900 多種寫法），系統裡沒有任何欄位記著這件事，報價單那邊雖有 `process_group_type` 但 2026 年只有 105 張訂單綁得到報價項目；所以做成管理員可設定的規則，並**把每條規則本期命中幾筆印在畫面上讓他自己校正**。**排名一律依「增減金額（或數量）」排序不是依百分比**（只看 % 的話 1 萬變 2 萬的小客戶會永遠排在 500 萬掉到 400 萬的大客戶前面）。**踩到一個只有真的渲染才看得到的坑**：Highcharts 橫條圖**只給 `chart.height` 是不夠的**，`.chart-box` 的 CSS 固定高度會把容器壓回 360px——30 條的客戶增減排名被壓成 360px，**衰退的那半段整個看不到而且完全不報錯**（實測圖上有 30 個類別、15 個負值點，畫面上只看得到 15 條正值）；要連**容器本身**的高度一起設。權限沿用訂單追蹤模組（`module='order_track'`）新增 `ot_analysis`／`ot_analysis_setting`，**不另開模組與角色**（另開一套只會讓管理員要在兩個地方各指派一次）；設定存 `system_parameters('ORDER_ANALYSIS')`，**沒有新增任何資料表**。驗證：`php -l`＋**渲染後** inline JS `node --check`（4 段）＋側欄與輸入規則健檢全過；真 HTTP 端對端（三種身分、未登入 401、無 `ot_analysis` 403、只有 `ot_analysis` 者看得到但存不了設定、CSRF、四種不合法參數被白名單擋下、區間重疊／無上限在中間／分類不合法／至少一條規則皆擋下、合法儲存後分析即時套用）；**無頭 Chrome 實際登入點擊 54 項全過**（9 張圖都真的畫出資料系列、7 張表有內容、期別下拉隨粒度變、客戶挑選與篩選 166→1、新料號關鍵字 287→5、設定即時驗證與存檔鈕停用、使用說明、三種寬度零橫向捲動、零 JS 例外）；**KPI 與數量區間的數字與直接 SQL 逐格相同**（971 筆／114,609 支／39,736,139 元／875 張有單價／733 支料號）。測試建立的角色與指派、設定、session、Chrome 暫存設定檔已全部以明確 id 刪除，**未寫入任何業務資料**。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：沒有做列印版（使用者沒要求，資料帶去 Excel 走 CSV；CSV 是一次匯出全部區塊不是只有一張表）。
> 前次：2026-09-22（三次）— **內部稽核：產品型態稽核表可依出貨資料抽樣自動建立＋年度單據點檢表四項判定修正**（使用者交辦五件）。**①事前／結束會議紀錄改成「同一個稽核期間只需要各一份」**——原本一張稽核通知單算一份，而同一次稽核分兩天查本來就會有兩張通知單、會議只開一次，於是第 2 張永遠被報成「還沒建立」；同一份會議掛在好幾張通知單上也只算一份（以 meeting_id 去重），已經有一份完成的就算齊、其餘未完成的降為說明（放在「還缺什麼」裡會變成「已完成」卻同時列著缺項，自相矛盾）。**②稽核報告表送出就是完成、不需要核准**（紙本本來就沒有核准格，畫面上也早就只有一顆「送出」）；判定與 `ia_year_status()` 同一個口徑，**舊資料 approved 一律照算**，不然以前核准過的年度會突然變回進行中。**③使用者問「尚未執行是什麼意思？兩次都已經有系統稽核紀錄表而且都結案了」——根因是 `ia_case.status` 只能由管理員在清單上手動改**，現場不會有人記得回去改，於是永遠停在「已發出」；而**年度計畫表的 ◎（實際實施）看的正是 `executed` 旗標**，結果「明明查完了、計畫表上卻沒有 ◎」。改成**看證據**：這張通知單底下只要建了任何一張查檢表就是執行過了；另新增 **`ia_case_exec_sync()` 順路觸發**把 issued 補成 executing／executed=1（日期取 audit_from，與管理員手動改狀態那條同一個口徑），實測把 2025 年 251203001、251204001 兩張補成執行中。**④「目前情形／還缺什麼」欄整欄靠左**，目前情形當標題、還缺什麼退縮一層。**⑤產品型態稽核表（2-DC-03-02）新增「自動建立」**：選月份 → 從那幾個月**實際出貨過的料號**隨機抽（預設 3 筆，管理員可改預設值）→ 一個料號一張稽核表，題目＝該料號型態識別文件管制表的項目列，每筆可「換一筆」或「移除」。**四個不處理就會變成「抽出來卻建不了」的現實問題**：⑴**出貨的料號多半還沒有管制表**（實測 2026 年出貨 1,881 個料號只有 129 個有），所以抽中沒有管制表的就在建立當下呼叫 `type_id_ctrl_sync_part()` 自動建起來（不另刻一份）⑵**查不到任何文件的料號一律不建立、也不可以先把管制表建起來再略過**——那會在型態識別文件管制表那一頁留下一堆 0 項、而且是「系統自動同步」建的空管制表，沒人知道哪來的 ⑶**管制表已經建了卻 0 項**（建立當時還沒有文件）要同步一次才有題目，但**已經有項目的管制表刻意不重新同步**——同步會把已確認的管制表改成「需重新確認」，等於稽核動作反過來改到被稽核的文件 ⑷**預設不抽本年度已經稽核過的料號**。**母體分類刻意不用 `type_id_ctrl_find_missing_parts()`**——那一支為了求完整會用 `JSON_CONTAINS` JOIN 報價附件，**實測跑超過兩分鐘**，抽樣跳窗一開就卡死；改用新的 `ia_type_doc_hint_map()`（料號附件＋PFMEA＋產品開發評估表，三個都有索引，實測 0.08 秒）當**偏保守的下界**，真正的項目數由 `ia_type_sample_eval()` 對抽中的那幾筆逐一精算。**排除客戶／料號在設定維護，一律打字從主檔搜尋點選、只存 id**（客戶編號是 `char(11)` 文字不可 intval；手打一個對不上的字，那條排除永遠不會命中而且完全不報錯），名稱一律即時查主檔所以改名不會失效。**訂單號碼**由「該料號最近一次出貨綁到的訂單」自動帶，但**出貨單多半還沒做訂單綁定（實測 2026-07~08 只有 11%）**，空白是正常的，畫面已寫明。驗證：`php -l`＋**渲染後** inline JS `node --check`（10 段）＋側欄與輸入規則健檢全過；真 HTTP 端對端（抽樣、建立、空料號略過且不留空管制表、五道守門、未登入 401、無稽核員權限 403 四支）；**無頭 Chrome 實際登入點擊 42 項全過**（點檢表四項判定、抽樣跳窗、換一筆真的換掉那一列、移除、沒選月份與筆數超上限在送出前被擋、設定的主檔搜尋與標籤增刪、三種寬度零橫向捲動、零 JS 例外）。測試建立的 2 張稽核表、2 張管制表、session 與 Chrome 暫存設定檔**已全部以明確 id 刪除**（`ia_check` 最大 id 與 `type_id_ctrl_doc` 最大 id 都回到測試前的值）。**尚未經使用者瀏覽器實際點擊驗收**。**已知未動**：`ia_check` 裡有一張 2026 年 kind=type、標題「T1」的查檢表（今天 15:40 由超級管理員建立），研判是使用者自己的測試，未刪除。
> 前次：2026-09-22（二次）— **異常矯正處理單（`views/QA/correction_order.php`）：補資料的代簽與自動存檔體驗、處理軌跡不再被洗版、列印版重做**（使用者連續交辦兩批）。**①按「代簽」就直接簽**（不再 confirm 一次又 alert 一次），結果寫在補資料面板的狀態列；**②動作完成改成原地更新不重畫整頁**——原本每做一個動作就 `openView()` 重建，**正在輸入還沒存檔的字會直接不見、畫面還跳回最上面**；新增 `snapView()/applySnap()/refreshView()`，只記「使用者真的動過（input/change 標記 dirty）」的欄位與捲動位置，沒動過的一律採用伺服器回來的新值。**③補資料模式改到哪存到哪**（900ms 防抖），三段內容與面板欄位都涵蓋。**④代簽人員下拉「排列亂七八糟」的根因很值得記**：前端去重時把人放進物件當 map 再 `Object.keys()` 取回，**員工編號是數字字串，JS 規格會把這種鍵自動改成「數字由小到大」**，後端排好的部門→職稱順序整個被打散（同一個部門的人散在清單各處）；改成保留陣列順序、最後再依 `dept_sort/position_sort` 重排（`bf_people` 一併回傳排序鍵）。**⑤章格自動帶人**：主管簽核＝責任單位裡職級最高的主管（責任單位是廠商時＝生管主管，與正式流程 `car_primary_pool_ids()` 同一份名單）、總經理核准＝全站 `org_role_setting` 綁定的最高核准人員（沒綁才退回本模組設定，**禁止寫死人名**）；扣款判定刻意仍留白。**⑥處理軌跡合併**：改成自動存檔之後打一段字會寫幾十列 `bf_edit`（實測使用者那張單累積 181 列），分兩層處理——寫入當下就合併（`car_log_merge()`：同一人、同一動作、10 分鐘內直接更新上一列，**欄位異動逐欄併成「最早的舊值 → 最新的新值」**，改回原值的欄位不再列出）；顯示時再合併一次（`car_acts_merge()`）把改版前的舊紀錄也收乾淨，並標「連續修改 N 次、從幾點開始」。**簽章／指派／核准／退回一律不合併**（使用者明確要求）。**⑦設為結案時自動檢查六格應簽章**（扣款判定除外——那是結案之後而且不一定要扣款），前端即時算、後端 `bf_save` 與 `bf_sign` 各回一次 warn，**刻意只提醒不擋下**：補的是幾年前的紙本，真的少蓋一格也要存得進去。**⑧列印「超滿版」**（使用者回報「很多之前調整好的列印都變成貼著紙邊」）：**瀏覽器列印跳窗的「邊界」被設成「無」時，CSS 的 `@page margin` 會被整個蓋掉**，凡是靠 `@page margin` 留白的頁面就會變滿版。單張列印改成 **`@page{margin:0}` ＋ 由 `.sheet` 自己給 padding**（白邊變成內容的一部分，列印跳窗選哪一種邊界都印得出來，站上 business_trip／doc_apply／training_record／inspection_entry_v2 等頁早就是這種寫法）；**多頁的清單只能靠 `@page`**（padding 只作用在第一頁與最後一頁），那一份維持 `@page` 並把邊界由 9mm 調回 14/12/16mm。**⑨圖章改成 Word 式文繞圖**（`float:right`，**要放在文字前面** float 才繞得起來），文字自動繞開、不再被擠成新的一列把欄位撐高。**⑩對應單號自動換行**：單張列印改 `table-layout:fixed` ＋ `word-break`，「2-GM-06-03 2025年度績效執行稽核查檢表」這種長單號只會換行、不會把欄位撐寬。**⑪限一張 A4 直式**：載入後量 `.sheet` 高度與 A4 可印高（297mm＝1123px），超出就把字級一級一級調小，**最小 10px**（使用者指定）；**到了 10px 還放不下就讓它自然換頁，不裁掉任何資料**（實測三段各 1320 字時是 3091px／4 頁）。驗證：`php -l`＋**渲染後** inline JS `node --check` 4 段＋側欄與輸入規則健檢全過；CLI 單元 24 項；真 HTTP 端對端新測 21＋14 項、前幾批 33／29／12 回歸全過；**無頭 Chrome 實際登入點擊 17＋18＋14＋22＋21 項全過**（含**以 `Page.printToPDF`＋`preferCSSPageSize` 真的排成 A4 直式數頁數＝一般單據 1 頁**、量出四周白邊、圖章 float、零橫向溢出、零 alert、零 JS 例外）。測試單據（45~55）、測試分類、暫時角色與 grant、session 與 Chrome 暫存設定檔**已全部以明確 id 刪除**，異常單 #40 已還原；**使用者自己在分類樹最上層建的「測」未動**（不確定是不是測試用的，要刪請自行決定）。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：其他頁面的列印滿版問題只做了說明與判斷方式，沒有逐頁改（要改哪幾頁等使用者指名）。
> 前次：2026-09-22 — **出貨紀錄分析（`views/Sales/Shipping_Analysis_new.php`）：修好列印超出紙張＋新增「客戶季度分析」（逐季趨勢／自動判斷成長衰退客戶）**（使用者交辦三件）。**①列印超出範圍**：列印版的格線寫 `grid-template-columns:25% 50% 25%` 再加 `gap:8px`——**百分比是相對容器寬度算的，gap 是額外加上去的，所以總寬必定超出**（實測 A4 橫式可印寬 1062px 時內容跑到 1066px），右邊那一欄的金額就被紙張邊界切掉。一律改成 **`minmax(0,25fr) minmax(0,50fr) minmax(0,25fr)`**（fr 會自己先扣掉 gap；**`minmax(0,…)` 不可省**，`1fr` 的最小值是 auto，長料號大金額照樣撐破）。同批補上表頭跨頁重複、資料列不被切開、多頁才印的左下角頁碼。**頁碼那個「多頁才印」的判斷有個全站都可能踩的坑**：ai-rules/16 的寫法是量 `body.scrollHeight`，但那是在**列印視窗自己的寬度（1280px）**下量的，真正列印卻是用 **A4 扣邊界的 1062px** 排版，寬度不同高度就不同——摘要版在 1280px 量到 753px 判成多頁，實際輸出只有 1 頁，於是單頁報表也印出「第 1 頁／共 1 頁」；正解是**先把 body 暫時縮到列印寬度再量、量完還原**（實測 753→714px，正確判定不印）。**②③客戶季度分析**（工具列按鈕開啟的新面板，**資料自己向後端查、不受上方日期區間影響**）：分頁一「客戶趨勢」＝選一家客戶（客戶欄打字搜尋名稱或編號，清空＝全部合計）看逐季訂單／出貨／退貨／淨額柱狀＋折線圖與明細表，直接給「較上一季」「較去年同季」，可含前一～三年度；分頁二「成長／衰退排行」＝自動把所有客戶跟去年同季（或上一季）比，**依「增減金額」排序不是依百分比**（只看 % 的話 1 萬變 2 萬的小客戶會永遠排在 500 萬掉到 400 萬的大客戶前面），並標出新客戶／本季掛零／**連續兩季以上下滑**（趨勢型警示，單季比較看不出來），兩期都低於門檻的小額變動收進摺疊區。**計算一律走新共用庫 `src/common/client_quarter_lib.php`**（客戶歸戶＋季歸屬的唯一實作，見下方路由表）。**④三個「不講出來就會被當成程式壞掉」的資料事實，全部寫進畫面說明列**：⑴**訂單金額只算得出「有填單價」的訂單**——實測 2024 年 2,916 張訂單只有 **9 張**有單價、2025 年 3,410 張只有 **10 張**，2026 年才開始正常填，所以舊年度的訂單金額就是一排 0（說明列逐季印出「有單價/總張數」）⑵**還沒過完的季不可以直接拿去跟完整的季比**，整批客戶都會看起來在衰退——排行頁預設把比較的那一季也**只算到同樣的天數**（`cap_days`，實測 2026Q3 已過 89/92 天，基期由 2361 萬縮到 2270 萬、成長率 6.6%→10.9%），分析季預設也停在「最近一個已結束的季」⑶對不到客戶主檔的客戶標「未建主檔」並指引去對帳作業建別名（實測 271 家只有 4 家對不到）。**⑤修掉一個只有真的跑資料才看得到的 bug**：**客戶編號在四張表都是 `char(11)` 文字**（C2005、T2001…）**不是整數**，第一版 SQL 寫 `NULLIF(col,0)`，**MySQL 會把 'T2001' 轉成數字 0 來比，整欄判成 NULL**，於是「綁了客戶主檔的資料照樣被當成沒綁」、歸戶安靜退化成只剩名稱比對且完全不報錯；一律用 `NULLIF(TRIM(col),'')`，PHP 端也不可 `intval()`（改完對不到主檔的由 8 家降為 4 家）。**⑥本頁原本沒有「使用說明」跳窗（鐵律7 既有缺口）已補上**，並補載入 `eg_date_fmt.js`（新面板日期顯示走 `egFmtDate()`）；**本頁在 `input_rules_baseline.txt` 基準線內（沒載 `eg_input_rules.js`），所以新加的下拉不可以掛 `data-eg-filter`——那會是死屬性**，客戶挑選改用本頁既有的自動完成模式。驗證：`php -l`＋**渲染後** inline JS `node --check`（4 段）＋側欄與輸入規則健檢全過；真 HTTP 測兩支 API（未登入 302、不合法的口徑參數被白名單擋下不進 SQL）；**無頭 Chrome 實際登入點擊 65 項全過**（客戶搜尋與切換、對齊機制實際生效、帳款季與日曆季切換、列印版、三種寬度零橫向捲動、零 JS 例外、既有出貨明細與 KPI 週報未受影響）。**未寫入任何業務資料**（三張來源表筆數與測試前完全相同），測試 session 與 Chrome 暫存設定檔已刪除。**尚未經使用者瀏覽器實際點擊驗收**。**測試方法上的兩個教訓**：⑴用 node 腳本插入大段程式碼時，**`String.replace` 的替換字串裡 `$'`／`$&` 是特殊樣式**，而 JS 區塊滿是 jQuery 的 `$('#...')`，用字串形式會把整份文件塞進去（實測暴增 4 倍）——一律改用索引切片或函式形式，**並在寫入前驗證長度變化**（本次就是靠這個擋下來的）⑵斷言要真的驗到東西：第一版用「衰退家數」驗對齊機制，結果對齊前後都是 39 家（**通過但根本沒驗到**），改成驗「基期金額有沒有變小」才測得出來。
> 前次：2026-09-21（七次）— **資料稽核：三個節點可以直接綁定、報價節點補上數量、新增分頁三「報價項目追蹤」**（使用者交辦一整批）。**①使用者回報的第一件事很小但很致命：報價欄只印日期與單價，沒有印報價數量**——而「報價數量與訂單數量不符」正是這一頁最常出現的那條缺失，看不到數量就根本核對不了。已補上數量（階梯報價標「階梯報價」）。**②「確認無誤就可以直接綁定」**（使用者原話）：缺失的成因十之八九就是**沒有做綁定**，查出來之後還要換到報價單管理、BOM 總表、快速出貨各綁一次，實務上就不會有人去綁，缺失永遠掛在那裡。所以報價／製令／出貨三欄各加一顆綁定鈕（限 `dqa_admin`，使用者拍板沿用現有角色不另開）。跳窗最上面**一定先把「這張訂單目前有沒有報價、有沒有製令、有沒有出貨單」列出來**（使用者要求「上面也要直接顯示是否有此筆出貨單」）；候選清單標出「已分配給幾張訂單／已掛在別張訂單／日期早於訂單／未開價」；報價候選附**製程、料號備註（`quotation_item.specification`）與階梯數量區間**，並可展開**整張報價單的所有項目**（含治具刀具與備註列，每列標有沒有訂單、有沒有出貨）。**寫入一律走 `trace_chain_lib` 的 `tc_link()`／`tc_unlink()`，一行寫入邏輯都不自己刻**——與快速出貨的「追溯對照」是同一套分配表，在哪一頁綁都一樣。**綁完只重算那一列**（`dqa_trace_rows` 新增 `order_ids` 篩選），不必整份重跑（重跑要掃幾千張訂單）。**會動到既有綁定一律先跳確認並講清楚會改到什麼**（使用者明確要求）。**③報價→訂單改成多對多**（使用者拍板「一併做成多對多」）：新增 `order_quote_map`，`order_track.quote_no/quote_item_id` **降為「主要報價」快取**、由 `tc_sync_order_quote()` 一處同步——**主要報價刻意取「料號與訂單相同者優先」不是分配量最大者**，不然治具那一列會被選成主要報價，既有程式讀 `quote_item_id` 就拿到治具的單價。`acc_recon_bind_quote()` 仍是 `order_track` 上那兩欄的唯一寫入者（新增 `force_part` 參數給快取同步用），對帳頁改走 `tc_link()` 但**維持原本「料號不同一律擋下」的嚴格規則**（那邊綁報價是為了比單價），料號檢查抽成 `acc_quote_part_match()` 兩邊共用。**④新增分頁三「報價項目追蹤」**：分頁一是**以訂單為主軸**，所以「報價了但訂單根本沒建立」那種缺失在分頁一**永遠不會出現**（那一列訂單不存在），主軸必須換成報價項目。最該看的是 **`q_line_no_order`「報價已成案，這一列卻沒有訂單」**＝整張報價單其他列都開了訂單、只有這一列沒有，**治具刀具最常漏開，漏了就是做了卻收不到錢**；「整張報價單都沒有訂單」多半只是沒接到案子，**預設關閉**（不然 3,000 列會報出 2,458 筆，真正的重點被淹掉——改成這個口徑之後是 108 筆）。**⑤新增金額檢核**：報價／訂單／出貨金額為 0（實測 2026 年訂單 894 筆、報價 32 筆、出貨 5 筆），**排除「被設定為備註」的**——報價看 `quotation_item.note_only`、出貨沿用出貨分析頁既有的 `is_list.anomaly_confirmed`（不另外發明一個旗標），訂單沒有這種旗標請用「標為例外」。收款依使用者指示**分兩項可各自關閉**：`amt_zero_ship`（出貨未開價）預設開、`ship_norecon`（未進對帳／未開發票）**預設關閉**——實測 `acc_invoice_item` **0 筆**、`acc_recon_line` 只有 84 筆，會計模組幾乎沒在用，打開會整片報未收款。**這一輪修掉三個既有 bug，都只有真的跑起來才看得到**：⑴**`tc_seed_bom_order()` 把指向「已經被刪掉的訂單」的 `bom.o_order_id` 補進分配表，外鍵擋下丟出 PDOException，而那一句在 `tc_link()` 的 try 之外 → 整支 API 變成 HTTP 500 空白回應**，畫面上就是「按了完全沒反應、也沒有錯誤訊息」（追溯對照那一頁也會踩）；已改成補列前先確認訂單存在，並把三個 seed 一律包 try。⑵**`CREATE TABLE IF NOT EXISTS` 在交易中會造成 MySQL 隱式 commit**，外層 `commit()` 爆「There is no active transaction」——**而資料其實已經寫進去了**，畫面顯示「解除失敗」實際上卻已解除，是最容易誤判的那種症狀（本專案 2026-08-03 在 `eg_org_save()` 踩過同一個坑，這是第二次）；已改成「先 `SHOW TABLES` 確認不存在、且不在交易中，才下 DDL」。⑶`DataAudit_API` 未捕捉例外一律回空白 500，已加 `set_exception_handler` 轉成 JSON。**效能**：分頁三第一版用兩個 `EXISTS` 判斷「整張報價單有沒有成案」，3,000 列要 **12 秒**；改成 JOIN 並補上 `idx_ot_quote_item`（`order_track.quote_item_id` 原本完全沒有索引）之後是 **0.12 秒**。驗證：`php -l`＋**渲染後** inline JS `node --check`（4 段）＋側欄與輸入規則健檢全過；**真 HTTP 端對端全過**（三種節點的候選、整張報價單、綁定／解除／多列綁定、超量被擋、未登入 401、檢閱者帶正確 CSRF 仍 403、CSRF 錯誤、訂單不存在、節點代碼不合法）；**無頭 Chrome 實際登入點擊 43 項＋真的從畫面綁一次 10 項全過**（報價欄真的印出數量、三個節點都有綁定鈕、跳窗表頭列出三節點現況、取消「只列同料號」候選 2→120 筆＝治具才找得到、整張報價單開得起來、分配量輸入框、分頁三 108 筆、設定頁五個新項目與「未進對帳預設關閉」、三種寬度零橫向捲動、零 JS 例外；**從畫面按下綁定→該列即時由「推測」變「綁定」且報價數量由 1 變 300→再綁一列料號不同的報價時確實先跳出確認並說明理由→解除後回到原狀**）。測試建立的綁定、稽核紀錄（`audit_log` ACC_RECON）、暫時指派的 `dqa_view` 角色、session 與 Chrome 暫存設定檔**已全部以明確 id 刪除**，`order_quote_map`／`is_order_map` 回到 0 筆、`order_track` 測試訂單的 `Modified_By/At` 已還原為 NULL。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：分頁三沒有做「留存稽核結果」與開立不符合通知單（那兩個目前只有分頁一有）；`is_list.sale_type` 與 `anomaly_confirmed` 全庫都是空的／0，所以「備註列」的排除對出貨目前等於沒有作用，要等現場真的開始用出貨分析頁的「確認非異常」才會生效。
> 前次：2026-09-21（六次）— **品質異常處理單：決策者顯示名稱自動帶、設定一鍵存檔、製令與客退單一定要綁定、料號跟著鎖定、檢驗數依抽樣規則**（使用者連續交辦五件）。①**決策者的「顯示名稱」不再手打**（使用者原話「顯示名稱應該自動抓取部門+職稱」）：`qab_decider_cfgs()` 一律即時由**部門＋職稱**組出（沒選職稱寫「不限職稱」），欄位改唯讀、選到什麼就即時顯示什麼，後端 `label` 一律存 NULL；使用者原本手打的那一列也已清掉。**存一份文字下來就是鐵律4 的老問題**——部門或職稱改名之後它會繼續顯示舊名稱而且不報錯。②**四張設定表各加一顆「一鍵存檔（本頁全部）」**：新增 `cfg_save_all`，**逐列套用與單列存檔完全相同的規則**——把三段驗證與寫入抽成 `qabSaveCause()`／`qabSaveOpt()`／`qabSaveDecider()`，**單列鈕也改呼叫同一支**，否則兩條路遲早走鐘；整批包在 transaction 裡，**有任何一列不合法就全部回滾並指出是第幾列**，不會只存一半。③**製令編號與客退單號「有輸入就一定要綁定」**（使用者要求）：前端只打字不選會即時紅字並擋住存檔，**後端同規則再擋一次**（製令不存在回 `BOM_NOT_BOUND`、客退單只送文字或 id 不存在回 `IR_NOT_BOUND`），**而且擋下時不可以把原本的值洗掉**。**客退單改以 `ir_id` 綁定不是單號**——同一個 IR 單號會有好幾筆明細，只靠單號分不出是哪一筆（這與料號主檔同名多筆是同一種坑）。④**料號比照客戶**：綁了來源就由來源自動帶並鎖定，連**料號主檔 id**（新欄位 `part_d_id`）一起存下來，兩者都沒綁才可自行填；新增 `qab_resolve_source()` 一次帶回客戶＋料號＋批量，四個呼叫點共用。⑤**檢驗數依線上檢驗的抽樣規則自動帶建議值**：抽樣規則查詢抽成 `qc_inspection_lib.php` 的 **`qc_suggest_sample_qty()`（全站唯一實作）**，`inspection_combined_prototype.php` 那一支內嵌 SQL 也改呼叫它（實測 `order_qty=40` 仍回 `sample_qty=5`，與改版前相同），**兩邊不會再算出不同的建議數**；異常單新增 `suggest_sample` 端點，開單與批量異動時自動帶、**人工改過就不再覆蓋**（那是他刻意填的數字）。填了不良數即時算出**不良率**（含分子分母）。驗證：`php -l`＋**渲染後** inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP 新測試 31＋20 項、前幾批回歸 94＋56 項全過；**無頭 Chrome 實際登入點擊 85 項全過**（料號欄確實唯讀並標示來源、抽樣建議出現、改不良數不良率跟著變、只打字不選的製令在按存檔前就被擋下）。測試單據（33~40）與測試分類、`live_event`／`print_log`／`qa_scrap_seq` 測試列、session 與 Chrome 暫存設定檔已全部刪除（**使用者自建的 3 列決策者設定與自己的 `qab_admin` 指派未動**）。**尚未經使用者瀏覽器實際點擊驗收**。**測試紀律再記一次**：這一輪有兩項失敗是**前一輪測試自己留下的**（上一次斷言把 `TYPED-NOT-PICKED` 留在製令欄、補章那一列已經有同一個人），斷言一律要相對於「這次自己建立的狀態」並在用完後還原欄位。
> 前次：2026-09-21（五次）— **內部稽核：①資料稽核查出來的缺失可以直接開成不符合通知單 ②③不符合通知單新增三段佐證附件**（使用者交辦三件）。**①使用者原話：「稽核要查詢訂單資料是否有缺失，無報價單、出貨後才開立訂單…這種，已經有做 `views/ADM/data_audit.php` 來檢核，但還是很難查」**——查證後根因是**兩個模組完全沒有串接**：`dqa_summary_for_docs()` 這支註解寫著「給內部稽核用」的函式**全站一個呼叫端都沒有**，所以資料稽核查出幾千筆缺失之後，只能用眼睛看完再到內稽從頭手打一張 IA 單，於是「一直開不出不符合通知單」，連帶「開立後還需要有改善」讓舊資料更補不起來。**做法：在缺失那一行直接開單**——**勾選的最小單位是「一條缺失」不是一張訂單**（同一張訂單的「查不到報價單」與「出貨早於訂單」是兩個不合格事實、兩張單，甚至是兩個不同的受稽單位；防重複的鍵也因此是**「訂單＋檢核項目」**＝新欄位 `ia_nc.src_code` 配合 `src_kind=dqa`，只比 `src_item_id` 會把第二張擋掉）。單筆按「開IA單」、多筆勾選後按工具列「開立不符合通知單（N）」（上限 50 張），**勾選在換頁與換篩選之後仍然留著**（存的是那一筆缺失的資料不是 DOM 狀態）；已經開過的那一條直接顯示 IA 編號與階段。**②最關鍵的設計決定：對照表的形狀是「檢核項目 → AS 文件」而不是「檢核項目 → 部門」**——IA 單一定要有受稽核單位，而資料稽核的一列是**一張訂單**不是一個部門；但每一種缺失本來就對應到一份 AS 表單，而 **AS 文件編號第二段就是部門代碼**（`ia_doc_dept_code()`＋新增的 `ia_as_dept_code_ids()`），所以一張對照表**一次推導出三個欄位**：受稽核單位（還會過 `ia_unit_key_of_dept()` 換成受稽單位群組的代表部門）、相關表單編號與名稱、違反條文（`ia_clause_map_by_doc_no()` 反查）。**多設一張「檢核項目→部門」就是同一份資訊有兩個來源（鐵律4）**。沒設定的項目退回「稽核對象」綁定的第一份文件，單照樣開得出來、只是單位要自己選。寫入一律呼叫既有的 `nc_create`，**不另開端點**。**③佐證附件（使用者問「改善有讓受稽單位上傳附件嗎」＝沒有）**：`ia_attach` 這張表 2026-08-25 就建好了，但**整個內稽模組從來沒有做過附件上傳 UI**，四段到這次為止只有文字欄位。新增 `section` 欄（sec1 稽核員／sec2 改善／sec3 驗證，空值＝舊資料視為段一），唯一實作 `ia_attach_rows/_add/_one/_del`。**能不能傳一律沿用 `ia_nc_stage_perm()`「這一段能不能填」，不另發明一套權限**——所以**段三要等受稽單位按了「送出回覆」才附得了檔**（實測 stage=issued 時後端就擋下，畫面上 `lockSec()` 本來就會停用 input、隱藏 button，唯讀時只要多做一件事＝**不輸出「刪除」那個 `<span>`**，因為 lockSec 管不到非 button 元素）；結案後三段都不能再動、但已上傳的仍看得到點得開。單檔 20MB，`.php/.exe/.bat` 這類可執行與腳本副檔名一律擋下（附件目錄在 NAS 上，點下去就執行了）。**這次踩到的兩個坑**：⑴**存檔名的時間戳一定要取 DB 的 `NOW()` 不是 PHP 的 `date()`**——本站 PHP 是 UTC、MySQL 是本地，混用會讓「檔名上的時刻」跟 `uploaded_at` 差 8 小時，在 NAS 上對帳完全對不起來（第一版就印出 `014830` vs DB 的 09:48）；⑵**DB 寫不進去時要把剛落地的實體檔 `unlink` 掉**，不然 NAS 上會留一個沒人認得的孤兒檔。**與平行 session 的交會**：工作到一半 `git log` 出現一支不是我下的 commit `d8db150`「資料稽核：開單前置資料改用 POST，修掉 HTTP 414」——那是**平行 session 因為共用 `.git/index` 把我未完成的檔案一起 commit 走、而且順手修掉我的一個真 bug**：`loadNcMeta` 把每一張訂單 id 接成查詢字串用 GET 送出，**1854 張訂單就超過 Apache 的 `LimitRequestLine`(8190) 直接回 414**，請求在到達 PHP 之前就被擋掉、後端連 log 都沒有。**我自己測不出來是因為測試期間只掃 9 月（214 張）**——**往後「把一整份清單的 id 送給後端」一律用 POST，不要用查詢字串**。依既有 SOP 只驗證不 reset：已確認他們的修正在我的工作目錄裡完整存在、我的程式也完整在他們的 commit 裡。驗證：`php -l`＋**渲染後** inline JS `node --check`（4＋10 段）＋側欄與輸入規則健檢全過；**真 HTTP 端對端 45 項全過**（對照推導出業務課與條文、不存在的項目與文件被擋、同訂單不同項目各一張、重複開單 409 且訊息講出是哪一張、三段附件上傳下載刪除、`.php` 擋下、不存在的段別擋下、下載內容逐位元組相同、五道權限守門、未登入 401）；**無頭 Chrome 實際登入點擊 30＋16 項全過**（缺失列真的長出勾選框、換頁再回來勾選仍在、開單跳窗自動帶好單位與不合格事實、對照跳窗 16 個項目、**從畫面真的選檔案上傳再刪掉**、段三唯讀時上傳鈕確實不見、1280/1600/1920 三種寬度零橫向捲動、零 JS 例外）。測試建立的 IA 單／附件／通知／`ia_report`／對照設定／session／Chrome 暫存設定檔**已全部以明確 id 刪除**，DB 與 NAS 資料夾都回到測試前狀態（`ia_nc` 3 筆、`live_event` 853 筆、附件 0 筆）。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：列印版（2-GM-06-07）沒有把附件清單印上去（紙本上沒有這一欄）；`dqa_summary_for_docs()` 在內稽端顯示「這份表單最近一次資料稽核發現幾筆問題」仍未串接（那是另一件事，本次未動）。
> 前次：2026-09-21（四次）— **品質異常處理單：最終決策者改成自動套用全站的組織角色綁定**（使用者要求）＋**順手修掉一個會讓徵詢流程整個斷掉的權限漏洞**。**①最終決策者（總經理裁示）＝`org_role_setting.php` 的「最高核准人員」**（`qab_gm_person()`：取 `eg_org_user($db,'top_approver')` 再過 `delegate_lib` 的代理解析），**本模組不再自己留一份最高決策者名單**——`qa_decider_cfg` 的 `kind=top` 停用、設定頁移除該選項、後端 `decider_save` 收到 `kind=top` 一律擋下並指回那一頁；設定跳窗與處理頁都直接顯示「現在是誰」並附連結。換人只在組織角色綁定改一次，全站表單一起跟著換——**這就是不留第二份設定的理由，兩份遲早對不起來**。**②`canGm` 刻意不含 `canAdmin`**：讓異常單管理員也能蓋最終裁示，等於把「統一綁定」整個架空；管理員要補歷史單的章走補資料（`canBackfill`）。實測三種身分：超級管理員 canGm=1／綁定的最高核准人員 canGm=1、canBackfill=0／`qab_admin` 管理員 canGm=0、canBackfill=1。代理人代簽時寫 `gm_by_deputy`，列印圖章右下角加「代」字（ai-rules/18）。**③測試抓到的真漏洞：被徵詢意見的人多半完全沒有本模組角色，原本通知點進來是 403，整個「相關單位意見」流程等於斷掉。**新增 `qab_can_view_order()`：沒有模組檢視權時，只要是「跟這張單有關的人」（開單人／共同編輯者／被徵詢的本人或該部門／追蹤人）就看得到、也回得了覆，但**仍然不能做決策、也看不到整份清單**；API、處理頁、列印頁三處共用同一條判定。**④這一輪最該記住的測試紀律（CLAUDE.md 講過、我又踩一次）**：測試帳號 109110201 在我跑第一批測試之後，**被使用者自己指派了 `qab_admin`**（那是正式設定，不可動），於是「非管理員應該被擋下」那幾條斷言整批失真。**寫死測試帳號就是在賭使用者不會動設定，而他隨時在動**——已改成**執行當下動態挑一個「本模組完全沒有角色」的在職者**當一般使用者，斷言才不會被即時設定打臉。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；**真 HTTP 56 項＋前批 94 項回歸全過**；**無頭 Chrome 79 項全過**（含設定頁的最終決策者說明與連結、處理頁顯示最終決策者是誰、不再有「新增最高決策者範圍」鈕）。測試資料、session 與 Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-21（三次）— **品質異常處理單：客戶改由來源自動綁定＋新增「補資料」模式（可指定補章人員與印章日期）**（使用者交辦三件）。**①客戶不給手打**：綁了製令或客退單就由來源的**料號主檔**自動帶（`qab_resolve_client()`：`d_setting.Customer_Id` → `customer_list`；主檔沒綁才退回來源單的客戶文字，並走會計模組既有的 **`acc_customer_by_name()`（含別名）**歸戶——ERP 寫「義高工業」主檔是「義高」，只比完全相同的字串**實測 366 筆客退單有 51 筆對不到**）。同一個料號文字在 `d_setting` 常分屬好幾家客戶，手打或用文字猜一定會歪；畫面上該欄位綁定時唯讀並註明來源，**後端 `save_head` 一律重新解析、不採信前端送來的客戶名稱**（實測送 `HACK-CLIENT` 進去改不動）。新增欄位 `qa_abnormal_order.client_id`。**②補資料模式**：填寫日期在「**今日往前 N 天以前**」（`backfill_days`，**預設 10**、管理員可調）的單自動視為補資料（`qab_is_backfill()`，實測 `-11 天=當期`／`-10 天=補資料`）。**③補章人員與印章日期**：補資料的單多出「補登簽章」區，六個簽章格（承辦／主管／總經理／生管／品管／核准，唯一登記表 `qab_sign_slots()`）逐格指定人員與日期——**人員清單依印章日期回推當時在職者**（`eg_people_list_asof()`，**當時在職、現已離職的人也選得到**＝ai-rules/22 第5坑），印章日期不可以是未來，後端以 `qab_user_asof_ok()` 同規則再擋一次；**時間戳依 ai-rules/21**：同一天多格時接在前一格之後隨機錯開 5~180 分、**不跨日**，所以不會印出「核准早於承辦」。補資料模式下的「相關單位意見」改成**直接補登**（填回覆人、回覆日期與內容）、**刻意不發通知**（幾年前的事件再發一次通知只會吵到人，對方也無從回覆）。**權限**：補資料**只有「異常單管理員」(`qab_admin`) 與系統管理者**有（`canBackfill`），決策者或填表人都沒有——使用者明確要求這個功能只有異常單才有。**這一輪值得記住的一條**：改判定口徑之後，**既有的測試斷言也要跟著改**——舊測試斷言「部分存檔要保住前端送的客戶名稱」，新規則下客戶本來就該被來源蓋掉，那是**規格改了不是壞了**；分辨方式是回去看使用者這次要求的是什麼，不要反射性地把程式改回去迎合舊測試。驗證：`php -l`＋渲染後 inline JS `node --check`（3 頁 9 段）＋側欄與輸入規則健檢全過；**真 HTTP 端對端 43 項全過**（客戶綁定／解除／重綁、補資料判定邊界、補章六格、未來日期與不在職者與非管理員三種擋下、同一天第二格時間必定晚於第一格、補登意見不發通知、天數上下限與權限）＋**前一批 94 項回歸全過**；**無頭 Chrome 實際登入點擊 74 項全過**（含從畫面真的補一個章、徵詢跳窗切換成補登模式、客戶欄位唯讀、三種寬度零橫向捲動、零 JS 例外）。測試建立的 9 張單、通知、列印紀錄、測試分類、session 與 Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-21（二次）— **品質異常處理單（2-QA-01-01）整個改版：照紙本的列印版、三層原因分類、逐輪徵詢意見、扣款確認與報廢單號**（使用者交辦：「之前有做不良會自動開立異常單，但異常單被反映不好用」）。舊版只有一個開單跳窗＋通知回覆，**列印不出那張紙本、回覆部門要在開單當下一次勾完、原因分類寫死在 enum、紙本左下角整塊「扣款確認」系統裡根本沒有**。新檔 `views/QA/qa_abnormal_list.php`（清單入口）＋`qa_abnormal_form.php`（單張處理頁，版面照紙本分區）＋`qa_abnormal_print.php`（列印版）＋`src/store/QaAbnormal_API.php`＋**唯一實作 `src/common/qa_abnormal_lib.php`**。**①三張代碼表全部交給管理員維護，且一律存 id 不存文字**：`qa_cause_cat`（異常原因分類，自關聯最多三層「人→方法→程式」，**刻意沒有「其他」**——這一欄要延伸到之後的異常分析）／`qa_option`（處置方式與總經理裁示共用一張，**「是不是報廢」「是不是轉總經理」用 `is_scrap`／`is_escalate` 旗標判定，絕不可比對名稱文字**，管理員改名才不會全部失效）／`qa_decider_cfg`（決策者可選範圍＝部門＋職稱，人員異動不必回來改）。**②相關單位意見改成逐輪徵詢**：一次只送一個部門（可指定職稱或某一位），**上一輪沒回覆就不給送下一輪**，收到回覆才決定下一個問誰或直接進決策。**③責任單位＝製程＋廠商**（皆模糊搜尋），廠商是 `maker_list.internal=1` 的**廠內加工廠商**時才長出部門與人員複選（換成外包廠商時自動清掉，免得留著對不上的人）。**④最終處置以總經理裁示優先**（使用者定調），含報廢者**在「結案當下」才配發報廢單號**（`F`＋民國年3碼＋MMDD＋流水3碼，比照 `car_alloc_numbers` 用 `FOR UPDATE` 配號），**寫進 DB** 因為後續別的單據要靠它追蹤；取消結案**不收回號碼**。**⑤扣款確認**：製程金額自動帶入 `bom_ing_transfer_log`（就是製程移轉一覽表讀的那張，可先「看自動帶入哪些金額」、逐列改價或取消計入）、加成倍率（管理員設預設值、單張可改，只作用在製程小計）、「其他」列由生管或業務自填、合計自動加總、生管／品管簽章與會計核准。**⑥不合格品管制記錄表自動連動且唯讀**：帶出狀態／最終處置／原因分類路徑／報廢單號，**前端唯讀＋後端 `row_save` 對 source=qa 一律擋下**（在登錄簿改一個字＝從側門繞過那張單的決策與簽核）。**這一輪踩到三個坑，都只有真的跑起來才看得到**：⑴**`.tag` 撞到 Gentelella 的全域元件**——`custom.css` 有一條 `.tag:after`，在右側用絕對定位畫一個 **11px 的三角形**，還有 `color:#fff !important` 會蓋掉暖色系配色；症狀是整頁**莫名多出 11px 橫向捲動**，而且逐一量每個元素的 rect **完全量不到任何東西超出邊界**（那是 `:after` 的偽元素）。**往後自訂 class 不要用 `tag`／`label`／`badge`／`well` 這種版型已經佔走的通用名稱**；**判斷有沒有橫向捲動要用「真的捲得動嗎」（`window.scrollTo(9999,0)` 後看 `scrollX`），不要用 `documentElement.scrollWidth − clientWidth`**（有垂直捲軸時它會多算，這次就是 1276 vs 1265 的假差值）。⑵**`save_head` 原本把沒送的欄位一起寫成 NULL**——測試只送責任單位那一次，把製令編號整個洗掉、扣款就再也帶不出金額；已改成 `array_key_exists` 判「有沒有送這個欄位」（本專案第 N 次踩，沒送＝不要動它，送空字串才是清空）。⑶**`department`／`position` 的欄位名是 `name` 不是 `department_name`／`position_name`**（`eg_people_list()` 回的是別名），**而且這個錯誤只有非管理員會踩到**——管理員在權限判定第一行就短路了，用管理員帳號測完全不會出錯。**測試方法上的兩個坑**：無頭 Chrome 裡**原生 `alert` 會把 `Runtime.evaluate` 整個卡死**（症狀是整批測試「沒有任何輸出」而不是失敗），一定要監聽 `Page.javascriptDialogOpening` 自動關掉；**node 的 stdout 在非 TTY 會緩衝**，程式卡住時連已經跑過的結果都看不到，測試腳本要**即時 `appendFileSync` 寫檔**。**列印版**依 ai-rules/16（公司全名動態取、表頭取綁定 AS 文件名稱、AS 編號右下且版次依填寫日期回推＝實測印出 `2-QA-01-01C`、頁碼左下且多頁才印）與 ai-rules/18（簽章走 `eg_stamp.js`、**圖章不縮小**）；**為了塞進 A4 一頁**，把「核准／生管／品管」三個簽章改成**同一列三欄**（原本各佔一列，三個 91px 圖章就吃掉三分之一頁），書寫區高度也壓過一輪——實測內容高 966px ≦ 可印 1039px、`printToPDF` 確實是 **1 頁**。新增角色 `qab_view`／`qab_fill`／`qab_decide`／`qab_gm`／`qab_deduct_fill`／`qab_deduct_approve`／`qab_admin`，**與組織角色的部門綁定並用**（品管或業務本來就能開單、生管或業務能填扣款金額、會計能核准、最高核准人員本來就是最終決策者，角色只是給「不在那些部門但需要權限」的人補授權）。依使用者指示**刪除 2026-07 的 5 筆測試異常單**（連同通知與流程）。驗證：`php -l`＋**渲染後** inline JS `node --check`（4 頁共 13 段）＋側欄與輸入規則健檢全過；**真 HTTP 端對端 94 項全過**（三層分類、上一輪未回覆不給再送、裁示優先於處置、加成與取消計入的合計、報廢單號格式與民國年月日、結案後唯讀、取消結案保留號碼、結案前必須勾好原因與處置、IR／BOM 兩種來源、五道權限守門、CSRF、未登入 401）；**無頭 Chrome 實際登入點擊 57 項全過**（設定四個分頁、開新單兩種來源互切、原因分類樹三層縮排、已回覆輪次、扣款列取消計入後合計真的變、自動帶入預覽、使用說明、1280/1600/1920 三種寬度零橫向捲動、零 JS 例外、列印版 6 個圖章與 A4 一頁）。測試建立的 9 張單、通知、列印紀錄、測試分類、session 與 Chrome 暫存設定檔已全部刪除（一律以明確 id 為條件，不用時間區間）。**尚未經使用者瀏覽器實際點擊驗收**。**已知未動**：舊的 `qa_abnormal_modal.php`／`qa_abnormal_view.php`（QC 檢驗跳窗開單與通知落地頁）仍在，開出來的單在新頁面照樣看得到；QC 那條路徑要不要一併改走新表單是下一步。
> 前次：2026-09-21 — **資料稽核新增「排除設定」（客戶／廠商／料號，每個分頁適用的排除不同）＋把四個節點的單號連結修對**（使用者交辦）。**①排除設定**：使用者實例——**某個客戶固定先下未來單，所以製令先開立、訂單事後才來綁**，流程順序稽核就一直報「製令早於訂單」，那是這家客戶的作業方式不是缺失。一條規則＝**排除什麼**（客戶／廠商／料號＋對象）＋**套用到哪個分頁**＋（選填）**只排除哪幾個檢核項目**；項目留空＝這個對象整筆不納入稽核（新表 `dqa_exclude`，`UNIQUE(dim,val)`＝同一個對象再存一次是更新不是長出第二條）。**維度決定它能套到哪些分頁**（使用者要求「每個分頁適用的排除不同」，唯一登記處 `dqa_excl_dims()`）：客戶→流程順序稽核＋基本資料稽核(客戶)；廠商→只有基本資料稽核(廠商)；料號→只有流程順序稽核（基本資料稽核查的是客戶廠商主檔，沒有料號）。前端依維度動態長出可勾的分頁與項目，後端 `dqa_excl_save()` 同規則再擋一次。**對象一律從主檔挑**（`excl_search` 查 `customer_list`／`maker_list`／`d_setting`）——打錯一個字那條規則就永遠不會命中，而且完全不報錯。**稽核結果上方一定要列出「這次套用了哪些排除、各排掉幾筆」**（`dqa_excl_applied()`），不講就看不出有排除在作用＝失去可追溯性；規則可停用（先留著不生效）或刪除，改動後畫面自動重算。與既有逐筆「標為例外」互補：例外＝這一筆的這一項已核可，排除＝往後凡是這個對象都不要再報。實測：對「和大」只排除「製令早於訂單」→ 該項 5→4 筆、該客戶其餘缺失照列；改成整筆排除→ 2847→2802 筆。**自己寫出又自己修掉的 bug**：`dqa_excl_map()` 用 `static` 快取，**同一個 request 內存完規則再重算會吃到舊快取**（症狀＝規則存進去了、當下重算卻完全沒作用），已改成可清除的請求內快取、三個寫入點都清。**②把單號連結修對（上一批留下的問題）**：⑴**已完工的製令要連「已完工BOM查詢列印」不是 BOM 總表**——實測 **BOM 總表只列未完工的製令，已完工的即使只帶 BOM 編號也是 0 列**（不是篩選寫錯；`bom` 列改為一併帶回 `processing_state` 決定連哪一頁）⑵各節點的連結一律改用**該節點自己的客戶與料號**（`bom.Client_Name`／`ql.client_name`／`is_list.Client_name`），不再拿訂單的值去篩別張表（拿訂單客戶去篩可能整個篩不到）⑶修掉報價單管理的作用域 bug：`PENDING_URL_CLIENT` 宣告成 ready 區塊內的 `var`，而 `loadQuoteList()` 是**全域函式看不到它**（`typeof` 就是 undefined），所以客戶下拉一直沒被設定又不報錯——已改掛 `window`，屬性選擇器也換成逐一比對 option（客戶名稱可能含選擇器特殊字元）。**③測試紀律又踩到同一條**：我沿用了三天前建立的「無權限測試帳號」109110201，但**使用者這期間已自行把 `dqa_admin` 指派給他**，於是那幾項「無權限被擋」其實是被 CSRF 擋下、根本沒驗到權限守門（測試通過但測錯東西）。已另建一個確實沒有任何角色的帳號重測 9 項全過（頁面不輸出工具列與排除設定鈕、用自己正確的 CSRF 也存不進去、DB 零殘留）。**使用者正在同一套系統上操作，測試一律不可以假設權限現況，要當場查 `user_roles` 確認。**驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP 端對端 31 項、無頭 Chrome 30＋28 項、權限守門 9 項全過。測試規則與三個測試 session 已全部刪除（`dqa_exclude` 回到 0 列）。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十八次）— **準時出貨率新增「寬限工作天數」**（使用者要求）。新增 `grace_days` 參數（KPI 設定頁→準時出貨率→參數，**逐年度可分開設定**、可勾開放前端試算），0＝不寬限、上限 60。判定用的截止日＝**交期＋N 個工作天**，走既有的 `kpi_as_add_workdays()`（依行事曆的假日與補班日）——**刻意用工作天不用日曆天**：交期落在連假前時，日曆天會把假日也算進寬限而失真。同一個交期只算一次並快取（一個月裡交期重複度很高）。**套用範圍**：C（出貨綁定）與 D（出貨單反推）都吃，因為這兩種才有出貨日可以比；**B（ERP 未交清單）沒有出貨日期，寬限在那個模式下沒有作用**，說明列已寫明。明細設了寬限之後多一欄「寬限後截止日」，原因寫成「出貨日 2026.08.14，晚於寬限後截止日 2026.08.10 4 天（交期 2026.08.03＋寬限 5 個工作天）」；說明列開頭寫出目前的寬限設定，沒設時也提示可以去哪裡設定。實測 D 模式 2026 各月：0 天 56~66.9%、3 天 60.8~74.4%、5 天 64.2~76.8%、7 天 66.7~79.1%、10 天 68.8~82.3%。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 第十二輪 8 項全過（設定頁欄位是數字輸入框、標題寫明工作天、可勾前端試算、試算端點實測 8 月 59.86%→71.13%），其餘十一輪回歸（69／31／12／24／21／16／66／15／9／11／39）全過。**測試小坑**：試算端點吃的參數名是 **`params`** 不是 `ov`，送錯名字時它會**安靜地用原設定算**，看起來像「寬限沒有作用」。**另：使用者已自行把 2026 年度的未交判定方式改成 D。尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十七次）— **查出 ERP 未交清單已半年沒匯入、移除該匯入、準時出貨率改用「料號主檔 id」反推**（使用者提問＋指示）。①**使用者問「ERP 未交來源是哪張表」**——是 **`order_list`**，匯入程式是 `views/pm/_upload_For_List.php` 的 `but=Order`。**最後一次匯入是 2026-03-12**（主批 2026-03-04，29,041 筆），之後半年沒再匯過。而且**這張表是「未交清單快照」不是全量訂單**（每一列都是 `Qty=Open_Qty`），交期分布也證實 2026-05 只有 56 筆、08 月 8 筆、09 月 2 筆——**所以先前 B 模式在 5~9 月算出的 98~99% 是假的**：不是幾乎都準時，而是過期快照裡沒有那些訂單，對不到就全被當成已交。已加 `kpi_as_order_list_covers()`：吃 `order_list` 的模式只要匯入日涵蓋不到該月月底就回 `null`（無資料）並寫明原因，**寧可沒有數字，也不要在 AS9100 紀錄上留一個看起來漂亮但是錯的數字**。②**使用者要求刪掉 `_upload_For_List.php`——確認後不可以刪**：它是「更新資料」頁全部 ERP 匯入的**共用處理程式**（3,720 行、19 個 `but` 分支），還在用的有出貨單匯入（2026-09-17）、製程移轉憑單（2026-08-19）、BOM／退貨單／報價單／報工等，刪掉整頁會壞。**改為只移除已停用的 `but=Order` 分支（293 行）＋那張本來就 `display:none` 的上傳表單＋頁尾 `enterToSubmit` 設定**（該檔自己的註解就要求這三處要同步清）。`order_list` 資料表保留、只是不再開放匯入。**教訓：使用者說「刪掉某支檔案」時，先看它是不是共用入口——`_upload_For_List.php` 這種 `_` 開頭的處理程式常常是一整頁的共用後端。**③**新增 D 模式「以出貨單反推」**（使用者選的路）：對應鍵用**料號主檔 id** `order_track.d_id_ID ↔ is_list.d_setting_id`，實測覆蓋率 98.9%／100%，而 `is_list.Client_id` 只有 5.8%（幾乎全空）。**料號主檔 id 本身就綁定客戶**（同一個料號文字在 `d_setting` 常有好幾筆、分屬不同客戶），對到主檔 id 就等於同時對到客戶，**不必再比客戶名稱**——舊訂單的客戶名稱寫法跟出貨單常常不一樣（2026-01 有「如陽」「快捷」「NA」「空字串」對不上）。準時＝該料號最早出貨日不晚於交期，已綁定的一律以綁定為準。實測 2026 年 1~9 月：**53.5／56.6／66.1／52.3／51.2／61.8／50.6／59.9／48.3%**（每月都算得出來）。`undone_mode` 預設改為 D。**自己寫出的 bug**：D 的回溯窗用了 `$ms`，但 `kpi_as_order_bind_rows()` 裡沒有這個變數，變成「從今天往前 120 天」，1~4 月被算成 0~1.6%——**跨函式搬程式碼時，被引用的區域變數要一起確認**。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；四頁 HTTP 200；無頭 Chrome 十一輪回歸（69／31／12／24／21／16／66／13／9／11／39）全過。**注意：2026 年度目前設定還是 B，會顯示「無資料」，要到 KPI 設定頁把「未交判定方式」改成 D 並按「重算本年」。****尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十七次）— **快速出貨：未完工的製令標出「目前製程」**（使用者要求：可出為 0 標「無完工」時看不出來製令走到哪一關）。三處：①**清單的製令欄下方直接加小字**（「拋光 9/12」＝共 12 關、目前走到第 9 關的拋光），不展開也判斷得出來 ②**展開製令明細新增「目前製程」欄**，含狀態（未發包／加工中／QC待驗／生管待移轉／已移轉）、廠商、該關的發包與檢驗日，**這一關已移轉時再寫「下一關：○○」**＝貨在等下一關發包（只有 `state=E` 才寫；`qc_completed=1` 但還在 P／Q 的是卡在「生管還沒移轉」，寫下一關會把責任指錯地方）③**CSV 匯出新增同一欄**。已完工的製令一律不標（顯示「已完工」，它不是卡住的原因）。**判定沿用製令追蹤頁（`BomTrack_API` 的 `get_matched_boms`／`get_bom_process_chain`）的規則**：排除 `skip`，取有發包日或 QC 檢驗日的製程中活動日期最新的那一關，一關都沒開始寫「尚未開工（第一關：○○）」。**唯一刻意的差別是同一天的 tie-break 取 `bom_sn` 較大者＝走得比較遠的那一關**——`outsource_date` 一律 00:00:00（沒有時分秒）、`QC_check_date` 有，直接比 datetime 會讓「同一天下一關已經發包」輸給「上一關當天剛驗完」，**實測 592 張未完工製令有 54 張因此被判成上一關**（例 `B-1150428012`：客供料 04-29 10:15 驗完、粗滾 04-29 發包且狀態=加工中，比 datetime 會答「客供料」）；**製令追蹤頁未動**，那邊只有同一天的那幾張會差一關（已寫進函式註解，要不要一起修是使用者的決定）。**顯示文字唯一實作 `sq_progress_label()`**（`shipping_lib.php`），清單／明細／CSV 共用同一份說法——前端只負責排版與狀態小籤，**不在 JS 再組一次**，否則三個地方遲早寫出三種說法。**效能**：只查未完工的（全部待出貨訂單 2,289 張製令裡只有 592 張未完工，平坦查詢 0.04 秒、不用相關子查詢），每支 API 多 0.05 秒。**日期一定要標明是「發包」還是「檢驗」**——狀態寫「未發包」卻印一個日期（那是廠內製程的 QC 檢驗日、本來就沒有發包日）看起來像自相矛盾。順帶一個 CSS 老坑：**表格裡的小籤一定要自己指定 `line-height`**（Gentelella 全站 `td span{line-height:28px}`，10px 的字會佔掉 28px 把整列撐高，已踩第三次）。驗證：`php -l`＋渲染後 inline JS `node --check` 四段＋側欄與輸入規則健檢全過；**與 git HEAD 並排跑六種參數組合（分頁／排序／關鍵字／只顯示可出貨／全量匯出），總計、合計、每一列每一個既有欄位與製令明細逐格完全相同**；無頭 Chrome 實際登入點擊 12＋7＋4 項全過（明細欄位順序與欄數對得上、內容與原始資料逐欄核對、已完工顯示「已完工」、尚未開工與「下一關」兩種型態、1280/1600/1920 三種寬度零橫向溢出、零 JS 例外）。測試 session 與 Chrome 暫存設定檔已刪除，未寫入任何業務資料。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十六次）— **準時出貨率 B 模式重做：以 ERP 未交清單逐筆對回訂單，不必做出貨綁定**（使用者回報）。使用者：準時出貨率變得非常低，而且 **ERP 還沒修好、也不建議做出貨單綁定**（ERP 修好之後要清掉所有出貨單重新載入）。**查證出一個關鍵事實（往後碰這兩張表一定要知道）**：**`order_list` 是 ERP 的「未交清單快照」——2026 年該表每一列都是 `Qty=Open_Qty`，也就是裡面只會有「還完全沒出貨」的訂單，已交的根本不在裡面**；而 **`order_track.Open_Qty` 是全表 NULL**（2705 筆一筆都沒有）。所以在沒有出貨綁定的前提下，系統唯一問得到的是「這張訂單現在還在不在 ERP 的未交清單上」，**問不到出貨日期**，也就判不出「是不是在交期前交的」——那個只有 C（要綁定）做得到。B 因此重新定義為「**以 ERP 未交清單為準，逐筆對回訂單**」：分母＝訂單追蹤該月交期的訂單（已排除取消），不準時＝能在 ERP 未交清單對到（**同客戶簡稱＋同料號，一張未交只佔用一張訂單**）的那幾張。**舊制是把兩張表的「筆數」直接相減**，但兩表是各自獨立的資料（2026-01：訂單追蹤 260 張、ERP 未交 364 列），**相減會變負數被夾成 0%**；改成逐筆對回去之後，未交數永遠不會超過訂單數，1~3 月就不再是 0%。實測 2026 年 1~9 月：**84.5／81.1／79.5／89.3／99.4／98.7／99.4／98.9／99.3%**（前一版要求出貨證據時是 13.1~59%，舊制是 0/0/0/54.7/90.2/96.6/97.1/97.9/99.3%）。B 的明細拿掉「出貨日」欄，原因改成「交期已到，ERP 未交清單上這一張還是完全沒出貨」；模式選單文字也講明 **B＝不必綁定、ERP 修好之前用這個**／**C＝最精準但要先綁定**／舊制＝不建議。**這一輪的教訓**：同一個指標我連改三次口徑，原因是**沒有先確認來源表「實際上裝了什麼」**——只要一開始就發現 `order_list` 只有未交列、`order_track.Open_Qty` 全空，就會知道「沒有綁定時根本判不出準不準時」，不會先做成需要綁定的版本再被退回。**碰不熟的來源表，第一件事是 `SELECT` 幾個彙總確認欄位到底有沒有值、裝的是全量還是快照。**驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；HTTP 實測 2026-03 明細 52 筆、說明與建議文字正確；無頭 Chrome 十一輪回歸（69／31／12／24／21／16／66／15／9／11／39）全過。**注意：改口徑不會自動重算既有快照，要按「重算本年」。尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十六次）— **新增「資料稽核」模組（`views/ADM/data_audit.php`＋`DataAudit_API.php`＋`data_audit_lib.php`，兩個分頁）**（使用者交辦）。**分頁一 流程順序稽核**：把同一支料號的 報價單→訂單→製令→出貨 四個節點攤開，比對日期順序、數量、單價、製程。使用者定的日期鐵則：**報價日≦訂單日、製令開立日≧訂單日、出貨日≧製令開立日；訂單設定「自動轉生管」(`order_track.pmGet_auto=1`) 者表示不必開製令，只要求出貨日≧訂單日、也不報缺製令**。**製令開立日一律用製令編號回推**（`B-`＋民國年3碼＋MMDD＋流水），不用 `Created_At`——那是 ERP 匯入這套系統的時間（實測 400 筆有 13 筆差到 3 天），拿它比訂單日會冒出一整批假的「製令早於訂單」。**這次最值得記住的是「推測配對」怎麼做才不會變成滿畫面假警報**：⑴**配對鍵不可以用客戶名稱**——報價寫「崴立(后里)」訂單寫「崴立」，**814 個報價客戶名稱只有 214 個（26%）對得上訂單**；改用**料號主鍵優先**（`quotation_item.d_setting_d_id` 填充率 99.4%、`is_list.d_setting_id` 100%、`bom` 2026 年 84%、`order_track.d_id_ID` 44%），主鍵本來就綁定了客戶，對得上就不必比名稱，比不到才退回「同客戶＋同料號文字」（與記憶 [[bom_client_name_cache]] 同一條）⑵**推測的製令只取「訂單日之後最接近的那一張」**——同料號長期重複下單，全抓會變成「製令 9 張合計 7,922 支 vs 訂單 600 支、首張還早訂單一年」，那根本是別張訂單的製令（實測 `b_early` 31→5 筆）⑶**推測的出貨累加到滿足訂單量就停**（再往後是下一張訂單的貨）⑷**日期先後只有「兩邊都是綁定」才判嚴重**，任一邊是推測就降為提醒或不判（`s_early_bom` 223→2 筆）⑸**推測配對一律不判定數量不符**；單價則照判，但**報價距下單超過設定天數（預設365）時改報「報價過期未重新報價」而不是「單價不符」**——實測抓到 2021 年的報價（883）拿來比 2026 的訂單（583），那是五年來調過價，真正的缺失是這麼久沒重報價。**製程比對預設關閉**（訂單製程是手打、出貨製程與規格混在同一欄，實測 31% 對不起來會淹沒真正的問題），製程詞庫即時取自 `process_no` 與報價子標籤共 322 個詞、**長詞優先比對**（免得「滾齒」先吃掉「精滾齒」）。14 個檢核項目可逐項調等級或關閉，可逐筆標記「已核可例外」。2026 年度實測掃 2,837 張訂單 0.8 秒。**分頁二 基本資料稽核**：客戶 980／廠商 904 筆主檔的**編號是否符合編碼原則**（使用者定調＝英文2碼＋數字3碼＋可選英文1碼，做成可設定；客戶 325 筆、廠商 314 筆不符）與**欄位完整性**（分重要／一般／建議三級，傳真與 EMAIL 預設不檢查，結帳方式·結帳日·付款方式列為重要缺失）；**已停用者一律不納入稽核**（客戶 `is_inactive=1`、廠商 `status='X'`）；確定無統編（現金交易）者可標例外。**實測現況：客戶 936 筆沒填統編、958 筆結帳付款條件未確認、903 筆沒填發票抬頭全名**。**一個只有實際跑資料才看得到的 bug**：`SELECT customer AS nm` 取了別名，而檢核是拿欄位定義裡登記的**真實欄位名**去讀那一列，於是**每一筆都說「客戶簡稱未填」而且完全不報錯**——欄位定義表與 SQL 的欄位名必須是同一個。**AS 表單綁定刻意不走 `AS_DOC_BIND`**：這裡綁的是「這次稽核涵蓋哪幾份 AS 表單（可多選）」，與 ai-rules/16 那種「某頁面＝某張表單的網頁版（一對一）」是兩回事，故另存 `system_parameters('DATA_AUDIT','scope_trace'/'scope_master')`，不會影響任何表單的表頭表尾編號；按「留存稽核結果」寫入新表 `dqa_run`，內部稽核用 `dqa_summary_for_docs()` 帶得出「這份表單最近一次稽核發現幾筆問題」（**刻意留存而不是讓內部稽核即時重算**——重算一次要掃幾千張訂單，而且稽核紀錄本來就該是「那一天查出來的結果」，事後資料補齊了不該回頭改寫當時的發現）。新增角色 `dqa_view`／`dqa_admin`、列印來源 `data_audit`、資料表 `dqa_exempt`／`dqa_run`，選單登記在「測試功能」。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄健檢＋輸入規則健檢全過；真 HTTP 端對端 28 項、無頭 Chrome 實際登入點擊 54 項全過（含分頁、從畫面標記例外與取消、列印版 A4 橫式與頁碼、1280/1600/1920 三種寬度零橫向溢出、零 JS 例外）。測試資料、列印紀錄、session 與 Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：`internal_audit.php` 那一端的畫面串接（資料介面已備妥，但該檔案正被平行 session 修改中，未動）。
> 前次：2026-09-18（十五次）— **KPI 不符合標準的明細可以選取複製料號／訂單編號**（使用者要求）。**根因是自己前一版造成的**：為了做「拖曳一次多選」，在 `mousedown` 就 `preventDefault()`，**等於把整張表的文字選取全部擋掉**，料號根本反白不起來。改成**用手勢區分，兩種都保留**：①**同一列之內拖曳＝不攔**，瀏覽器照常選字可以 Ctrl+C（儲存格另補 `user-select:text` 與文字游標）②**拖到別的一列才開始多選**，並在那一刻把剛剛拖出來的反白清掉、起始列補上勾選 ③直接點勾選欄那一格仍是立刻切換、不必拖。另外加了兩個複製方式：④**雙擊儲存格＝複製這一格**（會濾掉徽章與排除註記文字），下方跳出「已複製「○○」」提示；⑤頁尾「**複製整欄**」——把目前篩選出來的那一欄全部複製成一行一個（重複的自動合併），直接貼進 Excel。複製優先用 `navigator.clipboard`，不支援或被拒就退回 `textarea`＋`execCommand`。**通則**：在表格上做拖曳手勢（多選、排序、框選）之前先想清楚**它會不會把「選字複製」擋掉**——現場最常做的動作其實是把料號／單號複製去別的系統查；要擋也只能在「確定是拖曳手勢」之後才擋，不可以在 `mousedown` 就擋。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 第十輪 11 項全過（`user-select` 實測是 text、同列拖曳不會誤觸勾選、用 Range 實測選得到料號文字、雙擊出現複製提示且內容就是那一格、整欄複製筆數與預期相同 76 筆、拖到別列仍能一次選 5 列），其餘九輪回歸（69／31／12／24／21／16／66／15／9）與三種寬度 39 項全過。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十四次）— **廠商準時交貨率改吃「外包廠商績效」頁的唯一實作，兩頁完全合併**（使用者回報）＋**列印多一頁的真因**＋**準時出貨率 B 改用交期判定**。①**KPI 頁 2026-08 是 81.9%、`views/pages/vendor_kpi.php` 是 70.1%**——同一個指標**有兩套各自的判定**：vendor_kpi 用「**發包日**落在該月＋**容忍天數**（全域／廠商特殊／製程特殊）＋**只算已到容忍期的**」，KPI 頁用「**應交日**落在該月＋約定工作天」；排除清單也是兩套。依鐵律4 把 `vendor_kpi.php` 的工作日計算與 `vkPeriodRows()`（253 行）抽成 **`src/common/vendor_kpi_lib.php`**，原頁面改成一行 include，KPI 的 `vendor_ontime` 改呼叫它、準時與否直接用它判好的 `status`；registry 拿掉已無作用的 `default_days`／`days_by_process_type`。**排除設定雙向合併**：`vkPeriodRows()` 除了 `kpi_excluded_maker`／`kpi_excluded_process`，也一併讀 KPI 頁建立的 `kpi_as_excl_rule`（廠商／製程／客戶／料號，含「所有年度」），**任一邊設定都同時影響兩頁**；為避免兩個共用檔互相 require，那段直接讀表不呼叫 `kpi_as_lib`。結果 2026 年 1~9 月兩頁分子分母**逐月完全相同**（8 月 370/527＝70.2%）。**順帶盤點其他指標**：教育訓練達成率／廠商稽核按時執行率／量測儀器按時校驗率本來就呼叫該模組共用庫，沒有這問題；其餘指標的來源模組沒有另一套對外公布的 KPI 數字，目前沒有兩套對不起來的情形。②**列印還是超出邊界的真因**：Gentelella 的 `.right_col`／container 有 **`min-height`** 撐著（實測內容幾百 px 卻量到 1296px），列印時多出一整頁幾乎空白的第二頁；已在 `body.kpi-printing` 與 `@media print` 兩份都把 `min-height`／`height` 歸零，A4／A3 都由 2 頁變 1 頁。縮放也改成**套上去之後再量一次、還超出就再縮 6%（最多 10 次）**——先量再乘比例只是估，`zoom` 會改變字級與換行，實際高度常常不等於原高度×比例。③**準時出貨率 B 模式原本只看「ERP 未交清單有沒有掛著」＝只要有出貨就算準時**，2026-08 因此算出 99.3%；使用者確認判定就是「交期前有沒有交貨」，已改成與 C 相同的「出貨日 vs 交期」，差別只在出貨證據來源。另**分母排除已取消的訂單**（`Order_status=9`）、**出貨證據排除 ERP 重複轉出**（同單號有兩個以上不同出貨日）。④**排除客戶可填客戶ID**（`kpi_as_resolve_clients()`，ID 與簡稱都收、計算前一律解析成簡稱），設定頁長出客戶挑選器（980 家）並列出「已在不符合標準的明細設定的排除規則」。**⑤要提醒使用者的事：改判定口徑不會自動重算既有快照**，畫面上仍是上次結算的數字，要按「重算本年」。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；三頁 HTTP 200；`vendor_kpi` 的 `get_kpi_data` 實測與 KPI 頁一致；無頭 Chrome 九輪回歸（69／31／12／24／21／16／66／15／9）與三種寬度 39 項全過。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十四次）— **AS 表單 E 化盤點：補綁 9 份＋新增 3 個模組（倉庫庫存表／入庫單、客戶滿意度、不合格品管制記錄表），四階表單 E 化率 42%→54%**（使用者交辦）。**先講一個查了才知道的事實：「已網頁化」的數字本來就被低估**——`asdoc_page_lib.php` 的 `$LABELS` 沒登記名稱的模組，`$put()` 會因為 `url===''` 直接略過，所以**內部稽核 7 份、溝通管理 3 份、工程變更、出貨單共 11 份早就綁好了、AS 結構總覽的「網頁」欄卻一片空白**，看起來像沒做（已補登，往後新增模組一定要回 `$LABELS` 補一列）。**同批把 `linked_module` 的合法清單收斂成唯一登記表 `eg_asdoc_linked_modules()`**——原本同一份清單寫死在四個地方（AS文件管理下拉／API 白名單／`$MODULE_PAGES`／`asdoc_record_lib`），加一個模組要改四處，漏一處就是「畫面選得到、按儲存卻回不支援的模組」。**站上有兩套 E 化綁定機制，不要再造第三套**：有列印版的走 `AS_DOC_BIND`（只有它能用 `eg_asdoc_no_asof_id()` 做版次依業務日期回推）、沒列印版只要讓結構總覽指得到頁面的走 `linked_module`（不必動模組頁面，成本最低）。**補綁的 9 份**：品質異常處理單／異常矯正處理單／圖面變更簽收單／機器設備履歷表／檢驗設備一覽表（綁定值原本是 0）／成品包裝&出貨檢驗表／廠商採購單·請購單·詢價單。另修掉 `drawing_change_log.php` 寫死 `$AS_DOC_NO='2-PD-01-07'`（違反 ai-rules/16 禁寫死）。**新增三個模組**：①**倉庫庫存表 2-WH-01-01／入庫單 2-WH-01-07**（`views/pages/stock.php`，12,264 行巨檔只能 Edit）——入庫單**刻意不另建資料表**，資料源就是 `stock_transactions` 的 `txn_type='in'`、單號 `IN-YYYYMMDD` 即時組出不落庫，可挑日期補印過去的單（實測 2026-08-13 印出 `2-WH-01-07B`、今天印出 `2-WH-01-01`＝版次回推有效）；`get_stock_list` 新增 `for_print=1` 才把 100 筆上限放寬到 5000（ai-rules/08：列印要對全部符合條件的資料出表，一般瀏覽不變）；列印設定跳窗擴充成「一個跳窗管三張表單」，儲存改用 `array_key_exists` 判有沒有送欄位，**不然改版前的舊分頁按一次儲存就把新綁定洗掉**。②**客戶滿意度 2-SM-02-03／2-SM-02-04**（`views/Sales/customer_satisfaction.php`＋`CustSatis_API.php`＋`cust_satis_lib.php`，新表 `cs_score`／`cs_summary`／`cs_monitor`，角色 `cs_admin`／`cs_view`）——準交率／退貨率／客戶開立異常處理單件數全自動算；**準交率判定口徑直接沿用 KPI「準時出貨率」的年度設定**（讀 `kpi_as_indicator_year.params_json` 的 `undone_mode`），**刻意不另開開關**，否則同一家客戶在 KPI 頁與滿意度頁會出現兩個「都看起來對」的準交率而沒人查得出要信哪個（與 KPI 逐月加總交叉比對：2025 完全一致 1/3222）；**技術／服務／價格三項系統算不出來，只能來自 2-SM-02-02 問卷**，畫面用淺藍底標「問卷填入」、**留白＝尚未填不是 0 分**（平均只平均有填的項目），硬編分數就是假資料稽核一問就破。**逐客戶視角攤開一個既有資料問題**：`order_track`（自建）與 `order_list`（ERP 未交）是兩份獨立資料，2026 出現**盟英 0 張訂單卻 42 筆 ERP 未交、吉輔 60 vs 96、傳仕 11 vs 37**，全公司一起算時被 `max(0,…)` 吸收掉看不出來、逐客戶算就現形——已回傳 `ontime_over` 並在畫面標「準交率僅供參考」，**是既有結構問題不在這個模組偷偷修掉**。③**不合格品管制記錄表 2-QA-01-03**（`views/QA/ncr_control_log.php`＋`QaNcr_API.php`＋`qa_ncr_lib.php`，新表 `qa_ncr_log`，角色 `ncr_admin`／`ncr_view`，**品管的 `qc_manage_settings` 視同管理員不必重複指派**）——**登錄簿式設計**：四個來源對應程序書 2-QA-01 第 5 節的過程輸入（品質異常單／矯正單／客戶退貨／QC 檢驗不良·特採），2026 實測 408 筆 0.02 秒；**來源欄位一律即時讀來源單**（來源結案這裡跟著結案），只有人工補充存 `qa_ncr_log`；**刻意不回寫來源單**（CAR 有四段簽核、品質異常有總經理裁決，在登錄簿改一個字就回頭動它們的欄位＝從側門繞過流程）；**來源事件不可從本頁刪除**（只有紙本補登列可刪，刪掉一筆不合格品會讓紀錄憑空消失）。**三個測試方法上的教訓**：⑴**渲染後的 inline JS 一定要跑 `node --check`**——本次抓到 `doPrint` 函式宣告卻用 `});` 收尾，`php -l` 完全通過、但整頁 JS 一行都不執行；⑵**測試用 PHP 驅動不要用指令列傳中文**（Git Bash 轉 Big5 → `1366 Incorrect string value`），且偽造 session 的 `userName` 要放 **`user.user_uname`** 不是 id；⑶**測試自己造成的失敗要分清楚**——第二輪兩項失敗是上一輪測試存進去的資料造成（監控表已存 2 列，不再帶預設 3 列），斷言要相對於「這次自己建立的資料」。**同時修掉兩個真實設計缺陷（都是測試抓到的）**：`score_save`／`row_save` 原本會把「沒送的欄位」一併清空，已改成 `array_key_exists` 判有沒有送；DECIMAL 讀回來是 `"9.0"`，已統一正規化成 `9`（**null 不可轉成 0**，0 分與沒填是不同意思）。**產出交接文件 `ai-rules/work/AS表單E化盤點與待辦.md`**：剩下 55 份（3 份已廢止）逐份判定適合／不適合，適合的分 A/B/C 級排序（A 級最划算＝夾治具與天車保養**直接套 `equip_list_lib` 加一個 `equip_type`**、客戶與供應商資料表只缺列印版；人資那 6 張用 `review_form` 模板引擎一批做完），不適合的分五類講清楚理由（標籤吊卡是列印輸出物不是表單／對外要用印／指導書是文件不是表單／現場紙本反而快／一次性個資）。驗證：13 支改動檔案 `php -l` 全過＋渲染後 inline JS `node --check` 全過＋側欄健檢全過＋輸入規則健檢（唯一不合格的 `vendor_kpi_headtest.php` 是平行 session 今天新建的未追蹤檔案，與本次無關）；真 HTTP 端對端 33＋39 項、無頭 Chrome 54＋50＋38 項全過（含 1280/1600/1920 三種寬度）。**stock.php 有 75px 橫向溢出但與 git HEAD 原版數值完全相同＝既有問題（來源是隱藏的 `#tab-analysis`），本次未動。** 測試資料、列印紀錄、session 與 Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收。**
> 前次：2026-09-18（十三次）— **KPI 列印版加大頁面留白**（使用者回報「列印太滿版，頁首頁尾跟左右都要正常留空」）。①明細列印 `@page` 邊界由「上12／左右8／下16mm」改成「**上16／左右15／下18mm**」（**下緣要多留**，因為頁碼與 AS 編號是印在下方頁邊區 `@bottom-left`／`@bottom-right` 裡的）；**`body` 不再自己加 `padding`，留白一律交給 `@page`**——兩邊各留一次就會變成內容被擠到中間又切邊。②明細列印補上：**表頭 `thead` 跨頁重複**（`display:table-header-group`）、**資料列不可被切成上下兩半**（`page-break-inside:avoid`）、表頭區塊不與表格分頁分離。③年度總表列印邊界由「上10／左右8／下14mm」改成「上14／左右14／下16mm」，並把邊界抽成 **`KPI_MARGIN_MM` 一處定義**——原本 `@page` 寫一組數字、縮放計算又**寫死另一組**（8/10/14），兩份遲早對不起來（這是本次順手修掉的既有隱患）。**怎麼驗「有沒有被切到」**：把列印版**實際載進「A4 橫式扣掉左右邊界」的寬度**（267mm＝994px）量 `scrollWidth`，內容 994px／表格 994px ≦ 可印 994px 剛好放得下；再用 `Page.printToPDF`（`preferCSSPageSize:true`、四邊 margin 給 0）輸出**真 PDF** 確認頁面是 297×210mm A4 橫式。這比只看 CSS 字串可靠得多。**附帶查證（非本次造成，未動）**：年度總表在無頭 `printToPDF` 下是 **2 頁**，**改邊界前後完全相同**（舊邊界 zoom=0.866 也是 2 頁），所以不是這次改壞的；真實瀏覽器列印是否一頁待使用者確認。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 第八輪 11 項＋年度總表 9 項全過，其餘七輪回歸（69／31／12／24／21／16／66）與三種寬度 39 項全過。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十二次）— **KPI 明細列印版改成「給稽核老師看的正式清單」**（使用者要求）。使用者原話：列印是要給稽核老師看的，不能有「不符合標準明細」這些字樣、不要「不符合的原因」欄位、回廠日不要寫「（推估：製令結案日）」，也不要印那一長段判定方法與推估來源的說明，**要是本月的正式清單**。改法：①表頭只留綁定 AS 文件的**表單名稱**（拿掉「不符合標準明細」後綴），副標維持 指標／年月／判定目標／本月數值（分子／分母）／列印日期。②每個計算模組新增一句**正式的表格說明 `note_print`**（例「本表為應交日落在本月、未於應交日前回廠之委外加工明細。」），**列印只印它**；畫面上的 `note`（判定方法、推估來源、ERP 未交清單落差）與 `note_excl`（排除客戶）一律不印。③明細列新增 **`pvals`（列印專用值）**：回廠日只印日期，不印「（推估：○○）」。④欄位可標 **`p:0`＝內部作業欄不列印**（已套用在準時出貨率的「疑似已出貨」欄）。⑤列印表格拿掉「不符合的原因」與「類別」兩欄，只留資料欄＋序號，結尾改成「合計 N 筆」。⑥列印視窗標題與列印紀錄的文件名稱改成「指標名 年-月 明細」。**另檢查年度總表列印**：說明列（含「✱=手動覆寫」「⟳=快照過期」）、快照過期提示條、覆寫符號、過期符號、使用說明鈕列印時都已隱藏，符合正式使用。**這件事的通則**：畫面上的說明是寫給「要去修資料的人」看的（判定怎麼來、推估了什麼、排除了誰），**列印版是給外部稽核看的正式紀錄，兩者的文字必須分開存放**——不可以只靠 CSS 藏，因為那些字是組在同一個字串裡的；所以後端一律拆成 `note`（畫面）／`note_excl`（排除，永不列印）／`note_print`（正式說明），列與欄也各自有 `pvals`／`p:0` 的列印版本。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；**無頭 Chrome 第七輪 66 項全過——對 9 個指標的列印版逐字掃描 14 個內部字串**（不符合標準／不符合的原因／推估／排除客戶／排除規則／已排除／規則排除／指標設定／疑似已出貨／transfer_date／只用於判定／不寫回資料／建議怎麼處理／提醒／參考）**一個都沒有**，且都有公司全名、判定目標與本月數值、「本表為…」說明、頁碼左下與 AS 編號右下、零 undefined；並把列印版實際存檔逐行看過內容正確。其餘六輪回歸（69／31／12／24／21／16）與三種寬度 39 項全過。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十一次）— **廠商準時交貨率：沒登錄回廠日時改用「製程移轉憑單的單號日期」推估**（使用者要求＋指正）。使用者：很多發包沒有去建立回廠日期，能不能自動用 `views/pm/Transfer_Log_Analysis.php` 的日期；**並指正「應該依照單號解析出日期，避免日期因為帳款月份而手動修改過」**。新增 `kpi_as_transfer_dates()`：從 `bom_ing_transfer_log`（ERP 製程移轉憑單）**以單號解析日期**——單號 `J-1150821029` ＝ `J-`＋民國年3碼(115)＋MM(08)＋DD(21)＋序號；認定條件＝**同製令 `bom` ＋同 `bom_sn` ＋ `maker_from` 等於該廠商**（貨從這個廠商移轉出去＝它回廠了），取不早於發包日的最早一張。推估順序改成：**製程移轉憑單 → 下一製程發包日 → QC檢驗日 → 出貨日 → 製令結案日**。**為什麼不可以用 `transfer_date`**：它會為了帳款月份被人工改過——實測 2026 年 6,369 筆裡有 **60 筆**與單號日期不同、**最多差 49 天**（例 `J-1150821029` 的 `transfer_date` 是 2026-08-18）。全庫 96,852 筆裡 96,845 筆單號符合 `X-YYYMMDDnnn`。**準確度怎麼驗的（這招可以重複用）**：拿「已經有登錄回廠日」的 3,346 筆當**對照組**，看推估值跟真值差幾天——完全相同 44%、差 3 天以內 93%、中位數 -1 天；覆蓋率＝沒有回廠日的 5,062 筆裡有 2,261 筆（45%）查得到憑單。效果：2026-01 由 265/814(32.6%) → 714/814(87.7%)，**分母一格都沒變（2025 各月與 git HEAD 逐格相同），只有分子變多**。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；明細 API 實測 0.30 秒；無頭 Chrome 六輪回歸（69／29／12／24／21／16）與三種寬度 39 項全過。**測試紀律（本次連續踩到三次）：使用者正在同一套系統上操作，測試不可以假設任何現況**——寫死「排除規則只有 1 條」「未交判定預設是 bind」「前 5 列都可勾選」都被使用者的即時設定打臉；斷言一律改成**相對於這次測試自己建立的資料**，或先讀出現況再比對。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（十次）— **準時出貨率新增「未交判定方式」，逐年度可由管理員選，預設 C**（使用者回報＋拍板）。**症狀**：使用者在快速出貨把出貨單綁到訂單、按了「重算本年」，數值明細還是 `0/176 = 0%`。**根因（很重要，別的模組也可能踩）**：這個指標的「未交」讀的是 **`order_list`（ERP 未交清單）**的 `Qty=Open_Qty AND Order_status IS NULL`，而 **`order_list` 全站只有 ERP Excel 匯入那一支會寫**（`views/pm/_upload_For_List.php`）；網頁上的綁定寫的是 `is_list.Order_id` 與 `shipment_order_map`，**這個指標從來不讀那兩個**——所以綁多少張都不會變，重算也沒用（使用者的綁定其實有存進去：`shipment_order_map` 5 筆、`is_list.Order_id` 164 筆）。**使用者拍板**：做成**逐年度可設**、**預設 C**（原話：避免 ERP 修理完之前無法正確顯示）。實作 `undone_mode` 參數——新增 registry 參數型態 **`choice`**（KPI 設定頁的參數跳窗長出下拉；存在 `params_json` 所以天生就是逐年度分開）：**C `bind`（預設）**＝分母取訂單追蹤本月交期的訂單、分子＝有綁到出貨單（`shipment_order_map` 與 `is_list.Order_id` **兩個來源都要看**，見 `ship_order_bind_lib`）且最早出貨日不晚於交期，綁一張當下重算就看得到；**B `erp_ship`**＝沿用 ERP 未交清單，但查得到同客戶同料號、沒綁訂單的出貨單就不算未交；**`erp`**＝舊制。2026 實測（分子/分母）：C＝0/260、0/176、4/255、3/327…；B＝234/260(90%)、142/176(80.7%)、218/255(85.5%)、306/327(93.6%)…；舊制＝0%、0%、0%、54.7%…。**所以現在預設 C 會讓 2026 變得很低——那是因為整年只綁了 164 筆**，要先用 B 就到 KPI 設定頁把該年度改成 B。**順帶修好一個長期對不起來的問題**：C 與 B 兩種模式下「明細不符合筆數」與「分母−分子」**完全相等**（舊制因為分母取 `order_track`、未交取 `order_list` 這兩張不相干的表，本來就對不起來）。「疑似已出貨」的查詢抽成 `kpi_as_oo_ship_hint()` 由 compute 與明細共用。驗證：`php -l`＋渲染後 inline JS `node --check`（KPI.php 與 KPI_setting.php 各 4 段）＋側欄與輸入規則健檢全過；無頭 Chrome 第六輪 16 項全過，第一~五輪 69／29／12／24／21 與寬度 39 項回歸全過。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（九次）— **KPI 排除規則：適用範圍可分開設定（僅本年度／所有年度）、已排除的不再列出、候選預設只列本年度、講明沒有存檔鈕**（使用者交辦四件）。①`kpi_as_excl_rule` 新增 `scope`（year／all）。**全年度那一列 `year` 一律寫 0**——既有唯一鍵 `uk_rule(indicator_id,year,dim,val)` 因此天然保證「同一個值只會有一條全年度規則」，也不會跟某一年的規則撞鍵；建全年度規則時自動收掉同一個值原本各年度的規則（留著只會變成兩條意思一樣的）。②**適用範圍決定要重算哪些年度**（新增 `kpi_excl_resettle_scope()`）：scope=all 就把該指標有資料的每一個年度都重算，不然會出現「規則說是所有年度、別的年度卻還是舊數字」；**已結案鎖定又沒權限的年度一律跳過並在訊息裡回報，不可以偷偷改掉封存的數字**。**`excl_rule_del` 不可再用 `year` 過濾**（全年度那一列 year=0，用 year 比對會刪不掉），改以 rule_id＋indicator_id 守門。③已經排除掉的（指標設定或既有規則）**直接不出現在候選清單**，不再只是標灰。④**候選清單預設只列「這個年度的資料裡真的有的」**（新增 `kpi_as_calc_source()`＋`kpi_as_dim_year_values()`：每個計算模組定義來源表、歸屬年度的日期欄、各維度的值／代號欄，**欄位表達式要與 `kpi_as_rules_sql()` 的 colmap 一致**，不一致就會「清單挑得到、規則卻比不中」）；這個年度沒有的要打代號或名稱才從主檔搜出來，來源徽章分成**本月／本年度／主檔**。實測 0~89ms。⑤使用者回報「設定完沒有存檔按鈕」——這個跳窗**每個動作都是按下當下就存檔**，所以改成明講：footer 加說明框、鈕改名「排除選取並存檔」「建立排除規則（立即存檔）」、新增「完成，關閉」鈕。**本次自己造成的事故（已還原，務必記取）**：收尾清理測試資料時，`DELETE FROM kpi_as_change_log WHERE changed_at>=今天 AND action IN (...)` **用時間區間整段刪，把使用者自己在 11:11 建立排除規則的兩列稽核紀錄一起刪掉了**（使用者正在同一套系統上操作，時間區間根本分不出是誰做的）。已依先前的稽核 dump 內容原樣還原並在 note 註明是還原列。**往後清理測試資料一律要用「我這個測試帳號的 id」當條件**（本專案測試帳號 109110201），**絕對不可以用時間區間或 action 類型整段刪**——使用者是即時在用這套系統的。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 第五輪 21 項全過（預設只列本年度、打字才查主檔、適用範圍下拉、建立全年度規則回報影響 2025／2026 共 20 個月、2025 年度也吃得到該規則、已排除的不在清單、存檔說明與完成鈕），第一輪 69／第二輪 29／第三輪 12／第四輪 24／寬度 39 項回歸全過；計算口徑未動（與 git HEAD 029b4c0 比對 348 格，只有 vendor_ontime 22 格不同）。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（八次）— **更正：月銷貨額的「ERP 重複轉出」判定，同單號同料號同數量是正常出貨不算重複**（使用者指正）。前一版（七次）我用「**同一張出貨單號＋同一個料號＋同樣數量出現一次以上**」當重複判定，**這是錯的**——使用者原話：「有的出貨是真的這樣好幾筆出相同數量、金額，也在同一天」。實測證實：那幾筆的 **`IS_id` 是連號、`Created_At` 完全相同**，是**同一次匯入的正常明細**；2026-04 因此誤標 78 筆、還誤報「多算 468,340」，**實際上一筆都不是重複**。**真正的 ERP 重複轉出是另一種樣子**：同一張出貨單號**被匯入兩次、而且兩次的出貨日期不一樣**（IS1150825009 一次記 2026-08-25、一次記 2026-09-01；IS1150902004 甚至被記成 2026-10-02），等於同一張單被算進兩個月份。**全庫只有 4 張單**有這個特徵，判定很精準不會誤傷正常出貨。已改用這個特徵，並在說明列寫明「同一單號出現好幾筆相同料號、相同數量是正常的，不算重複」。實測 2026-04 誤判 78→**0** 筆、2026-08 正確抓到 4 筆（24,100）、2026-09 抓到 14 筆（304,862）。**教訓**：判「資料重複」之前一定要先確認**「重複長什麼樣子」是業務事實不是直覺**——同一張單多筆相同料號數量在製造業是常態；能分辨的是**匯入批次**（`Created_At`／`IS_id` 連號）與**同一張單有沒有兩個不同日期**，而不是欄位值一不一樣。**查證方法**：把可疑的那幾組整組印出來看 `IS_id` 與 `Created_At`，一眼就分得出是同批明細還是兩次匯入。計算口徑一行未動：與 git HEAD(029b4c0) 並排比對全部 348 格，仍只有 vendor_ontime 22 格與 order_ontime 4 格不同。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；無頭 Chrome 五輪回歸（69／29／12／26／39 項）全過；測試資料、列印紀錄、變更歷史、session 與 Chrome 暫存設定檔已刪除。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（七次）— **KPI 明細：列印版不出現排除資訊、指標設定的排除也要顯示、標出「未綁訂單的出貨」與「ERP 重複轉出」**（使用者回報三件）。①**列印版一個字都不可以出現排除資訊**（使用者原話：因為要給稽核老師看的）——排除客戶字樣（寶嘉誠／泳建／東芳）、本年度排除規則清單、排除人與排除時間與排除原因、狀態欄的「已排除／規則排除」一律不印，**被排除的列直接不列出來**；印出來的就是「排除後」真正計入的那幾筆。做法是後端把排除相關的說明**拆成 `note_excl`**（畫面顯示、列印只用 `note`）。②**指標設定裡本來就有的排除也要顯示在明細內**（`params_json` 的 `exclude_clients`，新增 `kpi_as_param_excl()`）：那幾筆是被 SQL 條件擋掉的、**根本不會出現在明細上**，畫面上看不到就會有人再建一條一模一樣的排除規則；現在以灰籤標「指標設定」列在規則區（不可在這裡改，要改到 KPI 設定頁），候選清單也標成「已排除，不必重複設定」且不可點，後端 `excl_rule_add` 同規則再擋一次並回報略過了哪幾項（鐵律8）。③**使用者指出準時出貨率難看的真正原因**：ERP 轉出的出貨單很多會**重複轉**，而且**沒有在網頁上做出貨單與訂單綁定**，未交量沒被沖銷就被判成完全沒出貨。據此做了兩件事——⑴準時出貨率新增**「疑似已出貨」欄**：反查「同客戶＋同料號、交期前後 60 天內、**沒有綁任何訂單**」的出貨單，把單號與出貨日列出來並建議到快速出貨綁定（**實測 2026-04 的 154 筆未交裡有 133 筆查得到**）；⑵月銷貨額達成率標出**ERP 重複轉出**的出貨單（同出貨單號＋同料號＋同數量重複，第二筆起標成不符合，**實測 2026-04 有 78 筆、金額多算 468,340（8,998,509→8,530,169）**）。**兩者都只做「指出來」不改判定口徑**——order_ontime 的 den 來自 order_track、未交來自 order_list，本來就對不起來（見前次紀錄），在明細把某些列改判會讓筆數與 den−num 更對不上。④順手修掉排除規則的主檔查詢**每次重畫明細就重送一支**的浪費。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；**與 git HEAD(029b4c0) 並排比對全部 348 格計算值，仍只有先前那 vendor_ontime 22 格與 order_ontime 4 格不同**（本次全是顯示與提示）；無頭 Chrome 第四輪 26 項全過（**列印版逐字檢查沒有「排除客戶／寶嘉誠／泳建／東芳／排除規則／已排除／規則排除／指標設定」**、實際排除一筆後列印版沒有排除人姓名與原因且該列不印），第一輪 69／第二輪 29／第三輪 12／三種寬度 39 項回歸全過。測試排除已還原，測試列印紀錄、變更歷史、session 與 Chrome 暫存設定檔已刪除。**尚未經使用者瀏覽器實際點擊驗收**。**測試環境小坑**：同一個無頭 Chrome 連跑四五輪之後 `/json/list` 會變成沒有 page target（分頁掛了），**測試整批跑到一半突然全部沒有輸出時先確認瀏覽器還活著**，重開一個再跑就好。
> 前次：2026-09-18（六次）— **KPI 排除規則：打一部分代號要列出主檔清單讓人挑**（使用者回報）。症狀：在排除規則輸入客戶代號的一部分（`1r`），畫面只回「這個月的資料裡沒有 1r，點一下可直接加進排除規則」，**使用者根本不知道那是哪一家，也比對不到**。**根因兩層**：⑴候選清單只取自「這個月明細裡出現過的值」，這個月沒出貨的客戶本來就不在裡面 ⑵更糟的是——**排除規則比對的是「名稱」**（`bom`／`is_list`／`order_track` 存的就是中文名），把打進去的**代號**直接存成規則值**永遠不會命中**，等於安靜地設了一條沒有作用的規則。**修法**：新增 `kpi_as_dim_lookup()`（唯一實作）直接查**主檔**——客戶 `customer_list`、廠商 `maker_list`、製程 `process_no`、機台 `machine_list`、料號 `d_setting`、設計者 `user`（只列在職）——名稱與代號都可模糊搜尋，**一律回傳「正式名稱＋代號」，存進規則的永遠是名稱**；新增 API `excl_dim_search`（維度過 `kpi_as_calc_dims()` 白名單，表名欄名一律取自程式碼不吃前端輸入）。前端打字先用本月資料即時篩（畫面不會空著等）再向後端查主檔補進來，清單同時顯示代號與來源徽章（「本月」＝這個月明細裡真的有的／「主檔」），打字很快時只採用最後一次查詢的結果；原本「查無資料就直接使用輸入值」降級成**只有主檔真的查不到才出現的連結**，並講明規則比對的是名稱。實測：打 `1r` → 寶嘉誠(1RT001)／板井(1R001)／鉑竣(1R002)；打 `C2005` → 和大／和大(醫材)；製程打 `126` → 外齒研。**這一類「候選清單只從當前畫面的資料生出來」的設計，只要那份清單是用來設定「往後都適用」的規則，就一定會踩到**——規則的值域是主檔，不是這一頁剛好看得到的那幾筆。驗證：`php -l`＋渲染後 inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP（三種查法、不合法維度擋下、未登入 401）；無頭 Chrome 第三輪 12 項全過（含「選到的值是客戶名稱不是打進去的 1r」），第一輪 69 項／第二輪 29 項／三種寬度 39 項回歸全過。測試列印紀錄、session 與 Chrome 暫存設定檔已刪除。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-18（五次）— **請假統計人員明細：調過部門者在姓名下方顯示「在本部門的期間」**（使用者要求；顯示全部部門時不顯示區間）。期間由職務異動紀錄推導（最早一筆之前用 `before_json`、之後每段用 `after_json`，相鄰同範圍合併），**只是職稱升遷、部門沒變的人不顯示**；開頭／結尾無邊界印成「～2026.03.08」「2026.08.03～」（日期走共用 `egFmtDate`）。順手修掉同一列的不一致：**有部門篩選時「職稱」也要用當時在那個部門的職稱**——原本印現況，所以林郁婷在「業務課」那一列印成她現在在管理課的「會計」，她在業務課時其實是「組員」；沒有部門篩選時維持現況主職（那種情況一個人可能橫跨兩個部門，沒有單一正確答案）。CSV 的部門欄一併帶期間，列印版沿用同一份表格內容並補上樣式。
> 前次：2026-09-18（四次）— **KPI 不符合標準的明細：排除免填原因、排除規則改成模糊搜尋、可列印「排除後」的月份明細**（使用者交辦三件，接續 09-17 五次那一批）。①**排除不必再填原因**（逐筆排除與排除規則都是）——誰在什麼時候設的本來就記在 `created_by`／`created_at`，畫面上的規則籤與明細列都直接印出來（逐筆排除仍留一個**選填**的原因欄）。②**排除規則的選項改成搜尋器**（使用者原話「客戶太多」）：原本一次攤開整份清單根本挑不到，改成「打字模糊搜尋 → 從清單點選 → 已選的列在下方可逐一取消」，**代號與名稱都搜得到**（客戶ID C2005／客戶名稱和大／廠商ID／廠商名稱／製程ID／製程名稱／料號）；**查無資料時可直接用輸入的值建立規則**（這個月沒有不代表其他月份沒有）；**換關鍵字不會把已選的洗掉**。上方的客戶／料號／製程／廠商篩選下拉也把代號印進選項文字，`data-eg-filter` 打字篩選用代號一樣找得到。**代號哪裡來**：`bom`／`is_list`／`order_track` 這些來源表存的是**名稱**不是代號，所以新增 `kpi_as_client_id_map()`／`kpi_as_maker_id_map()`（靜態快取、一個請求只查一次）由名稱回查代號；製程用 `process_no`、機台用 `machine_id`、設計者用 `user.id`、料號本身就是代號。**改 dims 的資料結構（字串→`{v,id}`）時要記得上方那排篩選下拉也吃同一份**，漏改就是印出一整排 `[object Object]`（本次實測抓到）。③**新增「列印明細」**（使用者原話：資料來源是排除後，列印月份總表要能從排除後的部份依月份列印）：可只印目前這個月，也可以選「本年度全部月份」一個月一頁一次印完（逐月向後端要，不要一次打十二支）。印的是**排除之後**的內容，每一列標「計入／已排除（誰、何時）／規則排除（哪個維度）／提醒／參考」，表頭列出本月數值與分子分母、本年度排除規則。版面依 ai-rules/16（公司全名、表頭取綁定 AS 文件名稱、頁碼左下 `counter(pages)`、AS 編號右下、A4 橫式），並依 ai-rules/23 寫列印紀錄（`print_log_lib.php` 新增來源 `kpi_as`）。④明細裡的日期一律改成 `YYYY.MM.DD`（ai-rules/20），由後端 `eg_fmt_date()` 產生。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；**以 git HEAD(029b4c0) 原版並排比對全部 348 格計算值**，仍只有 09-17 那批的 vendor_ontime 22 格與 order_ontime 4 格不同（本次純顯示層，計算一行都沒動）；真 HTTP 端對端（不填原因可建立、維度白名單、沒選項目擋下、逐筆排除記下誰何時、取消還原）；**無頭 Chrome 實際登入渲染點擊第一輪 69 項＋第二輪 29 項＋三種螢幕寬度 39 項全過**（代號搜尋 41→1、名稱搜尋、點選加入、換關鍵字不掉、× 移除、查無資料可直接使用、攔截 `window.open` 驗列印版的公司全名／頁碼／AS編號／A4橫式／無 undefined、零 JS 例外）。測試資料、列印紀錄、變更歷史、session 與 Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**測試小坑**：比對計算值時看到 complaint_rate／shipping_target_amount 等當月數字前後不一樣，那是**別人正在輸入資料的即時漂移**不是改壞——判斷方式是把 HEAD 版本與新版**背對背連續跑一次**再比。
> 前次：2026-09-18（三次）— **統計報表的「部門」要依業務日期歸屬，不是依現在掛在哪個部門**（使用者回報：年度中才調部門者，整年的假全記在最新部門）。這是 `ai-rules/22` 的延伸——那篇講的是「表單要解析**人**」，這次發現**統計報表的分組鍵**也適用同一條：部門統計問的是「這個部門那一年請掉多少假」，不是「現在在這個部門的人這輩子請過多少假」；年中調部門的人整年算在新部門，**舊部門憑空少一截、新部門憑空多一截，兩邊都不對**。`eg_leave_stats()` 改成逐筆以該張假單的**起日**（與年度/月份歸屬同口徑）解析當時職務（`eg_position_resolve_from_rows()`，沒補登異動紀錄的人一律回現況＝行為不變）。**連帶一定要改的是篩選**：原本部門篩選是用**現況會籍**在 SQL 過濾，調走的人在原部門就再也篩不出來（陳喬惠 2026-03-09 由業務課調到生產3廠，她 3 月前在業務課請的假篩業務課完全看不到）——改成先全撈、再依「當時部門」在 PHP 端篩。人員明細一人仍一列，部門欄顯示「業務課→生產3廠」並標「調動」；選了部門時人員下拉補上「期間內曾屬本部門、現已調走」的人，免得圖上有她卻選不到她。實測：陳喬惠 99.01 天由「全部算生產3廠」拆成業務課 35.00／生產3廠 64.01；2026 年生產3廠 127.21→92.21 天、業務課 48.83→85.33 天；**三個年度的 KPI 總天數、部門加總、人員加總、單數四者完全一致**（拆分沒有遺漏或重複，這是這種改動最該驗的一項）。
> 前次：2026-09-18（二次）— **請假統計三件（使用者回報）：部門篩選、跨年度分頁、列印版**。①**部門篩選是真的算錯，不只是顯示問題**：篩「資材課（含下轄）」時，命中的人裡有兼任者（何沐桐主職技術課、兼任生管組），而部門統計是依**主職**分組的，於是圖上冒出**根本不在篩選範圍內的技術課／管理課／業務課**——使用者看到的就是「上面的篩選似乎跟底下圖面沒有關係」。修法：`eg_leave_stats_people()` 新增 `scopeDeptIds`，挑「這個人的假要算到哪個部門」時**優先取命中篩選範圍的那一筆職務**（與 `eg_people_list` 的 `dept_ids` 優先邏輯同一套）。實測資材課含下轄 6→4 個部門、品管組 2→1。②**「含下轄」做成可切換的勾選框（預設開）並把實際範圍寫在畫面上**：組織樹的根是**董事長室**，含下轄＝全公司，所以選董事長室會看到全公司的圖而以為篩選失效（CLAUDE.md 早就記過「挑根部門會變成全公司」，這次是同一個坑換個地方再踩一次）。③**年度比較／趨勢分析本來就是跨年度、不受年度篩選影響**，但畫面沒講，使用者看到「年度 2026」底下卻列 2025 的逐月明細——兩個分頁各加一條說明列，列印版的條件列也改印「全部年度（本表為跨年度比較）」。④**列印版依 ai-rules/16 重做**（原本是把畫面元素原樣倒進新視窗：圖被放大到半頁、表格被擠到後面、印成 3 張紙）：A4 橫式、大標題＝本公司全名（動態取，禁寫死）、副標題＋條件列、KPI 橫幅、**圖表一定要限高**（`max-height:62mm`，不限高就是一張圖吃掉半頁）、表格 `table-layout:fixed`＋`thead` 跨頁重複、頁碼左下 `counter(pages)`（超過一頁才加）、表尾編號右下、列印時間與列印人，並依 ai-rules/23 留列印紀錄（新增來源代碼 `leave_stats`）。實測 A4 橫式頁數：月度 2／年度 1／趨勢 2／部門人員 2，零橫向溢出、零儲存格裁切。**測試方法值得沿用**：用 CDP `Page.printToPDF` 真的把列印版排成 A4 橫式 PDF 數頁數，再逐一量「有沒有元素超出右緣」「有沒有儲存格 scrollWidth>clientWidth」——光看截圖會誤判（截圖寬度剛好等於內容寬度時，最後一欄看起來像被切掉，其實沒有）。
> 前次：2026-09-18 — **修好「請假統計下方沒有圖表」（使用者回報）**。畫面上 KPI 數字都在，但**圖表連同下面的表格整區空白、而且一個錯誤訊息都沒有**。根因不在請假系統：版型檔 `resource/js/custom.min.js` 在 `document.ready` 裡執行 `Chart.defaults.global.legend = {enabled:false}`，**是整個物件取代不是改欄位**，原廠預設的 `labels.generateLabels` 一起被洗掉（順帶一提那一句本身就是錯的——Chart.js v2 關圖例的鍵是 `display` 不是 `enabled`，所以它連「關掉圖例」都沒做到）；於是任何帶圖例的圖表一建立就在 `buildLabels` 丟 `TypeError: reading 'call'`，**例外是在建圖當下丟出的，`renderStats()` 後面的其他圖表與四張表格全部不會執行**，才會整區空白。修法：新增共用補丁 `resource/js/eg_chart_legend_fix.js`（排在 `Chart.min.js` 之後、ready 之前載入，先抄一份原廠圖例預設；建圖前 `egChartLegendReady()` 檢查被洗掉才還原），`mkChart()` 第一行呼叫；**刻意不改 `custom.min.js`**（全站每一頁都載入的版型檔，動它等於動全站）。全站只有本頁用到 Chart.js 圖例（另三支載入 Chart.min.js 的頁面是自繪 HTML 圖例或沒有圖例，不受影響）。驗證：無頭 Chrome 實際登入點擊四個子分頁，五張圖的 canvas 著色畫素 39,499~129,638（**修正前是 0**）、四張表格都有內容、零 JS 例外；前一批 22 項端對端測試全數維持通過。**教訓**：這個例外前一天就量到了，但因為「拿改動前的原版比對結果一模一樣」就當成既有問題只寫進回報，沒有進一步確認它**實際上讓使用者看不到東西**——「不是我改壞的」跟「不影響使用者」是兩件事，測到例外一律要追到畫面上的後果。
> 前次：2026-09-09～09-17 — **新增「工程處理紀錄」模組（`views/TD/eng_log.php`＋`EngLog_API.php`＋`eng_log_lib.php`，eng_log）**（使用者交辦，跨多天分四個 commit：`f9b3e64`／`b526723`／`69aa651`／`cb86d0d`）。發包／批圖過程中「問了誰、對方怎麼回、最後怎麼處理」的紀錄，可用**客戶／料號／廠商三軸**查回來；**刻意沒有簽核**——站上既有的 CAR 只有 2 筆、工程變更 2 筆，而訂單追蹤那個沒有簽核的「設計備註溝通紀錄」有 45 筆，加了簽核就不會有人用。**最小單位是「問題項」不是對話串**：一次批圖常有十幾二十條問題，客戶不會一次回完也不會照順序回，所以每一條各自有對象、狀態、回覆與附件，且一則回覆可一次套用到多條問題。**回覆日期 `replied_on` 與補登時間 `created_at` 一定要分兩欄**（客戶多半電話回、隔兩天才補登，只留一個欄位會讓「拖了幾天」全錯）。**尚未做完，待辦與所有踩過的坑整理在 `ai-rules/work/待補-工程處理紀錄.md`**（最大一項是訂單追蹤設計備註的雙向連動，**動工前必須先得到使用者對做法的決定**——`ateNote` 是自由文字欄，兩邊同時改會互相覆蓋，且訂單那邊的「已處理」是把欄位清空搬進 `order_ate_note_log`，同步邏輯必須跟那套並存）。**三個值得全站記住的坑**：⑴**`html` 的 `font-size` 是 10px**（Gentelella 的 `custom.css`），CSS 裡的 `rem` 只有預期的 62.5%——本頁一度把回覆日期欄寫 `9.5rem`、實際只有 95px 只露得出年份，已全面改 px；⑵**跳窗裡的自動完成下拉不可以用 `position:absolute`**，`.m-body` 是 `overflow:auto` 的捲動容器會把它整個裁掉，症狀是「打了字卻沒有清單可以選」，而**只數 DOM 節點數的測試抓不到**（要驗 `elementFromPoint` 真的可見）；⑶**兼任職務不可以事後回推**——回推一律取職級最高那筆，實測陳俊宏同時是「董事長室 董事長」與「總經理室 總經理」，永遠只會對其中一個，一律在選定當下就把部門職稱記下來。另**順手發現但未擅自修改**：`resource/js/eg_input_rules.js` 第 435 行的 MutationObserver 會對「動態產生的任何 select」都長出打字篩選框，沒有比照初次掃描（第 424 行）檢查 `data-eg-filter`，症狀是翻頁鈕旁莫名多一個重複的搜尋框；本頁已用 `data-eg-skip` 迴避，要不要統一修正是使用者的決定（影響 35 支以上共用頁）。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP 端對端 41 項；無頭 Chrome 實際登入渲染與點擊 37＋34＋26＋15 項全過。測試資料、session、Chrome 暫存設定檔皆已刪除，使用者自建的 5 筆紀錄未動。**尚未經使用者瀏覽器完整驗收**。
> 前次：2026-09-17（六次）— **快速出貨（`views/Sales/Shipping_Quick.php`）出貨單列印重做＋備註可改＋刪除出貨單**（使用者交辦一整批）。①**品名規格**改成「料號規格＋齒輪規格 ／ 訂單製程 ／ 料號備註」，與報價單列印版**同一套組法**（唯一實作 `sq_desc_text()`；`gear_spec_lib.php` 新增批次版 `eg_gear_spec_map()`、原單筆版改為呼叫它，**不要再複製第三份那段樣板替換 SQL**）。②**列印版比照紙本 2-WH-01-04 重做**：大標題取客戶主檔標記「本公司」那一筆的全名／地址／電話／傳真（禁寫死）、**A5 橫式內容寬 215mm（中一刀）**、欄位＝客戶單號(`order_track.C_order`)／產品編號／品名規格／數量／單位／單價／金額／訂單號／備註（**依使用者指定不印製令、訂單號移到最右側備註左邊**）、頁尾右下印綁定的 AS 編號（版次依出貨日回推）、多頁才印左下頁碼；標題由「送貨單」正名為「出貨單」。③**建立完自動跳出列印畫面**（多張逐張各自開視窗排隊，錯開 700ms 避免被彈出視窗封鎖擋掉）。④**備註**：確認出貨跳窗每列可先填，建立後在明細跳窗直接改、離開欄位即存（`note_save`）。⑤**新增刪除出貨單**（`delete`）：**一併清 `is_bom_map` 與 `is_order_map`／`ir_reship_map`／`return_order_map`／`shipment_order_map`**，數量回到原訂單（未出量本來就是即時由 `is_list` 加總算的，沒有第二份數量要回寫），原訂單若因此不再出滿**自動取消結案**（`sq_reopen_orders()` 與自動結案共用同一條界線）；**已進對帳底稿（`acc_recon_line`）或已開發票（`acc_invoice_item`）的一律擋下**。⑥權限新增**獨立角色 `shipping_delete`**——刻意不含在管理員以下的階層裡（刪一張貨會退回訂單數量、還可能把已結案訂單重新打開），要另外指派。⑦出貨單可綁 AS 文件編號（走共用 `asdoc_lib`＋`eg_asdoc_picker`，模組代碼 `shipping_note`，已綁 2-WH-01-04）；列印留紀錄（ai-rules/23 新來源 `shipping_note`）；補上鐵律7 的使用說明跳窗。**同批修掉三個 bug**：⑴**「可出 0 卻標示無完工」的真因是殘留的 `is_bom_map`**——出貨明細被刪掉（使用者先前手動刪測試資料）後分配列還在，製令的「已出」永遠掛著、可出算不回來；`sq_bom_avail_map()` 的已出量改成 **JOIN `is_list`**，殘留列一律不計，另把可出 0 的原因細分成**無製令／無完工／製令已出完**（一律寫「無完工」會讓使用者以為系統壞掉）。⑵**底部動作條 `left:0` 會讓最前面的「已選 N 筆」整段躲到左側選單底下**（使用者：「合計旁的字被遮蔽」）——改成從內容區左緣起算（nav-md 230px／nav-sm 70px／≤991px 為 0），**只移動動作條、頁面寬度一個字都沒動**。⑶**近期出貨單捲動時列會穿過凍結表頭**：原本由 `.m-body` 捲動、它有 `padding:14px`，**sticky 只黏在 padding box 的邊**，內容會從表頭上方那 14px 內距穿過去；改成 `#rcList` 自己當捲動容器（無上內距）＋表頭補不透明底色與 z-index。**另一件很容易被忽略的事**：`is_list.Client_id` **全表都是空的**（ERP 匯入從來沒帶），所以出貨單抬頭的統編／電話／傳真／地址原本一定印不出來；改用會計模組的 `acc_customer_by_name()`（**含別名**，ERP 寫「高鋒工業」、主檔是「高鋒」只有它對得起來）以客戶簡稱歸戶，`acc_lib` 那支查詢補上 `customer_tel`／`customer_fax` 兩欄。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 4 段＋側欄與輸入規則健檢全過；真 HTTP 端對端（權限守門、CSRF、對帳底稿擋下、自動結案與取消結案、非管理員拿不到 as_docs 清單）；**無頭 Chrome 實際登入渲染並點擊 21＋21＋6 項全過**（動作條不再被側欄蓋住＝已選 1 筆 x=88 vs 側欄右緣 70、凍結表頭、品名規格、明細備註自動存檔、真的從畫面刪掉整張單、列印版量測 sheetW=813px＝215mm／9 欄順序正確／單頁 475px 放得下 A5 橫式 559px、零 JS 例外、零橫向溢出）。測試出貨單／稽核列／列印紀錄／session／Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**測試小坑**：用同一個 PHPSESSID 同時從 curl 與無頭 Chrome 打 API，session 檔會被互相覆寫而出現**假的 CSRF 失敗**（token 值其實一模一樣），單獨重跑就全過——比較前後行為時兩邊各用一個 session。**已知未做**：出貨單上的「業務人員／發票方式／發票日期／發票號碼／折讓沖款／未收款」目前印空格供手寫（`is_list` 沒有這些欄位）。
> 前次：2026-09-17（五次）— **KPI（`views/news/KPI.php`）「不符合標準的明細」大改**（使用者交辦五件）。①**廠商準時交貨率沒登錄回廠日不再一律算遲交**：依序用「下一製程發包日→QC檢驗日→出貨日→製令結案日」推估回廠日（使用者原話「已結案(出貨)卻一直顯示未回廠，這樣不合理」），推估值**只用於判定、不寫回任何一筆資料**，明細上標「（推估：○○）」；四個都查不到才算真的未回廠。**compute 與明細一律共用新的 `kpi_as_vendor_rows()`**——兩邊各寫一次 SQL 遲早走鐘（出圖準時率就踩過：明細筆數與 den-num 對不起來）。實測 2026-04 有 97 筆是推估出來的；**這一改讓 2025／2026 共 22 格的準時率大幅上升（分母一格都沒變，只有分子變多）**，例 2026-01 由 32.6% → 74.0%。②明細加**客戶／料號**欄（廠商準時交貨率、進料檢驗不良率）；**準時出貨率的客戶欄原本印的是代號 C2005**（`order_list.Client_name` 存代號、`order_track` 存中文名），已對回 `customer_list` 顯示中文簡稱。③**篩選與拖曳多選**：明細上方依「這份資料實際有哪些值」長出客戶／料號／製程／廠商／機台／設計者下拉（皆 `data-eg-filter`）＋關鍵字；表格上**按住左鍵往下拖曳就能一次勾選多列**（再拖一次＝取消）。**選項一律從資料收集，不預先寫死任何清單**（鐵律4）。④**新增排除規則**（新表 `kpi_as_excl_rule`）：把特定客戶／製程／廠商／料號**整年度**排除在該指標之外，與既有的 `kpi_as_adjust`（某一格的某一筆）互補；建立或取消時**該年度已結束月份全部重算**（不重算就會出現「規則加了、別的月份還是舊數字」這種完全看不出原因的落差），被規則排掉的列**仍然列出來並標「規則排除」**（排掉就看不到＝解不開）。維度代號走後端白名單 `kpi_as_calc_dims()`，前端擋一次後端同規則再擋一次（鐵律8）。⑤**補做六個指標的明細**（使用者：就算不可更改真實數據也要做，方便確認與聯動去調整）：月份受訂目標達成金額／月銷貨額達成率／報價單接單率／產能績效-創成·成型／齒研製程不良率。金額與產能這種「加總型」指標**連正常資料一起列出來當參考**（標「參考」，預設收起、可切「全部」），不符合標準的那幾筆才標紅——**截斷 500 筆前一定要先把不符合標準的排到最前面**，否則銷貨額那種幾百筆正常資料會把真正有問題的 109 筆整批切掉（實測只看得到 1 筆）。⑥**明細表格改成自動換行、不再有左右捲軸**（`table-layout:fixed`＋`overflow-x:hidden`；使用者指名出圖準確率那張）；踩到的點：`.vio-edit` 那一欄的 `white-space:nowrap` 與 78px 標籤會把內容撐出容器，**`table-layout:fixed` 擋得住欄寬卻擋不住儲存格內容溢出**。⑦**順手修掉三個既有缺口**：⑴出圖準時率／進料檢驗不良率／教育訓練達成率**排除寫得進 DB 卻完全不影響計算**（compute 根本沒吃 `$exclRows`）⑵當月即時試算與試算頁三個呼叫點都沒帶排除條件（收斂成 `kpi_as_compute_iy()`，**所有算這一格的地方都要走它**）⑶準時出貨率的「排除客戶」只擋到分母（order_track）沒擋到未交量（order_list 存代號永遠比不中），2026 年 4 格因此更正。⑧補上鐵律7 的**使用說明跳窗**（本頁原本沒有）。**另外查證後寫進說明列（未改口徑）**：準時出貨率的分母取自訂單追蹤、未交量取自 ERP 未交清單，兩張表各自獨立，舊月份常出現「未交筆數比訂單筆數還多」（2025-01 是 86 vs 346）而準時率被算成 0%，這是既有結構問題不是本次造成的。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；**以 git HEAD 原版並排比對全部 348 格計算值**，只有上述 vendor_ontime 22 格與 order_ontime 4 格不同（另 1 格是測試期間有人新增報價單造成的資料漂移）；真 HTTP 端對端（權限、未登入 401、維度白名單、必填、規則建立/取消與重算筆數、被規則排掉的不可再逐筆排除、逐筆排除與取消）；**無頭 Chrome 實際登入渲染並點擊 69 項＋1280/1600/1920 三種寬度零橫向溢出 39 項＋使用說明 8 項全過**（客戶料號欄、推估回廠日、四個篩選真的篩得動 261→4、關鍵字、拖曳一次選 5 列、規則區與值連動、六個新指標都開得起來、零 JS 例外）。測試建立的排除規則／逐筆排除／變更歷史／當月快照／session／Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**要注意的兩件事**：⑴廠商準時交貨率的數字會明顯上升，2026 年 1~8 月的快照已在測試中重算成新數字，**2025 年與準時出貨率的快照還是舊值**——開頁面時系統偵測到來源異動會自動重算，或由 KPI 管理者按「重算本年」（2025 已鎖年度，只有管理者能重算）⑵排除規則目前**只做到年度層級**，沒有做「只排除某幾個月」。
> 前次：2026-09-17（四次）— **行事曆的休假自動變成請假單、請假系統長出管理者設定（角色＋自動建單）、人員名單補上兼任者的主職務**（使用者交辦一整批）。①**先講最關鍵的發現：`leave_request` 整張表是空的（0 筆），行事曆卻有 484 筆休假**——人事一直都是直接在 `calendar.php` 挑「休假」類別＋假別＋發生者，那只是一筆 `evenement`，請假系統裡一張單都沒有，所以請假統計、特休額度、代理人通知全部看不到這些假。現在兩邊接起來：唯一實作 `src/common/leave_calendar_lib.php`，行事曆存檔／修改／刪除三個寫入點都會同步（`_events_setting.php` 兩處、`_deleteSchdule.php`）。**建單一律轉呼叫既有的 `eg_leave_submit()`**（新增 `_auto` 放寬參數），不另寫一份 INSERT——否則時數計算、代理人解析、簽核鏈會變成兩套。②**刻意不去動使用者畫的那筆事件**（不改類別、不改標題）：實測一筆事件最多掛 4 個人，改它等於改到別人的；只把 `leave_request.evenement_id` 指過去，**銷假時 `eg_leave_event_remove()` 新增 `$ownerUserId` 參數，事件上不只一個人就只抽掉這個人的 actor、不整筆刪掉**（一般由請假系統自己建的事件只有一個 actor，走的還是原本的整筆刪除，行為不變）。③**迴圈保護**：請假系統核准時自己也會建行事曆事件，那種事件一律不可回頭再建一張單（判定＝已經有 `leave_request` 指著它）。④**是否需要簽核做成管理員設定，預設「不需簽核」**（使用者指定）：`leave_cal_need_approval` 覆蓋假別本身的 `need_approval`；另有「自動建單」與「建立後通知當事人」兩個開關（通知預設關，人事整批登錄時不洗版）。⑤**舊資料已補建完成**：478 筆可建事件 → **建立 466 張假單**（2025 年 150、2026 年 325 筆事件），10 筆失敗是**整段落在颱風假／調配休假／週末＝沒有工作日**（本來就不扣假，已改成在待補清單直接標示「不需要請假單」而不是一直留在那裡碎念），6 筆是**行事曆上沒選假別**建不出來（要人工回去補）。⑥**踩到的坑**：全天事件的結束日在行事曆是**含當天**（實測 9/14~9/18 就是請 5 天），直接拿 `end` 的 `00:00:00` 會算成 0 小時然後整批建不出來，要換算成 `起日 00:00:00 ~ 迄日 23:59:59`。⑦**補舊資料不可以套送審那一套前置檢查**：補請假 7 天限制、特休額度、喪假親等、附件必填四關全部會擋下既成紀錄，改成 `_auto` 放寬並**把擋不下來的理由收成 warns 回報**（不可安靜吞掉）。⑧**請假系統新增「管理者設定」**：角色可新增／改名／刪除＋勾選功能（走全站共用 `Roles_API`，不另建一套角色表），**刪除角色前一律先查 `role_usage`**——有人被設成這個角色就不給直接刪，列出是誰（部門 職稱 姓名），再讓管理員選「一鍵轉換成另一個角色」或「確認一併移除」；`delete_role` 後端同規則再擋一次（以前是**安靜地把 `user_roles` 整批刪光**，人就這樣默默失去權限）。新增三個真的有守門的功能碼：`leave_stats`（非主管也能用統計，範圍限自己）、`leave_cancel_other`（代他人銷假）、`leave_cal_manage`（管行事曆自動建單）。⑨**請假統計的人員下拉：選了部門就只列該部門（含下轄）的人**，沒選部門才列全公司；部門篩選改用 `eg_dept_subtree_ids()` 含子部門，**統計 SQL 也一併改**，否則選「資材課」畫面選得到卻永遠 0 筆。⑩**人員名單補上兼任者的主職務**（使用者：好多頁面都有這個問題）：`eg_people_list()` 一人只回一列、挑的是**職級最高**那筆，所以何沐桐（主職 技術課 工程師、兼任 生管組 組長）只剩「生管組 組長」、**在技術課底下完全找不到他**；新增 `all_posts` 選項＝一個職務一列（38 人展開成 47 列），**下拉 value 改用 `post_key`（uid:職務id）**否則同一人兩列 value 重複；排序統一 部門→職稱→姓名（職位高者在上）。**要「代表身分」的情境（簽核、圖章、通知）刻意不開這個選項**，已寫進 CLAUDE.md 人員列表鐵則⑥與 `ai-rules/08` 第五節鐵則 7。⑪順手補上鐵律7 的「使用說明」跳窗（本頁原本沒有）。驗證：`php -l` 全過＋**渲染後**的 inline JS `node --check` 4 段全過＋側欄與輸入規則健檢全過；真 HTTP 端對端（行事曆新增→自動建 1 張單、改期間與假別→舊單刪除重建且時數重算、刪事件→假單一併撤除、多人事件 4 人各一張且刪其中一人不動事件、重跑不重複建、非管理員打四支設定 API 全部 403、角色使用中被擋／一鍵轉換／強制刪除／轉換成自己被擋）。測試用的事件、假單、兩個測試角色、測試 session 皆已刪除，`user_roles` 零孤兒；補建出來的 472 張單是正式資料（使用者指定要補）。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：行事曆上那 6 筆沒選假別的休假要人工回去補假別才建得出單；本頁在 `input_rules_baseline.txt` 基準線內，統計的人員下拉（47 列）目前**沒有**打字篩選框（載入 `eg_input_rules.js` 會改變本頁既有的 Enter 行為，未擅自加）。
> 前次：2026-09-17（三次）— **線上檢驗（`views/QC/inspection_entry_v2.php`）量具改成「整張檢驗單綁一次」，不再綁到個別檢驗項目**（使用者交辦）。使用者拍板的口徑：**追溯只需要查得到「這支量具用在哪幾張檢驗單」，不需要查到是哪一個檢驗項目**，所以清單上的量具欄整欄移除，改成一次顯示本單所有選定的量具編號與種類。①**新表 `qc_form_tool`（檢驗單↔量具，多對多）**，唯一實作放 `qc_inspection_lib.php`（`qc_form_tools_parse/save/rows/label`），`save_inspection`／`update_inspection`／`save_adhoc` 三個寫入點共用，不存在的量具後端一律擋下不寫。**`update_inspection` 沒送 `tool_ids`＝舊呼叫端，量具原封不動；送空陣列才是真的清空**（兩者是不同語意，比照製表人那次用 `array_key_exists` 判「有沒有送這個欄位」）。②**舊資料不能就這樣不見**：migration `views/QC/migrations/2026-09-16_qc_form_tool.php` 把 `qc_measurement.tool_id` 依檢驗單去重回填（8 列／6 張單；另有 1 張是 `qc_check_form` 早就被刪掉的孤兒明細，跳過）；`qc_form_tools_rows()` 另留一條**即時退路**——表列查不到時由該單讀值上的量具推回來，這樣連**沒走共用庫、自己寫 `qc_measurement` 的舊頁面**（`inspection_result_entry.php`）存的單子也看得到量具。`qc_persist_readings()` 則反過來：讀值上有帶量具（舊頁面）就同步成表單層級，讀值完全沒帶（新版 v2）才不動。③**跳窗維持原本的兩層大按鈕**（使用者中途指定「要跟原本的量具選單一樣，先點分類才選編號」）：① 先點**類型** → ② 再點**編號**，差別是編號**可連續點多支**（再點一次＝取消）、選完能「← 換一個類型」**繼續加**，類型按鈕上標「已選 N」，已選清單兩個步驟都看得到、每支可按 ×。④**存檔前檢核由「每個數值型項目都要選量具」改成「有數值型量測就至少要選一支」**，點提示列直接把量具跳窗打開（量具不屬於任何一列，原本那條「跳到該欄位」的路走不通）。⑤列印版／CSV／同料號歷次檢驗明細的量具欄一律改成**整張單印一行**（列印在基本資料區多一列「使用量具」），多製程合併列印同步；量具校驗頁的「使用紀錄」改查 `qc_form_tool`（仍 UNION 舊的 `qc_measurement.tool_id`）。⑥**順手修掉一個既有 bug**：還原讀值的分組鍵是「方法|量具」，**同一支量具量兩次（加量測）會被併成同一組、讀值互相覆蓋**；既有資料實測 0 筆踩到，但量具改綁單張之後主量測與加量測的 tool_id 都是 NULL＝**非修不可**，分組鍵補上 `reading_seq`。**踩到的兩個測試坑**：⑴頁面 JS 全包在 jQuery ready 裡**不是全域**，無頭 Chrome 只能用真實 DOM 點擊測，不能 `evalJs` 直接叫 `renderItems`；⑵**原生 `alert`／`confirm` 會把 `Runtime.evaluate` 整個卡住**（症狀是 timeout 不是失敗），一定要掛 `Page.javascriptDialogOpening` 自動關掉（`accept:false`＝一律按取消，確保不會誤寫入）。另外頁面參數是 `?bom_ing_fid=` 不是 `?fid=`，帶錯會進「示範模式」看起來像頁面壞掉。驗證：`php -l`＋**渲染後**的 inline JS `node --check`（3 頁共 15 段）＋側欄與輸入規則健檢全過；CLI 單元 10 項；真 HTTP 端對端（建單寫入量具、不存在的量具被擋下、改單換量具、未送 tool_ids 不動、送空陣列清空、舊單回填後仍查得到、量具反查使用紀錄、多製程列印帶量具、五支相關頁面 200）；**無頭 Chrome 實際登入渲染並點擊 49＋5 項全過**（兩層跳窗、同類型連點多支、跨類型累加、× 移除、清除全部、重開跳窗保留已選、沒選量具按儲存被擋下且點提示直接開跳窗、三種檢視與列印版每一列欄數都對得上、零 JS 例外、零橫向溢出）。測試檢驗單／草稿／測試項目／session／Chrome 暫存設定檔已全部刪除（`qc_form_tool` 回到回填後的 8 列／6 張單），未動任何正式業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**已知未動**：舊頁面 `inspection_result_entry.php`（選單上的「QC檢驗」）與 `inspection_combined_prototype.php`（「QC檢驗_NEW_TEST」）仍是逐項選量具的舊介面，只是存檔後會自動同步成表單層級；本頁的「使用說明」跳窗已同步改寫。**同批依使用者回報再修版面**：一次選五、六支時「本單已選」的標籤太大、擠成一大塊。⑴標籤縮小（高 28→22px）並重排層級——**編號是主角(深色)、種類改小字淡色**，已選區 `max-height:96px` 超過就自己捲，不把下面的類型／編號按鈕擠下去（主畫面那排標籤同一組樣式一起變小）；⑵跳窗 600→**720px**（固定像素，**不可用 vw**，會蓋過側邊選單），原本一列只放得下兩個標籤；⑶**順手修掉編號與規格重複印出**：量具「H-003-Q 三點式(20-25mm)」的規格欄是「20-25mm 三點式」，原本只比整串比不到，就接成「…(20-25mm)(20-25mm 三點式)」，光一個標籤 311px——改成**逐詞比對**，前端 `toolNoSpec()` 與後端 `qc_form_tools_label()`（列印用）同一套規則。實測 6 支：已選區 110px／3 列 → 61px／2 列，最長標籤 311→213px，選到 14 支仍維持上限可捲動；回歸 49＋5 項全過並實際截圖目視兩個步驟。**測試踩到的坑**：無頭 Chrome 固定用 9333 埠會**撞到平行 session 已經開著的那一個**（啟動時只印一行 `bind() returned an error` 很容易忽略），結果是**接到別人的瀏覽器**、截圖拍到別人的頁面——收尾終止程序也只能比對「本 session 專屬的 `--user-data-dir`」，往後一律換一個自己的埠。
> 前次：2026-09-17（二次）— **公告／通知（`views/liveEvent/createEvent.php`）：批次改對象可只勾 1 則；新增「設為自動已閱」與來源層級的「免點開自動已閱」**（使用者回報一件、交辦一件）。①**篩選後只剩一筆時「批次改對象」按不下去**＝那一筆永遠改不了對象（前端 `n < 2` 與後端擋門各一處），已一起放寬到 1 則；**勾 2 則以上時仍要比對「對象完全相同」**。②使用者要的是「**快速讓特定通知自動已閱，不需要自己再點開**」——先問清楚才動工（`AskUserQuestion` 三題全拍板）：**兩件一起做**（改通知方式＋把現有未閱者一次標掉）、**範圍是所有未閱的人**、**要做來源層級的全站規則**。於是工具列新增「設為自動已閱」：勾 1 則以上，一顆按鈕把 `read` 的對象改成 `autoread`，**並把目前還沒讀的人直接標成已閱**（鈴鐺當下就少掉，誰都不必再點開）；代標已閱**限系統管理員**（比照既有「快速已閱」），**回簽／回覆+回簽 的對象一律不動**（那要本人表態），留 `audit_log` 與 `live_event_history`。③來源規則（限系統管理員、全站套用）：指定來源往後發出的通知不必點開就視為已閱。**這裡有一個一開始想錯、查證後改掉的作法**：本來想在「通知建立當下」處理，但**全站有 31 支程式各自 `INSERT live_event_target`，根本沒有單一收斂點**；改成在**側欄鈴鐺與手機通知頁查詢「之前」懶惰補寫本人的已閱紀錄**——只要改兩個消費端、已讀紀錄是真的寫進資料表（所以「已讀／應讀」欄、統計、快速已閱面板全部自然一致）、使用者永遠看不到那則未讀＝等同「發出即已閱」。**沒設定規則時函式直接 return，零成本**（最常見的情況不多跑任何查詢）。**規則刻意只對「啟用之後才發布」的通知生效**（每個來源各記啟用時間），否則一勾下去會回頭把幾年份歷史通知全部標成已閱；現有積壓請用②清掉，兩個功能互補。共用實作 `src/common/notice_autoread_lib.php`（收件人通知方式判定／**已閱紀錄的唯一寫入點**／來源規則），`_eventQuickRead.php` 改為共用同一份；「這則可不可以改」的判定抽到 `notice_event_lib.php` 的 `eg_notice_event_editable()` 供兩支 API 共用（鐵律4）。`signed_via` 的語意：NULL＝本人自己按、>0＝某人代按、**0＝系統依來源規則自動標記**。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 12 段＋側欄與輸入規則健檢全過；真 HTTP 端對端（check/apply、來源規則存讀、**sign 對象不被誤標**、重複執行不重複標、未登入與非管理員擋下、`notice_edit` 非管理員只改通知方式不代標、規則啟用前的舊通知不被補寫）；**無頭 Chrome 實際登入渲染並點擊 20 項全過**（勾 1 則兩顆按鈕都可按、跳窗先跟後端要最新數字、來源清單載得到且已設定的預設勾起、零 JS 例外、零橫向溢出）。測試資料（6 則測試通知與其對象／已讀／歷史、稽核列、設定列）、三個測試 session 與 Chrome 暫存設定檔已全部刪除，SQL 確認零殘留。**尚未經使用者瀏覽器實際點擊驗收**。**測試小坑（又一次）**：用 curl 在指令列傳含中文的 JSON，**Git Bash 會以 Big5 送出**、`json_decode` 失敗，症狀是「存進去變空的也不報錯」——中文 payload 一律改從 UTF-8 檔案讀（`--data-urlencode name@file`）。
> 前次：2026-09-17 — **新增「溝通管理」模組（3-GM-01），把三份紙本做成一頁三分頁**（使用者交辦）。`views/GM/communication_mgmt.php`＋`src/store/CommMgmt_API.php`＋`src/common/comm_mgmt_lib.php`，對應 3-GM-01-03 溝通管制表／3-GM-01-01 利害關係者溝通記錄表／3-GM-01-02 回應利害關係者措施追蹤表。**三份表單是一個漏斗，程式設計直接照這個關係做**：管制表只寫「計畫好要跟誰溝通、多久一次」的常態機制（不寫單一事件）→ 記錄表記事件當下，當下解決的到此為止 → **需要後續行動的才按該列的「轉追蹤」進追蹤表**（追蹤表只列有負責人、有預計完成日的，結案後下次審查移出，故預設只顯示未結案）。**使用者以 AskUserQuestion 拍板四項**：①部門主管確認同部門找不到時**沿部門樹往上一層找**（不是直接免簽）②簽核**序列**跑「送出→部門主管確認→總經理確認」③一人身兼多職者**建單時自己選填表身分**（預設主職）④記錄表要能傳佐證附件。**主管解析是本次核心**（使用者指定規則）：候選＝同部門內「`position.sort_order` 職位編號小於填表人」且「`position_level.level` 階級不低於管理員設定門檻（預設三階主管以上）」者，同部門找不到才往上層部門找。**寫到一半發現平行 session 剛訂了 ai-rules/24「單位主管往上只追到課級」的全站規則，於是加上同一個天花板**——不加的話課級最高主管（品管課課長／資材課副理／技術課課長）開的單會往上抓到總經理當「部門主管確認」，**而總經理本來就要簽下面那一格＝同一個人蓋兩格章**，紙本不會這樣簽；天花板常數直接共用 `unit_supervisor_lib.php` 的 `EG_UNIT_SUP_TOP_LEVEL`，**但刻意不直接呼叫 `eg_unit_supervisor()`**（那支回「該單位最高主管」單一人選，本表單要的是「符合條件的全部人、任一人簽即可」且門檻可調，語意不同，已在程式內註明）。「總經理確認」預設取組織角色綁定的 `top_approver`（禁寫死人名），可改指定人員或指定階級以上；兩格都可由管理員設定誰能簽（使用者要求）。其餘：簽章日期一律用**該單溝通日期**不是按下確認那天、單號依溝通日期產生（補歷史紙本才對得起來）、代理人可代簽並加「代」字、退回必須填原因、送出與轉追蹤都套「點開即刷新」擋重複。列印依 ai-rules/16（公司全名動態取／表頭取 `doc_name`／編號右下／記錄表版次依溝通日期回推）、圖章依 ai-rules/18（`eg_stamp.js`＋可設模板，91px）、列印紀錄依 ai-rules/23（來源 `comm_mgmt`）。**測試踩到的坑**：⑴測試腳本用 curl 把含中文的 JSON 走 argv 送出，**Git Bash 會轉成 Big5**，後端 `json_decode` 失敗＝症狀看起來像「items 沒收到」（CLAUDE.md 早有記載，改用 PHP 驅動測試即解）⑵偽造 session 檔的 **PHPSESSID 不可含底線**，PHP 會判定為非法 session id 直接當沒登入⑶`<option>` 上不可掛 jQuery `data()`——打字篩選（`eg_input_rules.js` 規則7）會用 `innerHTML` 整批重畫選項，掛在 option 上的資料會整個不見，症狀是「篩選過一次之後簽核人預覽就空白」，只有實際打字篩選才看得到，已改成模組變數 `IDMAP`。驗證：`php -l`＋**渲染後** inline JS `node --check` 四段＋側欄與輸入規則健檢全過；真 HTTP 端對端 **50 項**（必填、冒用填表身分、CSRF、送出退回重送、免簽路徑、轉追蹤重複擋下、三種權限守門）；**無頭 Chrome 實際登入渲染並點擊 63 項**（分頁切換、跳窗、打字篩選後預覽仍正常、可增列、前端即時驗證、真的填完存檔、列印版內容、設定跳窗、零 JS 例外、零橫向溢出）＋**已結案單圖章列印 9 項**（兩格章都畫得出來、姓名正確、日期＝溝通日期且為 YYYY.MM.DD）。測試資料（3張記錄表／追蹤與管制各1筆／簽核紀錄5列／通知8則／session／Chrome 暫存設定檔）已全部刪除、SQL 確認零殘留。**已完成的設定**：三份 AS 文件已綁定（doc id 30/31/32）、選單登記在「測試功能」、角色 `cm_view`／`cm_admin` 已建立並在使用者權限設定頁長出區塊。**尚未經使用者瀏覽器實際點擊驗收**。**本次自己的疏失**：移除幾個無效的自創事件觸發時用了 `php -r` 拼字串改檔，違反記憶 [[feedback_no_phpr_file_rewrite]]（該記憶明講一律用 Edit）；當下有驗證替換次數與檔案長度、事後 `php -l`＋行數＋檔尾都確認無誤，但這條規則就是為了避免「驗證看起來沒事、其實檔案已毀」而訂的，不該便宜行事。
> 前次：2026-09-16（二次）— **全站「單位主管」要往上追溯到上級單位，但只追到「課」為止**（使用者回報：文件制修申請單 DA-202609-002 自動簽核，品管組組長蔣騏竹自己開的單，「單位主管」欄蓋的是他自己的章；組織圖上他上面還有品管課課長，而且「其他的單據也有相同問題」）。**規則（使用者定調，寫成新檔 `ai-rules/24-審核層級規範.md` 並登記進路由表）**：①單位主管＝**申請人所屬單位的最高主管** ②申請人自己就是該單位最高主管→**往上一層單位**（`department.parent_id`，設定入口＝`department_job_title_settings.php` 的「上層部門」）逐層往上 ③**天花板是「課」級**（`department.level<=3`）——課級以上（總經理室／董事長室）是所有單位的共同上級、不是誰的單位主管，所以**課級單位的最高主管本人開單時，單位主管從缺、由申請人自己簽**。**根因是一段從來沒成功過的程式**：各模組「往上一層找主管」清一色只認 `department_position.primary_user_id`（指定負責人），而**那個欄位全站一筆都沒有設定**，所以 `eg_resolve_supervisor()` 對組長以上一律回 null，呼叫端只好退回本人或最高決策者——這就是「自己蓋自己的章」的來源，`php -l` 與看程式碼都看不出來，要拿真資料逐人跑一遍才會現形。**唯一實作 `src/common/unit_supervisor_lib.php`**（`eg_unit_supervisor()`／`eg_unit_dept_head()`，吃業務日期回推當時職務＝ai-rules/22、留職停薪不列入候選、代理仍由呼叫端再過 `eg_resolve_signer()`）。**連動改的六處**：doc_apply 四格簽章（組長→課長；課級最高主管仍蓋自己）／eng_change「單位主管」關（新增來源 `unit_sup` 並設為預設，既有設定值 `ec_sign_sup` 已由 `apply_dept_mgr` 移轉）／business_trip 公出單核准人（組長公出改由該課課長核准，**課級最高主管本人公出才用最高核准人員**＝紙本附註不變）／training 教育訓練需求申請單／`delegate_lib` 的 `eg_resolve_supervisor()` 改成共用庫薄包裝（**連帶修好請假、申請採購、審核表單、供應商稽核清冊、設備一覽表、量測儀器校驗、專案管理**——這些模組原本對組長以上都解析不到人）／`form_signer_lib` 的 `fsd_supervisor_at()`＋`fsd_upper_dept_manager()` 補上課級上限。**另一個順手修掉的判定錯誤**：舊版用「職級比我高且最接近」找主管，而課長2、副課長3、組長3、副組長3 同階有好幾個職稱，嚴格比大小會讓**副組長跳過同組的組長直接找課長**（實測 張文俐→吳佳靜）；改成「誰是這個單位的最高主管」後才正確（張文俐→何沐桐）。又：`eg_resolve_supervisor()` 舊版拿**主職**的職級去比 `$depHint` 指定的那個部門（何沐桐主職是技術課工程師＝無職級），實測把同組的副組長判成他的上級。驗證：`php -l` 全過＋**全公司 48 個職務逐一跑一遍**（結果與組織圖逐欄對照相符）＋五個模組各自的解析函式逐人比對一致＋三頁真 HTTP 200＋**渲染後 inline JS `node --check` 12 段全過**＋側欄與輸入規則健檢全過。**已核准單據的簽章是當初存下來的，不受本次影響**：另附重算工具 `views/ADM/migrations/2026-09-16_doc_apply_unit_sup.php`（預設試算、`--run` 才寫入），試算結果＝16 張「單位主管＝申請人」的已核准單中 **8 張要改**（蔣騏竹×5→高志宏、陳彦驊×3→吳佳靜）、**8 張依規則本來就該自己簽**（課長／經理自己開的單）——**尚未執行，等使用者決定要不要回寫歷史簽章**。三頁使用說明已同步更新（鐵律7）。測試 session 已刪，未寫入任何業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**已知未動**：`as_form_flow.php` 的 `dept_manager` 是設計者自己設「N 階以上」門檻的候選池（不是自動解析單一人選）、供應商稽核計劃核准人來自設定綁定的部門（使用者另外定調過「找不到時允許同一人」）、內部稽核的「受稽單位主管」是被稽核那個單位的主管，三者都不適用本規則，已列在 ai-rules/24 的相容表。
> 前次：2026-09-16 — **內部稽核（`views/ADM/internal_audit.php`）：人員一律依「單據自己的業務日期」判定＋新增年度稽核小組＋審查/核准改依設定解析並可自動簽核**（使用者交辦一整批）。①**這次最重要的一條，而且是使用者已經在別的頁面看我修過、這裡又犯一次的**：「挑人的下拉」也算解析人員——`eg_people_list()`／`eg_people_posts()` **永遠只回今天在職的人、今天的職稱**，所以補歷史單據時「當時在職、現在已離職的人」一個都挑不到（實例：文管中心負責人葉卿雅在 2025-11-03／2025-12-04 都還在職，補那兩張單卻選不到她），而且**完全不報錯**——畫面上只是「這個人不在清單裡」，看起來像資料沒建好不像 bug。修法：共用庫新增 **`eg_people_posts_asof()`**（`people_lib.php`，**唯一實作**；`doc_apply_lib.php` 早就自己寫了一份 `da_people_posts_asof()`，內部稽核完全不知道它存在才又踩一次，現已改成薄包裝轉呼叫），內稽的稽核員／陪檢員／稽核組長／製表人／稽核人**全部改吃單據業務日期**（通知單＝稽核起日、查檢表＝稽核日期、製表人＝製表日期），**日期一改下拉即時重抓**（新 action `people_asof`），**後端存檔用同一個日期再驗一次**（鐵律8；只改前端＝挑得到卻存不進去又看不出原因）。**使用者問「沒辦法寫進 MD 內自動改善與避免嗎」，答案是可以**：`ai-rules/22` 新增**第5坑**、`check_asof_position.php` 新增 **C 區**自動掃出「有業務日期卻只用現況清單」的檔案（目前掃出 17 支嫌疑，已列進該篇相容表）。②**資格名單改成「職位」與「職位＋指定人員」兩種可並用**，指定人員可設**任期**——**本人請假由代理人暫代**就是用這個（同部門同職稱的其他人不會跟著有資格，期間一過自動失效不必回來刪）；另**AS 文件負責人自動具備稽核員資格**，期間比照 `as_document_management.php` 的任期設定（讀取邏輯抽成 `src/common/asdoc_editor_lib.php` 兩邊共用＝鐵律4）。**建表流程裡那段「把 user_id<>0 的列轉成 user_id=0」的一次性 migration 必須移除**——不移除的話每次開頁面就把使用者剛設的指定人員默默洗成「整個職務都有資格」，同職稱的其他人也跟著有資格而且不報錯。③**新增「稽核小組」（年度，可從其他年度複製）**：自動建立會議紀錄時**與會人員＝小組成員、主席固定為稽核組長**（沒建小組才退回用該通知單的稽核員與陪檢員）；**會議紀錄的部門職稱也修好了**——原本只傳 user_id 給 `ia_identity_asof()`，它會挑「職級最高」的那個兼任職，跟通知單上挑的職務對不起來（使用者回報「會議紀錄上的部門與人員不正確」的根因），現在一律連「他是以哪個職務出席」一起帶，再依**會議日期**回推名稱。④**年度計畫的「審查」跟設定好的不同**：送審時把 reviewer 寫成**按下按鈕的人**，列印卻依「設定→列印簽章→審查格」解析成管理代表，兩邊本來就對不起來；現在畫面與列印**共用同一支 `ia_sign_slot_person()`**（設定留白才退回操作者）。同批新增**自動簽核開關**（送審即完成核准，業務日期＝送出日、簽核時間錯開 5~180 分且不跨日＝ai-rules/21）。⑤**製表人下拉只列有稽核員或陪檢員資格者**，且改成**部門優先排序**（`eg_people_list()` 的排序是「職稱→部門」，所以同部門的人會被不同職稱拆散在清單各處）；**本人沒有資格時不預選成製表人**（否則超級管理員一開單就會冒出「超級管理員（已離職／無稽核資格）」這種像壞掉的選項，這是無頭 Chrome 實測抓到的）。⑥稽核通知單「受稽日期」「預定完成改善」表頭新增**「全部同一日期」**（表頭沒填時自動沿用第一列已填的值）。⑦**範本組合的成員範本改一列一個顯示**——原本用「、」串成一行，而每一項自己又有「→」，「客戶需求與合約審查　→　業務課、工程圖面　→　技術課」很容易被讀成「工程圖面是業務課」（使用者原話）。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 10 段＋側欄與輸入規則健檢全過；真 HTTP 端對端（`people_asof` 三個日期、小組 CRUD 與複製與兩位組長擋下與未登入 401、資格任期在區間內外的實測、送審審查人＝管理代表、開自動簽核後核准人＝最高核准人員且時間偏移正確）；**無頭 Chrome 實際登入渲染並點擊 33 項全過**（三個跳窗、候選互斥、任期欄、AS 自動資格說明只在稽核員分頁、全部同日真的套到三列、小組提示列、製表人只列有資格者且同部門相鄰、零 JS 例外、零橫向溢出）。測試資料（小組、測試會議與其出席人員、資格任期、計畫送審狀態、`ia_auto_sign` 設定、session、Chrome 暫存設定檔）已全部還原刪除，未動任何正式業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：`check_asof_position.php` C 區掃出的其他 17 支模組（審核表單、產品開發評估表、工程變更、專案管理、教育訓練…）尚未比照改，已列在 `ai-rules/22` 待檢表；本次未動查檢表「稽核人」以外的既有欄位。
> 前次：2026-09-15 — **內部稽核（`views/ADM/internal_audit.php`）建立查檢表大改＋績效表全自動判定**（使用者交辦一整批）。①**「建立查檢表看不到種類與日期」的根因不是沒做，是被自己藏起來**：`$('#nkHalfLab').closest('div')` 抓到的其實是**整個 `.ia-form`**（`#nkHalfLab` 是 `<label>`，它上面沒有自己的 div），所以只要種類不是績效表，`.toggle(false)` 就把種類／稽核日期／稽核人／所屬件號／標題**整區一起藏掉**——畫面上跳窗一打開就直接是挑題區，使用者當然說「要可以選擇建立日期」。**往後用 `.closest('div')` 藏東西前一定要確認那個 div 是不是整張表單**。②**系統稽核紀錄表改成「從部門挑表單」**：左欄不再只有 AS 的作業項目，種類是 system／kpi 時改列**部門**（依 AS 文件編號第二段的部門代碼推導，`ia_as_dept_code_names()`，同名代碼會標出代碼），點一個部門就把該部門的表單整批勾起來、再逐張取消不要的；每一列同時印出**這份表單對應到的品質管理系統要求**。③**「違反條文」不必再手打**：條文題庫的 `doc_ref` 本來就寫著「這一條建立了哪些文件、表單」，**反過來查就是「這份表單對應哪幾條要求」**（新增 `ia_clause_map_by_doc_no()`，**不另建對照表**＝鐵律4），開不符合通知單時連同受稽核單位（部門代碼→部門）、受稽人、相關表單編號、不合格事實一起帶好。④**AS稽核查檢表可以直接沿用系統稽核紀錄表的判定**（新增 `ia_as_apply_system_result()`）：該條列到的表單只要有一份不合格→這一條不合格，並在「所見證據」列出是哪幾份（含 IA 單號）；全部合格→合格；**一份都沒查到就留白不判定**（不可以亂猜成合格）。⑤**績效執行稽核查檢表（2-GM-06-03）改成全自動**：使用者拍板「這張表稽核的是**去年一整年**」（2026 年建立＝稽核 2025），**取消上／下半年**；受稽人＝KPI 頁面設定的**擔當者**，且**職稱一定要用「該指標登記的那個部門」去解析**——何沐桐主職技術課工程師、兼生管組組長，KPI 上兩個部門各有指標，取主職或職級最高都會印錯（實測已分別印出「工程師」與「組長」）；達成／沒達成一律走 KPI 模組自己的 `kpi_as_display_value()`／`kpi_as_below_target()`，**該年度只要有任一次未達標就算沒達成**。⑥**沒達成自動開「異常矯正處理單」不是 IA 單**（紙本備註欄印的就是 CAR 單號）：`ia_car_create_from_kpi()` 組出「年度／統計週期／KPI目標／未達標月份與當時數值」並固定附上「請說明原因及確認是否需要調整KPI目標?」，責任單位＝該指標的部門、回覆人＝擔當者（直接 assigned 免再指派），**配號／簽章／紀錄／通知全部用 CAR 模組自己的函式**，開完單號寫回該列備註。⑦**能自動建立的就自動建立**：查檢表分頁長出提示列（今年的績效表還沒建→一鍵建立只要填建立日期／已有系統稽核紀錄表→一鍵建 AS 查檢表並自動判定／還有幾張表判出不合格沒開單），填寫跳窗下方新增「**一鍵開立**」批次開單。⑧**管理員刪除鈕補齊**（年度計畫／查檢表／不符合通知單／稽核報告表，新增 `report_delete`；`plan_delete`、`check_delete`、`nc_delete` 其實早就有 API，只是畫面上從來沒有按鈕）。⑨**製表人下拉終於印得出主職位**：`eg_people_list()` 一人只回一列、而且挑的是**職級最高**那一筆，所以兼任會把主職蓋掉（何沐桐被印成「生管組 組長」）；改用 `ia_annotate_posts()` 帶回全部職務，前端 `personPostLabel()` 以主職為準、兼任放括號（「技術課　工程師　何沐桐（兼 生管組 組長）」）。⑩AS 作業項目標籤的已選區加印「這個項目掛在哪幾份文件表單」（使用者：點了標籤看不出選到哪張表單或程序書）。⑪**年度下拉預設停在今年**——選單第一個是「明年」（補未來排程用），而預設值取 `years[0]`＝**明年**，所以一開頁面清單永遠是空的（這次才發現）。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 9 段＋側欄與輸入規則健檢全過；真 HTTP 端對端（三種題庫、KPI 擔當者職稱、建立後逐欄比對、重複開矯正單擋下）；**無頭 Chrome 實際登入渲染並點擊 40 餘項全過**（種類／日期欄真的看得見、三種種類互切、左欄部門 chips 與筆數、預設 0 勾、點部門勾起 10 項、已選作業項目列出所屬文件、績效表標題含稽核年度、受稽人與判定自動帶入、沒達成列出現「開矯正單」、一鍵開立鈕、清單刪除鈕實際刪掉、零 JS 例外、零橫向溢出）。測試建立的查檢表／矯正單／通知／session／Chrome 暫存設定檔已全部刪除（矯正單連 `car_signature`／`car_activity_log`／`live_event` 一併清掉，`car_seq` 測試日那列也刪了），未動任何正式業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：系統稽核紀錄表的「一鍵開立」問不合格類型用的是 `prompt()`（其餘欄位全自動），沒有做成正式跳窗；`ia_check` 的 `half` 欄位保留但不再寫入。
> 前次：2026-09-14（二次）— **內部稽核（`views/ADM/internal_audit.php`）四項**（使用者交辦四件）。①**三張表單的「製表人」終於改得動**：`ia_plan`／`ia_case`／`ia_report` 的 `maker_*` 一律是**建檔當下寫死**、之後任何一支 UPDATE 都不碰它，所以補歷史資料或換人接手時只能眼睜睜看著印錯人。年度計畫（表格下方）、稽核通知單（基本資料區）、稽核報告表（補充文字下方）各加上「製表人／製表日期」，後端收斂成 `iaMakerFromPost()` 一支（**姓名一律回查 `user` 表，不採信前端送的名字**；**`array_key_exists` 判「有沒有送這個欄位」而不是判空字串**——沒送＝舊呼叫端＝不要動製表人，送空字串＝真的要清掉，兩者是不同語意）。**離職的原製表人一定要補進下拉選項**（`eg_people_list` 不列離職者），不然「開來存個檔」就把製表人洗掉了。②**稽核件號改依「稽核日期（稽核起）」**：畫面上同時看得到 `251103001`（西元＋通知日）與 `1141208001`（民國年＋建檔日）兩種格式，而 2024 兩張紙本轉入的 `241115001`／`241216001` **本來就是用稽核日期編的**（使用者拍板以紙本為準）。新增 `ia_case_sync_no()`（比照產品開發評估表的 `*_sync_doc_no()`）：稽核日期改過就重編並回報新舊號，**但只重編「還是草稿且未執行」的**——已發出／執行中／已結案的紙本上印著舊號，改了對不起來。**重編時 `ia_next_case_no()` 要把自己排除掉**，否則同一天重算會一直往後跳號。既有兩張草稿以 `views/ADM/migrations/2026-09-14_ia_case_no_by_audit_date.php` 重編（1141208001→251204001、251103001→251211001；`case_no` 全庫只有 `ia_case` 一欄，沒有別的表拿它當鍵）。③**建立查檢表改成預設一題都不勾**（原本「沒選標籤＝全部勾選」），且**作業項目標籤由攤在跳窗上方改成左側分割欄**（使用者原話：162 個標籤在上方「不方便閱讀」、點完「被選中的應該要另外顯示出來」）。分類依 **AS 文件編號第二段的部門代碼**（`as_dept_code`，不寫死；PD／PH 都對到生管組，**同名時要標出代碼**否則會出現兩個一模一樣的群組標題），群組預設收合、可打字篩選（打了字自動展開，否則篩完還要一個一個點開＝等於沒篩），已選的標籤另外列在右側題目清單上方、可逐一按 × 取消。④**新增「稽核範本組合」**（新表 `ia_process_tpl_set`／`_item`）：常一起稽核的那幾個範本存成一組，填通知單時按「帶入範本組合」一次帶入多列。**組合只記「有哪些範本」**，主過程／受稽單位／候選人員一律即時由範本算（鐵律4），所以範本改了組合跟著改、範本被停用或刪除那一列自動不帶（`tpl_delete` 要一併清 `_set_item`，否則組合裡會留一列「範本已刪除」）；帶入走的是**逐列選範本那支同一個 `applyTpl()`**，規則不會走鐘，且**表格上已有的起始主過程不重複加**（同一次稽核裡本來就不可重複，硬加只會被擋在存檔前）。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 9 段＋側欄與輸入規則健檢全過；真 HTTP 端對端 29 項（件號基準日、改日期重編、同日重存不跳號、執行中不重編、製表人換人／清空／不存在的人擋下、年度計畫改製表人不動排定格、範本組合 CRUD 與三種驗證）；**無頭 Chrome 實際登入渲染並點擊 51 項全過**（左右分割真的左在右之前、群組標題是部門名稱、預設 0 勾、點標籤→右側已選區出現且題目被勾起來、按 × 取消、換成系統稽核紀錄表左欄整個不見、清單上兩張草稿都是新編號、零 JS 例外、零橫向溢出）。**測試踩到的坑**：切換查檢表種類後若只等「`#nkPick` 有沒有 label」會抓到**上一種的舊畫面**（AJAX 還沒回來），要等筆數真的變成新種類的筆數；另外案件清單預設年度是今年，那兩張草稿在 2025，不切年度會看到「沒有符合條件」而誤判成壞掉。測試資料、測試 session 與 Chrome 暫存設定檔已全部刪除（年度計畫的排定格與製表人測完已原樣還原）。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：`ia_report`／`ia_plan` 的製表人開放了，但**查檢表（`ia_check`）的「稽核人」仍只有建檔當下決定**（使用者這次沒提，未動）。

> 前次：2026-09-14 — **批圖編輯器存料號附件改以「料號主檔 ID（d_id）」為準，並由圖面查閱帶入鎖定該筆主檔**（使用者回報）。問題：同一個料號文字在 `d_setting` 常常有好幾筆（不同客戶）——**OB321500200 就有旻成 #440、松田 #19799 兩筆**，存檔跳窗的下拉只印料號文字，**畫面上是兩行一模一樣的字**，使用者挑不出來、也一直存不進想要的那一筆；而且 `bom_viewer.php` 只帶 `?part_no=料號文字` 過去，編輯器端用 `find(p=>p.D_Setting_Id===料號)` 挑**搜尋結果的第一筆**（`ORDER BY d_id DESC`＝最新建立那筆），所以看的是旻成、存下去卻是松田，**不報錯**。**四件實作**：①`bom_viewer.php` 開編輯器時一併帶 **`?part_d_id=<_pk>`**——`_pk` 就是該頁已經鎖定在看的那一筆主檔（頂端「料號主檔切換器」換的也是它，唯一實作 `part_scope_lib.php`），**不要另外去猜**。②編輯器收到 `part_d_id` 就**鎖定**該筆（橘色鎖定列＋搜尋列停用），要改存別筆得按「改選其他料號」明確解除；**只帶料號文字時維持原本的自由搜尋**（舊呼叫端不受影響）。③下拉選項一律印「料號｜圖號｜**客戶**｜Rev.｜**#主檔編號**」，下方常駐「將存入：○○（客戶 ○○、料號主檔 #○）」，**存檔成功訊息也寫明實際存進哪一筆**（同名料號存錯家，事後才發現就來不及了）。④後端 `save_workfile` 同規則再擋一次（鐵律8）：**d_id 必須真的存在**、且與 `lock_d_id` 一致，不一致／不存在一律擋下**不寫入**；新增 `part_get` action、`part_search` 補回客戶與版次欄位。**踩到的坑**：寫 JS 字串時 `\n` 又被寫成實體換行（第 N 次），**把渲染後的 inline script 抽出來跑 `node --check` 才看得到**——`php -l` 一律通過。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP 端對端（`part_search`／`part_get`／鎖定不一致擋下／不存在的主檔擋下／未登入 302）；**無頭 Chrome 實際登入渲染並點擊 20 項＋圖面查閱帶參數 7 項全過**（鎖定列與客戶字樣、下拉只有鎖定那一筆、解除鎖定後兩筆都列出且分得出來、切換 `?pk=` 帶出的 `part_d_id` 跟著換、零 JS 例外、零橫向溢出）。測試 session 與 Chrome 暫存設定檔已刪，未寫入任何業務資料。**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-09-10 — **AS 文件「填寫紀錄」連動表單簽核設計器與審核表單，並新增「綁定但不列印編號」**（使用者交辦四件）。①**填寫紀錄改成一份合併清單**：原本只看得到「人工上傳的紙本掃描檔」與**寫死的兩個電子化模組**（CAR／品質異常，靠 `as_document.linked_module`），而公司實際上已經有兩套「線上把這張表單填完並簽核」的模組（`form_signer.php` 的 `fsd_case`、`review_form.php` 的 `rf_instance`），兩者**早就綁了 AS 文件編號卻完全不會出現在填寫紀錄裡**＝線上填的表單在 AS9100 的品質紀錄清單上查不到。現在四種來源合併成同一份清單、一律依日期新→舊、後端分頁，唯一實作 `src/common/asdoc_record_lib.php`。**怎麼認「這份文件有哪些線上表單」**：一般案件走**樣板**綁定（`fsd_tpl_{id}`）、補案件走**案件自己**挑的 `fsd_case.as_doc_id`、審核表單走**模板**綁定（`review_form_tpl_{id}`），反查一律走新的 `eg_asdoc_bound_ids()`（舊資料有的存 json 有的存純數字，各頁自己 parse 遲早漏掉）。使用者拍板**只收「已完成」**的。②**按「預覽」＝看蓋好簽章後的樣貌**（權限依使用者拍板＝**AS 文件檢閱權**，與既有紙本紀錄一致）：表單簽核案件直接串流合成 PDF，但**一定要再驗一次「這件真的屬於這份文件」**，否則有 AS 檢閱權的人把 case_id 換個數字就能看到任何一件案件（鐵律8）。**合成 PDF 後端自己產不出來**（圖章要由瀏覽器畫成圖再送後端疊圖），實測 41 件已完成案件裡**只有 4 件有 PDF**，所以沒有 PDF 的顯示「預覽（開啟案件）」——`form_signer.php` 新支援 `?case_id=` 直接開該案件，開一次就會自動補產。審核表單**沒有存起來的 PDF**（列印是前端即時產生的），改成開 `review_form.php?inst_id=N&print=1`；**刻意不用 `window.open`**——不是使用者點擊觸發的開新視窗會被彈出視窗封鎖直接擋掉、按了等於什麼都沒發生，改成同一頁蓋一層 iframe 顯示**完全同一份列印 HTML**（把 `egPrintWindow()` 拆成「組 HTML」與「開視窗印」兩段共用，版面不會走鐘），上方留一顆「列印」鈕（那才是使用者點擊、`print()` 一定叫得出來）。連帶把 `instance_get` 的唯讀檢視放寬：**已完成且模板有綁 AS 文件**時，具 AS 檢閱權者也看得到內容（看得到清單卻打不開沒有意義），**編輯／送出／簽核／刪除一律沒有放寬**。③**表單簽核新增「不在列印頁顯示編號」**（樣板一個、補案件逐案一個）：紙本掃描檔上本來就印好編號，再印一次會變成同一頁兩組編號；**綁定照樣成立、填寫紀錄照樣連動**，只擋在 `fsd_case_asdoc_no()` 這個唯一決定「要印哪個 AS 編號」的地方。**改樣板設定要把已產生的合成 PDF 一併作廢重產**——編號是產生 PDF 當下燒進去的，不作廢就只有畫面變、下載到的還是舊編號而且看不出來。④**順手修掉一個很難查的既有 bug**：檔名清洗用的 `preg_replace('/[\\/:*?"<>|]+/u', …)` 以「/」當定界字元、字元類別裡又有沒跳脫的「/」，**在 Apache 下編譯失敗直接回 null**（`preg_last_error()`=1），下載檔名默默從「案件名稱 業務日期.pdf」變成「2026.01.06.pdf」、案件名稱整段不見也不報錯；**同一段程式用 CLI 跑卻完全正常**，只有真的用 HTTP 打端點看 `Content-Disposition` 才看得到（全站有 4 處抄同一種寫法，已收斂成 `fsd_case_file_show_name()` 並改用 `~` 當定界字元，記憶 [[preg-slash-delimiter-trap]]）。驗證：`php -l`＋**渲染後**的 inline JS `node --check` 16 段＋側欄與輸入規則健檢全過；真 HTTP 端對端（清單、PDF 串流與檔名、換一份文件被擋、未登入擋下、無 AS 權限 403、只有 AS 檢閱權者可讀已完成的他人表單、無 AS 權限者一律擋下）；**無頭 Chrome 實際登入渲染並點擊 27 項全過**（合併清單來源欄與日期由新到舊、有/無 PDF 兩種按鈕型態、審核表單預覽 iframe 內確實有簽章 svg、只帶 `?inst_id=` 不會自動彈預覽、兩頁新勾選框、`?case_id=` 直接開案件、零 JS 例外、零橫向溢出）。測試資料（暫時綁上去的 `as_doc_id`、測試造成的列印紀錄、兩組 session、Chrome 暫存設定檔）已全部還原刪除，未動任何正式業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：`as_document_management.php` 仍沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；用法寫在填寫紀錄跳窗頂端的說明列與另外三頁的使用說明裡）。
> 前次：2026-09-04（二次）— **BOM總覽兩個顯示 bug＋QC待驗清單「報工了卻還掛著」**（使用者回報三件、詢問一件）。①**容器（料號欄右下角）在自動更新後整個消失**：`src/store/_fetch_data.php` 的 `ing_active_map` 只帶了 `QC_ps`，缺 `QC_ps2`／`pm_ps`／`pm_ps2`，而**生管回報的容器是存在 `pm_ps`/`pm_ps2`**，所以生管填過的（例 B-1150903018 的 2E）初載看得到、自動更新一跑就變空白。**這兩份 map 是兩個地方各建一次的**（頁面初載的 `$bom_ing_active_map` 與 AJAX 的 `ing_active_map`，外加前端後備的 `buildIngActiveMap()`），**欄位少一個就會出現「初載有、更新後沒有」這種只有實際等 5 秒才看得到的 bug**，往後改其中一份一定要三份一起看。實測 1780 筆製程零缺欄，修正前會整個消失 3 筆、顯示不完整 14 筆。②**訂單備註過長把「修改 BOM 資料」整個版面撐爆**：訂單號那格是 `white-space:nowrap`，87 字的備註把表格撐到 995px、中間欄被壓成 113px、右側「BOM 製程列表」被擠出畫面、整頁橫向溢出 62px。改用 `.ord-bind-sub` 限寬 330px 截字，**用 `scrollWidth>clientWidth` 量測到真的被截斷才標成可點**（沒被截的就不要長出假的可點樣式），點一下展開、再點收合，完整內容同時掛在 `title`。**展開時寬度要釘死 330px**，否則 `word-break` 會讓 min-content 縮成一個字、整張表跟著變窄＝按一下版面就跳一次。實測表格 995→529px、中間欄 113→290px、頁面橫向溢出 62→0px。③**QC待驗清單（`views/QC/QC_check_list.php`）「已經報工了卻還掛在清單上、QC完工紀錄又查不到」**——**先查證推翻了「沒有自動連結」這個前提**：報工其實有正確連結到該製程（同一個 `bom_ing_fid`，清單備註欄顯示的就是它）。真正的原因是**「報工」與「完工」是兩件事**：`qc_completed` 全站**只由【完成】按鈕（`_update_qc_completion.php`）寫入**，報工（異常／允收）永遠不會自動完工；而完工紀錄硬寫死 `WHERE qc_completed=1`，所以只報工沒按完成的在那裡一定查不到，BOM總覽卻讀 `qc_check` 所以看得到。使用者拍板三項：⑴狀態欄加**報工進度徽章**（已驗滿·待按完成／已驗 130/1500／後站已開工·滾齒）＋「報工」篩選群組三顆，純顯示不自動改資料 ⑵工具列**「一鍵完成」**（按下當下才向後端要最新清單＝點開即刷新，`all=1` 取全部符合篩選的資料而非只有這一頁，逐筆呼叫既有端點不另寫完工邏輯）⑶完工紀錄加**「含未完工的報工」勾選框，預設不勾＝完全維持現況**。**刻意不做自動完工**（使用者選的）——分批出的站會在最後一批報完的當下被關掉，品管少了最後確認那一關。**踩到一個看原始碼看不出來的坑：本頁除了後端篩選，另有一個 DataTables `ext.search` 前端篩選，結尾是 `return false`，新的篩選值沒放行就會變成「筆數顯示 3 筆、表格卻寫 No matching records found」**。**另外要更正我一開始給使用者的數字**：我用簡化 SQL 估「待驗清單 220 筆、其中 144 筆只差按完成」，但清單真正的查詢還有一個 `latest` JOIN（`bi.outsource_date = latest.max_date`，**沒有發單日的列會被整個排除**），實際是 **76 筆**，其中已驗滿 2、只驗一部分 3、後站已開工 8、尚未報工 71。**估影響範圍一定要用清單本身那支 SQL，不要自己另寫一個簡化版**。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；**以 git HEAD 原版另存基準逐欄比對 19 種參數組合**（待驗清單分頁／原本四種狀態篩選／製程／關鍵字／完工紀錄分頁與搜尋）total 與每一列每一個欄位值完全相同（完工紀錄的排序**只在勾選時**才改用 `COALESCE(完工時間, 最後報工時間)`——有 1 筆 `qc_completed=1` 但 `qc_completed_at` 是 NULL，不分開處理會讓預設畫面換位置）；**無頭 Chrome 實際登入渲染兩頁共 68 項全過**（容器自動更新前後相同、備註展開收合與不誤觸訂單勾選、徽章實測只讓列高多 9px 而長備註那列 0px、篩選連動、抽屜勾選與還原、一鍵完成逐筆送出與結果回報、零 JS 例外、零橫向溢出）。**一鍵完成的寫入端點在測試中被攔截，DB 確認三筆 `qc_completed` 仍全部為 0，未寫入任何正式資料**；測試 session 與 Chrome 暫存設定檔已刪。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：`QC_check_list.php` 仍沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；用法寫在篩選鈕提示、一鍵完成跳窗說明框與勾選框提示裡）。
> 前次：2026-09-04 — **修好圖面變更紀錄（`views/QC/drawing_change_log.php`）「自動換圖記錄帶進來後一直顯示載入中」**（使用者回報）。**根因又是同一種：JS 字串裡寫成實體換行**——2026-08-25 串接工程變更申請單時，`afterSubmit()` 的 `msg += '` 之後直接斷行（應為 `\n\n`），**整個 inline `<script>` 區塊語法錯誤＝全頁 JS 一行都不執行**，所以 `load()` 與 `openDetail()` 從來沒被呼叫，清單與明細就永遠停在「載入中…」、也不會有任何錯誤訊息。**`php -l` 完全抓不到**（PHP 語法沒問題），後端 API 直打也完全正常（`lookups`／`list`／`detail` 都 200 且資料正確），所以只看後端會查錯方向；**唯一看得到的方法是把「渲染後」的 inline script 抽出來跑 `node --check`**（這是第 N 次踩到，收尾一定要跑）。順手修掉同頁清單「摘要」欄的顯示 bug：草稿未填時的提示本身是 HTML，原本連標籤一起丟進 `esc()`，畫面會直接印出 `<span class="muted-help">…</span>` 原字。驗證：`php -l`＋渲染後 inline JS `node --check` 四段全過＋側欄與輸入規則健檢全過＋**無頭 Chrome 實際登入以 `?id=` 進入**（清單 6 列、明細跳窗自動開啟且內容正確、零 JS 例外、零橫向溢出、側欄 visible）。測試 session 與 Chrome 暫存設定檔已刪，未動任何業務資料（使用者剛建立的草稿 DWG-202609-001 原封不動）。
> 前次：2026-09-03（八次）— **訂單追蹤：訂單變更角色新增「更改已建立訂單的客戶」，客戶欄加鎖頭並可用本人密碼解鎖**（使用者交辦，並特別交代「絕對禁止影響現有使用者操作」）。背景：訂單一綁定料號，客戶就由料號決定——**這不只是前端鎖住輸入框，`src/store/_NewOrder_Track.php` 存檔時本來就會用 `d_setting.Customer_Id` 覆寫 `Client_name_ID`**，所以「改客戶」實際上等於「把料號換成另一家客戶底下的料號」，這是本次所有設計的前提。**三件實作**：①角色設定「訂單變更」群組新增功能碼 `ot_order_change_client`（預設全部未勾選）②客戶欄標題旁新增鎖頭圖示（無權限者只是把「為什麼不能改」講清楚；有權限者點下去輸入**本人登入密碼**解鎖）③解鎖後客戶欄恢復可輸入，改法與新增訂單完全相同（先選客戶→料號清單只列該客戶底下的、查無料號可按「新增」快速建立並綁定；或直接輸入料號→客戶名稱與ID自動帶入）——**這兩條路原本就存在，不必新寫，只要把鎖打開既有的 autocomplete 就直接可用**。**密碼刻意用「本人登入密碼」而不是 `confirm_password_lib` 的操作確認密碼**：後者只有超級管理員與被授權的管理員才有，業務人員拿到這個角色會一輩子解不開。**四個做的時候才想到的點**：⑴**改客戶時要自動解除原料號綁定**——不解除的話後端會用舊料號的客戶把客戶欄蓋回去，畫面上就是「改了按存檔卻沒反應也不報錯」；解除時**料號文字刻意留著**，方便在新客戶底下直接搜尋或快速新增。⑵料號被清掉後 `renderClientLockIcon` 若跟著 `hasPart` 一起隱藏，使用者就看不到自己還在解鎖狀態、也沒得按取消解鎖，所以解鎖中一律顯示。⑶**解鎖必須逐張訂單＋有有效期**（session 記 `uid:order_id`，30 分鐘，存檔後清掉），不然解鎖一次之後這個人當天改任何一張訂單都不用再驗密碼。⑷`ot_has_feature()` 對「完全沒被指派角色」的人是 **fail-open 回 true** 的（過渡期相容），**這種解鎖動作不可以沿用**，故另寫不 fail-open 的 `ot_can_change_client()`。**一個必須主動告知使用者的行為變更**：舊版其實可以用「先雙擊清掉料號、再挑別家客戶的料號」換掉訂單客戶（欄位提示文字本來就這樣寫），**這條路現在同樣需要本權限＋解鎖**，否則這個角色選項等於裝飾；沒權限時前端在存檔前就紅字說明，不會等到後端才擋。共用實作 `src/common/order_track_perm_lib.php`（`ot_can_change_client`／`ot_verify_own_password`／`ot_client_unlock_*`／`ot_client_change_guard`／`ot_client_change_audit`），`or_update` 與 `save_order_ids` 兩個寫入點共用同一份判定（鐵律8）並寫 `audit_log`（`target_type=order_client`）。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄與輸入規則健檢全過；真 HTTP 端對端 22 項（無權限者呼叫解鎖被擋、未登入擋下、密碼錯誤、未解鎖直打 API 改客戶回 403 且 DB 一個字都沒動、解鎖後可改且留稽核、存檔後解鎖自動失效、解鎖 B 單改 A 單仍被擋、快速綁定同樣守門、未綁定訂單補綁客戶照舊可用）＋**無頭 Chrome 實際登入渲染並點擊 46 項全過**（鎖頭出現與可點、密碼錯誤不解鎖、解鎖後欄位真的可輸入、改客戶自動解除料號綁定並冒出「新增」鈕、別家客戶的料號查不到＝客戶篩選確實生效、選料號自動帶出客戶、取消解鎖還原原值、重開訂單回到鎖定、新增訂單模式不受影響、沒解鎖就換客戶在按下存檔前就被攔下、零 JS 例外、零橫向溢出）。測試訂單／稽核列／session／Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**已知未動**：舊複本 `views/Sales/NewOrder_Track222.php` 沒有解鎖 UI，但它存檔走同一支 `_NewOrder_Track.php`，所以在那頁換客戶會被擋（該頁本來就是不再維護的舊複本）。本頁仍沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；用法寫在解鎖跳窗與鎖頭提示裡）。
> 前次：2026-09-03（七次）— **主檔管理製程分頁：技術備註新增「失效」機制**（使用者交辦：已失效的不顯示在前端列表、編輯製程視窗內要反灰並鎖定不可編輯、要看得到是誰在什麼時候設為失效、有編輯權限者都可以解開、解開一樣要留下是誰在什麼時候解的）。`process_notes` 新增 `is_void`／`void_at`／`void_by(_id)`／`unvoid_at`／`unvoid_by(_id)`；**解除失效時刻意不清掉 `void_*`**，畫面才能同時顯示「上次由誰設為失效」與「這次由誰解除」——兩邊都要留紀錄是使用者明確要求的。實作：①前端列表（`list_process`）直接在 SQL 過濾 `is_void=0` ②編輯視窗（`get_process_notes`）仍撈得到，卡片灰底＋淡化＋內文刪除線＋「已失效」徽章，**編輯／刪除／上傳附件三顆鈕都不輸出**，只留「解除失效」③只對技術備註開放（作業備註沒有按鈕，直打 API 也擋下），**連動來源的唯讀備註不給改**（要改請到來源製程）④**複製備註與取消連動時的複製都排除已失效的**，否則失效內容會在別的製程重新變成有效的 ⑤重複失效／重複解除擋下並提示重新整理（ai-rules/08 第六節）⑥每次操作都寫 `audit_log`。**這次最花時間的坑（已寫進記憶 [[mdm_migration_version_lock]]）：本頁所有 `ALTER`／`CREATE` 都被 `MDM_MIGRATION_VERSION` 版本鎖包住**（第 157 行 `if (!$_mdm_skip_migration):` 一路包到第 1060 行），**沒把版本號往上跳，新加的 ALTER 一次都不會執行、而且完全不報錯**——頁面照常 200、完整渲染，只是欄位沒建出來。更難查的是它用 `if():` 替代語法不是大括號，所以用 token 的 `{`/`}` 深度分析會判成「在最外層沒有被任何區塊包住」，grep `exit`／`die` 也找不到東西；正確的定位法是 `awk '/^(function |if |} *else)/'` 直接列行首的控制結構。本次版本號已跳到 `20260903_01`。驗證：後端真 HTTP 端對端 22 項＋**無頭 Chrome 實際登入渲染 26 項全過**（列表確實看不到已失效那筆、編輯視窗灰底 opacity<1、徽章與失效者時間、三顆編輯鈕都不見、有效那筆維持白底可編輯、作業備註區零失效鈕、**真的從畫面按下解除失效**後卡片變回白底且編輯鈕回來、列表同步恢復顯示、零橫向溢出、零 JS 例外）；`php -l`＋inline JS `node --check`＋側欄與輸入規則健檢全過；測試備註／稽核列／session／Chrome 暫存設定檔已全部刪除。**尚未經使用者瀏覽器實際點擊驗收**。**本次自己造成的事故（已還原，寫進記憶 [[feedback_no_phpr_file_rewrite]]）**：用 `php -r` 單行指令拼字串改檔想移除測試探針，指令裡的 `$pdo` 被當成變數展開、`preg_replace` 編譯失敗回傳 `null`，`file_put_contents($f, null)` **把這支 1.6MB 的檔案寫成 0 bytes**，約 30 秒後由 scratchpad 的暫存備份還原（還原後頁面大小與內容與先前完全相同）。**最可怕的是 `php -l` 對空檔案是通過的**，只看 lint 完全看不出檔案已經沒了。**往後改巨檔一律用 Edit 工具，不要用 `php -r`／`sed` 拼字串改；真的要用腳本就一定要驗證替換次數並在寫入前檢查長度沒有暴跌。**
> 前次：2026-09-03（六次）— **主檔管理（`views/pages/master_data_management.php`）搜尋由「最久 28 秒、廣泛關鍵字直接逾時沒有結果」改為 0.7 秒**（使用者回報「常常需要刪除瀏覽資料才能順利使用，搜索非常久甚至沒有結果」，並問能不能自動清快取）。**先查證推翻了「清快取」這個前提**：跟瀏覽器快取無關——本頁 localStorage 只存規格快速按鈕、`push-sw.js` 沒有 fetch 事件（不攔任何請求、什麼都沒快取）、搜尋走 POST 瀏覽器不會快取、搜尋條件也沒有被保存下來，所以「清除瀏覽資料」其實什麼都沒清到，**要修的是查詢本身**。**根因一（很難看出來、也很容易再犯）：`ORDER BY` 參照了 SELECT 別名 `kw_direct`，MySQL 就必須先把整份 SELECT 清單（含 `has_quote` 這種相關子查詢）對「所有命中列」算完才排得了序、才取得到 `LIMIT 10`**——於是關鍵字愈寬鬆愈慢，實測「RC」命中 128 列＝28.5 秒、「A」命中 8765 列＝逾 90 秒直接空白（128×224ms 正好對上 28 秒）。修法是**拆成兩段**：第一段只 `SELECT d_id` 與排序鍵挑出「這一頁的 id」（8765 命中仍只要 0.11 秒），第二段只對這一頁的列算重欄位，並用 `FIELD(d_id,…)` 原樣保留第一段的順序；成本自此**與命中總數無關**。出貨統計衍生表抽成 `$ss_join` 由兩段共用，**不複製第二份**（鐵律4）。**根因二：六個缺失索引**，每評估一次就全表掃描——`quotation_item(d_setting_d_id)`／`(d_setting_d_id,quote_id)`、`quotation_list(quote_no)`／`(pending_review)`／`(quote_no,pending_review)`、**`d_setting(D_Setting_Id)`（同時是排序鍵又是搜尋欄位，原本竟只有主鍵）**、`bom(d_id)`（有無圖面判定 100 列 270ms→0.7ms）。**順帶修掉一個潛在分頁 bug**：原本 `ORDER BY` 只到 `D_Setting_Id`，而料號有同名並列（「#3頂針座」兩筆、「....................」三筆），並列順序不穩定會讓**同一筆在兩頁重複出現、或整筆被跳過**，已補上 `d.d_id` 當決定性排序鍵。驗證：**以 git HEAD 原版另存一份當基準**，同一組參數逐欄比對 7 種情境（關鍵字／分頁／出貨排序／類型篩選／lmt=50／零命中）**total 與每一列每一個欄位值完全相同**；原本逾時的 A/E/1/0/R/齒 六種廣泛關鍵字現在 0.47~0.70 秒全部回得來；連續五頁各跑兩次結果完全相同、零重複、排序遞增。`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過，測試基準檔與兩個測試 session 已刪。**尚未經使用者瀏覽器實際點擊驗收**。**已知未處理**：匯出（`lmt=999`）無關鍵字仍約 30 秒，瓶頸是 `has_quote` 的 `JSON_CONTAINS(linked_parts)` 無法走索引（每列都要掃 549 筆附件並逐筆解析 JSON），要更快需加生成欄位＋索引，屬另一件事。**兩個測試踩到的坑**：①用**同一個 PHPSESSID** 併發打 API 會被 **PHP 的 session 檔鎖序列化**，看起來像「每一支都很慢」——比較新舊版效能一定要**各用一個 session**；②測到一半的慢查詢若 client 先斷線，它會**變成孤兒繼續佔著 metadata lock**，我的 `ALTER TABLE` 卡在它後面，連使用者的正常請求也一起排隊（`SHOW FULL PROCESSLIST` 會看到一整排 `Waiting for table metadata lock`）——要找出來 `KILL`，且**只殺自己那幾支**。
> 前次：2026-09-03（五次）— **訂單追蹤清單「載入超慢／卡在載入中」的真兇：`$_SESSION` 裡的 19,545 個 NAS 檔名，以及每個 BOM 編號都掃一次全部檔名**（使用者回報「頁面載入變超慢」「完全顯示載入中，但進不去」，並要求先查剛改過的地方）。**先講結論：不是當天新加的設計備註功能**——把同一支 `load_page_data` 在三個版本（改動前／急件那次／設計備註那次）各跑三次，時間完全一樣（1.55~1.58 秒），新增的溝通紀錄筆數查詢實測 **0ms**。**真正的三個問題都在「這個料號有沒有圖面」這段既有邏輯**：①它把 NAS 圖檔資料夾 `scandir()` 的 **19,545 個檔名整份存進 `$_SESSION`**，session 檔因此膨脹到 **637KB**，於是**全站每一支頁面的每一次請求**都要讀寫這 600KB（`ls -laS` 看 session 目錄，每一個都是 636KB，這是最快的判斷方式）；②比對是「每個 BOM 編號 × 全部檔名」逐一 `strpos`＋`pathinfo`，**掃不到的編號會把 19,545 筆全部掃完**——新訂單的 BOM 多半還沒有圖檔，所以**越新的年度越慢**（實測 2026 年 1.6 秒、2025 年 0.19 秒，這個落差就是線索）；③快取（5 分鐘）一過期就要重掃，遇到 NAS 冷連線實測 **97 秒**＝畫面就停在「載入中」進不去。**修法（畫面永遠不等 NAS）**：清單只讀快取（放系統暫存檔、**全站共用**，不再進 session）＋依編號長度做**前綴雜湊索引** O(1) 比對；沒有快取或過期時，清單先沿用既有的「NAS 讀不到就以 `bom` 表有記錄視為有圖面」退路，並回旗標讓**前端在畫面畫完之後**非同步呼叫新 action `bom_file_cache_refresh` 去重建（鎖檔限一人在掃、掃前先 `session_write_close()`）。共用實作放 `bom_dir_lib.php`：`eg_bom_file_cache_read()`／`eg_bom_file_cache_refresh()`／`eg_bom_file_prefix_index()`。**教訓（值得記住的三條）**：⑴**不要把大清單放進 `$_SESSION`**——那不是「這一頁的快取」，是全站每個請求的固定成本；⑵**任何會碰 NAS 的動作都不可以放在畫面要用的路徑上**，一定要能退回「查得到就用、查不到就用退路」＋背景補；⑶查效能不要用猜的，**同一支 API 拿舊版本檔案並排跑**就知道是不是自己改的，再用 `microtime` 打點縮小範圍（本次是先誤把 1.5 秒歸給整個批次查詢區，補上細部打點才看到是圖面那一段）。驗證：三個年度×兩頁共 **48 列輸出與修改前逐字元完全相同**（判定規則一個字都沒改）；清單 1.6~2.1 秒→**0.11~0.14 秒**、頁面 HTML 0.12 秒；刪掉快取後清單仍 0.13 秒並要求背景重建，重建端點 0.6 秒掃出 11,004 個圖檔；`php -l`＋渲染後 inline JS `node --check`＋無頭 Chrome 實際登入渲染（653ms 就有資料、沒有停在載入中、零 JS 例外、零橫向溢出、側欄正常）全過。另在頁面載入時順手清掉舊版殘留在 session 的大清單，使用者下次開頁面 session 就會縮回正常大小。
> 前次：2026-09-03（四次）— **訂單追蹤（`views/Sales/NewOrder_Track.php`）設計備註新增「已處理」＝把當下內容轉成溝通紀錄，並可按出紀錄查看**（使用者交辦三件：能標示處理完了沒、批圖溝通中只算「有內容且尚未轉成紀錄」的、紀錄要看得到）。**關鍵是不必改任何統計 SQL**：「批圖溝通中」的判定本來就是 `ateNote` 有內容且未轉生管，所以「已處理」只要把欄位清空、內容搬進新表 `order_ate_note_log`，那張卡片剩下的自然就是還沒處理完的訂單——若改成加一個「已處理旗標」而把文字留在欄位裡，就要同時改 4 處統計／篩選 SQL 且每處都可能漏改。**兩個做的時候才想到的點**：①按下已處理時要送**畫面上當下的文字**（含還沒按 Enter 存檔的新字），而不是 DB 裡的舊值，否則剛打完就按會把新字丟掉；②同一列別人可能剛改過，故後端先比對 DB 現值與畫面 `data-orig`，不同就回 `CONFLICT` 並把畫面換成最新內容請使用者再確認一次（**不覆蓋、也不安靜地吃掉別人的字**）。「已處理」按鈕靠 `input` 事件委派即時顯示／隱藏（AJAX 換頁重繪的列也涵蓋）；「溝通紀錄 N」唯讀、看得到本頁的人都能看，跳窗由新到舊列時間（`egFmtDate`）／處理人／內容，尚未處理的內容置頂另外標示。權限沿用 `ot_design_note`（前端擋一次、後端同規則再擋一次＝鐵律8）；刻意不動 `Modified_At/Modified_By`（那是訂單變更比對的基準）。驗證：`php -l`＋**渲染後**的 inline JS `node --check`＋側欄／輸入規則健檢全過；真 HTTP 端對端（無權限者直打 API 被擋、未登入 302、空內容擋下、`orig` 不符回 CONFLICT、連按兩次、批圖溝通中 8 列全部仍有內容）；**無頭 Chrome 實際登入渲染並點擊**（打字→按鈕出現、按下→欄位清空且按鈕隱藏、紀錄筆數＋1、跳窗內容與人員時間正確、零 JS 例外、零橫向溢出、齒輪／鍵槽等既有功能未受影響）。測試資料已全部還原（新表 0 筆、測試訂單 `ateNote` 復原）、session 與 Chrome 暫存設定檔已刪。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：本頁仍沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；用法寫在兩顆按鈕的提示與確認訊息內）。
> 前次：2026-09-03（三次）— **（同批第三項，使用者回報）**：訂單追蹤清單的**急件徽章把整列撐高**（「急件標示太高，導致整列變高影響閱讀」）。**根因是一個很容易再犯的 CSS 疏漏**：`.urgent-badge` 只寫 `font-size:10px` **沒寫 `line-height`**，就會去繼承表格列的 line-height（**實測 computed 28px**）——一個 10px 的字佔掉 28px 高，客戶欄多這一行就把整列撐高（急件列 75.5px vs 一般列 69.7px）。修法依使用者指定「跟轉生管按鈕一樣高」：該按鈕是 `.btn-xs`＋inline `font-size:11px`（line-height 16.5px）＋`padding:2px`＋框線 1px＝**22.5px**，徽章照抄同一組數字組出 22.5px（字級維持 10px、外觀不變）。修正後實測徽章 22.5px＝按鈕 22.5px、急件列 69.7~70.2px＝一般列 69.7px。**往後在表格裡放小徽章一律要自己指定 `line-height`**，否則字級再小也會佔掉整列的行高。（本次 commit 前後有平行 session 的 `c6f94cd` 插入，已依既有 SOP 只驗證我的 commit 內容正確、不 reset。） **（同批追加兩項，皆使用者回報）**：⑴**鍵槽視窗 920px→740px**——量測發現右欄是 `flex:1`，920px 時被撐到 **423px 但內容只用得到 227px**，每一列右側都空著 196~238px（使用者：「右側不需要那麼多空白」）。左欄 210＋間距 8＋示意圖 245＋間距 8 是固定的，右欄留 243px 剛好容納最寬的表頭（226px）與最寬的資料列（227px，含紅字換算值），故 740px；**只改視窗寬度、不動示意圖**，所以標注線左右各外溢 50px 的相對位置完全不變（改示意圖寬度才會讓那些絕對定位的引線指錯地方）。⑵**標籤管理跳窗新增搜尋篩選**（名稱／#標示／分類，同時篩私人／部門／公司三欄）——側欄本來就有搜尋框、管理跳窗沒有，124 個標籤只能用眼睛找。比對規則抽成 `labelQueryWords()`／`labelHitQuery()`／`labelTagHay()` **由側欄與跳窗共用**（鐵律4）。**兩個不做就會出事的點**：①**改關鍵字時要把「已被篩掉」的選取自動取消**——不做的話畫面寫「已選 12 個」卻只看得到 3 個，按下「刪除選取／設定分類」會**連看不見的 9 個一起處理**（這是設計時就要想到的，不是測出來的）；②**篩選中一定要顯示「顯示 N / 共 M 個」**，否則使用者會以為標籤被刪掉了。另每次開啟跳窗都清空關鍵字。驗證：縮窄後兩頁各 12 項＋標籤搜尋 24 項無頭 Chrome 端對端全過（以標籤實際擁有者 005 何沐桐、124 個標籤實測，未新增或修改任何標籤資料）。 **批圖編輯器（`views/Sales/image_editor.php`）底部狀態列在「齒輪計算」右側新增「鍵槽計算」按鈕；鍵槽計算工具抽成共用檔，與訂單追蹤頁共用同一份**（使用者要求「可以在齒輪計算右側增加鍵槽計算按鈕嗎，功能要跟 NewOrder_Track.php 的鍵槽計算一樣」）。**刻意不複製一份過去**：該工具原本 CSS（74行）＋視窗 HTML（249行）＋JS（345行）共 670 行全部內嵌在 `NewOrder_Track.php`，複製＝兩套鍵槽公差算法各自演進（鐵律4），故比照 2026-08-25 齒輪計算工具的做法抽成 `views/Sales/_keyway_tool_ui.php`（CSS＋浮動視窗 HTML＋JS，唯一實作）＋`src/common/keyway_tool_lib.php`（`keyway_tool_can_use()`＝誰看得到這顆按鈕），訂單追蹤頁改為一行 include。**這次抽得比齒輪那次乾淨**：鍵槽是純前端計算（不讀寫任何資料表、沒有後端 API），HTML 裡一行 PHP 都沒有、JS 完全不依賴 jQuery 也不引用呼叫端的全域函式，所以抽出內容與 git HEAD **逐行比對零差異**（596＋74 行全部原封不動），不像齒輪那次要改 `$.post`／`escapeHtml`。**唯一要追加的是深色主題**：批圖編輯器 `body{color:#e8e8e8}`，白底工具視窗沒寫死 `color` 就會**繼承成淺字＝整張表看不到字**（已量測：修正前後 `#kw-tool-window` 的 computed color 從 `rgb(232,232,232)` 變 `rgb(51,51,51)`），追加時只釘「會被繼承的」基本樣式到 `#kw-tool-window` 本身，**不可寫 `#kw-tool-window input{...}`**（ID 特異度會蓋掉 `.kw-ni`/`.kw-ti` 讓外觀走鐘）。權限沿用該工具原本規則（系統管理員或持有 `ot_keyway_calc` 功能碼），**訂單追蹤頁自己算好的 `$can_keyway_calc` 優先沿用**（該頁舊制是「一律可見」，共用檔不可覆寫掉）；順手把視窗 HTML／JS 也包進權限判斷（原本無條件輸出）。批圖編輯器的兩支全域按鍵處理都已 `tagName==='INPUT'` 提前 return，所以在視窗裡打字不會誤觸 Delete 刪物件／Ctrl+Z 復原。驗證：`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過；真 HTTP 兩頁 200；**無頭 Chrome 實際登入渲染批圖編輯器端對端 29 項全過**（按鈕就在齒輪計算右側且 icon 為 fa-key、視窗白底 `rgb(255,255,255)`＋深字、z-index 10400 壓得過本頁 `.modal-mask` 的 960、軸件／片狀分頁互切、真的算出結果、「清除」只清目前分頁、標題列可拖曳、齒輪計算同頁共存仍算得出 d=60/da=64、零橫向溢出、零 JS 例外）**＋訂單追蹤頁回歸 16 項全過**（按鈕與 CSS 都還在、jQuery／showToast／openGearTool 未受影響、側欄可見）；**兩頁以相同輸入算出的每一個輸出值逐值 diff 完全相同**。測試 session 與 Chrome 暫存設定檔已刪、未寫入任何業務資料。**尚未經使用者瀏覽器實際點擊驗收**。**git 小坑**：本機 `core.autocrlf=true` 但 `NewOrder_Track.php` 的 blob 在 repo 裡是 CRLF，用 python 讀檔改寫（`newline=''` ＋ `
`）會把整檔換成 LF，`git add` 後變成 **20,624 行的全檔差異**看起來像整個檔案被重寫；修法是改回 CRLF 後用 `git -c core.autocrlf=false add`，差異才回到正確的 -670／+10 行。**新抽出的共用檔維持 LF**（與 `_gear_tool_ui.php` 等既有共用檔一致）。
> 前次：2026-09-03（二次）— **快速出貨新增「追溯對照」：報價單→訂單→製令→出貨單→退貨單→(重出)出貨單 五段鏈，每段都可拖拉建立「多對多＋拆分數量」的對應**（使用者交辦；另修一項：純篩選用的下拉雙擊＝整欄篩選一起解除）。**動工前查證推翻了「要另開對照表」的直覺**——資料庫其實已經有三張多對多＋數量的分配表：`bom_order_process_map`(訂單↔製令，**2,297 筆已在用**，`B-1150528008` 一張製令分給 6 張訂單是真的)、`is_bom_map`(出貨↔製令)、`return_order_map`(退貨↔訂單，可帶 IS_id)；後兩張是 0 筆。**唯一存不下的是「一張出貨單分屬多張訂單」**（`is_list.Order_id` 是單一欄位），故本次只新增兩張：`is_order_map(IS_id, Order_id, allocated_qty)` 與 `ir_reship_map(IR_id, IS_id, qty)`（退貨後重新補出）。**`is_list.Order_id` 的地位變了**：降為「主要訂單」快取＝分配量最大的那一張，**只由 `tc_sync_is_order()` 一處同步**（使用者拍板），沒有拆分時與分配表完全一致，所以讀它的 20 多支既有程式（對帳、毛利分析、報價…）一行都不必改。唯一實作 `src/common/trace_chain_lib.php`（`tc_chain()`／`tc_link()`／`tc_unlink()`／`tc_parts()`），報價→訂單一律轉呼叫 `acc_lib.php` 既有的 `acc_recon_bind_quote()`，不另刻一份。**畫面為什麼是「分段長條」而不是列文字**：多對多又會拆量，只印文字看不出「這張單的量分給了誰各多少、還剩多少沒分配」；五個泳道並排，每張卡片的數量切成幾段、每段一個顏色＝分給哪一張對方單、尾端灰色＝未分配，顏色取自**固定暖色盤依卡片序號**指派（ai-rules/10，**不用隨機或 HSL**，同一張單到哪都同色否則兩次重畫就對不起來）；滑過卡片把相關單據一起打亮其餘變淡；**拖卡片本體＝跨欄建立對應**（跳窗填分配量）、**拖左上角 ⠿＝同欄調整順序**（純畫面順序，重載會照原順序排回去）、**點彩色長條＝改量或解除**。預設最近一年、可改由舊到新、每欄一次 100 筆可再載（和大單一料號就有 **2,220 筆出貨**、417 張訂單、270 張製令）。**三個實作時才現形的坑**：①**舊欄位上的既有綁定會被安靜洗掉**——某張出貨靠回填只寫了 `is_list.Order_id=A`（分配表 0 列，畫面是靠回退邏輯畫出來的），在追溯視窗再分一部分給 B，分配表就只有 B、同步後 **A 消失且不報錯**；修法是寫分配表前先 `tc_seed_is_order()`／`tc_seed_bom_order()` 把舊值補成一列（`bom.o_order_id` 同理）。②**報價→訂單不可套數量與客戶檢查**：一份報價本來就會對應非常多張訂單（不是把量拆掉），且報價單的客戶欄位與出貨用的簡稱格式不同，硬比會全部擋掉。③**料號不同只警示不擋**（訂單常下組合件名稱、製作時才拆成子件料號，硬擋會一筆都綁不了），**客戶不同則一律擋下**（鐵律8）。**另一項**：`eg_input_rules.js` 規則7 新增 **opt-in 屬性 `data-eg-filter-reset`**——雙擊打字篩選框或雙擊下拉，都等於把這一欄的篩選整個解除（關鍵字＋選取一起清）。**刻意 opt-in**：一般表單的下拉選的是要存檔的值，清關鍵字順手洗掉使用者選好的人會是災難；已量測 `print_sign_log`／`org_role_setting` 兩支未掛屬性的頁面行為完全不變。驗證：`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過；CLI 單元 16 項＋寫入與拆分同步 18 項＋舊綁定不遺失 13 項；真 HTTP 端對端（未登入 401／CSRF／缺參數／GET 皆擋下）；**無頭 Chrome 實際登入渲染 30 項＋真正從畫面拖放寫入 17 項＋篩選雙擊 13 項全過**（分段寬度 100/252=39.68%、改量、解除、畫面與資料庫同步、五欄各 260px 等寬、零 JS 例外、零橫向溢出）。測試資料已全部還原（`is_order_map`／`ir_reship_map` 皆 0 筆、測試出貨的 `Order_id` 回 NULL）、session 與 Chrome 暫存設定檔已刪。**尚未經使用者瀏覽器實際點擊驗收**。**測試小坑**：用 jQuery 合成 `$.Event('dragover')` 測拖放時，假的 `originalEvent` **一定要補上 `preventDefault`／`stopPropagation`**（jQuery 會轉呼叫它們），否則測試自己爆掉；另外「篩選後再用選擇器重新挑元素」會挑到別的元素而看起來像回歸失敗——要把元素釘住再比對。
> 前次：2026-09-03 — **快速出貨（`views/Sales/Shipping_Quick.php`）的「舊資料訂單回填」：新增客戶／料號兩層篩選，並可自行指定要綁哪一張訂單**（使用者交辦兩件事，自動判定完全保留未動）。起因：ERP 匯入的出貨多半沒帶訂單編號（`is_list.Order_id` 待回填 **37,492 筆**），原本這支工具只能「整段日期區間一次試算、系統推得出來的才顯示」——畫面上明明寫著「無法對應 2,326 筆」，那 2,326 筆卻**一列都看不到、也無從處理**。**三件實作**：⑴**客戶／料號篩選**（皆掛 `data-eg-filter` 可打字篩選，選了客戶後料號只列該客戶底下的）。這裡有一個必須先想清楚的點：**篩選會不會弄壞 FIFO？不會**——FIFO 是在 (客戶,料號) 群組內做的，而客戶／料號這兩個鍵是「整組保留或整組排除」，不會像日期區間那樣把同一組的出貨切成兩半；已用實測比對確認（篩選前後每一列的對應訂單／信心／剩餘量完全相同）。⑵**「無法對應」顯示開關**：勾了才向後端要（2,326 列量大，預設不撈），並回傳**推不出來的原因**（`no_order` 查無此客戶＋料號的訂單 1752／`later` 訂單下單日晚於出貨日 536／`used_up` 已被出完 38），未指定訂單前該列勾選框停用、不可能被誤回填。⑶**每一列都可以「改選」訂單**（第二層跳窗）。使用者拍板的候選範圍：**限定同一個客戶**，但**料號可自行選擇是否限定完全相同**——因為**訂單常下組合件名稱、製作時才拆成子件料號**，強制同料號會一筆都選不到（實測某筆出貨：只列相同料號 0 張、放寬後 63 張）；**已被其他出貨吃完的訂單照樣列出但整列反灰不可選**（使用者明確要求，只做顯示、方便確認「這張已出完所以才輪到下一張」）。**三個做的時候才發現的坑**：①候選 `LIMIT 300` 若照訂單日期由舊到新取，像和大這種同一料號上千張訂單的客戶會**把出貨日附近真正該綁的那幾張整批切掉**（實測 total=404 只取到 2024-01 起的那批），改成 `ORDER BY ABS(DATEDIFF(訂單日, 出貨日))` 取最近的 300 張、撈回來再依日期排序顯示。②**手動指定過的列會在重新試算後消失**——取消「無法對應」等於重新跟後端要一份不含那些列的結果，剛剛一筆一筆指定好的訂單會整批從畫面上不見、也不會被回填；修法是指定當下把該列原始資料一併記進 `mtManual[isId]._row`，每次試算完補回清單（**這個 bug 只有實際渲染點下去才看得到**）。③`match_apply` 原本只有 `WHERE Order_id IS NULL` 一個條件，手動選單開放之後**直打 API 就能把出貨綁到別家客戶的訂單**；改成 `UPDATE is_list JOIN order_track … AND ot.Client_name = il.Client_name`，客戶不符一律寫不進去（鐵律8；料號刻意不擋，那是使用者的選項）。驗證：`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過＋**真 HTTP 端對端 18 項**（篩選前後 FIFO 結果完全相同、跨客戶綁定 applied=0/skipped=1 且 DB 未變、重複回填略過、CSRF／未登入 401／日期格式錯誤皆擋下）＋**無頭 Chrome 實際登入渲染 45 項**（篩選連動 1161→6 個料號、2,622 列與 2,326 列原因、停用的列全選勾不到、手動指定後標「手動」並自動勾選、改回系統建議、反灰列無「選用」鈕且底色不同、零橫向溢出、零 JS 例外）全過；測試寫入的那一筆 `Order_id` 已還原為 NULL、session 與 Chrome 暫存設定檔已刪、SQL 確認零殘留。**尚未經使用者瀏覽器實際點擊驗收**。**已知缺口**：本頁仍沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；新功能的用法寫在回填跳窗與候選跳窗的說明文字內）。
> 前次：2026-09-01 — **報價單製程標籤（`views/Sales/quotation_list_NEW.php` 設定→製程標籤）：新增「搬移到其他群組」，並擋下刪除還在被報價單使用的標籤**（使用者交辦四題，其中「群組→子標籤快速多重篩選（可多重、管理員可開關、預設關閉）」使用者拍板**先不做**，本次未實作）。**先查證回答的兩題**：①**列印顯示的是子標籤名稱**（由 `quotation_item.process_notes` 的 sub_tag_id 反查 `sub_tag_name`、以「・」串接成 `process_names`），**不是連結製程名稱**——連結製程只攤平進 `quotation_item_process_map` 給下游用，不會出現在報價單上（檢視畫面同一套邏輯）。②**刪除標籤原本一句檢查都沒有**：`delete_process_sub_tag` 只刪 map＋sub_tag、`delete_process_tag_group` 更是整組子標籤一起刪；而報價單的製程是把 **sub_tag_id 存在 `process_notes`**，刪掉後那些 id 變孤兒＝**該報價單的製程整欄空白、不報錯、也沒有任何提示**（齒研 id 8 被 **2499 筆**項目使用、全製 2086、粗滾 1063，按一下 × 就沒了）。**兩件實作**：⑴**刪除保護**（使用者選「有人用就完全擋下」）：子標籤列顯示「用 N」徽章、刪除鈕變灰，點下去說明原因並引導改用搬移；刪群組時只要底下任一標籤在用就整組擋下並列出是哪些標籤；前端擋一次、後端 `qtag_usage_of()` 同規則再擋一次（鐵律8）。⑵**搬移子標籤到其他群組**（新 action `move_process_sub_tag`）——**搬移比想像中安全**：`sub_tag_id` 不變，所以 `process_notes` **一個字都不用改**，報價單指到的還是同一個標籤、不會遺失。唯一要跟著處理的是 `quotation_item.process_group_type`（使用者選「一併更新項目的類型」）：**只在兩群組類型不同（全製 vs 單一）時更新，且只更新「搬完之後所有標籤類型一致」的項目**；**同時掛著兩種類型標籤的項目（全站 136 筆）維持原值不動**——那種項目的類型本來就是依最後點選的群組決定，自動改反而會改錯。搬移跳窗即時顯示目標群組類型是否相同、會影響幾筆。另補上刪除與搬移的**後端權限驗證**（`qtag_can_settings()` 比照頁面判定；完全未指派角色者維持原本相容行為，不會被鎖死）。驗證：`php -l`＋inline JS 語法檢查＋側欄／輸入規則健檢＋**真 HTTP 端對端 28 項**（刪除被擋後標籤與製程連結都還在、搬移後 `process_notes` 一字未改、類型同步 changed=2／混合略過 skipped=1、搬到自己群組／群組不存在／標籤不存在／未登入皆擋下、未使用標籤仍刪得掉）＋**無頭 Chrome 實際登入渲染 28 項**（徽章「用 2499」、32 列都有搬移鈕、兩種擋下跳窗、搬移跳窗 9 個目標與類型提示即時切換、空群組不被誤擋、零 JS 例外、零橫向溢出）全過；測試資料（測試群組／標籤／報價單與項目、session、Chrome 暫存設定檔）已全部刪除、SQL 確認零殘留。**尚未經使用者瀏覽器實際點擊驗收**。**測試小坑**：偽造 session 打 API 只要 `id`，但**開頁面還需要 `userName`**（`quotation_list_NEW.php` 第 4 行是 `isset($_SESSION['userName'])`），只寫 id 會被導回 index.php 而看起來像「頁面壞掉」。
> 前次：2026-08-28（二次）— **把兩件「主檔資料沒串到會計端」的事做完**（使用者交辦的待辦，兩件同根）。①**臨時變動結帳日終於真的有作用**：`customer_settlement_exceptions` 這張表與主檔管理→客戶編輯的設定 UI 早就存在，但 `src/` 底下**沒有任何程式讀它**——填了完全不影響帳款月份、也不會報錯（全表 0 筆，等於從來沒人成功用過）。唯一實作放 `acc_lib.php`（`acc_billing_month_ex()`／`acc_bm_for()`／`acc_settle_ex_*()`），`acc_ar_summary`／`acc_ar_detail`／`acc_invoice_candidates`／帳款月份指定作業／跨月找單／`acc_default_billing_month` **全部自動吃到**，不在對帳頁自己算一份（鐵律4）。口徑：`target_year_month`＝被調整的那個**日曆月**、`adjusted_date`＝該月實際結帳日，**允許落到次月**（延後結帳），提前結與延後結都測過。**踩到的坑**：`acc_scan_range()` 原本尾端只掃到當月月底，延後結帳（例 7 月拖到 8/2 結）那幾天的單**會整批撈不到也不報錯**，已把尾端一併放寬一個月。**寫入點只有一個**：主檔管理原本自己 upsert 的那段改成呼叫同一支 lib（兩個寫入點＝規則必定走鐘），對帳頁的「對象設定」跳窗下半部也長出同一份設定（會計管理員限定），因為要調整的當下人通常正在對帳。②**未建主檔的出貨對象可以直接對應**：`is_list.Client_id` **全表都是空的**（不是「近期多為空」），應收一路是拿出貨簡稱 `Client_name` 對 `customer_list.customer` 歸戶；但 ERP 常寫得比主檔長，近一年 **41 家對不到主檔**＝統編／發票資料／結帳日／臨時結帳日全部套不上，而畫面上完全看不出來。**上次規劃的「只自動綁完全相同的字串」在這裡一筆都綁不到**——實際情況是「高鋒工業」對主檔的「高鋒」、「如陽科技」對「如陽」、「榮田精機」對「榮田」，長度不同。故新增 `acc_customer_alias`（別名對照）：**一筆別名同時涵蓋歷史與往後 ERP 匯入的新資料**，比逐列改 `is_list` 安全得多；歸戶收斂成 `acc_customer_by_name()`（含別名）＝全站唯一入口，`acc_settlement_for`／收款客戶解析／發票資料頁的出貨統計一併改走它，否則別名只會在其中幾個畫面生效。對帳頁工具列新增紅色提示條「本月有 N 個對象在客戶主檔裡找不到」＋「對應到客戶主檔」跳窗（**僅會計管理員**，可多筆一次對應、可解除、每列附建議與理由），對象下拉一併標「⚠ 未建主檔」。**系統只給建議、絕不自動對應**（「泓創綠能」與「泓創綠能科技」有可能是兩家）；**別名一律不覆蓋真正的主檔簡稱**，連「指到同一家」都擋（那筆別名永遠不會被用到，只會留一列垃圾資料——這是測試抓出來的，第一版只擋了指到別家）。依使用者指定**綁定時一律回填 `is_list.Client_id`**（只補原本是空的、不動金額與月份、留稽核），解除時原樣還原。驗證：CLI 單元 24 項（提前結／延後結／跨年／掃描區間／候選猜測）＋**真 HTTP 端對端 49 項**（權限守門、CSRF、回填 92 列與解除後 92 列完全還原、加了例外後吉輔的單 2026-07 36→27 列且 2026-08 15→24 列、總數不變、七支會計頁面 200）＋**無頭 Chrome 實際登入渲染 28 項**（提示條、下拉標示、跳窗 41 列、建議理由、全部套用／清空、「其他客戶…」載入 931 家並由共用檔長出打字篩選框且真的篩得動、臨時結帳日區塊、前端即時驗證紅字、零橫向溢出、零 JS 例外）＋`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過；測試資料（別名、例外、回填的 Client_id、暫時指派的角色、session）已全部刪除、SQL 確認零殘留。**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：應付（廠商）側的臨時結帳日（使用者拍板「先只做客戶」，廠商側是用 `maker_from` 廠商 ID 歸戶，沒有名稱對不上的問題）；`acc_customer_alias` 建表時 `sql.php` 不放行 ALTER，欄位註解是另外用一次性腳本補上的。
> 前次：2026-08-28 — **對帳作業（`views/ACC/reconcile.php`）依使用者一次交辦的十項全部做完**。①對象下拉可打字篩選（`data-eg-filter`，選項文字帶出客戶編號）②**對帳單內料號／單號即時搜尋**，打字出清單、點選跳到那一列並打亮（聚焦全選與雙擊清空一律走共用 `eg_input_rules.js`，不自刻）③**跨月找單／帶入本月**跳窗：交接日期前後的單常被對方做在前一個或後一個月份，列出該對象本月以外的出貨／退貨（應付為加工移轉）勾選後一鍵改成本月（改的是既有的 `billing_month_override`，已開發票者擋下）④**「依勾選順序自動排序」開關**（預設關閉，會計管理員可改全站預設；順序寫回 `sort_order` 所以暫存後留得住）⑤**拖移排序改成開關**（預設關閉），關閉時**點列＝開右側「報價／訂單對照」面板**⑥該面板做**報價單→訂單→出貨單三段單價自動比對**，數量比對依使用者指示先做粗略（階梯報價用區間含該階容差、非階梯用 ±10%），並可**直接綁定／解除綁定訂單與報價**（寫回 `is_list.Order_id`、`order_track.quote_no/quote_item_id`，客戶或料號不符後端一律擋下並留稽核）⑦新增**類別欄**，退貨（出貨退回）紅色徽章標示（應收本來就含退貨、金額為負，只是畫面看不出來）⑧**部分客戶不提供對帳單**：對象設定勾起來後不再顯示對方紙本合計與差額、確認鎖帳也不再提醒（新表 `acc_recon_party_opt`）⑨**列印全面重做**成 A4 橫式正式對帳單（原本是 `window.print()` 直接印網頁，使用者回報「不利對帳」），公司全名動態取／表頭取綁定 AS 文件名稱／頁碼左下／AS 編號右下依帳款月份回推版次／製表圖章走 `eg_stamp.js`，並依 ai-rules/23 留列印紀錄（來源 `acc_recon`）⑩補上鐵律7 的使用說明跳窗、日期顯示改走 `egFmtDate()`。**過程中挖出一個既有 bug，不修的話第③項根本不會動**：`acc_ar_summary`／`acc_ar_detail` 只用「出貨日期區間」掃描（`acc_scan_range` ＝該月往前放寬一個月），所以**手動指定帳款月份、但日期落在區間外的單永遠撈不到**——把 7 月的單指定到 6 月的帳，畫面上完全不會出現也不報錯；已改成「日期區間 **OR** `billing_month_override` 命中」。另外**底稿存起來之後來源還會變動**，所以 `sheet_load` 每次都跟來源即時對一次：`missing`（本月有、底稿沒有）可一鍵加入、`stale`（底稿有、已不屬本月）可一鍵移除，兩者只提示不自動改（底稿上的加總／拆分是人對出來的，不可被自動覆蓋）。**列印踩到的坑**：取「對方紙本合計」不可以拿 `sheet.their_total` 一起判斷——剛組出來還沒暫存時它是 `null`，會變成「打了紙本金額卻印不出來也不報錯」，一律以畫面輸入框為準（已寫進 ai-rules/16 第五節）。驗證：`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢＋真 HTTP 端對端（CSRF、權限守門、綁定料號/客戶不符擋下、跨月改月後 missing 確實跳出來）＋**無頭 Chrome 實際登入渲染**（應收 52 列與應付 130 列、搜尋定位、對照面板真的載得到、勾選順序排序三次點擊順序完全相符、拖移把手只在開關開啟時出現、跨月找單 334 筆、攔截 `window.open` 驗列印版在 A4 橫式可印寬度零溢出且無 undefined、零 JS 例外）全過；測試資料（暫存底稿、對象選項、帳款月份指定值、稽核列、session）已全部刪除，**尚未經使用者瀏覽器實際點擊驗收**。**已知未做**：報價數量比對的細節（使用者說「之後詳細討論」）；綁定訂單時我方測試曾覆寫 `order_track.Modified_By`（單筆、已回報）。
> 前次：2026-08-27 — **內部稽核（`views/ADM/internal_audit.php`）的稽核員與陪檢員都改成可以指定多位**（使用者交辦，也是 2026-08-26 那筆待辦）。起因：一個受稽單位原本只存得下各一位（`ia_case_dept` 的單一欄位），而 2024 紙本第 2 次稽核實際上是**林國棟／葉卿雅兩位**，當時只能把兩個名字併成一個字串塞進 `auditor_name`（`auditor_id` 留空）、另抄一份到備註。**資料改法**：新表 `ia_case_dept_person` ＝人員的**唯一來源**；`ia_case_dept` 上原本那四個欄位**降級為顯示用快取**（`*_name`＝全部姓名以「、」串接、`*_id/dept/position`＝第一位），**只由 `ia_cd_people_set()` 一處寫入**，其他地方一律讀 `ia_cd_people_map()`（對還沒回填的舊資料會即時由快取欄位回推，所以 migration 沒跑畫面也不會空白）。留著快取欄位是刻意的——清單關鍵字搜尋、稽核報告表的稽核員欄都直接吃它，不必為了多人改寫成一堆 JOIN。**UI**：單一下拉改成「已選的人＝標籤（按 × 移除）＋下方『＋加入』下拉」，每種上限 10 位；**兩邊互相排除**（已被選走的人不管在哪一邊都不再出現在候選裡）＝從源頭就排不出「同一人兼兩種身分」；下拉仍掛 `data-eg-filter` 可打字篩選（實測篩選框在 flex 容器裡沒被壓扁、打字真的篩得動）。**後端同規則再擋一次**（鐵律8）：重複的人（**不同職務也算同一人**）／稽核員與陪檢員同一人／超過上限／無資格職務。**一個很容易漏的點**：`ia_resolve_post()` 的合格清單只含在職者，補歷史紀錄時**已離職的舊稽核員一定解析不到**，不特別處理就會變成「只是改個地點也存不回去」——所以 `case_save` 會先撈出「這張單原本就掛著的人」，在名單內的即使沒資格也放行（舊版單一下拉是**安靜地把人洗掉**，那更糟）。**列印**稽核員／陪檢員一位一行；**自動建會議紀錄**時全部人都帶進與會人員；**IA 單權限判定**改成「該案件被指派的稽核員（可多位）＋稽核組長」都算稽核員身分（順手補上組長原本填不了驗證段的既有缺口），`nc_list` 可見範圍一併放寬。**回填工具** `views/ADM/migrations/2026-08-27_ia_multi_auditor.php`（預設試算／`--run`／`--verify`，可重複執行）把「林國棟／葉卿雅」拆開並**依稽核日期回推當時職務**（ai-rules/22），順手清掉備註裡那行重複的「【紙本稽核員】…」；已執行＝回填 18 組人員、清理 1 張通知單。`2026-08-26_ia_2024_import.php` 也改成直接寫多位（`--rollback` 會連子表一起清）。驗證：真 HTTP 端對端 23 項＋**無頭 Chrome 實際登入渲染 36 項**（標籤渲染、加人／移除、候選互斥、篩選框版面、零橫向溢出、攔截 `window.open` 驗列印版兩位各一行、零 JS 例外）＋`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過，測試資料與 session 已刪；**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-08-26（二次）— **會議紀錄管理（`views/ADM/meeting_record.php`）挑出席人員：①名單改抓「會議日期當日」的在職狀態 ②名字右方顯示當天行程 ③自動判定與會議時段重疊 ④行程來源做成可改的設定**（使用者明確要求）。①**還沒入職與已離職的都不可顯示**，但「當日還在職、之後才離職」的**要顯示**（補舊會議紀錄才選得到當時的人）；部門／職稱一併依 `user_position_history` 回推**當時**的（ai-rules/22），所以當時在業務部、現在調到管理部的人，補當時的紀錄要在業務部底下找得到。唯一實作＝`people_lib.php` 新增 **`eg_people_list_asof()`**（另在 `eg_people_list` 加 `asof_date` 選項；**離職者只有在有 asof 時才納入候選**，一般呼叫端完全不受影響）。**使用者拍板：離職日沒登錄的離職者一律不顯示**（站上有 6 位 state=0 但 `leave_date` 是 NULL，不知道哪天走的，列出來會混進近期名單）。②③新增共用庫 **`src/common/person_schedule_lib.php`**（四種來源：請假／公出單／教育訓練／其他會議），顯示成「陳俊宏（總經理）10:00~11:00 會議」；**重疊判定用半開區間**（09:00~10:00 與 10:00~11:00 不算重疊）；**使用者拍板：只有「請假且與會議時段重疊」（含全天假）不可勾選**，公出／外訓／其他會議只提示仍可加入（現場常有人開完前一場再過來）——這**取代 2026-08-06「請假者一律不出現」的舊口徑**，`meeting_filter_available_people()` 已無呼叫端。④來源開關存 `system_settings.person_schedule_sources`，**設定 UI 依 `eg_psched_sources()` 動態長出**，新增來源不必改 UI（鐵律4）。**使用者問過「這個設定是否應該同步在行事曆設定內」——不適合**：`evenement` 是**全公司的事件表、沒有 user_id**，行事曆設定管的是事件類別與顏色，根本沒有「某個人當天有什麼事」這份資料，故收在共用庫、入口掛在會議紀錄的模組設定。**後端把關（鐵律8）刻意只驗「這次新加進來的人」**——全驗的話，舊紀錄事後被補一張請假單，之後連改個地點都會存不進去。群組套用與從行事曆帶入**都走同一套判定**（原本行事曆那條是直接塞進名單、繞過檢查，會變成填完整張表才在存檔時被擋）。**踩到的坑**：寫 JS 字串時 `\n` 被寫成真正的換行，整段 inline script 語法錯誤＝**全頁 JS 失效**，`php -l` 完全抓不到，是把 inline script 抽出來跑 `node --check` 才現形的（這是第 N 次，收尾一定要跑）。驗證：CLI 15 項＋偽造 session 跑真 API 19 項（含非管理員存設定被擋、離職者被擋、請假重疊被擋、公出重疊可存）＋**無頭 Chrome 實際登入渲染 26 項全過**（行程文字、紅色 `#DD5138`、disabled 與「← 不可選」、全選不會勾到被擋的人、改成不重疊時段後變回可勾、模組設定四個來源、零橫向溢出、零 JS 例外）＋`php -l`＋側欄／輸入規則健檢全過，測試假單／會議／session 已刪、SQL 確認零殘留；**尚未經使用者瀏覽器實際點擊驗收**。**待辦（使用者已指定下一輪做）**：內部稽核的**稽核員／陪檢員可能不只一位**，目前 `ia_case_dept` 每個受稽單位只存得下各一位（單一欄位），要改成多人須動資料表、稽核通知單、IA 單權限判定與列印。
> 前次：2026-08-26 — **①修好圖面查閱（`views/pm/bom_viewer.php`）附件的「下載」與「用小畫家開啟」②新增圖面旋轉（轉完直接覆蓋原檔）③料號附件搬到 AS9100 根目錄、全站不再依賴 Z: 磁碟機代號**（使用者回報＋拍板）。①**使用者問「是儲存路徑的關係嗎」——不是**：附件分頁的 path 本來就是附件 API 網址（`../../src/store/Order_Attachment_API.php?action=download&id=…`），但下載一律被包進只收 `/nas/` 開頭的 `bom_download.php`＝400（畫面顯示「無法存取網站」）；小畫家則是把那條相對路徑直接接在 host 後面組出非法網址（症狀「呼叫 open 方法後，才能呼叫此方法」＝VBScript 的 `oHTTP.Open` 失敗後才跑到 `setOption` 的錯），**而且用戶端 VBScript 用 `MSXML2.ServerXMLHTTP` 抓檔不帶登入 cookie，就算網址修對附件 API 也一定回 403**。修法：下載改直接打附件 API 並帶 `dl=1&dl_name`（**Chrome 會讓伺服器的 `Content-Disposition` 檔名蓋掉 `<a download>`**，使用者自訂檔名一定要由後端指定才生效；共用實作 `eg_attach_send_disposition()` 放在 `attach_lib.php`，料號／訂單／報價三支 API 共用）；小畫家改成先向 `bom_open_token.php` 換一張**3 分鐘、用過即失效的一次性權杖**，VBScript 再打不需登入的 `bom_open.php`（**網址結尾一定要帶副檔名** `&x=.jpg`，否則 VBScript 會依「最後一個點」把暫存檔存成 `.php`）；PDF 不再顯示小畫家按鈕（mspaint 開不了）。②新增「左轉／右轉」：使用者拍板**覆蓋原檔**（目的就是讓所有人看到的都是轉正後的）、**全部圖片檔都能轉**（PDF 也支援，走 FPDI 匯入原頁不轉點陣圖）、**權限＝看得到就轉得動**（原話「旋轉不是真的修改」；後端仍依鐵律8 再驗一次）。旋轉唯一實作 `src/common/image_rotate_lib.php`（站上原本已有兩份各自寫的旋轉，不要再刻第三份）。**三個踩過的坑**：⑴BOM 掃描圖是 **7016×4961＝3480 萬畫素**，GD 要吃約 280MB，預設 `memory_limit=128M` 一律 fatal（白畫面不是回錯誤），要**依實際尺寸動態 ini_set**（上限 1.5GB，超過就明白回報）⑵**`is_writable()` 對 NAS 的 UNC 路徑一律回 false**（Windows 只看唯讀屬性不看網路share ACL），拿它做預檢＝每次旋轉都被自己擋掉；改成先寫暫存檔、驗證成功才覆蓋正本⑶JPG 旋轉必先**依 EXIF 方向正規化**（瀏覽器 `<img>` 會自動轉正、GD 不會，不處理就是「按順時針、圖卻跳到別的角度」），且 GD 重新編碼後檔案會變大（實測 1.0MB→2.5MB @q95，這是 GD 的量化表與原編碼器不同，不是 bug）。附件 inline 快取由 3600 秒降為 **60 秒**，否則旋轉後別人一小時內都看到舊圖。四種來源（nas／part／order／quote）的**權限判定與實體路徑解析收斂成 `src/common/bom_view_file_lib.php`**，下載／小畫家權杖／旋轉三個功能共用同一份。順手補上鐵律7 的「使用說明」跳窗（本頁原本沒有）。③**路徑**：使用者選定「料號附件搬到 AS9100 根目錄」。**737 個檔、2523.7MB** 從 `\\excellentnas\生產課\BOM\ERP\業務\料號資料` 搬到 `\\excellentnas\AS9100維護\ERP AS9100文件(勿刪)\料號附件`——**只要複製檔案＋改一個設定值**（`part_attach_nas_dir`），因為 DB 只存檔名（鐵律5），四支消費端都是讀同一個設定值即時組路徑；**整包複製不是只複製 DB 有登記的檔**，裡面還有 114 個批圖編輯器工作檔引用的底圖 `img_*.png` 也吃同一個設定值。工具 `views/pm/migrations/2026-08-25_part_attach_to_as9100.php`（`--copy`／`--verify`／`--switch`／`--rollback`，可重複執行；驗過數量大小全符＋隨機 6 檔 md5 相同才切換），舊資料夾原本保留當備份，**2026-08-26 使用者指示刪除**（原話「確認資料都過去，舊刪除就資料夾，避免誤認以為還有在使用」）：刪除前跑過**全部 737 檔逐一 md5 比對**（不是只比大小）＋確認切換後沒有任何程式再寫進舊位置，才刪掉 737 檔／279 子資料夾／根目錄；刪完新資料夾 746 檔全部對得上（DB 登記 632＋批圖底圖 88＋Thumbs.db 26，無來路不明的檔），料號附件下載／縮圖／批圖底圖實測仍正常。**`--rollback` 已加防呆**：舊資料夾不存在時拒絕只改設定值退回。另把 `bom_scan_dir`／`bom_erp_scan_dir` 設為 UNC（與 Z: 同一個位置，19187／6033 檔實測可讀）並在 `bom_dir_lib` 新增**免傳 PDO 的 `eg_bom_scan_dir_auto()`／`eg_bom_erp_scan_dir_auto()`**（靜態快取、回傳已過 `eg_bom_fs_path` 的路徑），把 **14 支頁面**寫死的 `Z:/BOM/` 一次換成設定值版本（Z: 是 session 層級的持續連線、實測會變「無法使用」，這才是圖面時好時壞的根因）。**未動**：各模組自己的附件資料夾預設值（採購／技術備註／個人工作／量測儀器校驗）仍寫著 Z: 開頭、屬另一批設定鍵；`NewOrder_Track222.php`／`Sales_Track_test.php`／`X Product_Profit_Analysis.php` 等舊複本。**測試小坑**：`is_writable` 與 Git Bash 會把 `/nas/...` 參數自動轉成 `C:/Program Files/Git/nas/...`（要 `MSYS_NO_PATHCONV=1`），害我一度以為端點壞了。驗證：`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過＋**真 HTTP 端對端**（四種來源下載與 `dl_name` 檔名、權杖只能用一次與未登入 403、旋轉 90／轉回／料號附件旋轉／不合法角度／路徑穿越擋下、13 支受影響頁面 200、上傳新檔確實落在新資料夾）；測試檔案、DB 測試列與測試 session 皆已刪除，**尚未經使用者瀏覽器實際點擊驗收**。④**同日追加：JPG 旋轉改成真正的無損**（使用者問「有辦法像 Windows 照片檢視器那樣不影響畫質嗎」）。先實測釐清一件事——**Windows 內建那套（WPF `JpegBitmapEncoder`）其實也不是無損**，一樣解碼再重新編碼（1,046,313→1,958,196 bytes、來回後 md5 與原圖不同），MAMP 內建的 ImageMagick 也一樣；**這兩條路以後不用再試**。真正無損只有「不解碼、直接搬 JPEG 的 DCT 係數」＝jpegtran，所以把 libjpeg-turbo 3.2.0 的 `jpegtran.exe`（58KB）＋`jpeg62.dll`（738KB）放進 **`src/bin/`**（隨版控走，換機或還原備份自動就有，**不必安裝、使用者電腦完全不用動**——旋轉是後端做的，跟小畫家那種要每台裝一次的情況完全不同；官方安裝檔只是暫時靜默解壓取檔，機器上沒留任何安裝痕跡，登錄檔已確認無殘留）。`eg_rotate_file()` 改兩段式：JPG 先試無損、走不通落回 GD，回傳新增 `mode=lossless/reencode`。**自動落回 GD 的四種情況**：PNG／PDF、jpegtran 不在或執行失敗、`proc_open` 被停用、**以及帶 EXIF 方向標籤的照片**（jpegtran 會原樣複製 EXIF 但不更新 `Orientation`，瀏覽器會照標籤再轉一次＝總共轉兩次；GD 那條路徑會先把方向烙進畫素）。**刻意不加 `-trim`**：圖高不是 8 的倍數時最後一個不完整區塊無法轉置，不加 `-trim` 時 jpegtran 保留它（內容平移 1 個畫素、邊緣補白，實測那 8 欄 mean=255 標準差=0 是乾淨白邊不是雜訊）且**來回轉完全還原**；加了 `-trim` 位移一樣存在還會每轉一次削掉一列畫素。實測 7016×4961 掃描圖面連轉四圈回到原角度：**PSNR=inf、MAE=0（每個畫素都與原圖相同）**、1,046,313→1,046,317 bytes（差 4 bytes 在 JFIF 標頭區）、每次約 100ms（GD 約 1100ms）。報價單附件的旋轉因為共用同一支 lib **一併變成無損**，前端不必改；圖面查閱的使用說明已同步更新（鐵律7）。驗證：CLI 測 PNG 走 reencode／jpegtran 暫時移走會自動退回不會失敗／放回後恢復無損，＋**真 HTTP 測**（Apache 端 exif 有開）帶 EXIF 方向的真實手機照片複本回 `reencode`、一般圖面回 `lossless`，測試資料已刪。
> 前次：2026-08-25（五次）— **新增「內部稽核」模組（2-GM-06），一頁控管整個內稽流程**（使用者要求：把 `FOR CODEING 說明文件/2024內稽` 的紙本流程做成網頁，並特別交代「像會議紀錄已經有頁面就不要重複建立」）。新檔 `views/ADM/internal_audit.php`＋`src/store/InternalAudit_API.php`＋`src/common/internal_audit_lib.php`，六分頁：總覽／年度計畫(06-01)／稽核通知單(06-02)／查檢表(06-03·04·06)／不符合通知單(06-07)／稽核報告表(06-08)。**使用者以 AskUserQuestion 拍板八項**：①不符合通知單**自建 IA 單**不併入既有 CAR（紙本 IA 與 K 是兩種編號、稽核報告表統計的是 IA）②三張查檢表題目**全部自動帶＋可勾選**③會議**自動建 meeting_record 草稿＋新分頁開啟**④IA 開立即發通知、期限前 N 天與逾期自動提醒⑤IA **分段鎖定但稽核員可代填**（留紅字紀錄）⑥稽核報告表**全自動彙總**⑦年度計畫表**○手動排、◎自動打**⑧**單位主管簽核要能填「核示」**（使用者中途追加，列印版一併印出）。**「不重複建立」怎麼做到的**：`meeting_record.php` 只新增一個 `?id=` 參數（原本只吃 `?sign=`），內稽這邊建好草稿、帶入與會人員（稽核組長＋各稽核員陪檢員）後 `window.open` 過去，會議模組本身零改動；同一張通知單重按不會建出第二筆。**題庫三種來源都不另存一份**（鐵律4）：AS條文走新表 `ia_as_clause`（從 2024 年 xls 匯入 71 條，含 7 條章節標題列，建一次每年沿用、可增修）、系統稽核紀錄表**即時查 `as_document` 的表單清單**、績效查檢表**即時查 `kpi_as_indicator`**（實測 KPI 模組的 21 項指標與 2024 紙本查檢表的 1~19 項完全對得起來）。**編號一律依表單上的日期而不是建檔當天**：稽核件號＝民國年3碼+MMDD+3位流水（1131105001）、IA 單＝IA+西元後兩碼+MMDD+2位流水（IA24121601），補歷史紙本時編號才跟日期對得起來。**年度計畫表的 ◎ 不存 DB**，由「該部門在該月有沒有已執行的稽核案件」即時推導——所以沒排 ○ 卻做了也會自動長出 ◎（2024 年紙本就是這種情況）。受審查單位主管與圖章職稱**一律依稽核日期回推當時職務**（ai-rules/22），回推不到時留白不退回現況解析器。**測試抓到兩件事值得記**：⑴`ia_check_build_items()` 把「空的 pick」當成不篩選＝整份題庫全帶，所以直打 API 送 `pick=[]` 會安靜建出一張 71 題全表，前端擋得住但後端沒擋（已補，鐵律8 的典型漏法：**「空陣列」與「沒給」在後端是兩回事**）⑵測 IA 代填(proxy) 時第一版測試挑了**董事長室**當受稽單位，而「是不是受稽單位的人」是用**該部門＋子部門**判定的，根部門的子樹＝全公司，於是所有人都算受稽單位的人、proxy 永遠測不出來——**測這種「部門範圍」邏輯一定要挑末端單位**。驗證：偽造 session 跑真 API **端對端 95 項全過**＋**無頭 Chrome 實際登入渲染**（側欄可見、六分頁可切、11 個跳窗與 18 個函式都在、零執行期錯誤、無橫向溢出）＋**攔截 `window.open` 驗證五張列印版**（公司全名／AS編號右下／`counter(pages)`／A4／圖章／單位主管核示都在、無 undefined）＋`php -l`＋inline JS `node --check`＋側欄／輸入規則健檢全過，測試資料與 session 已刪、SQL 確認零殘留；**尚未經使用者瀏覽器實際點擊驗收**。備註：`ia_attach` 表已建好但本次**還沒做附件上傳 UI**（佐證附件之後要補）；`2-GM-06-05` 在 `as_document` 裡不存在（紙本也沒有這一號），故未涵蓋。
> 前次：2026-08-25（四次）— **齒輪／花鍵計算工具新增「停靠側邊面板」模式，批圖編輯器預設改用它**（使用者回報「齒輪計算畫面太大，可否縮小像是 FIGMA 頁面內側視窗那樣…希望使用這種版本，或是要像其他分頁可以拖移出去」——兩種都做，預設停靠、一鍵可浮出）。原本是 1280px 置中浮動視窗，蓋掉整張圖面；停靠後貼在畫布右緣、寬 430px（左緣可拖曳調寬，記在 localStorage），版面自動改成上下排。**只改共用檔 `views/Sales/_gear_tool_ui.php`，不複製第二套**：新增呼叫端參數 `$gear_tool_dock`（要貼哪個容器的右緣，批圖編輯器＝`#canvas-wrap`）／`$gear_tool_dock_default`／`$gear_tool_dock_offset`（同容器內另一個也貼右緣的面板，批圖編輯器＝標籤庫 `#label-lib`，它一打開齒輪面板自動往左讓開並排，用 MutationObserver 跟著 class/style 調整）；**沒設 `$gear_tool_dock` 的頁面完全不受影響**（訂單追蹤頁仍是原本浮動視窗、連切換鈕都不輸出）。標題列「停靠／浮出」互切，浮出＝回到原本可拖曳視窗（停靠時標題列刻意不給拖），選擇記在 localStorage。**窄面板的版面調整全部只寫在 `.gear-docked` 之下**，浮動視窗外觀零改動；內層寫死的 `min-width:260px` 要用 `!important` 才蓋得掉（那是 inline style）。**踩到一個很難查的 flex 陷阱（寫進記憶 [[imgedit_module]]）：`.gear-tabs` 有 `overflow-x`＝它是捲動容器，捲動容器的「自動最小尺寸」失效，於是被 flex 直欄的負餘裕壓成一列高，第二列的花鍵內／花鍵配合／栓槽／出尾計算／預留量管理整排被切掉、看不到也點不到**——修法是釘 `flex:0 0 auto`（順手把 header 也釘住）。這個 bug `php -l`／`node --check`／看原始碼都抓不到，是**量測渲染後每顆分頁鈕的 rect 是否超出容器**才現形的（截圖用肉眼看也差點看漏）。驗證：**無頭 Chrome 實際登入渲染端對端 37 項全過**（停靠位置貼齊上下右緣、白底深字沒被深色主題繼承、欄位標籤顏色沒被 ID 選擇器蓋掉、兩欄變上下排、零橫向溢出、分頁鈕零切邊、停靠狀態下 mn2×z30 真的算出 d=60/da=64、浮出後寬度回 1280 且拖得動、停靠時拖不動、左緣拖曳調寬並記住、關閉重開沿用、標籤庫開關時自動讓開與貼回、訂單追蹤頁 jQuery/escapeHtml/浮動視窗/mn3×z40=d120/da126 皆未受影響、全程零 JS 例外）＋`php -l`＋inline JS `node --check`，測試 session 已刪；**尚未經使用者瀏覽器實際點擊驗收**。備註：側欄／輸入規則健檢目前有 `views/ADM/internal_audit.php` 未過，**那是平行 session 進行中的新頁面、與本次無關**（本次兩支檔案都不在清單內）。
> 前次：2026-08-25（三次）—  **批圖編輯器（`views/Sales/image_editor.php`）底部狀態列新增「齒輪計算」按鈕；齒輪／花鍵計算工具抽成共用檔，與訂單追蹤頁共用同一份**（使用者要求「可以在下方增加按鈕呼叫 NewOrder_Track.php 的齒輪計算視窗嗎」）。**刻意不複製一份過去**：該工具原本 CSS（167行）＋視窗 HTML 與 JS（3637行）＋`gear_*`/`spline_*` 的 API action（215行）共 4019 行全部內嵌在 `NewOrder_Track.php`，複製＝兩套計算規則與兩份預留量／花鍵公差資料各自演進（鐵律4），故抽成三支共用檔並把訂單追蹤頁改為一行 include：`src/common/gear_tool_lib.php`（權限判定＋全部 action，唯一實作）／`views/Sales/gear_tool_api.php`（唯一 API 端點，登入＋權限守門後才交給 lib）／`views/Sales/_gear_tool_ui.php`（CSS＋浮動視窗 HTML＋JS，**任何頁面 include 再放一顆 `onclick="openGearTool()"` 就有了**）。**抽出時必改的三件事**：①原本 JS 一律 `$.post('', …)` ＝打「當前頁」，共用後要改打固定端點（網址由共用檔用自己的 `__DIR__` 對 `DOCUMENT_ROOT` 推導成網站絕對路徑，這樣任何目錄的頁面 include 都指得到）②**批圖編輯器沒有載 jQuery**（全頁零 `$.` 用法），所以 11 處 `$.post` 改成用 `fetch` 的 `gearAjax.post`，原本借用訂單追蹤頁全域 `escapeHtml` 的那一處也要自帶一份，否則搬到別頁就是 ReferenceError③**批圖編輯器是深色主題**（`body{background:#2c2f33;color:#e8e8e8}`），白底工具視窗會**繼承到淺色文字＝整張表看不到字**，CSS 追加時只釘「會被繼承的」基本樣式（`color`/`font-family`/`line-height`/`box-sizing`）到 `#gear-tool-window` 本身，**不可以寫 `#gear-tool-window input{color:…}` 這種**——ID 選擇器特異度會蓋掉原本的 `.gear-input`/`.gear-field-group label` 而讓外觀走鐘。權限沿用該工具原本規則（系統管理員或持有 `ot_gear_calc` 功能碼），**沒權限者後端連按鈕與整個視窗 HTML 都不輸出**。驗證：逐行比對 git HEAD 被刪的 4019 行與新檔內容確認零遺漏（只有上述三處刻意改動）＋**無頭 Chrome 實際登入渲染端對端**（批圖編輯器 20 項：按鈕在、視窗白底深字、z-index 蓋得過本頁跳窗、mn2×z30 真的算出 d=60/da=64、預留量表確實走新 API 載得到、花鍵分頁可切、零 JS 例外；訂單追蹤頁 16 項：jQuery/showToast/openKwTool/全域 escapeHtml 都還在、template 只輸出一份、mn3×z40 算出 d=120/da=126）＋三種身分權限實測（管理員／只有批圖角色者看不到按鈕且直打 API 被擋／持 `ot_gear_calc` 的非管理員可用但看不到管理員設定面板）＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試 session 已刪；**尚未經使用者瀏覽器實際點擊驗收**。**注意**：`views/Sales/NewOrder_Track222.php`（舊複本）仍是自帶一份的舊版，本次未動。**git 小坑**：本次 5 個檔案被並行 session 的 commit `06d7fac` 夾帶走（`.git/index` 共用的既有問題），內容無誤，依既有 SOP 只驗證不 reset，另補一筆空 commit `c655676` 記錄本次工作。
> 前次：2026-08-25（二次）— **料號附件新增「照片相簿」：同一次上傳自動成冊、九宮格檢視、可各別點開放大**（使用者要求，原話是「像產品照片這種應該要像相簿一樣…檢視時自動變成9宮格檢視，都要可以各別點開放大」）。**三頁一起改**（使用者指定）：料號主檔附件跳窗、`views/pm/bom_viewer.php`、`views/pm/part_viewer.php`。新增兩支共用檔，日後任何頁面要展示一堆照片一律沿用、禁止自刻：前端 `resource/js/eg_photo_album.js`（`EGAlbum.grid/viewer/thumb`）＋後端 `src/common/photo_album_lib.php`。**使用者以 AskUserQuestion 拍板四項**：①相簿可自選是否在上傳當下自動建立，事後也能先建相簿再多選照片加入 ②**「資料夾」只在系統裡分組，NAS 不開實體子資料夾**（所以檔案位置與既有下載/列印路徑完全不動＝鐵律5 的即時組路徑不受影響，改相簿名稱或搬照片都不必動硬碟）③哪些標籤走相簿用**標籤設定加勾選欄**（`quotation_file_categories.is_photo_album`，已先勾「產品照片」；**不寫死標籤名稱**＝鐵律4）④三頁一起改。資料：新表 `part_attach_album`＋`part_attachments.album_id`；API 新增 `album_list/create/rename/delete/assign`，`upload` 可帶 `album_id`（**先建相簿再逐檔上傳**——上傳是一檔一請求，邊傳邊建會建出好幾本同名相簿），`download` 新增 `thumb=1`。**九宮格一定要走後端縮圖**：實測一張手機照片 1.5~3.3MB，九張原圖＝20MB 會卡死跳窗；GD 縮成 400px 約 26~30KB、首次約 300ms、之後走快取 0ms（快取放系統暫存、檔名帶原檔 mtime，原檔一換就自動失效）。**縮圖要處理 EXIF 方向**否則手機照片會東倒西歪——注意 **CLI 的 php.ini 沒開 exif、Apache 的有開**，所以用 CLI 測不出旋轉問題（本次即如此，是在瀏覽器才看到 300x400 的直式縮圖被正確轉正）。「解散相簿」刻意**只拿掉分組、照片退回「未分相簿」不刪檔**（按這顆多半只是想重分）。驗證：偽造 session 跑真 API 23 項全過（空名稱/超長名稱被擋、跨料號附件不給掛入、解散後照片還在）＋**無頭 Chrome 實際登入渲染三頁端對端 58 項全過**（九宮格 3 欄、縮圖確實走 thumb 參數且真的載得出來、放大計數/換張/鍵盤/繞回第一張、建立與多選搬移與解散、切回其他分頁版面要還原、唯讀頁不出現勾選框）＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試相簿與 session 已刪、SQL 確認零殘留；**尚未經使用者瀏覽器實際點擊驗收**。備註：這三頁都還沒有鐵律7 的「使用說明」跳窗（既有缺口，本次未補；相簿用法寫在標籤設定、上傳區塊與相簿跳窗的說明文字內）。**測試小坑**：第一輪跑會有兩項假失敗（縮圖只載 6/8、原圖 naturalWidth=0），那是**冷快取現產縮圖來不及**不是壞掉，等待拉長到 9 秒即全過——量測顯示熱快取時 1 秒內 8 張全到。
> 前次：2026-08-25 — **批圖編輯器：「有建立工作檔」的存檔，連同它產生的輸出圖 PNG 一律只在批圖編輯器裡看得到，其他頁面一概不列**（使用者回報「存為私人的工作檔不要出現在圖片檢視中，避免造成誤會」）。**這條推翻 2026-08-21「圖大家都要看得到，只有可再編輯的工作檔才私人」的口徑**——使用者澄清那句指的是勾了「只存圖片，不建立工作檔」的正式出圖；有建立工作檔＝暫存（原本就不填發行章日期、不做圖面變更判定），那張圖卻帶著 BOSS圖／原圖等附件標籤混在圖面查閱清單裡，會被當成正式圖面。**判定刻意與登入者無關**（使用者明確要求）：不是「私人的別人看不到」而是「有工作檔的大家都看不到」，否則外來文件清單、型態識別文件管制表這種 AS9100 管制清單會因登入者不同而內容不同。唯一實作 `src/common/imgedit_visibility.php`：`imgedit_strip_workfiles($rows, $pdo)` 新增 `$pdo` 參數後一併濾掉配對輸出圖（配對鍵只用檔名不用 d_id，因為有些清單的 SELECT 根本沒帶 d_id；**刻意不濾 `deleted_at`**——工作檔被「保留上限」輪替軟刪除後那張圖仍是暫存圖，不該突然變成全公司看得到），另備 SQL 版 `imgedit_sql_not_draft($alias)` 給「在 SQL 裡算數量／存在旗標」的呼叫端（附件總數、型態識別可用份數、`project_lib` 的「有外來文件」旗標——**數量沒跟著改就會出現「顯示 4 筆、點進去只有 3 筆」**）。順手修掉一個既有空轉：`type_id_ctrl_lib` 的外來文件查詢 **SELECT 沒有 `pa.filename`**，而過濾是靠檔名判定的，所以那裡的 strip 從頭到尾沒有作用過。實測影響：未刪除的 65 張批圖輸出圖中 34 張（暫存圖）從各清單消失、31 張正式出圖不受影響。驗證：CLI 對正式資料比對 PHP 版與 SQL 版判定完全一致＋偽造 session 跑真 API 端對端（bom_viewer 附件清單、`Part_Attachment_API` list/list_latest 與筆數、外來文件清單、批圖編輯器工作檔清單仍開得起來）＋`php -l`＋側欄/輸入規則健檢全過，測試 session 已刪；**尚未經使用者瀏覽器實際點擊驗收**。**已知未涵蓋**：`master_data_management.php` 的「有附件」小圖示與附件標籤篩選仍會把暫存圖算進去（該處本來連已刪除的附件也算＝既有問題，本次未動 1.5MB 巨檔）。
> 前次：2026-08-24（三次）— **批圖編輯器（`views/Sales/image_editor.php`）依使用者回報做四項 Figma 化修正**。①**框選搬移「框起來只有一塊白底、原本的文字沒被框走」**：原本只切走「完整落在框內」的物件，文字外框只要壓到框線一點點就被歸進 `others`、匯出前 `visible=false` 藏起來——切下來是純白、文字還留在原地被白塊蓋住。改成**碰到框線就一起帶走**（Figma 框選語意，新增 `contentObjectsTouchingRect()`），但**不烙進圖**，而是與切塊圖組成 activeSelection 讓使用者拖整組（文字保持可編輯、也不會被切一半）；套索版同步套用。②**新增「圖框裁切」**（底部狀態列，預設開、記入個人偏好）：白色畫布＝Figma 的 Frame，物件**中心**在框內＝屬於圖框、超出邊界的部分畫面/匯出/列印一律切齊；中心在框外的物件完整顯示不受影響。**刻意不用物件 clipPath**——clipPath 會被 `canvas.toJSON()` 序列化進 undo 快照與工作檔，換圖或改框架尺寸後會留一份對不上的舊裁切框；改成包住 `fabric.Object.prototype.render` 在場景座標下 `ctx.clip()`（`renderCanvas` 已先套 viewportTransform，匯出/列印走同一條路徑自動一致），零序列化成本、關掉即完全復原。③**直線／箭頭改成 Figma 式端點拉伸**：帶箭頭的線是 fabric.Group，選取時只有外框等比例縮放（使用者形容「很像圖片」）。新增 `reshapeArrowGroup()` 就地依新頭尾重畫（不換物件、拖曳中即時更新），選取後只出現頭尾兩個圓點各自可拉，線粗與箭頭大小不變形。④**#標示放寬為同一標籤可設多組**（上限 3→10，每個最多 12 字）；原本超過是 `array_slice` 默默截掉不報錯＝使用者以為系統壞了，現在後端 `$parseLabelTags`（唯一實作，兩個 action 共用）丟例外講明「目前幾個、要刪幾個」，前端 `parseTagsInput()` 同規則即時紅字＋紅框（鐵律8）；縮圖只有 104px 寬故徽章顯示前 3 個、其餘收成「+N」，但**收起來的一樣搜得到**。**過程中挖出兩個很難查的既有 fabric 座標陷阱，寫進記憶 [[imgedit_module]]**：⑴`fabric.Line` 用 `width` 算端點、卻用 `width+strokeWidth` 算中心，`lineAbsEndpoints()` 回傳的端點一律多出 `strokeWidth/2`；因為拖曳中**每個 mousemove 都會「讀端點→寫回端點」**，偏差逐次累加，實測連拖 30 次固定端會跑掉 100px 以上（症狀＝「拉一端另一端自己歪掉」），已在該函式一處修正、所有呼叫端一併受惠。⑵`getBoundingRect(true,true)` 對「被 activeSelection 收編的物件」回的是**群組座標系**不是絕對座標，任何依絕對位置做的判斷（本次是圖框裁切）一被多重選取就算錯、畫面跳動；要絕對中心請取 `calcTransformMatrix()` 的 `m[4]/m[5]`。**寫測試時也一樣會被騙**（我第一版測試連兩輪量到群組內相對值誤判成失敗）。**另一件影響往後所有工作方式的事：推翻「這台環境沒有瀏覽器可實測」**——本機有 Chrome，用 `--headless=new --remote-debugging-port` ＋自寫的極簡 CDP over WebSocket（node 內建 net/crypto 就夠、不必裝 puppeteer）就能實際登入渲染頁面並**程式化派送真滑鼠事件**跑端對端；上述三個 bug 全部是 `php -l`／看原始碼／`node --check` 都抓不到、只有實際渲染才現形的。測試期兩個坑：偽造的 session 檔會被 PHP 的 GC 清掉（跑久了突然全部變 null＝被導回登入頁，每輪重寫一次即可）；收尾一律依 `--user-data-dir` 比對命令列**只終止自己啟動的 PID**（鐵律9）。驗證：無頭 Chrome 端對端 62 項全過＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試標籤與 session 已刪除、SQL 確認零殘留；**尚未經使用者瀏覽器實際點擊驗收**。
> 前次：2026-08-24（二次）— **查出並封鎖「使用者的 Chrome 一直自己關掉」的真兇：平行 session 執行 `taskkill /F /IM chrome.exe`**（使用者回報，非程式改動問題）。新增**鐵律9**與兩道防線：`.claude/hooks/block_chrome_kill.sh`（PreToolUse hook，Bash 與 PowerShell 都攔，實測 14 種指令樣式全判對）＋`.claude/settings.json` 的 `permissions.deny` 11 條。**查案過程本身值得記下來，因為這種 bug 幾乎查不到**：外部強制終止會繞過所有例外處理，所以 **crashpad 零 dump、Application 記錄檔零錯誤事件、可靠性監視器查無記錄**，三個來源都顯示「沒有當機」；記憶體充足（15GB 可用）、Defender 乾淨、排程工作時間對不上、擴充功能全是主流的。唯一破綻是 `Preferences` 的 **`exit_type=Crashed` 卻沒有任何當機檔**（正常關閉會寫 `Normal`）。**中途走過的兩個彎路**：①先怪罪 Chrome 8/21 的自動更新（時間吻合但重開後照壞）②用「當下還活著的程序」快照判定「死前兩分鐘沒有可疑程序」而排除了 taskkill——**錯的，`taskkill.exe` 只活幾十毫秒，快照永遠抓不到**，要用 `Win32_ProcessStartTrace` 即時追蹤。最後是直接搜 `~/.claude/projects/*/*.jsonl` 對話記錄裡的實際指令定案：8/21 砍 9 次（當天 Chrome 正好啟動 9 次）、8/24 砍 4 次，其中 **09:13:51 執行、監視器記錄 Chrome 09:13:55 死亡，差 4 秒**。另注意：使用者機器上累積了 **34 個 claude.exe、佔 8.4GB**（8/13 起未結束的舊 session），與本事故無關但值得清理。
> 前次：2026-08-24 — **AS 文件管理（`views/ADM/as_document_management.php`）的「AS 文件結構總覽」新增「網頁」欄：已經做成系統頁面的文件可直接點開新分頁**（使用者要求，並指定兩件事：**一樣要檢查權限**、**這一欄不可列印出來**）。使用者原本被問「路徑要怎麼設定」時反問「有辦法自動抓取已綁定的 AS 文件編號做比對嗎」——**可以，而且不必人工登記任何一筆**：`views/ADM/as_flow_guide.php` 早就有一份「哪份文件已有線上表單」的判定，本次把它抽成共用庫 **`src/common/asdoc_page_lib.php`**（`eg_asdoc_page_map()`／`eg_asdoc_page_can_open()`／`eg_asdoc_page_registered()`），兩頁共用同一份來源、**日後新模組綁定 AS 編號後兩頁自動長出，不必回頭登記**（鐵律4：兩邊各刻一份必定走鐘）。來源四種：`AS_DOC_BIND` 整個 group 動態全掃（新模組都走這裡）＋舊式各自存放的綁定（供應商稽核／教育訓練／報價單／外來文件清單／線上檢驗，只能手動登記）＋`as_document.linked_module`＋AS 線上表單設計器。164 份文件判出 **37 份已網頁化**。**使用者拍板：表單簽核設計器（`form_signer`）只是「紙本簽好再補送掃描檔」的流程，不算電子化，一律排除**（3 份；lib 以 `skip_sources` 參數控制，as_flow_guide 那邊維持全列不受影響）。**權限**：有登記進選單的頁面＝比照左側選單 `sideAndTopBarMenu.html` 的判定（`user_module_permissions` 的 page scope→group scope→角色機制）；**沒登記進選單的帶參數子頁**（總覽表列印版 `as_tree_approval_view.php`、線上表單填寫頁 `as_form_fill.php`）沒有自己的選單權限，改用本頁的檢視權限。沒權限者只看到**灰字「無權限」，後端連網址都不回傳**（不是只擋前端＝鐵律8）。**列印不帶**：列印版是 `treePrintBody()` 另外組的表格本來就不含此欄，另加 `@media print{.tree-web,.tree-web-col{display:none}}` 當第二道保險。**同批依使用者要求：文件清單（主表格）也加上同一欄**（「標籤」與「操作」之間），與結構總覽共用同一份判定與權限；資料改成**頁面載入抓一次** `WEB_PAGES` 兩處共用（開結構總覽不再重打 API），CSS class 由 `tree-web*` 改名 `doc-web*`。**加欄位一定要同時改的兩個 colspan**（漏改整列會位移）：無資料列 11/10→12/11、「搜尋命中附件」的黃底附掛列 8→9；已用無頭 Chrome 逐列比對 colspan 總和與表頭欄數確認全部相符。**使用者實測回報後再修兩個連錯的目標**：①2-DC-01-02 文件管制總覽表原本連 `as_tree_approval_view.php`，那是**送審通知點進去的核准檢視頁、要帶 `approval_id`**，直接開會顯示「找不到這筆送審內容」——改連 `as_document_management.php?tree=1`（新支援的參數＝載入後自動開啟結構總覽跳窗，因為那張表就是這個跳窗印出來的）②訂單變更改連 `views/Sales/NewOrder_Track.php`（裸開得起來、訂單變更就在該頁）——本來想連選單入口「未交訂單」，但**實測 `NewOrder_List.php` 目前是 HTTP 500（既有問題，與本次無關：`Unknown column 'order_list.ateGet'`，已回報使用者）**；因為 NewOrder_Track 沒登記進選單、權限沒有依據會變成人人看得到連結，故共用庫新增 **`perm_url`「權限參考頁」**（連結去 A 頁、權限照選單上的 B 頁算）。**教訓：這種自動產生的連結一定要逐一實際打開一次**——22 個目標全掃過，靠看原始碼是看不出「這頁要帶參數才進得去」的。順手修正兩處既有問題：①訂單變更（2-SM-01-03）原本連 `views/Sales/NewOrder_Track.php`——**那支要帶訂單才進得去、也沒登記進選單**，改連選單入口「未交訂單」（`src/store/_cleanNewOrder_Track.php`）②補上 `business_trip`／`doc_apply`／`pfmea`／`td_dev_eval`／`type_id_ctrl`／`stock_req`／KPI／專案管理 等模組的頁面對照（原本 as_flow_guide 只顯示模組代碼、沒有連結）。驗證：偽造 session 跑真 API（管理員 37 筆全可開／低權限使用者 2 可開 35 無權限且**無網址外洩**／未登入擋下／無 AS 檢視權限擋下／form_signer 的 3 份確實不在清單）＋**無頭 Chrome 以 CDP 實際登入渲染並點開結構總覽**（欄位在、連結 `target=_blank rel=noopener`、灰字提示文字、`treePrintBody()` 輸出不含此欄）＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試 session 已刪；**尚未經瀏覽器實際登入點擊測試**。備註：本頁仍缺鐵律7 的「使用說明」跳窗（既有缺口，本次未補；欄位用法已寫在結構總覽跳窗頂端的說明列）。
> 前次：2026-08-21（三次）— **AS 文件管理（`views/ADM/as_document_management.php`）新增管理員「文件備註」，並抽出全站第一套富文字（有格式的文字）共用元件**（使用者明確要求：備註要跟 WORD 一樣可以部份粗體、部份加底線、部份文字顏色或底色不同）。入口＝列表右側 **⚙ 齒輪 →「文件備註」**開跳窗（使用者中途改的口徑，原本放在「編輯資料」跳窗內；已填的選項會標示「（已填）」），存好直接顯示在**清單的文件名稱下方**。使用者以 AskUserQuestion 拍板三項：編輯入口／**列印版不帶備註**（「文件管制總覽表」的備註欄維持只印廢止狀態）／格式功能＝粗體·斜體·底線·刪除線＋文字色·底色＋條列。**新增兩支共用檔，日後任何頁面要做有格式的備註一律沿用，禁止各頁自刻**：前端 `resource/js/eg_richtext.js`（`EGRichText.attach/get/set/clean/render`，工具列 8 顆；**配色只給 ai-rules/10 的固定暖色系調色盤**〈文字 9 色＋底色 9 色含「無底色」〉，不給任意取色器，否則各頁備註顏色會走鐘）＋後端 `src/common/richtext_lib.php`（`eg_richtext_sanitize()`／`eg_richtext_to_text()`，以 DOMDocument 走白名單清洗，白名單外的標籤**脫殼保留文字**，貼 Word 內容才不會整段消失）。資料：`as_document` 新增 `remark_html`（API 內 `asRemarkEnsure()` 可重複執行）；**寫入唯一路徑＝新 action `save_doc_remark`（僅 `asIsAdmin()`）**，刻意**不**併進 `update_document_meta`——兩個寫入點規則會走鐘，且一般使用者存「編輯資料」時會把管理員寫的備註整段洗掉。**安全**：備註是原樣輸出到清單上的 HTML，沒清乾淨就是一個**存起來的 XSS**，故鐵律8 的「前端擋一次後端同規則再擋一次」在這裡不是體感問題而是必須（實測擋下 `<script>`／`<img onerror>`／事件屬性／`javascript:` 連結／`style` 內 `url(javascript:)`）。**最大的坑（日後做富文字欄位一定會再遇到）：style 白名單不能只留 `color`／`background-color`**——瀏覽器 `execCommand` 在 `styleWithCSS` 模式下，**粗體產生的是 `<span style="font-weight:bold">`、底線是 `text-decoration:underline`**，濾掉的話就是「按了粗體、存完卻變回普通字，而且完全不報錯」；修法兩道並用：①粗體/斜體/底線/刪除線改用 `styleWithCSS=false` 讓瀏覽器產生 `<b>/<i>/<u>/<strike>` 標籤 ②白名單補上 `font-weight`/`font-style`/`text-decoration`（**貼 Word 內容的粗體底線也是走這兩個屬性**，補了才留得住）。**這個 bug 看原始碼與 `php -l` 都抓不到，是用無頭 Chrome 實際渲染＋程式化點擊工具列才發現的。** 驗證：CLI 偽造 session 跑真 API **22 項全過**（存取、清單帶出、五種 XSS、編輯資料存檔不洗掉備註、清空存 NULL、非管理員擋下、不存在文件擋下、CSS 版粗體底線保留、超長截斷）＋**無頭 Chrome 實際登入渲染本頁並程式化點擊**（⚙ 選單 10 列都有、跳窗帶對 doc_id、工具列 8 顆、重開第二筆不會疊加工具列、存檔後清單即時長出備註且粗體與底色都在、選單標示「（已填）」、清理後回 NULL）＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試資料已全部清除；**尚未經瀏覽器實際登入點擊測試**。備註：本頁仍缺鐵律7 的「使用說明」跳窗（既有缺口，本次未補；備註跳窗內已有自身用法說明）。CLI 測試小坑：**Windows 的 `escapeshellarg()` 會把參數裡的 `"` 換成空白**，JSON payload 走 argv 會被打爛（症狀＝action 讀不到、回「無效的操作」），要改走檔案傳。
> 前次：2026-08-21（二次）— **新增「列印與簽核紀錄」模組，並訂為全站鐵則：往後列印與簽核一律自動記錄到這裡**（使用者明確要求）。新頁面 `views/admin/print_sign_log.php`＋`src/store/PrintSignLog_API.php`＋共用庫 `src/common/print_log_lib.php`＋前端 `resource/js/eg_print_log.js`，規則寫在新檔 **`ai-rules/23-列印與簽核紀錄.md`**（已登記進下方路由表）。①**列印紀錄**（新表 `print_log`）記列印時間／列印人／登入電腦（IP＋電腦名稱）／文件名稱／相關料號；已掛上四支會列印「附件本身」的頁面：料號主檔、BOM檢視器、料號檢視器、批圖編輯器；另**唯讀彙整**既有五張各模組列印紀錄表（報價單／PFMEA／產品開發評估表／文件制修申請單／表單簽核案件），舊表不搬家但**新的不准再開一張**。②**簽核紀錄**直接讀全站共用的 `approval_record`（14 個模組、含自動簽核產生的紀錄），只在 `eg_sign_modules()` 補「module→主檔表/名稱運算式」把 entity_id 解析成看得懂的文件名稱，不另存一份；**畫面/列印/匯出一律不出現「自動簽核」字樣**（使用者明確要求，API 刻意不輸出 is_auto 旗標）。③使用者以 AskUserQuestion 拍板四項：列印範圍＝附件列印＋彙整既有表單列印紀錄／電腦識別＝IP＋自動查電腦名稱／簽核範圍＝共用 approval_record 全部模組／權限＝新建 RBAC 角色。④頁面：兩分頁共用篩選（資料來源、列印人/簽核人、日期區間、關鍵字），**預設本月**、每頁 20 筆、**分頁鈕在列表右上**、「列印全部篩選結果」以**全部**符合資料為準（非只有本頁）；權限 `psl_admin`/`psl_view_all`，**沒有角色的人只看得到自己的紀錄且由後端強制綁定**。⑤使用者另要求「說明要寫哪些表單已涵蓋、哪些未涵蓋」——一律**即時掃描**產生：列印掃 views 底下每支檔案「有沒有列印呼叫」與「有沒有真的掛上紀錄程式」（**不可只憑登錄表判定，否則登錄了卻忘記掛會自稱已涵蓋**），簽核查 `approval_record` 實際 module ＋掃 DB 裡其他自建簽核表（表名比對要排掉 design/assign/attendee，否則 `design_notes`、`roster_assignment` 會被誤判成簽核表）。四個坑寫在 ai-rules/23：**nbtstat 的中文欄位是 Big5 亂碼**故只能抓 ASCII 服務代碼 `<20>`／第一個 `<00>`；**本機 IP 要用 `gethostbynamel(gethostname())` 判掉**（只比 SERVER_ADDR 會漏，一漏就退到 DNS 反查撿到 `host.docker.internal` 這種 hosts 檔假名字）；**一次列印只能記一筆**（有退路的列印路徑不可各記一次）；**記的是「按下列印」不是「印出來」**（瀏覽器不回報取消，故取消也留一筆，這是刻意的）。電腦名稱快取**沿用既有的 `ip_hostname_cache`**（與 `audit_log_report.php` 同一張，不另建）。驗證：CLI 直呼函式＋偽造 session 跑真 API（權限守門、一般人硬指定他人 user_id 被綁回本人、來源/模組/關鍵字/日期篩選、per=0 全部）＋**無頭 Chrome 實際渲染頁面並程式化點擊**（簽核分頁欄位、確認全頁無「自動簽核」字樣、涵蓋範圍面板、攔截 window.open 驗證「列印全部」輸出 432 筆與公司全名/橫式/頁碼）＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試資料已刪；**尚未經瀏覽器實際登入點擊測試**。備註：舊的空表 `client_host_cache`（本次中途建立、已改用 ip_hostname_cache）需要時請自行 DROP，工具不放行 DDL。
> 前次：2026-08-21 — **①修掉全站「連線憑證失效 (CSRF)」存不了檔的根因（session 被 GC 誤刪）②圖面變更紀錄（`views/QC/drawing_change_log.php`）大改**。①使用者回報表單填一半按儲存跳「連線憑證失效，請重新整理頁面後再試 (CSRF)」、重整也沒用。**根因不是 CSRF 寫錯，是 session 檔已經被 PHP 的 GC 刪掉＝人其實已被登出**：Apache 的 `php.ini` 是 `session.gc_maxlifetime=1440`（24 分），`_config.php` 雖然 `ini_set` 成 12 小時，但站上有**二十幾支 API（`Part_Attachment_API`／`Quotation_API`／`Roles_API`…）是自己 `session_start()` 沒載入 `_config.php`** 的，那些請求跑的就是 24 分鐘；再加上 PHP files handler 預設 **`lazy_write`（session 內容沒變就不重寫檔案，mtime 會凍住）**，於是「還在用但 24 分鐘內沒寫過」的 session 檔會被抽中 GC 刪掉。修法：`_config.php` 每次請求把 session 檔的 **mtime touch 到現在**（只動時間不重寫內容，620KB 的大 session 也沒有寫入成本）；另外**這類頁面的 CSRF 檢查一定要先驗有沒有登入**——token 會在同一個請求裡重新產生，比對必定不過，訊息講錯的話使用者只會一直重整卻永遠存不進去（已分成 `code=LOGIN`／`code=CSRF`，後者前端自動換 token 重送一次）。**session 檔在 `C:\MAMP\bin\php\sessions\`**（Apache 的 php.ini 指定；CLI 的 php.exe 是另一個目錄，查案時別找錯地方）。②圖面變更依使用者要求全面改寫：**版次分「客戶版次」與「廠內版次」兩種**（廠內一律用發行章日期＝ai-rules/15），新增「變更範圍」——客戶版次變更＝兩邊都換版且**存檔後回寫料號主檔 `d_setting.Revision`**（使用者拍板）／僅廠內版次變更＝客戶版次欄位反灰不動主檔；**廠內版次的變更前/後自動帶出此料號舊圖與新圖的發行日**（取值範圍與換圖判定完全一致，同一天上傳視同同一版，所以「變更前」要往前找到第一個日期更舊的）；**料號綁定後自動帶出客戶名稱**；**新增草稿狀態**（自動偵測到換圖先建 DRAFT：不通知、不複製檢驗標準版次，補完內容按送出才正式成立）；**全站預設簽收對象**（設定跳窗限管理員，`eg_ack_picker.js` 新增 `setLocked()` 讓 chip 變鎖頭不給移除，**真正的守門在後端送出時一律再併回去**＝鐵律8）；**修改權限＝只能改自己填的，管理員改他人的要輸入操作確認密碼**（走共用 `confirm_password_lib`，action_key `dwg_change_edit_other`）；補上鐵律7 的使用說明跳窗（本頁原本沒有）。**自動換圖記錄**：上傳／批圖編輯器判定為換圖時改成 confirm 問「要不要現在自動建立」，按否也沒關係——料號附件跳窗新增**「自動換圖記錄」按鈕**隨時可補建，一律自動帶好料號/客戶/新舊發行日建成草稿並**另開分頁**填寫（唯一實作 `dwg_auto_draft_from_part()`，同一張圖不重複建）；**順手移除 `master_data_management.php` 內重複的圖面變更登錄跳窗**，填寫介面只留圖面變更紀錄頁一套，避免兩邊欄位走鐘。另**料號主檔「版次」右側新增「版次紀錄」按鈕**（新表 `d_setting_revision_log`：誰改的、何時改的、客戶版次與對應的廠內版次、來源可點進變更單；主檔手動改版次也會留紀錄）。驗證：CLI 直呼函式＋偽造 session 打真 API 端對端（雙版次、草稿送出、三種修改權限、密碼錯誤鎖定、自動換圖不重複建、未登入擋下）＋`eg_ack_picker` node 單元測試 12 項＋**無頭 Chrome 實際渲染**（清單由 JS 填好、`?id=` 自動開明細且草稿內容正確、料號主檔新按鈕都在）＋`php -l`＋inline JS `node --check`（含渲染後的結果）＋側欄/輸入規則健檢全過，測試資料已全部刪除；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-20（二次）— **外來文件清單（`views/Sales/external_doc_list.php`＋`ExternalDoc_API.php`）新增「同一份文件只留最新版」自動判定**（使用者要求：原圖等標籤有新版本時自動改用新版、舊版自動不使用）。使用者以 AskUserQuestion 拍板四項：①分組鍵＝**同料號＋同標籤**，且**同一天上傳的多筆視同同一版全部保留**（現場一張圖常掃成兩三個檔一起傳，只留一筆會吃掉半張圖）②舊版**預設不出現在清單/列印/CSV**，工具列「顯示歷史版本」可切換檢視（灰底標「舊版（已由 ○○ 版取代）」，檔案不刪）③要有人工覆寫：**釘選現行版**＋**「兩份都要」**（該組不做版本判定，用於同標籤下本來就是不同文件，例如 D1A/D1B 兩張加工圖掛同一料號）④全部標籤一律套用，不做逐標籤開關。新表 `external_doc_version_override`（以 料號×類別 為單位，migration `views/Sales/migrations/2026-08-20_external_doc_version.php`）。三個要點：**版本判定一定要在年度/類別/PFMEA 篩選「之前」算**（原本年度是寫在 SQL 的 `YEAR(uploaded_at)=?`，已改到 PHP 端；否則篩 2025 年時該年的舊版會變成「該範圍內的最新版」重新冒出來）；**一列附件掛多個標籤時只要在任一組是最新版就保留**；**已排除的列不參與判定**也不會把別版擠成舊版。判新舊只能用清單上的**發行日期**（＝附件上傳日，可用鉛筆改）——`ai-rules/15` 的發行章日期判準明講原圖/報價圖不要求該欄位；另實測**用檔名比對版本完全沒用**（掃描檔名都是日期流水號，379 列裡只抓得到 1 列）。驗證：CLI 單元測試 10 項＋偽造 session 跑真 API 端對端＋暫時改附件日期模擬新版上傳（已回復原值）＋HTTP 實際載入頁面 200＋`php -l`＋inline JS `node --check`＋側欄/輸入規則健檢全過，測試覆寫資料已刪；**尚未經瀏覽器實際登入點擊測試**。另注意：本次工作期間「報價圖」「客戶訂單」兩個標籤的 `is_external_doc` 被取消勾選，清單範圍由 379 列縮成 85 列（目前 4 個標籤：原圖/規格書/合約文件/說明文件）。
> 前次：2026-08-20 — **①產品開發評估表／PFMEA 的表單編號改依「表單上的日期」產生（使用者明確要求，既有資料一次重編）②型態識別文件管制表新增三種自動偵測來源**。①`td_dev_eval` 依**填表日期**、`pfmea_doc` 依**業務日期**產生 `YYYYMMDD+3位流水號`（原本一律用建檔當天，補歷史紙本時編號跟表單上的日期對不起來）；`*_next_doc_no()` 加日期參數、新增 `*_sync_doc_no()` 讓**日期事後被改時編號跟著重編**（使用者拍板：編號前八碼永遠＝表單日期；管理員「全部自動簽核」會把填表日期改成簽核業務日期，這條路徑也串了）；既有 88＋71 筆以 `views/TD/migrations/2026-08-20_docno_by_date.php` 重編（**兩階段寫入先改暫時值再寫正式編號**，否則新舊編號在同一號碼空間交換會撞 UNIQUE；含已刪除的單據一起重編，不然舊號會跟新號相撞；可重複執行、有 `--dry`）。②`views/TD/type_id_ctrl_doc.php` 的項目列自動偵測從「只有外來文件附件」擴充為四種來源：新增 **產品開發評估表**、**PFMEA**（型態項目名稱取該表單綁定的 AS 文件名稱、生效日期取表單日期、**版別/文件編號直接帶表單編號**、型態類別固定「其他文件」＝使用者指定；眼睛圖示開對應頁面並帶 `?kw=編號` 自動搜尋，兩頁都加了這個網址參數），與 **part_viewer 的 ERP/資材報告檔名標籤**（新增「BOM檔案標籤設定」跳窗，**逐標籤**設定要不要列入／型態項目名稱／型態類別；標籤本身仍以 part_viewer 既有的 `system_parameters(BOM_FILE_TAGS/tags_config)` 為唯一來源不另存一份＝鐵律4；使用者拍板**每個標籤只帶最新一份**，日後有更新的同標籤檔案時同步會**原地把該列換成新檔**、不另長一列，靠新欄位 `type_id_ctrl_item.ref_bom_tag` 認列；檔案沒有 id，識別鍵是新欄位 `ref_file_name`；NAS 檔案下載走新 API `download_bom_file`（權限守門＋只准單純檔名，擋 `..` 與路徑分隔字元，不給瀏覽器直連 UNC＝鐵律5）；掃描用 `glob($dir.$bom.'*')` 不整個 `scandir`（該資料夾近 6000 個檔），中文目錄在 Windows 要轉 Big5 才讀得到）。驗證：CLI 直呼函式＋**偽造 session 跑真 API 端對端**（設定讀寫、型態類別不合法被擋、同步不重複長列、換檔原地更新、改設定套回既有列、檔案下載 200／路徑跳脫 404／未登入 403）＋`php -l`＋inline script 抽出跑 `node --check`＋側欄/輸入規則健檢全過，測試資料與設定已全部刪除；**尚未經瀏覽器實際登入點擊測試**。注意：**ERP/資材報告這個來源預設是關的**（一個標籤都沒設定列入＝不啟用），要先在「BOM檔案標籤設定」勾選；**既有的管制表要按「同步」才會長出新來源的項目列**（本次沒有批次回填全部既有管制表，會把已確認的清單洗成「需重新確認」，留給使用者決定）。
> 前次：2026-08-19（六次）— **領料需求單（`views/pages/stock.php` 領庫需求單分頁）新增 AS 文件編號綁定，列印版依 ai-rules/16 全套重做**（使用者明確要求）。①新增「列印設定」跳窗（限 stock 模組 A 級權限）：AS 綁定走共用 `asdoc_lib.php`＋`eg_asdoc_picker.js`（模組代碼 `stock_req`），另可指定**領料人圖章模板**（`stamp_template`，存 `system_parameters(STOCK_REQ/req_stamp_tpl_id)`）。②單筆列印＝正式表單：大標題公司全名動態取、表頭取綁定文件 `doc_name`、頁尾右下 `doc_no`（四階附版次且**依該單建立日期回推當時版次**）、頁碼左下且只有多頁才印、A4 橫式、字型統一。③**每一列「已領貨」欄蓋領料人（申請人）圖章**（使用者拍板的簽章位置）：走 `eg_stamp.js`＋`eg_stamp_tpl.js`，日期＝業務日期（已出庫用出庫日、否則建立日）並包 `egFmtDate()` 成 YYYY.MM.DD；**圖章一律不縮小**（使用者特別要求）——模板章用模板設計的實際尺寸並以 `height:auto !important` 蓋掉 `fillRatio` 的百分比高度，掃描實體章／預設回墨印用 91px。兩個坑：**列印新視窗拿不到 `eg_stamp.js` 注入的 CSS**（圖章樣式要在列印 CSS 自己寫齊）；**尺寸規則要看 svg 自己的 class（`car-stamp` vs `eg-stamp-tpl`）而不是「有沒有傳模板」**——有掃描實體章的人即使指定了模板，回來的仍是 `car-stamp`。圖章職稱依業務日期回推當時職務（ai-rules/22）。④清單那顆「列印」改為**逐筆各自開視窗排隊**（三之五，>15 筆先 confirm），原本多筆合併成一份的版面保留為另一顆「清單彙總」（內部用、不印 AS 編號，避免三之三的混排疊字）——使用者選擇兩顆都留。⑤新增 API `req_print_meta`（**讀取不卡管理員**，ai-rules/18 鐵則9：卡了一般人列印永遠拿不到圖章模板）／`req_print_setting_get`／`_save`（限 A 級，後端另驗文件與模板存在且未停用）。驗證：偽造 session 跑真 API 7 項全過（2026-08-18→`2-WH-01-07B`、2026-08-07→`2-WH-01-07` 版次切換正確、非管理員讀得到寫不了、不存在/停用目標被擋）＋**無頭 Chrome 實際渲染列印版目視比對**（模板章與預設章尺寸、19 筆多頁才注入頁碼、2 筆單頁不印頁碼）＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試綁定值已刪；**尚未經瀏覽器實際登入點擊測試，且尚未指定要綁哪一份 AS 文件（待使用者在「列印設定」選，`2-SM-01-04 訂單領料單` 看起來最接近）**。**同批依使用者回報再改：「已領貨」欄的簽章是**倉管**（實際出庫的人）不是申請人**——姓名一律以 `issued_by` 回查 `user.user_cname`（`issued_by_name` 存的是登入帳號/工號如 `008`，直接印會變代號章）、日期改出庫日、部門職稱用倉管本人並依出庫日回推，且**只有該列真的出庫才蓋章，未出庫留白給現場手簽**。過程抓到一個很難查的坑：**`eg_stamp.js` 的 `whenReady` 佇列在 `markReady()` 內是 `try{cbs[i]()}catch(e){}` 逐一呼叫、會把 callback 的例外整個吃掉**——列印函式裡只要有一個 ReferenceError，按鈕按下去就是「完全沒反應、console 也沒錯誤」，只能用無頭 Chrome 跑真函式才抓得到（本次就是殘留一個已移除的變數）。**本頁仍缺鐵律7 的「使用說明」跳窗**（既有缺口，本次未補）。
> 前次：2026-08-19（五次）— **文件制、修申請單（`views/ADM/doc_apply.php`）：申請人（填表人）支援「本人請假、代理人代為填表」**（使用者回報：葉卿雅請假由何沐桐代理，下拉卻只有何沐桐自己的技術部/生管組職務，選不到葉卿雅的管理部／文管中心）。作法＝申請人下拉多出一列「部門　職稱　代理人（代理 被代理人）」，選它就把**申請部門帶成被代理人的那個部門**、申請人仍印**代理人本人**姓名、圖章右下角自動加「代」字（ai-rules/18），清單/明細/CSV/列印都顯示「代理人（代理 ○○○）」。使用者以 AskUserQuestion 拍板三項口徑：①蓋代理人的章加「代」字（不是蓋本人的章）②**只有被代理人在申請日期當天真的請假才出現這一列**（否則長期常設代理會把下拉灌爆——何沐桐本來就有兩筆 2025-11-01~3000-12-31 的全域代理）③所有人都看得到（管理員補歷史單也要選得到）。新增 `applicant_on_behalf_id/_name` 兩欄與 `da_users_on_leave_asof()`／`da_delegates_asof()`／`da_people_posts_pick()`／`da_pick_post()`；請假與代理都是**依申請日期回推**（不是 CURDATE()，比照 ai-rules/22），存檔時後端用同一份候選清單再驗一次（鐵律8，冒充代理/期間外/部門不符三種都擋）。同批：①單據狀態不再顯示「（自動）／（自動簽核）」字樣（使用者要求，列表/明細/CSV）②改了申請日期後原本選的職務不成立時改為**清空要求重選**，不再默默改選成清單第一個人。**順手修掉一個既有致命 bug：`btnGenNo` 的 alert 字串裡有實體換行，整頁 inline script 語法錯誤＝頁面 JS 全失效**（`php -l` 抓不到，要用 node --check 掃 inline script 才看得出來）。**另發現既有缺口（未修，屬全站資料）：文管中心的「負責人」在 `position_level` 沒有職級，所以文管中心開的單「單位主管」欄解析不到人會留白**——要在職級設定給它一個 level 才會有章。同批再兩件：③**母文件改為由文件編碼推導**（手動輸入 `3-MM-02-04` 就自動選到母文件 `3-MM-02`，推不到就清空；編碼還沒填時不動已選的母文件，保留「先選母文件→自動產生編碼」的反方向流程）。④**修掉共用檔 `resource/js/eg_input_rules.js` 規則7（下拉打字篩選）的既有 bug**：選項快照只在頁面載入當下取一次，**凡是 AJAX 撈完才填選項的下拉，使用者一在篩選框打字就會把整份清單洗成空的**（本頁的申請人、母文件都中招，全站 35 支共用頁都可能踩到）；改成篩選前先比對「目前選項是不是上次由篩選寫進去的那批」，不是就重新快照（無頭 Chrome 實測：修正前剩 0 個選項、修正後正常）。**這種 bug `php -l` 與看原始碼都抓不到，要把 inline script 抽出來 `node --check`、或用無頭 Chrome 實際跑一次才看得見。** 驗證：CLI 直呼函式＋偽造 session 跑真 API 端對端（候選清單、三種冒充擋下、取消代理清欄位、送出核准後 `sign_applicant_dep=1`）＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試單據與通知已刪；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-19（四次）— **外來文件清單（`views/Sales/external_doc_list.php`＋`ExternalDoc_API.php`）新增 PFMEA 連動**（使用者明確要求）：①清單每列顯示該料號有沒有在 `views/TD/pfmea.php` 建過表（「PFMEA已建立」，比對以 `pfmea_doc.part_d_id` 為主、手打料號另比 `part_no_text`），工具列新增 PFMEA 篩選（全部/已建立/未建立）並連動列印與 CSV。②新增**「PFMEA 缺件偵測」**：找出「PFMEA 已建立、但外來文件清單一列都沒有」的料號（判定基準使用者拍板＝完全沒有任何外來文件，料號附件與報價附件都算；已排除的不算數），跳窗多選/全選批次建立成待補項目（新表 `external_doc_pending`，migration `views/Sales/migrations/2026-08-19_external_doc_pending.php`）。③新增**「待補檔案」分頁**：待補項目可「上傳補檔」或「不列入」（可加回）；**待補項目不進正式清單/列印/CSV**（沒有檔案的空列不該進 AS9100 管制清單，使用者拍板）。④**補檔上傳存成該料號的「料號附件」**（使用者拍板，走既有 `part_attachments`＋`part_attach_nas_dir/料號d_id`，不另開一套儲存機制），必填檔案/外來文件類別（只准勾 `is_external_doc=1` 的標籤）/文件日期，前端即時紅字＋後端 API 同規則再擋（鐵律8）；**清單的發行日期＝附件上傳日**，故補歷史文件時把 `uploaded_at` 直接寫成使用者輸入的文件日期（時分秒取當下，僅供同日排序）。⑤正式清單的**發行日期加鉛筆可改**（使用者拍板；回寫附件本體、只改日期保留原時分秒，料號/報價附件皆可，擋未來日期）。⑥順手把本頁日期顯示改走 `egFmtDate()`（ai-rules/20 該列狀態可改為已完成）、CSV 匯出補上原本漏傳的類別篩選參數。上傳 UI 依記憶 file_upload_change_event 三鐵則（原生可見 file input／類別勾選與存檔鈕常駐／送出直讀 `input.files`）。驗證：偽造 session 的 CLI 跑真 API（偵測 37 筆、篩選 3+372=375 對得起來、建立/略過/不列入/加回、六種必填與日期驗證、無權限四支全擋）＋**真 curl multipart 上傳端對端**（`uploaded_at` 寫成 2024-05-01、改日期保留時分秒、pending 轉 done、檔案落在 NAS 料號資料夾）＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試附件與資料列已刪；**尚未經瀏覽器實際登入點擊測試**。踩到的坑（測試用）：Git Bash 直接在指令列打中文會以 Big5 送出、觸發 utf8mb4 欄位 1366 錯誤，中文參數要改從 UTF-8 檔案讀。
> 前次：2026-08-19（三次）— **新增 `ai-rules/22-表單日期與當時職務.md`：全站表單一律以「業務日期當時」為準解析人員與職稱**（使用者明確要求）。起因：表單簽核設計器實測發現業務日期 2025-11-28 的案件，把「2025-12-09 才兼任品管部課長」的人解析成當時主管並蓋章。資料來源是既有的 `user_position_history`（員工管理→異動紀錄），解析走 `position_history_lib.php`；**沒補登過異動的人回推結果＝現況**，故導入不會弄壞既有資料。**四個坑寫在該篇，第一坑最關鍵也最難發現：回推不到人時退回 `eg_resolve_supervisor()`／`eg_org_dept_manager()` 這類「現況」解析器，會正好抵銷回推的意義**——那正是實測抓到蓋錯人的那一段，我自己第一版就犯了；已改為業務日期在過去時一律不退回現況（寧可少一個章也不要蓋錯人），今日/未來的單據才保留原鏈。另三坑：兼任常才是簽核身分（取職級最高那筆而非主職）、部門指定負責人要驗當時是否真的在該部門、過去日期要放行已離職者。新工具 `ai-rules/tools/check_asof_position.php` 掃出 **10 支待檢模組**（公出單/文件制修申請單/設備一覽表/審核表單/產品開發評估表/教育訓練/供應商稽核）列在該篇相容表；**該工具只比對呼叫痕跡、抓不出第一坑那種邏輯錯誤**，該項一律要用「職務異動前後各建一張單」實測。同批 `form_signer` 三項全做（自動解析簽核人／圖章部門職稱／挑人清單），建立案件改業務日期時填表人下拉即時換成當時的在職人員與職稱。
> 前次：2026-08-19（二次）— **新增「文件制、修申請單」模組（2-DC-01-01）**：全新 `views/ADM/doc_apply.php`＋`DocApply_API.php`＋`doc_apply_lib.php`（表 `doc_apply`/`_change`/`_cosign`/`_dist`/`_print_log`/`_cosign_default`）。**文件編碼自動產生完全比照 `AS_Document_API.php` 的 `suggest_doc_no`**（表單依母文件遞增、其餘依 階級+部門代碼 遞增，階級由文件類別推導：手冊一階/程序二階/標準書三階/表單四階）；**版次規則**：手冊/程序/標準書一律必填，**表單「制訂」沒有版次**（欄位反灰、前後端都擋），之後 A→B→C 遞增（`da_next_version()`）；「修正」自動由 `as_document_version` 最早一筆 `revised_date` 帶入首次發行日期，版本變更日期＝本次申請日。送出前必填檢查前端即時標紅＋後端 `da_validate()` 再擋一次（鐵律8 不做半套）。**會簽**走 live_event（`DOC_APPLY_COSIGN` 會簽／`DOC_APPLY_RESULT` 結果）並在 `sideAndTopBarMenu.html` 加路由直接開會簽跳窗；**必須先選同意/不同意才能填意見**（意見非必填），文管中心負責人/管理員勾選「採用並簽」且看得到所有人的意見。**四格簽章**（核准/管理代表/單位主管/申請人）來源在模組設定各自可選，預設＝最高決策者/管理課主管(`hr_dept` 主管)/申請人上一級主管/填表人，一律即時查 `org_role_lib` 不寫死人名，並經 `delegate_lib` 的 `eg_resolve_signer()` 解析代理。管理員另有**建議建立**（掃描 AS 文件管理「有新文件或改版卻沒有申請單」的版本，可設起始日期、多選/全選批次建草稿，日期以修訂日為準並帶入頁次/摘要）、**批次自動簽核**（需操作確認密碼 `doc_apply_auto_sign`，可手動改本次填表人與日期，簽核日期＝申請日期並依 ai-rules/21 錯開 5~30 分不跨日）、批次列印/刪除、**會簽預設**（以部門分類設定，也可對單一 AS 文件覆寫，解析序＝文件→部門→全站）。**列印是 A4 直式 1:1**（`@page{size:A4 portrait;margin:0}`＋版面全部以 mm 定寸、不縮放，避免圖章大小失真＝使用者特別強調的重點）；表頭表單名稱與頁尾右下角編號取自 AS 綁定且**版次依申請日期回推**，簽章走 `eg_stamp.js`，會簽簽名欄/核發回收簽收欄各有獨立圖章模板設定。**回收記錄**簽收者＝填寫單位主管（非申請人）、回收者固定文管中心負責人。與 AS 文件管理**自動連結**：`doc_apply.as_doc_id/as_version_id` 由編碼＋版次比對，`AS_Document_API` 的 `get_document` 每個版本回傳 `online_apply`，歷史版本跳窗「申請單」欄多出「線上申請單／補線上單」。同批依使用者指示**移除 `views/ADM/as_form_list.php`**（連同選單列與 `as_flow_guide`/`as_form_designer`/`as_form_fill` 內指向它的連結；designer/fill/render 檔案保留但已無入口）。驗證：CLI 直呼函式＋偽造 session 逐一跑真 API（必填擋下、會簽先選再填、退回必填原因、核准四格簽章解析、自動簽核需密碼、print_meta）＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試資料與測試通知已刪；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-19 — **表單簽核設計器：案件重新開放上傳 PDF，並在完成時自動產生合成 PDF 存檔**（`views/ADM/form_signer.php`＋新檔 `src/common/form_signer_pdf_lib.php`）。**最重要的一件事：2026-08-14「PDF 轉圖列印畫質不如圖片」而禁止案件傳 PDF 的那個判斷，根因抓錯了**——糊的是「前端 pdf.js 把整頁重畫成點陣圖」這個動作，跟 PDF 本身無關。改用 **FPDI 直接 `importPage()` 匯入原始頁面**後畫質完全不損：實測 12 份真實文件，掃描檔的內容串流 **md5 逐一比對 100% 相同**（影像是位元組原封不動搬過去的），輸出檔只大 0.3%（差額是新加的圖章與字型）；且 **FPDI 2.6.8 免費版連 PDF 1.5/1.7 的壓縮交叉參照表都讀得動**（`attachment_lib.php:325` 那句「只能解析 1.4」是舊資訊），**不需要**經 LibreOffice 重存正規化——那步才是真正會重新編碼影像掉畫質的地方，這次刻意不做。唯一讀不了的是**加密 PDF**，一律在上傳當下擋下並說明原因，**不偷偷降級成轉圖**（使用者拍板）。畫面上的預覽底圖仍是 pdf.js 轉圖，但只供拖曳定位、不參與輸出。其餘：①案件完成時自動產生合成 PDF 存進 NAS（`fsd_case` 新增 `export_pdf_name`/`export_pdf_at`/`export_mode`，只存檔名不存絕對路徑），詳情頁「PDF」開啟可直接列印、「下載PDF」存檔，列表上已產生者亦可直接開；同一案件重複開啟拿到同一份檔案，重新產生會刪舊檔不累積；**未完成的案件一律不可匯出**（使用者拍板，避免半成品被當正式文件流出去），已完成但沒有 PDF 的舊案件開啟詳情時會背景補產。②**圖章由前端以 450dpi 等效解析度渲染成去背 PNG 再送後端疊圖**——章的長相是 `eg_stamp.js`/`eg_stamp_tpl.js` 在瀏覽器算出來的（含掃描實體章、模板、代理「代」字），後端不重寫一套渲染器，確保「畫面看到的章＝PDF 裡的章」；回覆內容不轉圖，交 TCPDF 用內嵌標楷體畫成向量文字。圖章大小/位置比照列印版（未綁模板＝91px、有綁＝模板實際尺寸，皆置中於框內）；版面比照 ai-rules/16（A4 自動直/橫式、頁碼左下、AS 編號右下）；頁面旋轉(0/90/180/270)與 A4/A3 裁切框的座標換算已全部以無頭 Chrome 渲染逐一目視驗證。③抽出 `fsdBoxContent()` 讓「畫面疊圖層」與「匯出 PDF」共用同一份「這個框該顯示什麼」的判斷，避免兩邊規則走鐘。④**修掉補案件自動簽核紀錄的既有缺口**：`fsd_backfill_submit()` 寫 `fsd_case_response` 時漏了 `reply_text`，導致補案件的自動簽核**連管理員在畫面上都看不到任何標示**；現補上並在簽核紀錄區加「系統自動簽核」橘色標記——**只有管理員看得到，文件本身/列印版/匯出的 PDF 一律不顯示**（維持 2026-08-14 的口徑）。使用者問「自動簽核紀錄要去哪裡看」的答案就是這裡：案件詳情下方的簽核紀錄區。⑤`eg_stamp.js` 新增 `EGStamp.whenReady()`（純新增 API 不影響既有呼叫端）：掃描實體章對照表是非同步載入的，**要把章轉成圖片存檔的頁面一定要等它**，否則有實體章的人會被存成預設 SVG 章、跟畫面上看到的不一樣。**踩到的坑（測試用）**：以偽造 session 的 CLI 跑真 API 時，`_config.php` 會 `session_start()` 並**鎖住 session 檔**，子行程用同一個 SID 再 `session_start()` 會**永遠卡死等鎖**——取完 CSRF token 要立刻 `session_write_close()`；另外 Chrome `--headless --screenshot` **不會等非同步渲染完成**（拍到全白），改成頁面渲染完把 canvas dataURL POST 回一支小 PHP 存檔才穩。驗證：CLI 直呼函式＋偽造 session 跑真 API 端對端 **34 項全過**（權限守門、加密 PDF 擋下、自動簽核四鐵則、串流端點、重複匯出換檔、輸出檔無損比對）＋無頭 Chrome 渲染輸出 PDF 目視比對（旋轉四方向／裁切／圖片案件）＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試資料與檔案已全部刪除；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-18 — 兩件事。①**教育訓練需求申請單（2-MM-01-05）列印版重做**（使用者回報「醜到不行」）：原本標籤欄無框線、列高極小、整張表只佔紙面上方三分之一，改為全框線表單、簡述內容／學習重點各留 30mm 書寫高度並支援換行、受訓人員改**流式清單**（人數不固定，16 人也不撐版）、受訓時間改**一列排兩天**的子表（至少保留 6 格，8 天仍單頁）、補印受訓地點／費用／簡章份數、簽章區以 `margin-top:auto` 壓在頁尾、表頭改讀綁定 AS 文件的 `doc_name`（原本寫死）。②**新增公出單模組（2-MM-01-06）**：全新 `views/ADM/business_trip.php`＋`BusinessTrip_API.php`＋`business_trip_lib.php`（表 `business_trip`/`business_trip_day`）。使用者以 AskUserQuestion 拍板四項——外訓「確認開課」時每位參加人員各自動產生一張草稿（可關閉，另有手動帶入）／多天＝一張涵蓋整個期間＋每日時段明細／簽核只有**單位主管一關**且超管可設**免簽核**／**全體在職員工不需角色**就能開看印自己的單（另有檢閱、管理員兩個加值角色）。紙本附註已實作：主管本人公出改由最高核准人員核准；主管請假走 `delegate_lib` 代理解析。自動核准遵 ai-rules/21（業務日期與時間戳分離、錯開 5~30 分不跨日）。通知走 live_event（`BUSINESS_TRIP_APPROVAL` 簽核／`BUSINESS_TRIP_RESULT` 結果）並在 `sideAndTopBarMenu.html` 加路由直接開核准跳窗。列印比照紙本（會計／課長／組長三格＋右側直書「外出時交管理課存查」＋附註），三格簽章來源可在設定各自選（留白／實際核准人／會計部門主管／上一級主管／最高核准人員），不寫死人名。**踩到的坑：PHP `date()` 是 UTC、MySQL `NOW()` 是本地**，混用會讓 `submitted_at` 比 `approved_at` 早 8 小時、業務日期還可能差一天——時間戳一律改取 DB 時間（`SELECT NOW(), CURDATE()`），新模組務必比照。驗證：CLI 直呼函式＋偽造 session 跑真 API＋外訓自動產生 9 項＋無頭 Chrome 渲染 A4 列印版＋`php -l`＋`node --check`＋側欄/輸入規則健檢全過，測試資料已刪；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-17（四次）— 表單簽核設計器（`views/ADM/form_signer.php`）三件事。①**修掉送出全壞的 bug**：2026-08-14 案件改成多圖上傳後檔名改存 `fsd_case_page.file_name`、`fsd_case.file_name` 一律 NULL，但 `fsd_case_submit()` 仍用 `if(!$case['file_name'])` 擋，導致**每一件新案件送出都回 400**（畫面「沒反應」的另一半原因：API 用 HTTP 狀態碼回錯，jQuery 非 2xx 不呼叫 success，各處 `if(!res.ok){alert()}` 全跑不到，錯誤只掉進 console——已加 `$(document).ajaxError` 統一顯示）。②圖章日期漏包 `dispDate()`，印成 `2026-01-05`，已改回 ai-rules/20 的 `YYYY.MM.DD`（本頁其他日期本來就有包，只有圖章這處漏）。③**新增「補案件」**（使用者明確要求）：管理員（含一般管理員＝既有 `fsd_template_admin`，不另建角色）把「紙本上已經簽好章」的歷史文件掃描檔補進系統——**不需要樣板**（`fsd_case.case_kind='backfill'`、`template_id` 存 0，故 `fsd_case_list` 由 INNER JOIN 改 **LEFT JOIN**）、**固定自動審核**（送出即 approved，不跑關卡不發通知，每章留一筆 `fsd_case_response(is_auto=1)`）、**上傳後才自訂圖章**（每頁「＋新增圖章」拖放定位，`fsd_case_field` 新增 `signer_user_id/signer_name/stamp_tpl_id`，**每章各自選人員與圖章模板**、左側預設模板可「一次套用到全部」再逐個改、**上限 30 個**、最小尺寸依該章模板實際大小換算，前後端同步擋）。使用者拍板三個口徑：**簽章日期一律用案件業務日期**（不逐章設）、**人員候選含已離職者**（補舊表單，比照文件管制總覽表任期回推的既有決定）、**AS 文件編號可逐案挑選**（走共用 `eg_asdoc_picker`，版次依業務日期回推）。DDL 走 `ai-rules/tools/migrate_form_signer_backfill.php`（可重複執行）。驗證：CLI 直呼函式 27 項全過＋以偽造 session 跑真 API（權限守門、POST 路徑）＋`php -l`＋JS `node --check`＋側欄/輸入規則健檢，測試資料已刪；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-17（三次）— `views/ADM/as_document_management.php` 列印「文件管制總覽表」的**修改（製表）簽章人員改依「任期」回推當時的 AS 負責人**（使用者發現：各階簽章日期＝該階文件最新修改日期，一階 2024.01.17、二階/四階 2026.07.09，但 2026.07.13 前後製表人不同，列印卻統一印同一位＝簽章不實）。新增 `as_doc_editor_term`（user_id/start_date/end_date/note，起日空＝最早、迄日空＝至今、**任期不可重疊**後端驗證），設定入口在系統設定→結構總覽列印；**候選人含已離職者**且**任期內就算數、不再檢查在職／請假**（使用者拍板：那天他本來就是負責人，事後離職不該讓舊表印不出章）；同一階跨交接日時整頁用「最新修改日」那位（使用者選定，維持一頁一組簽章的版面）。任期未涵蓋的日期才用跳窗左下手選的人（手選者仍檢查該日在職未請假）。跳窗左下新增「各階簽章」逐階預覽（顯示來源＝任期／手選）；送審快照改**逐階**存 editor_id/editor_name（相容舊快照單一欄位）、核准有效性比對鍵加入各階簽章人（任期改動＝要重新送審）。API 新增 `tree_editor_terms`(view)／`save_tree_editor_terms`(settings)。驗證：以偽造 session 的 CLI 直接跑真 API case（任期邊界 07-12→前任／07-13→新任、重疊與起迄倒置與人員不存在皆被擋、離職者可入選）＋`php -l`＋JS `node --check`＋側欄／輸入規則健檢通過，測試資料已刪除；**尚未經瀏覽器實際登入點擊測試**。另注意：本頁**尚無鐵律7 的「使用說明」跳窗**（既有缺口，未在本次補）。
> 前次：2026-08-17（二次）— 供應商定期評核（`views/pm/vendor_audit.php` 定期評核分頁 + 合格清冊）依使用者逐項拍板重做計分口徑，四批改動皆使用者明確要求：①**改用紙本 2-PH-01-05 口徑計分**——品質分 60×(1−(不良+特採)/進貨數)＋交期分 40×(1−遲交/進貨數)，半年**加總後**計算、**無條件捨去**（比照 Excel ROUNDDOWN），半年判定＝總分對等級門檻，**總判定＝上下半年總分平均**（不是拿全年數量重算）；②**單位由批數改 PCS 數量**，且**進貨數＝同月 max(被檢驗量, 回廠量) 由品質與交期共用同一分母**（同一批檢驗日與回廠日常跨月，各自當分母會出現「品質 0／交期有量」對不起來，使用者實測發現），畫面欄位改顯示**數量**（不良數/特採數/遲交數），比率移到滑鼠提示；③合格清冊**取消「廠商備註」欄**，改在評核等級右側加**備註下拉（老闆指定／客戶指定）**，**未達標者預設老闆指定**——未達標＝等級設定中**最低的一階**（等級設 A/B/C 時＝C，設 A/B/C/D 時＝D）或無等級，一律由設定推導不寫死 A/B/C（鐵律4）；④新增**廠商層級「約定工作天」覆寫**（`maker_list.audit_lead_days`，NULL＝用全域預設，設定入口在門檻設定跳窗，熱處理等交期長的廠商個別設）。過程查證出兩個 ERP 欄位陷阱並寫進記憶 [[vendor_eval_scoring]]：`qc_check.QC_ng_sqty`（驗退數）**全庫皆 0 沒人填**（唯一有量的是 `QC_QQ_sqty`，故不良/特採顆數改「優先取該批 QC_QQ_sqty，抓不到才算整批 sqty」）；`QC_check` 的 **AOD（特採）全庫 0 筆**（只有 ok/ng/QQ），使用者選定特採＝AOD 故特採數目前恆為 0，已回報待定案是否改抓 QQ。另修掉 `periodic_eval`／`periodic_eval_all` API 漏回傳 `full` 導致畫面「全年」分數與卡片等級一直空白的既有 bug。本頁**不在** `eg_input_rules.js` 覆蓋範圍（列於 `input_rules_baseline.txt`），故新加的廠商下拉**不用 `data-eg-filter`**（會是死屬性），沿用本頁既有「關鍵字查 API 重建下拉」模式。全部改動以 CLI 直接呼叫函式驗證（計分手算對帳、廠商工作天 10天→遲交172PCS/14天→20PCS、未達標判定各等級、schema 建欄）＋`php -l`＋JS `node --check`＋側欄健檢通過；**尚未經瀏覽器實際登入點擊測試**。
> 前次：2026-08-17 — 新增「機台設備一覽表」（`views/pm/equipment_machine_list.php`，全新頁面）＋「檢驗設備一覽表」（整合進既有 `views/QC/tool_calibration.php` 新分頁，非獨立頁面——使用者用 AskUserQuestion 明確選擇整合方案，因為 tool_calibration.php 已有「檢驗設備履歷表」AS文件綁定/四級權限基礎設施可直接沿用，避免重複建置）兩份 AS9100 文件。共用邏輯收斂進新檔 `src/common/equip_list_lib.php`（`equip_type`='machine'/'qc_tool' 參數化）：①保養人/保管人員指派歷程（`equip_assignee_history`，日期區間自動接續，現任者離職時前端標紅警示，判定直接用 `people_lib.php` 的 `state` 語意不重新發明）②機器/檢驗設備履歴表（`equip_service_log`，故障維修紀錄：日期/廠商/問題/解決方式/執行者/核准，跟校驗歷史是不同概念）③年度整份送簽核准（`equip_list_plan_lock`，比照 `vendor_audit_plan_lock` 但**刻意不鎖主檔**——機台/量具是日常會增減的活動主檔，只做送簽存證+`snapshot_json`凍結快照供列印，核准鏈沿用 ai-rules/19 三段式+SoD迴避，`org_role_lib.php` 新增 `equip_machine_list_approver`/`equip_qc_list_approver` 兩筆綁定）。機台一覽表沿用既有 `machine_list`（新增 `manufacturer` 欄位，與 `kpi_main.php`「機台資產設定」共用同一張表即時同步）；檢驗設備一覽表沿用既有 `qc_tool`（新增 `manufacturer`/`spec_desc`/`purchase_date`/`note` 四欄，`tool_calib_lib.php` ensure_schema）。四份文件（機台一覽表/機台履歴表/檢驗一覽表/檢驗履歴表）各自 AS 文件綁定＋列印固定左核准右製表；履歴表列印比照 ai-rules/16 第三之五節批次排隊模式。`user_permissions.php` 補上機台設備一覽表角色區塊+導覽連結（檢驗設備沿用既有 `tool_calib` 角色不重複建）；選單註冊於 `system_module_pages` group_id=6（生管BOM管理）。修正過程踩過一個坑：`equip_list_lib.php` 的 `EQUIP_ASDOC_MODULES` 常數 qc_tool 那組模組代碼字串一開始跟 `ToolCalib_API.php` 實際存放位置不一致（`equip_qc_list` vs `tool_calib_equip_list`），導致綁定存了讀不到，已用 CLI 功能測試抓出並統一。同批修正 `kpi_main.php` 機台停用狀態顯示 bug（JS `===` 嚴格比對字串但後端回傳數值型別，恆為 false）。全部改動已用 CLI 直接呼叫函式（權限/歷程自動接續/離職偵測/履歴表CRUD+核准/年度送簽/AS綁定）+ HTTP 煙霧測試 + JS 語法檢查(`node --check`) + 側欄/輸入規則健檢驗證通過；**尚未經瀏覽器實際登入點擊測試**（AI 沒有登入密碼），待使用者協助驗收。
> 前次：2026-08-13（二次）— 產品開發評估表（`td_dev_eval.php`，2-TD-02-01）多輪修正，皆使用者實測回報／明確要求：①部門簽核改重用 `org_role_setting.php` 既有部門綁定（技術/業務/管理/生產/品保本來就設定過），只新增「資材部門」角色，不再讓使用者重複設定；順手補齊 `user_permissions.php` 漏掉的三個角色區塊（型態識別文件管制表/產品開發評估表/PFMEA）快速切換導覽連結（角色區塊本來就有，只是導覽列忘了加，跟審核表單先前同一種失守模式）。②新增送出流程：`status`(draft/submitted/closed)+`submit_date`/`submitted_at` 分離存放（比照 ai-rules/21），送出後鎖定表頭，32項確認結果改由六部門在自己的簽核關卡自填負責項次（前後端都驗證：未填完不能填意見與簽核）；六部門不限順序、全簽完才能生產課決行、決行完才能總經理決行（決行區塊會顯示生產課已選結果供確認）、總經理簽完自動結案；新增系統管理員專用「全部自動簽核」（指定業務日期，補歷史紙本資料，可選套用管理員設定的32項預設值）。③新增「建議建立料號清單」子頁（`td_dev_eval_suggest.php`+`TdDevEvalSuggest_API.php`+`td_dev_eval_suggest_lib.php`）：管理員設定客戶名單（標籤式多選，篩選可用客戶名稱或客戶編號）+查詢區間，掃描訂單/報工/BOM/出貨找出候選料號批次建立草稿，含相關記錄查看（含報價）、忽略清單、區間外更早訂單提醒；清單改為前端純顯示分頁（預設10筆/頁，見 ai-rules/08 補充）、忽略動作改局部移除不全量重查（原本很慢）。④新增料號固定顯示名稱設定（評估表管理員/系統管理員可設，選料號自動帶入產品名稱仍可手動改）、主清單搜尋可篩選客戶編號、新增批次列印（依搜尋結果逐筆各自開視窗排隊，訂入 ai-rules/16 第三之五節新規則）、列表右下角回到頂端按鈕、列印未填意見顯示「無意見」。詳見記憶 `td_dev_eval_module`。
> 前次：2026-08-13 — 審核表單（review_form）三項使用者明確要求的改動：①負責單位/負責人挑選器可選到**兼任人員**——`people_lib.php` 的 `eg_people_list()` 新增 `dept_ids` 欄位（該人所有部門id含兼任，不只主要部門），全站其他 15 支呼叫端純加欄位不受影響。②新增「小項」功能：一個項目可拆多個小項；欄位/負責部門/負責人/簽名如何處理歷經兩輪討論才定案——**最終版是負責部門/負責人與簽名都改成掛在小項上各自獨立設定、各自簽**（不是掛在項目上整個共用），因此新增 `rf_instance_subitem` 子表（項目 `rf_instance_item` 降級為純項次分組容器），`rf_instance_item_confirm.item_id` 改名 `subitem_id`（改名當下 0 筆舊資料安全），既有 4 筆舊項目已遷移進新表未遺失；畫面上每個小項一整列都是完整獨立欄位，沒有 rowspan 合併，只有項次編號與「刪除整個項次」鈕只在該項目第一列顯示。③日期／下拉選單自訂欄位表頭新增「整欄套用」，選一次值一鍵套用到目前所有項目/小項的同一欄，取代逐列手填相同值。詳見記憶 `review_form_module`。
> 前次：2026-08-12 — 新增審核表單（review_form）模組後續多輪修正，皆使用者實測回報／明確要求：模板欄位定義改為單一 `fields[]` 清單可混合排序（文字/下拉/日期/項次自動編號四種類型任意穿插，不再固定日期排最後）＋拖拽重排序；新增「項次」自動編號欄位類型；欄位增減列改掛共用 `eg_input_rules.js` 的鍵盤 ↓↑ 自動增刪列（不自刻）；修正 `dispDate()` 未定義導致存檔/列印/清單全部壞掉的緊急 bug（`eg_date_fmt.js` 只匯出 `egFmtDate()`，`dispDate()` 是各頁要自己包一層的既有慣例）；拿掉多餘的「標題」輸入欄改用模板名稱；「業務日期」顯示文字改標示為「建立日期」；負責人挑選器改依已選負責部門過濾候選名單；欄位一多時表格會撐爆頁面，改用 `overflow-x:auto` 包裝＋部門/人員下拉選單改 `position:fixed`（原 `absolute` 會被捲動容器連帶裁切縱向，CSS規則無法只裁單軸）；新增模板列印方向設定（預設橫式，可個別模板調整為直式）；新增逐列簽章／製表核准頁尾兩種圖章模板綁定（比照 `meeting_record.php` 既有模式）；修正頁尾印章被紙張邊界切掉（縮放比例計算時機延後、footer留白加大）；`user_permissions.php` 補上「審核表單」快速切換導覽項目（角色區塊原本就有只是導覽列漏了，被誤以為不存在）；新增「免審核/免核准時要能自動簽核」需求，訂出 `ai-rules/21-自動簽核記錄邏輯.md` 三條鐵則（業務日期=送出日且與精確時間戳分離、僅超管可回改送出日、簽核時間隨機錯開5~30分鐘不跨天），供未來其他模組比照。
> 前次：2026-08-10（二次）— 供應商稽核計劃（`views/pm/vendor_audit.php`）核准流程兩處改進，皆使用者明確要求：①核准日期不再固定用系統時間，核准/退回改成真正跳窗（`planDecideMask`），核准人可自行輸入核准日期（`vendor_audit_plan_lock` 新增 `approved_date` 業務日期欄位，即時清單/跳窗顯示改讀這欄，比照既有 `submit_date`／`submitted_at` 分離模式；列印簽章日期本就固定用 `submit_date` 不受影響，維持 ai-rules/18 規則）。②核准人解析改為管理員可設定「優先序鏈」（`vendor_audit_plan_approver_chain()`）：部門或人員綁定／自動抓送出者上一階主管（沿用 `delegate_lib.php` 既有 `eg_resolve_supervisor()`，同部門找不到更高主管就往上一層部門找負責人，跟請假/採購簽核同一套）／全站共用最高決策者(`top_approver`)，依序嘗試取第一個有結果的，設定入口在「簽核設定」跳窗；並新增**強制 SoD 迴避**——解析到的核准人剛好是送出計劃本人時視同該順位無結果，自動改試下一順位，避免球員兼裁判。`ai-rules/19` 新增第五節記錄這個「優先序鏈＋強制SoD」的可複用寫法供其他模組比照。
> 前次：2026-08-10 — `views/ADM/training_record.php` 補上 `ai-rules/20` 日期顯示格式（`ai-rules/20` 相容表該列已改 ✅）：新增 `dispDate()` 顯示用包裝呼叫 `egFmtDate()`，`fmtDate()` 保留不動繼續給內部用（`DAYS`/`REQ_DAYS`/`addDaysStr`/現場簽到日期比對鍵都吃它的 Y-m-d 原始值，改了會壞），逐一區分後只把清單/跳窗/列印/CSV 等顯示用呼叫點換成 `dispDate()`。同時修**現場簽到圖章跟模組設定選的「人員簽章(長方)」模板對不上**的問題：`egStampHtml()` 補上 `dept` 參數並轉呼叫 `EGStamp.stamp(...,dept)`，簽到/簽到表/訓練紀錄的學員圖章都補上部門（該模板 schema 是 `{部門} {姓名}` 兩列，先前沒傳部門只顯示姓名，圖章看起來跟圖章管理頁的模板預覽不同）。使用者發現後回報，自行判斷修復（使用者當下忙碌無法一一確認細節，要求自行決定）。
> 前次：2026-08-06（四次）— 完成 `ai-rules/work/待補-AS編號版次依業務日期回推.md` 交辦的兩個模組串接（該檔已依指示刪除，`ai-rules/16` 第三之四節相容表已更新為 ✅）：**供應商稽核**查核表/記錄表依 `vendor_audit_target.audit_date`、稽核計劃(2-PH-01-06)依該年度 `vendor_audit_plan_lock.submit_date`、定期評核表(2-PH-01-05)全部評核彙總比照同年度稽核計劃的 `submit_date`（使用者明確要求兩者共用同一套業務日期認定；合格供應商清冊維持印現況不變）；**教育訓練**簽到表依場次最新一筆 `training_session_day.day_date`（無則退回 `done_date`→`created_at`）、需求申請單(2-MM-01-05)依 `training_request.apply_date`（年度計畫表/結果明細表為整年度彙總清單維持印現況不變）。皆用「改版生效日前一天／當天」實測資料驗證版次切換點正確（供應商稽核 doc 78/79、教育訓練 doc 39 均有實際改版紀錄可測）。
> 前次：2026-08-06（三次）— 新增 `ai-rules/20-日期顯示格式規則.md`（西元年日期**顯示**一律 `YYYY.MM.DD`，唯一實作 `resource/js/eg_date_fmt.js` 的 `egFmtDate()`／`src/common/date_fmt_lib.php` 的 `eg_fmt_date()`；只管「顯示」不管查詢/儲存/`<input type=date>`）。使用者明確要求全站適用，起因見供應商稽核記錄表列印同批修正時發現各頁日期顯示格式不一致（dash/slash 混雜）。**優先處理「有綁定 AS 文件編號」的頁面**：`views/pm/vendor_audit.php` 已完成，`meeting_record.php`/`training_record.php`/`NewOrder_Track.php`/`inspection_entry_v2.php`/`quotation_list_NEW.php` 因當時有其他工作同步編輯中避免衝突延後，狀態表見 `ai-rules/20`。同批：供應商稽核記錄表列印雷達圖高度改固定避免超出 A4 一頁、`printRecordSheet()` 補回佐證附件並改直式具名頁、附件清單點擊改跳窗預覽不直接下載。
> 前次：2026-08-06（二次）— `ai-rules/16` 新增**第三之四節「版次要依業務日期回推，不是一律印現在最新版」**（唯一實作點 `asdoc_lib.php` 新增 `eg_asdoc_version_asof()`/`eg_asdoc_no_asof_id()`/`eg_asdoc_no_asof()`，用 `as_document_version.revised_date` 回推該業務日期當時生效的版次；業務日期優先用單據本身日期欄位、無日期欄位才用單據建立日期）。使用者明確要求，起因發現列印舊單據（如會議紀錄）時 AS 編號版次一律印「現在最新版」而非該單據業務日期當時生效的版次（例：AS 表單 A 版 2025.01.01 生效、B 版 2025.12.09 生效，列印 2025.09.08 的單據應顯示 A 版）。已補上會議記錄/簽到表、報價單、線上檢驗單一製程與多製程合併列印；供應商稽核與教育訓練模組的共用函式已加上可選日期參數但呼叫端尚未串接，列入 `ai-rules/16` 第三之四節相容表待補。
> 前次：2026-08-06 — 新增 `ai-rules/19-核准人員解析規則.md`（三段式：綁部門＝該部門任一職級不低於送出者的主管皆可核准／綁人員＝固定該人／都未設＝自動抓表單綁定的AS文件所屬部門套用同一套規則）並登記進路由表；`org_role_lib.php` 新增 `dept_or_user` 綁定類型與對應設定頁區塊。使用者明確要求，起因供應商稽核計劃的核准人原本寫死用全站共用的單一 `top_approver`，但這張表單需要自己的核准規則，不該每個表單都共用同一個人。
> 前次：2026-08-04（二次）— `ai-rules/08` 新增**第六節「點開即刷新鐵則」**（會造成資料重複/動作重複又存不進去的按鈕，點下去當下先向後端抓最新狀態，不一致就擋下＋提示＋重新整理，分頁切換要連帶刷新所有分頁資料）；第五節人員列表鐵則新增第 6 條**「欄位順序固定部門/職稱/姓名，排序依部門/職稱 sort_order 非姓名筆畫」**。皆使用者明確要求全站適用，起因是教育訓練頁「A 已確認實行、B 畫面還沒更新仍能點開重複操作」。同時教育訓練頁角色說明改為即時查 `roles`/`role_features` 現況（不再寫死角色清單）、簽到表加評鑑方式獨立列＋空白簽到表列印＋多天日期清單化顯示＋跨頁時課程表頭與頁碼透過 `<thead>` 自然重複（`egPrintWindow` 新增 `pageCount` 參數）。
> 前次：2026-08-04 — 鐵律新增**第8條「修改/建立一律用最省 token、最聰明快速完美的方式」**（使用者明確要求，全程適用；原散落於 UI 規則末段的「工作方式」併入此條，避免重複）；同條內附**附件標籤鐵則**（上傳附件沒勾類別標籤一律不准存檔，前端即時擋＋後端 API 同步擋雙保險）。起因：`master_data_management.php` 的料號附件上傳可以完全不選標籤就存檔，之後附件會混在一起難以分類查找；順便把使用者對「工作方式」的既有要求正式收斂進鐵律，不再只是散在 UI 規則裡的一句話。
> 前次：2026-08-03（三次）— `ai-rules/16` 新增**第一之三節「AS 文件編號綁定的做法全站統一」**（後端唯一實作 `src/common/asdoc_lib.php`、前端唯一實作 `resource/js/eg_asdoc_picker.js`＝打編號即時篩選＋清單；**禁止純下拉、禁止各頁自刻**；表頭/表尾唯讀反灰由綁定推導）。起因：AS 文件已 163 份，教育訓練、報價單等頁面的綁定 UI 只是一個長下拉，使用者要在 163 個選項裡用眼睛找編號；且每頁存放位置與顯示格式都不一樣。同時「品管/生產/技術部門」三處模組內設定全部改為**反灰唯讀**，只能在組織角色綁定設定頁改。
> 前次：2026-08-03（二次）— `ai-rules/16` 新增**第三之二節「簽章」**（簽章欄不可只印人名，一律走 `resource/js/eg_stamp.js` 產生帶日期印章；簽章日期＝該單據的業務日期＝已存檔用檢驗日、未存檔才用列印日；印章自帶日期故**簽章旁不再開日期欄**，文件日期改印在資料表格最右上角；**代理人代簽右下角加「代」字**，代理人一律由 `delegate_lib` 的 `eg_resolve_signer()` 解析、禁各頁自己猜；自動核可要有開關＋指定核可人＋獨立功能碼授權）。起因：線上檢驗記錄表列印版定案，使用者要求全站簽章比照辦理。同時該頁列印改為 `@page{margin:0}` 去除瀏覽器頁首頁尾。
> 前次：2026-08-03 — UI 規則新增**長清單下拉一律可打字篩選**（`data-eg-filter`＋`eg_input_rules.js` 規則 7，禁止各頁自刻）；使用者明確指出這點**已經重複犯很多次**，所以從口頭要求升格成寫進本檔的鐵則。同時組織角色綁定改為**含子部門認列**（`eg_org_dept_ids()`／`eg_org_in_dept()`，路由表該列已更新）並新增「倉管部門」；修掉 `eg_org_save()` 內呼叫 `eg_org_ensure_schema()` 造成 DDL 隱式 commit、外層 `commit()` 爆 "There is no active transaction" 的儲存失敗 500。
> 前次：2026-07-31（二次）— 路由表新增 `ai-rules/16-列印文件標準.md`（列印文件三固定元素：大標題＝本公司全名發票用、動態取自 `customer_list.is_own_company=1` 的 `customer_full` **禁寫死**；頁碼「第X頁／共Y頁」統一**左下角**、`@page @bottom-left`＋`counter(pages)` 由列印引擎算、多頁才顯示；綁定的 AS 文件編號統一**頁尾右下角** `@page @bottom-right`、每頁都印）。起因：外來文件清單列印版定案時使用者明確要求全站列印文件比照辦理，而這三件事過去只散在個別頁面（quotation 頁碼、vendor_audit 公司名）沒有成文標準。標準示範頁 `views/Sales/external_doc_list.php`。（同日）鐵律5 擴充**全站統一附件根資料夾**（預設 `\\excellentnas\AS9100維護\ERP AS9100文件(勿刪)`＋以頁面名稱為子資料夾自動建立，唯一實作 `src/common/attach_lib.php` 的 `eg_attach_dir()`／`eg_attach_ensure_dir()`；附件下載一律走 API 不設 URL 前綴）。使用者明確要求，新模組不得再各自寫死 `Z:/BOM/...` 預設路徑。（前次 07-30 五次） UI 規則新增**可增列表格鐵則**（末列↓自動加列、沒填東西的末列↑自動移除，一律走共用檔 `eg_input_rules.js` 的 `data-eg-row-add`/`data-eg-row-del`，禁止各頁自刻）與**推導欄位鐵則**（來源欄位一改就要重算、算不出就清空；「休息時間」一律系統算不給手填）。起因：教育訓練頁把上課時間改成 11:00~12:00 後，休息仍停在班別帶入的 60 分，變成「上課 60 分不足以扣休息 60 分」、時數還留著改之前的舊值——**休息本來就該看課有沒有跨到午休，是不該讓人填的欄位**；使用者同時要求自動增刪列「所有可以增加列的都應該這樣設定」。（同日四次） 路由表新增 `ai-rules/15-圖面變更判定依據.md`（判斷「這張圖是不是改過」一律用**發行章日期**，不是版次）。起因：同一個判定問題連續三版提案被使用者推翻——拿「上傳」「作廢」「版次」當觸發點都不成立，因為客戶圖多半沒版次、舊版不一定作廢、原圖上傳也會作廢且操作者不是設計、原圖更新常常只是報價階段。**這些是問了才知道的現場事實，不寫進 repo 只寫進私人 memory，下一個 AI 會再繞一次同樣的路。**（同日三次）UI 規則新增**人員列表鐵則**（只列未離職／長期請假標記假別與期間／依職稱排序並顯示職稱／跨部門要顯示部門），唯一實作點 `src/common/people_lib.php`，路由表加對應條目。使用者明確要求全站適用。起因：量測儀器校驗的「校驗人員資格」名單用 `user_status<>90` 當在職判定，把離職者也列出來——**在職判定的正確欄位是 `user.state`**，各頁自己拼人員 SQL 遲早寫錯，故收斂成一支共用庫。（同日二次）UI 規則的輸入欄位那條改成**載入共用檔 `resource/js/eg_input_rules.js` 取代各頁手刻**，並加上收尾必跑 `ai-rules/tools/check_input_rules.php`。起因：「有值雙擊清空」等規則早就寫在這裡，但會計 7 頁裡有 4 頁完全沒做、有做的也只綁了跳窗沒綁篩選列——**又是「規則只寫怎麼寫、沒寫怎麼驗收」的同一種失守**（側欄已經教過一次）。共用檔走 document 事件委派，連 AJAX 後才畫出的欄位都涵蓋；既有 76 支頁面列在 `input_rules_baseline.txt` 基準線內不強迫改（硬塞會改變 Enter 行為、可能誤送出，違反鐵律4），新頁面漏載才算不合格。（同日）UI 規則新增「時間（時刻）欄位一律直接輸入、禁用下拉選時間」與「表單三總則（填寫方便性／畫面一致性／錯誤即時偵測並顯示原因）」（使用者明確要求全站適用，完整條文見 `ai-rules/08` 第零節與第二之二節）。起因：教育訓練頁把時間做成 10 分下拉被使用者退回——「對填寫人員很不友善」，且錯誤只在送出時才報。（前次 07-29 四次）路由表新增 `ai-rules/14-職務調動連動檢查.md`（職務調動要留紀錄＋連動點檢表＋前後對照；起因：代理設定因調動被終止後沒人重設，數月後才以「請假頁顯示尚未設定代理人」爆出來）。（同日三次）鐵律6「若新增頁面」加上**收尾必跑 `ai-rules/tools/check_sidebar.php` 側欄健檢**（新工具，掃全站三種側欄失效根因）。原因：規則文字其實已經寫得很清楚，但 purchase_request.php 仍然踩到——因為規則只規範「怎麼寫」、沒規範「怎麼驗收」，而 `php -l` 和搜尋原始碼有無 `sidebar-menu` 字串都驗不出被 CSS 隱藏的側欄。**靠記憶遵守的規則會失守，把它變成一行指令才擋得住。**（同日二次）鐵律6「若新增頁面」再補上**側欄 `visibility:hidden` 必須連恢復 JS 一起抄**（Shipping_Quick.php 五支 JS 都載對了、側欄仍整片消失，因為只抄了 CSS 沒抄 `$('#sidebar-menu').css('visibility','visible')`；這是側欄第三次出事，前兩次是缺 custom.min.js，屬不同根因故並列）。（同日）鐵律6「若新增頁面」補上**版型必備 JS 載入順序**（缺 `custom.min.js` 左側欄選單就死；此陷阱原本只記在 `ai-rules/00-診斷.md`＝壞了才去查的文件，導致新頁面重複踩坑兩次，故移到動工前就會讀到的地方）。（同日）路由表新增 `ai-rules/13-共用帳號通知與綁定.md`（現場共用帳號成員綁定/通知轉送/鎖密碼；規格已定案待實作）。（前次 07-28）路由表新增 `ai-rules/12-請假系統製作說明.md`（請假動工前必讀，代理走 delegate_lib、勿用 leave_agent_setting）；鐵律6 推送改用 **`git pushall`**（雙 remote：origin=excellentgear/EG、backup=ellentravel1003/EGsystem；只 git push 會漏備份）；路由表新增 `ai-rules/11-代理系統設計.md`（代理人解析一律走 delegate_lib）；鐵律5 附件暫存機制（temp/active，見 ai-rules/07）。

PHP + MySQL 內網 ERP（倉庫管理），MAMP 本地執行，Windows 10。**已用 git 版本控管**，分支 `master`，改壞可用 git 復原——但前提是每個檔案改完都有立刻 commit＋**`git pushall`**（雙 remote，見鐵律6），沒 push 的部分一樣救不回來。

## 環境速查
- 網址 http://192.168.2.128/EGsystem ｜ PHP 8.3.1 ｜ MySQL 9.4.0（utf8mb4）｜ Apache 2.4.33 ｜ phpMyAdmin 5.2.3
- 資料庫名 `EGsystem`；資料字典：`MYSQL 資料字典.txt`（196KB，用 Grep 查，勿整讀）
- **本機同時裝了 3 套 MySQL/MariaDB**：EGsystem 固定連 **port 3306**（官方 MySQL 9.4 服務 `MySQL94`，datadir=`C:\ProgramData\MySQL\MySQL Server 9.4\Data\`）；port 3307=MAMP 內建、3308=MariaDB 11.8，皆與 EGsystem 無關。系統層級操作 DB（非透過 sql.php）務必指定 3306，別接錯，詳見 `ai-rules/00-診斷.md` 陷阱表。
- 執行 SQL 唯一正道：`& C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\sql.php "SQL語句"`（中文 SQL 改用 `--file`；一次一句）。**勿用 mysql.exe**（舊 client 連不上 MySQL 9.4）。
- PHP 語法檢查：`& C:\MAMP\bin\php\php8.3.1\php.exe -l 檔案路徑`

## 鐵律（違反任一條 = 本次工作不合格）
1. **改檔前確認可回復**：本專案已用 git 版本控管，**不再手動 Copy-Item 備份 .bak 檔**（舊 .bak-* 已加入 .gitignore，不進版控）。改檔前用 `git status` 確認上一版已 commit 乾淨；真正的保障來自鐵律6——改完立刻 commit+push，才有真正救得回來的版本。
2. **巨檔保護**：本專案多個 view 超過 500KB（stock.php 721KB、master_data_management.php 1.5MB）。超過 2000 行的檔案：禁止整檔 Read（先 Grep 定位，再 Read offset/limit ±100 行）；禁止 Write 整檔覆寫（只能 Edit，錨點含前後 2–3 行原文）。
3. **不猜，但問要有效率**：不確定的需求、業務邏輯、UI 取捨 → 問使用者；不確定的欄位名 → 先 `SHOW COLUMNS` 或查資料字典。**問的方式（使用者明確要求）**：小事用合理預設自行決定、做完一次回報；真正要使用者拍板的問題**先收集齊、一次問完**（AskUserQuestion 多題並列），不要一問一答打斷使用者；工具/指令盡量批次執行，減少授權次數。何時該問、何時自己判斷：見 `ai-rules/02-判斷力rubric.md`。
4. **不破壞既有功能**：只新增或修 bug，不重構已正常運作的程式。DB 寫入用 transaction。**改動可自訂的設定/名稱/角色時，禁止在別處寫死一份對照表或預設值當說明文字**——那份文字會在使用者改名/刪除後繼續顯示舊內容而不報錯（本身測不出來，只能靠事後才發現）；一律改成**當下查詢目前實際資料**組出說明（如角色說明要秀哪些角色，就查 `roles`/`role_features` 現況，不要放一份寫死的角色清單）。
5. **檔案路徑一律「即時組路徑」＋附件暫存機制＋全站統一根資料夾**：任何跟磁碟/NAS 路徑相關的功能（附件、圖檔、匯出、備份等），DB 只能存檔名／相對值，完整路徑一律在讀取當下用「目前設定值＋即時算出的子資料夾」現場組出；
   - **根路徑一律用共用庫**（使用者明確要求，2026-07-31）：新模組的附件**預設放在 `\\excellentnas\AS9100維護\ERP AS9100文件(勿刪)` 底下，再以「頁面名稱」為子資料夾自動建立**（例：教育訓練頁＝`…\ERP AS9100文件(勿刪)\教育訓練\`）。唯一實作點 `src/common/attach_lib.php`：`eg_attach_dir($db,'模組設定鍵','頁面資料夾')` 取目錄、`eg_attach_ensure_dir($dir)` 確保存在（不存在自動建，含多層）。**不要再各自寫死 `Z:/BOM/...` 預設值**；根路徑要整批搬家時改設定鍵 `eg_attach_root` 一處即可。模組若已在設定鍵填了完整目錄，以該設定優先（既有模組不動、不必回頭改）。
   - 附件下載一律走該模組 API（權限守門＋路徑現場組），**不要**再另外設 URL 前綴讓瀏覽器直連（UNC 路徑瀏覽器也連不到）。**絕對不可**把組好的完整絕對路徑寫死存進 DB 欄位（換 NAS 硬碟或資料夾位置後，舊資料會全部讀不到、且無法只靠改設定修復）。另：**任何有附件的頁面，新增單據時就要能立刻上傳附件，不可要求「先存單據才能上傳」**——用 temp/active 暫存狀態機解決（新增中先存 temp、存檔時轉正、逾期懶惰清除）。兩者的實作標準、可直接抄的範例、尚未合規模組見 `ai-rules/07-附件路徑儲存規範.md`。
6. **收尾四件事**（每完成一個修改立刻做，勿累積到最後、勿等整個任務做完才一次處理）：
   - `php -l` 通過每個改過的檔案
   - `git add <改過的檔案>` → `git commit -m "一句話說明"` → **`git pushall`**（單一檔案改完驗證通過就立刻推，不要累積多檔一起 commit，才能保證每個 commit 都是可回復的獨立檢查點）
     - **推送一律用 `git pushall`，不要只用 `git push`**：本專案設了**兩個 remote**——`origin`＝公帳號 `excellentgear/EG`（共用/目前進度）、`backup`＝私帳號 `ellentravel1003/EGsystem`（備份，防公帳號被他人改動）。`git pushall` 是 alias（`!git push origin master && git push backup master`），一次推兩邊；只 `git push` 會漏掉備份庫。
     - 兩個 remote 的 URL 都已內嵌各自帳號的 PAT。若某邊 push 出現 `Repository not found`(404) 或 401＝該帳號 PAT 失效/無權限：**勿自行改 remote/憑證**，回報使用者重產該帳號 PAT（iOS App 不能產、需 github.com 網頁）。細節與踩坑見記憶 `git_push_broken`。
     - 注意：`views/ADM/db_backup.php` 的「GitHub 帳號綁定」只會覆寫 `origin` 的 token（保留 excellentgear/EG 路徑），不會動 `backup` remote。
   - 寫入 page_change_log（範本見下）
   - 若新增頁面：**頁面底部必須依序載入 `jquery.min.js`→`bootstrap.min.js`→`fastclick.js`→`nprogress.js`→`custom.min.js`（缺 custom.min.js 左側欄選單就死，已重複踩過兩次）；若你的 CSS 抄了 `#sidebar-menu{visibility:hidden;}`，就必須連同 `$(document).ready` 裡那段 `$('#sidebar-menu').css('visibility','visible')` 一起抄（只抄 CSS 沒抄 JS＝側欄整片消失，即使五支 JS 都載對了；照抄 `views/pm/vendor_audit.php` 底部）**；**`.right_col` 的第一個子元素一律加 `clear:both`**（Gentelella 的 `.top_nav` 高度是 0、浮動溢出，第一個子元素若自成 BFC＝`overflow:hidden`/`display:flex`/`float`，會被壓成寬度 0 → 標題整條消失＋上方空一大段；已踩兩次）；**收尾一定要跑側欄健檢：`& C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\check_sidebar.php`，三項必須全是「（無）」才算過**（側欄已連四次出事，共三種根因；靠記得遵守擋不住，改用這支工具驗收——`php -l` 與看原始碼有沒有 `sidebar-menu` 字串都抓不到被 CSS 隱藏的情況）；到 `views/user/user_permissions.php` 仿照報價單加上該頁角色設定區塊；並登記進選單＝`system_module_pages` INSERT 一列（page_name＋page_url 格式 `/EGsystem/views/...`、sort_order=MAX+1）再把 group_id 綁到「測試功能」主項目（system_module_groups.group_name='測試功能'），等同 `views/admin/system_module_setting.php` 的「子頁面設定＋主項目綁定」操作；帶參數才能開的子頁（設計器/填寫頁等）不登記，只登記入口頁
7. **每頁必附「使用說明」**（功能面，違反＝本次工作不合格）：任何有實質操作的頁面，一律在**頁首右上角**放一顆「使用說明」按鈕（`class="page-help-btn"`，`@media print` 隱藏），點擊開跳窗（`id="helpUseMask"`），內含**功能說明＋操作步驟＋重要行為/常見疑問＋設定入口＋權限角色**。位置與樣式**全站統一**：照抄 `views/pm/vendor_audit.php` 的 `.page-help-btn`／`.help-doc` CSS 與 `#helpUseMask` modal 結構（按鈕放 `.page-title` 內 `margin-left:auto` 靠右；handler `$('#btnPageHelp').on('click',…openMask('helpUseMask'))`）。改動頁面功能時說明內容要**同步更新**。既有頁面陸續補上。詳見記憶 [[page_usage_help]]。
8. **修改/建立一律用最省 token、最聰明快速完美的方式**（使用者明確要求，全程適用不限 UI）：先查證再動手、不重複讀已讀過的檔案、大檔先 Grep 定位再讀局部、指令盡量批次下減少來回、回報只講結論與影響不貼冗長過程；同時效果要完美——前端即時驗證＋後端同規則再擋一次（不可只做半套），不留下只擋 UI、API 仍可繞過的漏洞。**附件標籤鐵則**（同一原則下的具體案例）：任何「上傳附件」功能，沒有勾選至少一個類別標籤一律不准存檔——前端即時紅框＋原因提示，後端 API 同步擋（防止略過前端直打 API），禁止只做前端擋（範例：`views/pages/master_data_management.php` 的 `submitPartAttachUpload`/`submitAttachEdit` + `src/store/Part_Attachment_API.php` 的 `upload`/`update_meta`）。
9. **絕對不可按「程序名稱」砍掉使用者也在用的程式**（2026-08-24 實際事故，已用 hook 硬性封鎖）：測試用的無頭 Chrome 一律**自帶專屬設定檔**啟動（`--user-data-dir=<暫存目錄>`），收尾只終止**自己啟動的那一個 PID**（記下 `proc_open`／`Start-Process` 回傳的 PID，或用指令列比對含該暫存目錄字串的那一個程序）。**禁用 `taskkill /F /IM chrome.exe`、`Get-Process chrome | Stop-Process`、`Stop-Process -Name chrome`、`wmic ... chrome.exe delete`** ——這些會把**使用者正在使用的瀏覽器連同所有分頁一起強制關閉**。事故經過：平行 session 為了清理測試 Chrome 執行了 `/IM` 全砍，8/21 砍 9 次、8/24 一個上午砍 4 次，使用者只看到「Chrome 一直自己關掉」。**這種 bug 極難查**：外部強制終止會繞過例外處理，所以**不會產生 crash dump、Windows 事件記錄零筆、可靠性監視器也查無記錄**，看起來完全不像有人在砍它（唯一線索是 `exit_type=Crashed` 卻沒有任何當機檔）；而且 `taskkill.exe` 只存活幾十毫秒，用「當下還活著的程序」快照永遠抓不到兇手，只能用即時程序啟動追蹤。**同理適用於任何使用者也在用的程式**（`php.exe` 可能是 MAMP 的、`node.exe` 可能是別的服務、`mysqld.exe` 更不能碰）。防線唯一實作：`.claude/hooks/block_chrome_kill.sh`（PreToolUse hook，Bash 與 PowerShell 都攔）＋ `.claude/settings.json` 的 `permissions.deny`。

## page_change_log 寫入範本
```
& C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\sql.php --file 暫存sql檔絕對路徑
```
暫存 SQL 檔內容（用 Write 工具建立，確保 UTF-8）：
```sql
INSERT INTO page_change_log (page_name, summary, detail, changed_at, created_by)
VALUES ('views/xx/xxx.php', '一句話說明', '詳細說明可分點', NOW(), 'Claude');
```
完成後在回覆中告知已寫入哪一筆。

## UI 規則（所有頁面一體適用）
輸入欄位（全部欄位適用）：**新頁面不要自己手刻這些行為，載入共用檔即全頁生效**——`<script src="../../resource/js/eg_input_rules.js?v=<?= @filemtime(__DIR__.'/../../resource/js/eg_input_rules.js') ?>"></script>`（放在 `custom.min.js` 之後；它走 document 事件委派，AJAX 之後才畫出來的欄位也涵蓋，不會有「忘記綁某個欄位」的問題）。**收尾必跑驗收：`& C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\check_input_rules.php`（兩項全「（無）」才算過）**。共用檔實作的規則：有值雙擊清空（篩選欄雙擊＝同時解除該欄篩選）；聚焦已有資料的欄位自動全選；Enter 跳下一欄、最後一欄按 Enter＝觸發該區塊主要動作鈕（textarea 內 Enter 仍為換行）；多列輸入表格 ↑↓ 鍵切換上下列同欄（日期欄會攔截原生↑↓改日）；數字輸入框無上下增減按鈕、小數尾 0 省略（3.50→3.5、3.00→3）；**可增列表格的「末列↓自動加一列、沒填東西的末列↑自動移除」**。個別欄位要排除加 `data-eg-skip`。頁面自己已 `preventDefault` 的按鍵共用檔不插手，所以可安全與既有處理並存。
**可增列表格鐵則（使用者明確要求，凡是「可以增加列」的表格都適用）**：不要各頁自刻增刪列按鍵邏輯，在該表格 `<tbody>` 掛 `data-eg-row-add="全域函式名" data-eg-row-del="全域函式名"` 即由共用檔接手（含重繪後的游標定位）；「沒填東西」＝該列全空、**或**該列是剛用 ↓ 加出來且使用者一個字都沒動過（所以新列自動帶入上一列的值也不影響）。刪列函式只剩 1 列時要自己擋掉。範例照抄 `views/ADM/training_record.php` 的上課日期明細表，完整說明見 `ai-rules/08` 第一之二節。
**推導欄位鐵則**：由其他欄位算出來的值（時數、金額、休息分鐘…）在來源欄位改變時**一定要重算，算不出來就清空，不可留改之前的舊值**；其中**「休息時間」一律系統算（上課時間 ∩ 休息時段），不給使用者手填**——欄位設 `readonly`＋灰底（`.ro-auto`）＋`data-eg-skip`，後端存檔時再算一次、不採信前端送的值。
**點開即刷新鐵則**（會造成資料重複/動作重複又存不進去的按鈕都適用，完整說明見 `ai-rules/08` 第六節）：點下去當下先向後端抓這筆的最新狀態，跟畫面快取的不一樣就擋下＋提示＋重新整理清單，不要直接開窗讓使用者繼續填舊狀態。分頁切換（如有多分頁共用同一批資料）也一律連帶重新整理所有分頁的資料，只是畫面留在切換到的那個分頁。
日期欄位：一律要能點出月曆選擇，不可只讓使用者手動打字。**顯示**西元年日期一律 `YYYY.MM.DD`（唯一實作 `egFmtDate()`/`eg_fmt_date()`，禁自寫，詳見 `ai-rules/20-日期顯示格式規則.md`）；此規則不適用 `<input type=date>` 的 value 與 SQL/DB 用途。
時間（時刻）欄位：一律**直接輸入**（text，寬容接受 0900/900/9 並於離開欄位正規化成 HH:MM），**禁用下拉選時間**；即時檢查 0-23/0-59 與「同日結束不可早於開始」。
**欄位提示不可用 `placeholder` 冒充「已填好的值」**（2026-09-16 使用者回報）：提示文字只要長得像該欄位的合法值（時間 `09:00`、日期、金額、料號…），一律改掛 `data-eg-hint="提示文字"`＝共用檔 `eg_input_rules.js` 規則 8，**點一下（或 Tab 進去）才在欄位旁浮出小氣泡、離開就消失，欄位本身永遠是空的**；禁止各頁自刻 tooltip。判準：把提示當成已輸入的值唸一次，唸得通就不能放 placeholder（純欄位標籤如「主題」「開始」仍可用）。完整說明見 `ai-rules/08` 第一之三節。
表單三總則（使用者明確要求，全站適用）：①填寫方便性優先（以現場人員最快填完為準，參照資料仍走下拉且要能當場新增進主檔）②畫面一致性（同種資料全站同元件同格式）③**錯誤即時偵測並顯示原因**（輸入當下就驗，紅框＋該欄旁紅字寫「為什麼錯」，不可只在送出時丟一句「資料有誤」；後端同規則再驗一次）。完整條文見 `ai-rules/08-UI互動規範.md` 第零節與第二之二節。
配色規範（重要，違反＝不合格）：任何上色一律**暖色系**（橘/琥珀/砂/赭/暖棕/珊瑚紅），**禁止冷暖混雜、禁止亂數或 HSL 隨機上色**；分類色用事先定好的固定調色盤（同語意同色、跨頁一致）；有急件燈號語意時一律用固定三色（一般件`#F7E0BD`/急件U`#F0A24B`/特急件E`#DD5138`，`E`最急）；淺底配深棕字、深底配白字，確保對比可讀；顏色不可是唯一資訊，需搭配文字標籤。完整調色盤與檢查清單見 `ai-rules/10-配色與文字可讀性.md`。
**長清單下拉一律可打字篩選（使用者明確要求，並指出「這點已經重複犯很多次」）**：凡是人員／料號／客戶／供應商／部門等**超過約 10 筆或內容是「全公司的人」**的下拉，一律要有關鍵字篩選框，禁止讓使用者用眼睛在幾十個選項裡找。**不要自刻**——`<select data-eg-filter="輸入人員姓名篩選…">` 即由共用檔 `eg_input_rules.js`（規則 7）自動長出篩選框（含 AJAX 後才畫出的 select）；比對選項顯示文字、多關鍵字空白分隔、**目前選中的選項永遠保留**（篩選不會把已選的人洗掉）、只剩一筆自動選起來。完整條文見 `ai-rules/08` 第五節鐵則 5。
**人員列表鐵則（凡是列出「人」的地方都適用，使用者明確要求）：一律呼叫 `src/common/people_lib.php` 的 `eg_people_list($db, $opt)`，禁止各頁自己寫人員 SQL**（**單據有業務日期就改用 `eg_people_list_asof()`／逐職務的 `eg_people_posts_asof()`**，見 `ai-rules/22` 第5坑）——①只列未離職者（判定欄位是 **`user.state`**，不是 `user_status`；0離職/90特殊帳號不列）②留職停薪、育嬰留停等長期請假者仍列出但要標記「假別＋請假期間」（`leave_note`）③一律依**職稱**（`position.sort_order`）排序並顯示職稱 ④同一列表會出現不同部門的人時，用 `eg_people_multi_dept()` 判斷並**連部門一起顯示** ⑤**表格欄位順序固定「部門/職稱/姓名」（前後可依表單調整但相對順序不可倒），排序鍵依部門/職稱 `sort_order` 高低，不是姓名筆畫**⑥**兼任者的「主職務」也要列得出來**（2026-09-17 使用者回報，並指明「好多頁面都有這個問題」）：`eg_people_list()` 一人只回一列、挑的是**職級最高**那筆，所以何沐桐（主職 技術課 工程師、兼任 生管組 組長）在名單上只剩「生管組 組長」，**在技術課底下完全找不到他**。凡是「**給人挑**」的名單（下拉、勾選、篩選）一律加 `'all_posts' => true`＝一個職務一列，**下拉 value 要用回傳的 `post_key`（`uid:職務id`）不可用 `id`**（同一人兩列 value 會重複），送後端的仍是 `id`；**要「這個人的代表身分」時（簽核對象、圖章職稱、通知收件人）刻意不要開**，那種情境只能有一個身分。完整條文與踩坑見 `ai-rules/08-UI互動規範.md` 第五節。
資料列表：>10 筆分頁（改分頁邏輯前先問使用者）；分頁鈕在列表右上；每頁筆數可選 5/10/20/50；一律有 PDF+CSV 匯出、表頭表尾設定；只預載第 1 頁，其餘頁面/篩選/排序需要時才載入或背景載入。**總計、依運算欄位排序/篩選、匯出等任何要看過全部資料才能算出結果的情況，一律後端對全部符合條件的資料算完才回傳，不可只用前端已載入的那一頁計算**——完整說明與正反例見 `ai-rules/08-UI互動規範.md`。
**全表（全域）搜尋鐵則**：一律 `LIKE '%詞%'` 掃過該單據畫面上看得到的所有欄位（客戶／料號／製程／業務備註／設計備註／設計者／各種單號／容器樣品治具／日期…），多個關鍵字＝每個都要命中（可分散在不同欄位）；**禁止用 ngram FULLTEXT `MATCH…AGAINST` 當全表搜尋**——料號 `RC105-N03-A` 這種含「-」的字串，ngram 把 `-` 當分隔字元，片語比對回傳 0 筆（實測 LIKE 找得到 1 筆、MATCH 0 筆），而萬筆等級的表 LIKE 全掃只要數十毫秒，沒有用 FULLTEXT 的理由。新增可搜尋欄位時兩處（主列表 SQL 與統計 SQL）要同步改，否則筆數與內容對不上。
**工作方式**：見鐵律8（最省 token＋最聰明快速完美）。

## 權限（RBAC）
- 細節見 `RBAC角色權限機制說明.md`。每頁的新增/修改/刪除/檢閱角色**分開設定**；唯「管理者」固定擁有全部權限。
- 頁面標頭右側顯示目前使用者角色 + `?` 圖示（點擊出現各角色權限說明跳窗）。

## 制度檔案路由（ai-rules/）
| 情境 | 讀這份 |
|---|---|
| 想知道規則背後原因、環境陷阱表 | `ai-rules/00-診斷.md` |
| 要派 subagent、選模型、任務失敗要不要升級 | `ai-rules/01-模型調度守則.md` |
| 拿不定主意（該問嗎/算完成嗎/方向錯了嗎） | `ai-rules/02-判斷力rubric.md` |
| 要寫派工 prompt（搜尋/實作/重構/研究/審查） | `ai-rules/03-派工模板.md` |
| 要修改 ai-rules 或 CLAUDE.md 本身 | `ai-rules/04-維護協議.md` |
| session 開始想快速接手景況 | `ai-rules/05-給未來session的信.md` |
| 使用者要在 claude.ai App 沿用這套工作方法 | `ai-rules/06-ClaudeApp執行判斷邏輯.md`（貼入 Project Instructions 用） |
| 要新增/修改任何「檔案路徑存 DB」相關功能（附件、圖檔、NAS） | `ai-rules/07-附件路徑儲存規範.md` |
| 要做輸入欄位／日期欄位／資料列表相關頁面 | `ai-rules/08-UI互動規範.md` |
| 要列出「人員」（勾選名單/下拉/簽核對象/排班/收件人） | `ai-rules/08-UI互動規範.md` 第五節（**必讀**，走 `people_lib.php`） |
| 網頁列印畫質不夠（要接近本機看圖列印） | `ai-rules/09-網頁列印高畫質技巧.md` |
| 要上色（圖表/長條/標籤/狀態色）或改配色 | `ai-rules/10-配色與文字可讀性.md` |
| 做簽核/待辦派送/請假/異常判定，涉及「某人不在由誰代理」 | `ai-rules/11-代理系統設計.md`（**必讀**，禁各頁自寫代理 SQL） |
| 要做/改請假系統（申請、簽核鏈、假別、額度） | `ai-rules/12-請假系統製作說明.md`（**必讀**，代理走 delegate_lib、勿用 leave_agent_setting） |
| 要處理共用帳號（現場多人共用登入）的成員綁定、通知轉送、鎖密碼 | `ai-rules/13-共用帳號通知與綁定.md`（**必讀**，改收件人展開層一處全站生效；改完須回歸測試 ROSTER 通知） |
| 要做/改「員工部門職位異動」及其連帶影響（代理、權限、指定負責人、簽核鏈） | `ai-rules/14-職務調動連動檢查.md`（**必讀**，異動要留紀錄＋連動點檢表＋前後對照；代理不可自動猜人） |
| 要判斷「這張圖是不是改過」（圖面變更、料號附件、檢驗標準換版） | `ai-rules/15-圖面變更判定依據.md`（**必讀**，判準一律用發行章日期；版次／上傳／作廢都不成立，別再重推一次） |
| 表單/列印文件要有**簽章欄**（核准／審核／人事／主管） | `ai-rules/18-簽章圖章規則.md`（**必讀**，一律用共用 `eg_stamp.js` 圖章＋日期，禁止只印姓名文字或底線） |
| 要做/改任何**簽核、審核、送審通知** | `ai-rules/17-審核通知標準.md`（**必讀**，通知上要有核准/退回鈕＋退回填原因＋內容完整可看＋附件含類別說明） |
| 要用「品管部門／業務部門／人事部門是哪一個部門」「最高核准人員是誰」等**全站共用的組織綁定** | `src/common/org_role_lib.php`＋設定頁 `views/admin/org_role_setting.php`（**禁止各頁自己寫死部門 id 或人名**）。**部門判定一律用 `eg_org_dept_ids($db,$key)`／`eg_org_in_dept()`（回傳含子部門的 id 陣列），不要用 `eg_org_dept()` 的單一 id 去 `=` 比對**——組織是樹狀的（品管部→品管組、資材部→生管/採購/倉管組），只比單一 id 會把子部門的人判成「不是該部門」 |
| 要「找一個當天真的在的人來承接某件事」（逐層往上找主管、找不到人也不能什麼都不做，例如自動開單） | `src/common/unit_supervisor_lib.php` 的 `eg_unit_supervisor_available()`（**必讀，唯一實作**）：**不卡課級**，逐層往上爬到最頂層部門，每一階都用 `doc_apply_lib.php` 的 `da_user_on_leave_asof()` 確認候選人當天在職／沒請假，同單位沒人才往上一層；全部爬完仍找不到人，退回全站 `top_approver`。與下一列的 `eg_unit_supervisor()`（卡課級、給簽核關卡用）是**刻意分開的不同用途變體，不要混用**。參考實作：`qab_auto_open_from_bom_ing()`（報工NG累積開立品質異常單的開單人解析） |
| 表單上有**「單位主管」**欄位／關卡（簽章、簽核、核准第一關） | `ai-rules/24-審核層級規範.md`（**必讀**，唯一實作 `src/common/unit_supervisor_lib.php` 的 `eg_unit_supervisor()`）：單位主管＝**申請人所屬單位的最高主管**；**申請人自己就是該單位最高主管時往上一層單位取**（組長→課長）；**天花板是「課」級**——課級單位的最高主管本人開單時**單位主管從缺、由申請人自己簽，不可以再往上追到廠長／總經理**（那是「核准」欄的事，見 ai-rules/19）。兼任者一定要傳「以哪個單位的身分申請」，有業務日期一定要傳日期（ai-rules/22）。**禁止各頁自刻**，更不要再用 `department_position.primary_user_id` 當往上找主管的依據（該欄位全站一筆都沒設定，舊版就是因此從來找不到上級，才會蓋到申請人自己的章） |
| 要做「這張表單自己的核准人」（非全站共用同一人，可能是某部門任一主管、或固定關鍵人員、或沒設時要自動判斷） | `ai-rules/19-核准人員解析規則.md`（**必讀**，三段式：綁部門＝該部門任一職級不低於送出者的主管皆可核准／綁人員＝固定該人／都未設＝自動抓表單綁定的AS文件所屬部門套用同一套規則；參考實作 `vendor_audit_lib.php` 的 `vendor_audit_plan_approver_pool()`） |
| 頁面要畫 **Chart.js 圖表**（尤其要顯示圖例） | `resource/js/eg_chart_legend_fix.js`（**必載**，排在 `Chart.min.js` 之後、`document.ready` 之前；建圖函式第一行呼叫 `egChartLegendReady()`）。**症狀**：頁面有資料、KPI 數字也出得來，但**圖表連同它下面的表格整區空白、畫面上零錯誤訊息**。**根因**：版型檔 `custom.min.js` 在 ready 裡執行 `Chart.defaults.global.legend = {enabled:false}`——①Chart.js v2 關圖例的鍵是 `display` 不是 `enabled`（根本沒關成）②它是**整個物件取代**，原廠的 `labels.generateLabels` 一起不見 → 建圖時丟 `reading 'call'`，而**例外是在建圖當下丟出的，呼叫端後面的圖表與表格全部不會執行**，所以是整區空白而不是只壞一張圖。不要去改 `custom.min.js`（全站每頁都載）。使用端：`views/ADM/leave_request.php` |
| 要做／改**請假系統**，或要處理「行事曆上的休假」與請假單的連動 | `src/common/leave_calendar_lib.php`（**唯一實作**）＋ `views/ADM/leave_request.php` 的「管理者設定」。五件事：①行事曆事件類別是**休假**＋有假別＋有發生者時，存檔會自動幫每位發生者建假單，**建單一律轉呼叫 `eg_leave_submit()`**（帶 `_auto` 放寬參數），不要另寫一份 INSERT，否則時數／代理人／簽核鏈會有兩套 ②**不去動使用者畫的那筆事件**（一筆常掛好幾個人，改它等於改到別人的）；銷假時只把該人從 `evenement_actor` 抽掉 ③**迴圈保護**：請假系統核准時自己也會建行事曆事件，那種事件不可回頭建單（判定＝已有 `leave_request` 指著它） ④要不要簽核由 `leave_cal_need_approval` 決定（**預設不需簽核**），覆蓋假別本身的 `need_approval` ⑤全天事件的結束日是**含當天**，換算成假單是 `起日 00:00:00 ~ 迄日 23:59:59`，直接拿 `end` 的 00:00:00 會算成 0 小時然後整批建不出來 |
| 要做「某一家客戶的訂單／出貨／退貨」跨季跨年度分析，或要判斷「哪些客戶成長、哪些衰退」 | `src/common/client_quarter_lib.php`（**唯一實作，禁止各頁再寫一份客戶歸戶或季彙總**）。五件事：①**客戶歸戶**依序「該筆綁定的客戶主檔編號 → 料號主檔 `d_setting.Customer_Id` → 客戶名稱經 `acc_customer_by_name()`（**含別名**）」，都不成立才自成一組並標 `unmatched`；②**客戶編號是 `char(11)` 文字不是整數**（C2005、T2001…），SQL **絕對不可寫 `NULLIF(col,0)`**——MySQL 會把 'T2001' 轉成 0，整欄判成 NULL 而且不報錯，一律 `NULLIF(TRIM(col),'')`，PHP 端也不可 `intval()`；③**季預設是「帳款季」**（沿用 `billing_cutoff_day`，例如截止日 25 時 Q1＝前一年 12/26～3/25），這樣季的數字才跟帳款月份卡片對得起來，要日曆季傳 `q_basis='calendar'`；④**訂單金額只算得出有填單價的訂單**（2025 年 3,410 張只有 10 張有），一定要把 `order_quality` 一起顯示出來說明「是資料沒填不是沒接單」；⑤**進行中的季要用 `cap_days` 把比較季截到同樣天數**，否則拿半季比完整季會整批看起來在衰退；⑥**出貨性質範圍要跟呼叫端頁面「上方那個篩選」一致**（`cqa_sale_type_sql()`），不要自己另訂一套「排除 is_count=0」——上面篩掉的性質，這裡照樣算會讓兩處數字對不起來（2026-09-24 使用者實測回報）；⑦**排行類功能（拿全部客戶互相比較）如果跟「單一客戶」篩選共用同一個欄位，選了客戶就要讓排行只顯示那一家**，不要讓共用欄位看起來有作用、實際上排行仍是全部客戶。使用端：`views/Sales/Shipping_Analysis_new.php` 的「客戶季度分析」面板 |
| 要做「訂單」的分析報表（新訂單／訂單趨勢／全製單製／數量區間／客戶與料號排名） | `src/common/order_analysis_lib.php`（**唯一實作**）＋`src/store/OrderAnalysis_API.php`＋`views/Sales/Order_Analysis.php`。六件事：①**`order_track.Order_status` 6＝暫停/取消、9＝已結案**（依 `NewOrder_Track.php` 的 `toggleOrderStatus()`），本庫排除 6、計入 9；**`kpi_as_lib` 與 `client_quarter_lib` 把 9 當成「已取消」排除掉，口徑不同**，動到任何一邊之前先確認要用哪一種並寫在畫面上；②**訂單金額只算得出「有填單價」的訂單**（2024 年 2,922 張只有 9 張、2025 年 3,628 張只有 11 張），每個金額都要同時回報覆蓋率，**基期覆蓋率過低時要自動改用「數量」**，且**成長率超過 10 倍時不可以印百分比**（會印出 +338368%）；③**新料號＝出貨／訂單／製令／退貨四個來源裡第一次出現**（`oa_first_seen()`），料號一律以主檔 id 歸戶並把「同名同客戶被建兩筆主檔」的併起來（**料號文字掛多家客戶時刻意不併**）；畫面一定要印出四個來源各自最早的資料日期，講明那不等於「公司從來沒做過」；④**製令開立日呼叫 `dqa_bom_open_date()`**（編號回推，不要用 `Created_At`）；⑤**全製／單製只能用關鍵字規則判定**——`Processing_items` 是手打自由文字、系統沒有任何欄位記著這件事，所以規則要可設定並把「每條規則命中幾筆」印出來讓管理員校正；⑥**Highcharts 橫條圖只給 `chart.height` 沒有用**，`.chart-box` 的 CSS 固定高度會把它壓回去，**條數多的排名圖要連容器高度一起設**（`sizeBox()`），不然負值那半段整個看不到而且不報錯。權限沿用訂單追蹤模組 `ot_analysis`／`ot_analysis_setting`，設定存 `system_parameters('ORDER_ANALYSIS')`，沒有自己的資料表；⑦**自動分析／KPI 未達標提醒／金額移動平均監控**（2026-09-24 新增）一律呼叫 `oa_insights()`／`oa_kpi_alert()`／`oa_moving_avg()`，**KPI 判定不自己另寫一套**，直接呼叫 `kpi_as_lib.php` 的 `kpi_as_display_value()`／`kpi_as_below_target()`；⑧**移動平均連續低於安全水平的自動通知走 `src/common/order_analysis_notify.php`**（站內通知＋Web Push＋Telegram，比照 `comm_ctrl_notify.php` 同一套做法），每月評估一次、順路觸發（`order_analysis_tick.php`／`order_analysis_run.php`，掛在 `_config.php`），**當月「有填單價的訂單」佔比過低要視為資料不足不納入評估**，不擋的話本站 2026-03 以前的空白單價資料會發出假警報；⑨**上下限夾範圍不可以「重新讀資料庫再合併」**——`oa_settings($db) + $cur` 這種寫法會被 PHP 陣列 `+` 運算子的「左邊鍵優先」規則坑到，用舊值蓋掉剛存的新值，正確寫法是抽成 `oa_settings_clamp()` 直接對記憶體裡的陣列正規化；⑩**列印報告固定 A3 橫式，不做「A4 放不下自動升 A3」**（紙匣實際紙張是設備決定的，`project_mgmt_ui.js` 已踩過這個坑），圖表一律用畫面上既有 Highcharts 實例的 `getSVG()` 轉向量圖嵌進報告，不要重新畫一次 |
| 要做/改任何「列印文件」（報表、表單、清單列印版）或**任何簽章欄** | `ai-rules/16-列印文件標準.md`（**必讀**，大標題本公司名動態取禁寫死／**表頭＝已綁定 AS 文件的表單名稱 doc_name，禁寫死**／頁碼左下 counter(pages)／AS 文件編號右下角／**簽章一律走 `eg_stamp.js` 帶日期印章、代理人代簽右下角加「代」字＝第三之二節**／**單一筆有業務日期的單據，版次要依業務日期回推＝第三之四節**） |
| 要做「有格式的文字」欄位（備註/說明要能部份粗體、底線、換文字色或底色） | 前端 `resource/js/eg_richtext.js`（`EGRichText.attach/get/set/render`）＋後端 `src/common/richtext_lib.php`（`eg_richtext_sanitize()`）（**必讀，唯一實作禁止各頁自刻**）。三件事：①配色只給 ai-rules/10 的固定暖色系調色盤，**不給任意取色器** ②**存檔前後端一定要再 sanitize 一次**——這種欄位是原樣輸出到頁面的 HTML，只擋前端＝一個存起來的 XSS ③style 白名單**不能只留 color/background-color**，`execCommand` 的粗體是 `font-weight`、底線是 `text-decoration`，濾掉會「按了沒反應也不報錯」。參考實作 `views/ADM/as_document_management.php` 的文件備註 |
| 要顯示一堆**照片**（產品照片、現場照片、檢驗照片）給人看 | 前端 `resource/js/eg_photo_album.js`（`EGAlbum.grid/viewer/thumb`＝九宮格＋點開放大）＋後端 `src/common/photo_album_lib.php`（相簿分組＋GD 縮圖）（**必讀，唯一實作禁止各頁自刻**）。四件事：①**哪些標籤走相簿由 `quotation_file_categories.is_photo_album` 逐標籤勾選**，不寫死「產品照片」這個名字（鐵律4）②**相簿只是系統內的分組，NAS 上不開子資料夾**——檔案位置照舊由鐵律5 的即時組路徑決定，改相簿名稱／搬照片都不動硬碟上的檔案 ③九宮格**一定要走後端縮圖**（`download&thumb=1`，有快取），把原圖塞進 `<img>` 縮小＝一次載十幾 MB 會卡死整個跳窗 ④相簿的建立/改名/解散/搬移都要後端再驗一次（`pa_album_can_edit()`／`pa_album_check_name()`）。參考實作：料號主檔附件跳窗、`views/pm/bom_viewer.php`、`views/pm/part_viewer.php` |
| 要把磁碟上的**圖檔／PDF 旋轉並存檔**（不是只改畫面角度） | `src/common/image_rotate_lib.php`（`eg_rotate_file($fsPath,$degCW)`，**唯一實作禁止各頁自刻**）。**JPG 走無損**：專案自帶 `src/bin/jpegtran.exe`＋`jpeg62.dll`（libjpeg-turbo，隨版控走、換機不必安裝），不解碼直接搬 DCT 係數，實測連轉四圈 MAE=0、檔案大小不變、每次約 100ms；**Windows 內建的 WPF 與 ImageMagick 都不是無損**（已實測，別再花時間試）。走不通時自動落回 GD（PNG／PDF、jpegtran 不在、`proc_open` 被停用、**帶 EXIF 方向的照片**——jpegtran 不更新 Orientation 標籤會被瀏覽器再轉一次）。**jpegtran 刻意不加 `-trim`**：不加才一個畫素都不丟且來回完全還原，補的是乾淨白邊。 另外四件事：①**GD 的角度是逆時針**，順時針要送負值 ②**JPG 一定要先依 EXIF 方向正規化**，瀏覽器 `<img>` 會自動轉正、GD 不會，不處理就是「按順時針、圖卻跳到別的角度」（**CLI 的 php.ini 沒開 exif、Apache 的有開，用 CLI 測不出來**）③**大圖要先動態拉高 memory_limit**——掃描圖面 7016×4961＝3480 萬畫素，GD 要吃約 280MB，預設 128M 直接 fatal 白畫面 ④**先寫暫存檔、驗證成功才覆蓋正本**，且**不要用 `is_writable()` 預檢**（對 NAS 的 UNC 路徑一律回 false）。使用端：`views/pm/bom_rotate.php`（圖面查閱的左轉／右轉） |
| 要處理**圖面查閱頁那四種來源的檔案**（圖面/nas、料號附件、訂單附件、報價附件）——權限、實體路徑、或要讓「不帶登入 cookie 的用戶端程式」取檔 | `src/common/bom_view_file_lib.php`（`eg_bvf_parse_url()`／`eg_bvf_can_read()`／`eg_bvf_resolve()`／`eg_bvf_token_issue()`＋`_consume()`，**唯一實作**）。三件事：①前端 path 有兩種型態（`/nas/…` 與附件 API 網址），**不可以無條件包進 `bom_download.php`**——那支只收 `/nas/` 開頭，附件送過去就是 400 ②附件要讓瀏覽器「另存新檔」請帶 `dl=1&dl_name`（`eg_attach_send_disposition()`，**Chrome 會讓伺服器的 Content-Disposition 蓋掉 `<a download>`**）③用戶端程式（如小畫家的 VBScript 用 `MSXML2.ServerXMLHTTP`）**不會帶登入 cookie**，要取檔一律先換一次性權杖再打 `views/pm/bom_open.php` |
| 要做**齒輪／花鍵／栓槽的計算**，或想把這個計算視窗放到別的頁面 | `src/common/gear_tool_lib.php`（權限＋`gear_*`/`spline_*` API）＋`views/Sales/gear_tool_api.php`（唯一端點）＋`views/Sales/_gear_tool_ui.php`（CSS＋浮動視窗＋JS）（**必讀，唯一實作禁止各頁自刻**）。要在新頁面用：`$GEAR_TOOL_PDO = $pdo; include __DIR__.'/_gear_tool_ui.php';` 放在 `</body>` 前，再放一顆 `onclick="openGearTool()"`，按鈕顯不顯示看 `$show_gear_tool`（共用檔會自己算好）。要**停靠成頁面內的側邊面板**（不要 1280px 浮動視窗蓋住畫面）再多設三個變數：`$gear_tool_dock`＝面板要貼哪個容器的右緣（CSS 選擇器，該容器須能當定位父層，共用檔會自動補 `position:relative`）／`$gear_tool_dock_default`＝預設停靠或浮動／`$gear_tool_dock_offset`＝同容器內另一個也貼右緣的面板（它一開，齒輪面板自動往左讓開並排）；使用者可用標題列的「停靠／浮出」自行互切，選擇與寬度記在 localStorage。三件事：①**不要再寫一份 `gear_*`/`spline_*` 的 action**，預留量表與花鍵公差表是全站共用同一份資料 ②視窗內的 AJAX 一律走 `gearAjax.post`（`fetch`，**不依賴 jQuery**），不要改回 `$.post('')` ＝那會打到 include 它的那一頁 ③深色主題頁面 include 時只能釘「會被繼承的」樣式到 `#gear-tool-window` 本身，用 ID 選擇器去改 `input`/`label` 會蓋掉原本的 `.gear-*` 外觀。使用端：`views/Sales/NewOrder_Track.php`、`views/Sales/image_editor.php` |
| 要做**鍵槽（軸件／片狀）的公差與極限值計算**，或想把這個計算視窗放到別的頁面 | `src/common/keyway_tool_lib.php`（`keyway_tool_can_use()` 權限判定）＋`views/Sales/_keyway_tool_ui.php`（CSS＋浮動視窗＋JS）（**必讀，唯一實作禁止各頁自刻**）。要在新頁面用：`$KEYWAY_TOOL_PDO = $pdo; include __DIR__.'/_keyway_tool_ui.php';` 放在 `</body>` 前，再放一顆 `onclick="openKwTool()"`，按鈕顯不顯示看 `$can_keyway_calc`（共用檔會自己算好；呼叫端若已自行算過就沿用不覆寫）。三件事：①**純前端計算、沒有後端 API**，不要為了它另開端點或資料表 ②視窗 HTML 是 `<template>` 首次開啟才注入，所以**不可以在頁面載入當下就去 `getElementById('kw-tool-window')`**（要先 `openKwTool()`）③深色主題頁面 include 時只能釘「會被繼承的」樣式到 `#kw-tool-window` 本身，用 ID 選擇器去改 `input`/`label` 會蓋掉原本的 `.kw-ni`/`.kw-ti` 外觀。使用端：`views/Sales/NewOrder_Track.php`、`views/Sales/image_editor.php` |
| 要知道／顯示「哪一份 AS 文件已經做成系統頁面」（結構總覽的網頁欄、流程說明手冊的線上表單標示） | `src/common/asdoc_page_lib.php`（**唯一實作，禁止各頁再掃一次綁定**）：`eg_asdoc_page_map($db,['skip_sources'=>…])` 回 `[as_document.id => 用途/網址/來源]`，來源＝`AS_DOC_BIND` 動態全掃＋舊式綁定＋`linked_module`＋線上表單設計器；`eg_asdoc_page_can_open()` 判該使用者能不能開（比照左側選單），`eg_asdoc_page_registered()` 判該頁有沒有登記進選單（沒登記的子頁要由呼叫端用自己模組的權限決定）。新模組只要照 ai-rules/16 綁定 AS 編號就會自動出現；只有「舊式各自存放的綁定」才需要回這支加一列 |
| 要做／改**內部稽核**（年度稽核計畫、稽核通知單、AS／系統／績效三種查檢表、不符合通知單、稽核報告表） | `views/ADM/internal_audit.php`＋`src/store/InternalAudit_API.php`＋`src/common/internal_audit_lib.php`（**唯一實作**）。五件事：①**會議紀錄不在這裡重複建立**，一律建好 `meeting_record` 草稿再開 `meeting_record.php?id=` ②**三種查檢表的題目都不另存一份**（AS條文走 `ia_as_clause` 題庫、系統稽核即時查 `as_document`、績效即時查 `kpi_as_indicator`）③**編號依表單上的日期產生**不是建檔當天（`ia_next_case_no()`／`ia_next_nc_no()`）④年度計畫表的 **◎ 實際實施不存 DB**，由 `ia_plan_actual_map()` 即時推導 ⑤IA 單四段分段鎖定走 `ia_nc_stage_perm()`，受稽單位判定用**該部門＋子部門**（挑根部門會變成全公司都算）⑥**稽核員／陪檢員可多位**：人員的唯一來源是 `ia_case_dept_person`，寫入只走 `ia_cd_people_set()`、讀取只走 `ia_cd_people_map()`；`ia_case_dept` 上的 `auditor_*`／`escort_*` 只是顯示用快取，不可在別處各自更新 |
| 要做／改**品質異常處理單**（2-QA-01-01：異常原因分類、處置方式、總經理裁示、相關單位意見、扣款確認、報廢單號） | `views/QA/qa_abnormal_list.php`（清單入口）＋`qa_abnormal_form.php`（單張處理頁）＋`qa_abnormal_print.php`（列印版）＋`src/store/QaAbnormal_API.php`＋**唯一實作 `src/common/qa_abnormal_lib.php`**。七件事：①**三張代碼表都由管理員維護、一律存 id 不存文字**（`qa_cause_cat` 三層原因分類／`qa_option` 處置與裁示／`qa_decider_cfg` 決策者部門＋職稱）；**「是不是報廢」「是不是轉總經理」一律看 `is_scrap`／`is_escalate` 旗標，禁止比對名稱**——管理員改個名就會全部失效 ②**最終處置＝總經理裁示優先**（`qab_final()`），含報廢者**在結案當下才配發報廢單號**（`qab_scrap_alloc()`，`F`＋民國年3碼＋MMDD＋流水3碼，寫進 DB 供後續單據追蹤；取消結案不收回）③**相關單位意見一次只送一個**，上一輪沒回覆不給送下一輪 ④**責任單位＝製程＋廠商**，廠商是 `maker_list.internal=1`（主檔管理→廠商編輯的「廠內加工廠商」）才可再複選部門與人員 ⑤**扣款的製程金額來源是 `bom_ing_transfer_log`**（製程移轉一覽表同一張表），`qab_deduct_autofill()` 只重建 process 列、手動加的「其他」列不動；加成只作用在製程小計 ⑥**狀態不存欄位、由資料推導**（`qab_status()`／`qab_status_map()`），不合格品管制記錄表就是讀 `qab_status_map()` 且**一律唯讀** ⑦**客戶一律由來源綁定**（`qab_resolve_client()`：料號主檔優先、退回 `acc_customer_by_name()` 別名歸戶），畫面唯讀、後端重算，不採信前端送的客戶名稱 ⑧**補資料模式**（`qab_is_backfill()`：填寫日期在今日往前 `backfill_days` 天以前，預設 10）＝可逐格補登簽章人員與印章日期（`qab_sign_slots()`／`qab_backfill_time()`，人員依日期回推當時在職者、時間接在前一格之後錯開不跨日），**只有 `qab_admin` 異常單管理員有**；補資料時的單位意見直接補登且不發通知 ⑨**最終決策者不在本模組設定**＝`qab_gm_person()` 取全站 `org_role_setting.php` 的「最高核准人員」再過代理解析；`canGm` 刻意不含模組管理員 ⑩**被徵詢的人多半沒有本模組角色**，看單一律走 `qab_can_view_order()`（開單人／共編／被徵詢的本人或該部門／追蹤人），只擋 `canView` 會讓通知點進來變 403、徵詢流程直接斷掉 ⑪**權限是「組織角色部門綁定＋RBAC 角色」並用**（`qab_perms()`）：品管/業務可開單、生管/業務可填扣款、會計可核准、`top_approver` 是最終決策者，`qab_*` 角色只給不在那些部門的人補授權 ⑫**報工NG可自動/補開異常單**——**累積到完工才一次開，不是每筆報工各開一張**（`qab_auto_open_from_bom_ing($bomIngFid,$triggerReportId=null)`，加總該站 `qab_pm_uncovered_reports()` 查出的全部未歸入NG，開單人＝`eg_unit_supervisor_available()` 解析出的現場主管）；持續性生產（一直不完工）改由 `qab_pm_suggest_check()` 每兩週提醒一次是否要先開累積單（新表 `qab_pm_suggest_state` 記最後提醒日期，週期從最早一筆未歸入NG的報工日期起算）；**建單邏輯唯一路徑是 `qab_create_order()`**（不要在別處再刻一份 INSERT） |
| 要讓「**報廐／良品數**」正確反映在待包裝、BOM總覽、快速出貨可出量 | `qa_abnormal_lib.php` 的 `qab_bom_scrap_qty()`／批次版 `qab_bom_scrap_rows()`+`qab_bom_scrap_sum_rows()`（**唯一實作**）：只算「已結案且配發報廐單號」的異常單（`is_closed=1 AND scrap_no IS NOT NULL`——使用者拍板結案配號才正式扣，不是報工/檢驗當下就扣），依 `pm_report_id` 或 `resp_process_no` 回推是哪一站發現的；`$uptoBomSn` 傳 `null`＝整張BOM口徑（待包裝/BOM總覽/快速出貨這種整批畫面用），傳站別編號＝只算「該站之前（含）」（檢驗表算自己這一站的良品數用）。**一次列很多BOM的畫面一定要用批次版**（如 `OreadyReply_ForPm_BaseOfTime.php`／`_fetch_data.php`／`_fetch_data2.php`），不要逐列各呼叫一次（N+1）。使用端：`inspection_combined_prototype.php`／`packing_schedule.php`／`shipping_lib.php` 的 `sq_bom_avail_map()` |
| 要讓使用者**挑「異常原因分類」**，或要**維護那棵三層分類樹**（新增／改名／停用／排序／刪除） | `resource/js/eg_cause_picker.js`（**全站唯一實作，禁止各頁自刻**）＋後端 `src/common/qa_abnormal_lib.php`。兩種模式共用同一套逐層大方塊：**挑選模式**（`multi:true/false`，各表單用）與**維護模式**（`manage:{can,url,csrf}`，設定頁用＝點方塊往下一層、右上角 ✎ 開修改面板、**刪除只在 ✎ 裡面**、＋ 新增、改了當下就存）。五件事：①**改名／刪除前一定要先查使用情形**（`qab_cause_usage()`，含子孫），而且**三個來源要一起算**——`qa_abnormal_cause`、`car_order_cause`、`car_order.cause_cat_id`，少算一個就會說「沒有人在用」然後把別張單的分類刪成空白 ②**刪除有單據在用的分類一律要先指定移轉對象**（`cause_del` 帶 `to_cat_id`，移轉＋刪除同一個交易；沒帶時回 `need_transfer` 讓畫面去問），候選要排除自己與子孫 ③**移轉一律「先插目標再刪來源」不可以用 UPDATE**（`qa_abnormal_cause` 是 UNIQUE(order_id,cat_id)，同一張單同時選了來源與目標會撞鍵）④**分類是各表單共用的**（品質異常處理單／矯正單／客退單），改了三邊一起變，畫面上要講明 ⑤停用比刪除安全（既有單仍看得到、新單不再出現）。使用端：`views/QA/qa_abnormal_list.php`（設定）、`qa_abnormal_form.php`、`views/QA/correction_order.php`、`views/Sales/IR_Track.php` |
| 要做／改**溝通管理**（利害關係者溝通記錄表、回應利害關係者措施追蹤表、溝通管制表） | `views/GM/communication_mgmt.php`＋`src/store/CommMgmt_API.php`＋`src/common/comm_mgmt_lib.php`（**唯一實作**）。五件事：①**三份表單是漏斗不是三張獨立的表**：管制表＝常態機制（不寫單一事件）、記錄表＝事件當下、追蹤表＝只列需要後續行動的（有負責人＋預計完成日），記錄表每列靠 `comm_record_item.track_id` 一對一連到追蹤表，已轉過的不可再轉 ②**「部門主管確認」的解析規則是使用者指定的、和 `eg_unit_supervisor()` 不同**：候選＝同部門內「職位編號 `position.sort_order` 小於填表人」且「階級 `position_level.level` 不低於設定門檻」的**全部人**（任一人簽即可、門檻可在模組設定調），同部門找不到才往上層部門找；**往上的天花板共用 `EG_UNIT_SUP_TOP_LEVEL`（ai-rules/24 的課級上限），不要在這裡另寫一個 3** ③**簽章日期一律用該單的溝通日期**（業務日期），單號也依溝通日期產生，不是建檔當天 ④「總經理確認」預設取 `top_approver`，**禁止寫死人名**；兩格都可由管理員改成指定人員或指定階級 ⑤**填表身分要讓兼任者自己選**（`cm_identities()`），後端 `rec_save` 會拿同一支函式重驗一次，擋冒用不存在的身分 ⑥**挑人一律「先選部門再挑人」兩段式**（`cm_dept_people()`／前端 `loadDeptPeople()`），**人員資料放模組變數 `PEOPLECACHE`、絕對不可掛在 `<option>` 的 jQuery `data()` 上**（打字篩選會用 innerHTML 整批重畫選項，掛上去的資料會整個不見）；下拉的 value 用**陣列索引不是 user_id**——同一人在同一部門可能有兩個職稱＝兩列，用 user_id 會撞號 ⑦**客戶／供應商是「在對象欄位本身打字即時搜尋」的自動完成**（`acSetup()`，唯一實作），不要再做成「關鍵字欄＋選擇下拉」兩個欄位；**手動改字一定要自動解除綁定**，否則會出現「畫面上寫 A、實際綁著 B」而且完全看不出來；建議清單用 `position:fixed` 由 JS 定位（跳窗的 `.cm-mbd` 是捲動容器，absolute 會被裁掉）⑧**到期提醒掛在全站共用的 `_config.php`**（`eg_comm_ctrl_tick()`），不是掛在本頁——只掛本頁的話沒人開溝通管理就永遠不檢查。與 `personal_task` 的關鍵差異：**本模組一定要寫 `live_event` 站內通知**（使用者要求：不綁手機或 Telegram 也要收得到），同一期用 `remind_sent_for` 搶佔只發一次，改過 `next_due_date` 才會重新發 |
| 要讓人**挑量具／檢具**，或要顯示某一支量具（檢驗設備一覽表 `qc_tool`） | `ai-rules/25-量具顯示規範.md`（**必讀**，唯一實作 `src/common/qc_tool_display_lib.php`）：**`qc_tool.state` 是 1＝停用、0/NULL＝在用**（寫反過一次，害在用的 QC-001／QC-002 整批挑不到、停用的反而列出來且不報錯）；**停用的量具與機台不可以直接不見**，補舊單據時要依業務日期判「那一天還沒停用」並標示出來；**顯示哪幾個欄位逐類別設定**（設定入口只有量測儀器校驗頁的「類別設定」，`qc_tool_disp_label()` 取字），各頁不可自己拼字串；**一律存 `Tool_id` 不存編號文字**（改名就失聯而且不報錯）；挑選欄位 `readonly` 只給點選、沒挑一律寫 `N/A` |
| 頁面上要**顯示**西元年日期（列表/列印/跳窗文字） | `ai-rules/20-日期顯示格式規則.md`（**必讀**，一律 `YYYY.MM.DD`，唯一實作 `eg_date_fmt.js`/`date_fmt_lib.php`；不適用 `<input type=date>` 與 SQL/DB） |
| 要做/改「綁定 AS 文件編號」的設定（哪張表單對應哪份 AS 文件） | `ai-rules/16` **第一之三節**（必讀）：後端只走 `src/common/asdoc_lib.php`（`eg_asdoc_get/list/save`，值存 `system_parameters('AS_DOC_BIND',模組代碼)` 且只存 id）、前端只走 `resource/js/eg_asdoc_picker.js`（`EGAsDoc.open()`＝**打編號即時篩選＋清單**）。**禁止純下拉**（AS 文件已 163 份，眼睛找不到）、**禁止各頁自刻**；表頭／表尾欄位一律唯讀反灰、由綁定推導不給手填 |
| 要做/改「送出後免人工的自動簽核」 | `ai-rules/21-自動簽核記錄邏輯.md`（**必讀**，三條鐵則：業務日期＝送出日且與精確時間戳分離存放`submit_date`/`submitted_at`／僅超級管理員可回改已送出單據的送出日／簽核時間刻意隨機錯開送出時間5～30分鐘、不可跨天；簽核人歸屬走該關卡原本的合格簽核池取第一位，池空才退回`top_approver`；參考實作 `review_form_lib.php` 的 `rvf_instance_submit()`） |
| 表單要「解析人」或顯示部門/職稱（簽核人、主管、挑人清單、圖章職稱） | `ai-rules/22-表單日期與當時職務.md`（**必讀**，一律以**該單據的業務日期**為準回推當時職務與在職狀態，走 `position_history_lib.php`；**回推不到人時絕對不可退回「現況」解析器**——那會把現在才上任的人蓋到舊文件上；兼任常才是簽核身分不可只取主職；過去日期要放行已離職者。**第5坑（2026-09-16 新增，最容易重犯）：讓使用者手動挑人的那個下拉也算解析人**——`eg_people_list()`／`eg_people_posts()` 永遠只回今天在職的人與今天的職稱，有業務日期的模組一律改用 `eg_people_list_asof()`／`eg_people_posts_asof()`（唯一實作在 `people_lib.php`，**禁止各模組自己再寫一份**），否則補歷史單據時當時在職現已離職的人一個都挑不到、而且完全不報錯。收尾跑 `ai-rules/tools/check_asof_position.php`，**C 區**就是在抓這一類） |
| 新增/修改**任何會列印的頁面**，或**任何有簽核**的模組 | `ai-rules/23-列印與簽核紀錄.md`（**必讀**，兩條鐵則：會列印的頁面一律呼叫 `eg_print_log_add()`／`EGPrintLog.record()` 留下列印時間·列印人·登入電腦·文件名稱，**禁止各頁自己再開一張列印紀錄表**；簽核一律走共用 `approval_lib.php` 寫 `approval_record`，自建簽核表的模組在紀錄頁查不到＝沒有可追溯性。查詢頁 `views/admin/print_sign_log.php`；**自動簽核與「超級管理員補簽核」的紀錄要收，但畫面／列印／匯出絕不可出現那些字樣**——意見欄一律過 `eg_sign_note_public()` 遮蔽，只有管理員按鈕＋操作確認密碼才在畫面上暫時解除，列印永遠遮蔽） |

原始版 CLAUDE.md 備份：`CLAUDE.md.bak-20260706` 與 `CLAUDE(原始 不可更改).md`（勿動；該備份檔名未帶時分屬歷史命名，新備份一律用 `.bak-yyyyMMdd-HHmm`）。
