# 日期欄位西元年 4 碼修正 — 派工說明
> 寫於 2026-07-16。規則本文見 `ai-rules/08-UI互動規範.md` 第二節。
> 用途：交給 AI session 執行「找出全站尚未修正的日期欄位並修正」。

## 問題與根因
- 【症狀】`<input type="date">` 鍵入年份時打滿 4 碼不會自動跳到月份，可連打出「202607」這種 6 碼年。
- 【根因】HTML date input 未設 `max` 時，瀏覽器允許年份最long到 6 碼（上限 275760 年），故 4 碼不觸發跳欄。
- 【解法】每個 `<input type="date">` 一律加 `max="9999-12-31"`（若該欄位有實際業務上限日期，用該上限取代）。加了之後年份滿 4 碼即自動跳月份。

## 已完成的全站防護（動手前先了解，不要重工）
`views/partPage/sideAndTopBarMenu.html` **檔尾**已加共用 script（2026-07-16，commit 5db56f9）：
- 頁面載入時對所有 `input[type=date]:not([max])` 自動補 `max="9999-12-31"`；
- 對動態產生的日期欄位（modal、AJAX 渲染）在 `focusin` 當下補。

**因此：凡有 `include '../partPage/sideAndTopBarMenu.html'`（或等效路徑）的頁面已自動修正，不需要改。**

## 派工任務（要做的事）
目標：找出「不經過 sideAndTopBarMenu 共用修正」的日期欄位，逐一補上 `max`。

### 步驟
1. 用 Grep 找出所有含日期輸入的檔案：
   - 樣式一：`type="date"`（含單引號變體 `type='date'`）
   - 樣式二：JS 動態建立：`type=date`、`attr('type','date')`、`<input type=\"date\"` 出現在 .js 或 <script> 內
2. 對每個命中檔案判斷是否已被共用防護涵蓋：
   - 檔內（或該頁的主檔）有 `sideAndTopBarMenu` include → **已涵蓋，跳過**。
   - 沒有 include 的（登入頁、列印頁、獨立跳窗頁、對外頁、iframe 內容頁）→ 要修。
3. 修正方式（擇一，優先 a）：
   a. 靜態 HTML：直接在該 `<input type="date">` 加 `max="9999-12-31"`。
   b. 大量動態產生、不便逐一改：在該頁尾加上與 sideAndTopBarMenu 相同的小段 script（從該檔檔尾複製，含 DOMContentLoaded 與 focusin 兩段）。
4. 已自帶 `max` 屬性的欄位不要動（可能是業務上限）。

### 禁止事項
- 不可改動 `sideAndTopBarMenu.html` 既有 script（已在用）。
- 不可把 `type="date"` 改成 text＋外掛 datepicker（規範要求原生月曆）。
- 巨檔（>2000 行，如 stock.php、master_data_management.php）遵守 CLAUDE.md 鐵律2：先 Grep 定位再 Edit，禁止整檔 Read/Write。

### 驗收標準
- 全站 Grep `type="date"` 的每一處：所在頁面「有 include 共用側邊欄」或「該 input 有 max 屬性」或「該頁有等效補丁 script」，三者必居其一。
- 每個改過的檔案 `php -l` 通過。
- 抽測 2~3 頁（含一個列印/獨立頁）：鍵入年份第 4 碼後游標自動跳月份。

### 回報格式
列出：掃到的檔案總數／已被共用防護涵蓋數／實際修正檔案清單（每檔一行：路徑＋改了幾處）／跳過原因清單。
完成後依 CLAUDE.md 鐵律6 收尾（php -l、逐檔 commit+push、寫 page_change_log）。
