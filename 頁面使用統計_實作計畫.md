# 頁面使用統計 — 實作計畫（做完請刪除本檔）

> 目的：記錄每個頁面實際被誰、何時開啟，累積 2–3 個月後即可用數據判斷「哪些頁面沒人用、該下架或改良」，不再靠印象。
> 本檔為一次性派工計畫。**實作完成並驗證後，請 `git rm` 刪除本檔並 commit，避免無用 MD 累積**（使用者明確要求）。

## 背景（實作前先讀）

- 先讀 `CLAUDE.md` 鐵律與收尾四件事（php -l、逐檔 commit+push、page_change_log、UI 規範）。
- 幾乎所有頁面都會 `include '../partPage/sideAndTopBarMenu.html'`（PHP include 會執行 .html 內的 PHP 代碼）。這是全站唯一共同進入點，記錄邏輯掛在這裡，AJAX 請求（`?action=` 之類不渲染側欄）自然不會被記錄——這正是想要的行為（只記「頁面開啟」）。
- 動工前先 Grep 確認：`sideAndTopBarMenu.html` 內目前是否已有 PHP 片段、以及是否所有主要頁面都走這個 include（抽查 stock.php、quotation_list_NEW.php、inspection_result_entry.php 等 5–6 頁）。若有頁面不走此 include（例如 src/store/_clean*.php 系列），把清單列出來回報即可，第一期不強求涵蓋。

## 第一期：記錄（必做）

### 1. 資料表（用 ai-rules/tools/sql.php 建立，一次一句）

```sql
CREATE TABLE IF NOT EXISTS page_visit_stats (
    page_path   VARCHAR(191) NOT NULL COMMENT '頁面路徑，取 $_SERVER[SCRIPT_NAME] 去掉 /EGsystem 前綴',
    visit_date  DATE NOT NULL,
    user_id     INT NOT NULL DEFAULT 0 COMMENT '對應 session id；未知填 0',
    visit_count INT NOT NULL DEFAULT 1,
    last_visit_at DATETIME NULL,
    PRIMARY KEY (page_path, visit_date, user_id),
    KEY idx_date (visit_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='頁面使用統計（依日彙總，非逐筆log，量極小不需清理）';
```

設計說明：直接以「頁 × 日 × 人」彙總，不存逐筆 log，一年了不起幾萬列，永遠不用清理；又保留「誰在用」的層級，之後可回答「這頁只剩一個人在用」這種問題。

### 2. 記錄程式：新增 `src/common/page_visit_logger.php`

- 內容：一個函式 + 立即呼叫。用 `INSERT ... ON DUPLICATE KEY UPDATE visit_count=visit_count+1, last_visit_at=NOW()`。
- `page_path` 取 `$_SERVER['SCRIPT_NAME']`，去掉開頭 `/EGsystem`，統一斜線方向。
- `user_id` 取 `$_SESSION['id'] ?? 0`（記錄時 session 已由頁面啟動；若未登入就不記，直接 return）。
- **整段包 try/catch 完全靜默**：任何 DB 錯誤都不可影響頁面顯示（這是硬性要求，記錄功能壞了寧可不記）。
- 不可自己 new 第二條 DB 連線拖慢頁面——優先沿用既有連線；若在 include 時點拿不到連線物件，才自行建立（連線失敗也靜默）。
- 防重複膨脹：同一 request 只記一次（用 static 旗標）。

### 3. 掛載點：`views/partPage/sideAndTopBarMenu.html` 最上方

```php
<?php @include_once __DIR__ . '/../../src/common/page_visit_logger.php'; ?>
```

- 用 `@include_once` + logger 內部 try/catch 雙保險。
- 改這個檔案影響全站，改完務必抽開 3–4 個不同模組的頁面實測正常顯示，再 commit。

## 第二期：簡易報表（同一次做完即可，很小）

- 新增 `views/admin/page_visit_report.php`，**僅管理者（is_system=1 角色）可見**，不建獨立 RBAC 模組（比照「項目控制」等管理者群組頁面慣例）。
- 內容一張表：每個 page_path 的「近30天次數、近90天次數、使用人數、最後使用時間」，預設依近90天次數**由小到大**排（沒人用的排最前面，一眼看到該砍誰）。
- 對照 `system_module_pages`：LEFT JOIN 標出「掛在選單上但 90 天零使用」的頁面（這是本功能的核心產出）。
- 遵守 UI 規範：>10 筆分頁（5/10/20/50、右上）、CSV 匯出；這頁資料量小，PDF 用列印視窗即可。
- 選單：INSERT 到 `system_module_pages`（group_id=16 管理者，page_name='頁面使用統計'）。

## 驗收清單

1. 開任意 3 頁 → `SELECT * FROM page_visit_stats` 有對應列，重整同頁 visit_count +1。
2. 故意把 logger 內 SQL 改錯再開頁面 → 頁面完全正常顯示（改回來）。
3. 未登入狀態開登入頁 → 不產生紀錄。
4. 報表頁：非管理者開啟被擋；管理者可見、排序正確、CSV 可下載。
5. 收尾四件事逐檔做完（php -l / commit+push / page_change_log / 選單）。
6. **刪除本 MD**：`git rm 頁面使用統計_實作計畫.md` + commit + push。
