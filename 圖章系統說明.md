# 圖章（簽章印章）系統說明

> **AI 必讀**：凡要修改簽章印章顯示、或建立「掃描實體印章」功能頁面，先完整閱讀本文件再動工。
> **現行實作位置（2026-07-13 起已抽成共用檔）**：`resource/js/eg_stamp.js`（`window.EGStamp.stamp(name,date,isDeputy)` / `.row(stampHtml,leftHtml)`，前端 SVG 產生，CSS 自動注入）。原本內嵌於 `correction_order.php` 的 `carStamp()/stampRow()` 已移除，改為引入此共用檔呼叫 `EGStamp.stamp/.row`；`esc()`/`fmtDT()` 因 correction_order.php 別處仍在用，未搬移。
> 建立日期：2026-07-08 ｜ 適用模組：異常矯正處理單 (CAR)、報價單管理主管簽核（2026-07-13 起，僅 `quotation_list_test.php` 測試版）。
> 使用方式：`<script src=".../resource/js/eg_stamp.js"></script>` 引入後，設定 `window.__ownCompany`（本公司全名字串）即可呼叫 `EGStamp.stamp()`。

## 一、現行做法（SVG 動態產生）

三段式回墨印風格，單一 `<svg viewBox="0 0 100 100">`，紅色 `#cf3a2b`：

| 區塊 | 內容 | 比例（總高 2.3cm） | 字體 |
|---|---|---|---|
| 上段 | 本公司全名，分兩列：上 4 字、下最多 6 字，超過以 `textLength` 自動縮字 | 0.6cm（y 3~27.5） | 標楷體 DFKai-SB |
| 中段 | 簽章日期 `2026.07.08` | 1.0cm（y 27.5~68.5） | Times New Roman（7 有下勾的襯線數字），字級 14.5 |
| 下段 | 人員中文名（2-3 字 19 級、4 字以上 15 級自動縮） | 0.7cm（y 68.5~97），貼近上分隔線 | 標楷體 |

要點：
- **代理簽章**：代理人代簽時，`carStamp(name, date, isDeputy=true)` 會在印章**右下角加「代」字**（同色紅、標楷體）。
  - **鐵則：責任單位回覆人的三段簽章（cause/correction/prevention）禁止代理**——`sign_section` 只認 `assigned_to` 本人，不得為代理人開口。
  - 代理僅適用於主管（primary）／總經理（final）層級簽核；實作代理簽核時參考 QA 的 `user_delegate` 表與 `eg_qa_active_deputies()`（src/common/qa_notify.php），並於 `car_signature` 記錄實際簽章者，顯示時帶 isDeputy。
  - **鐵則：扣款資訊（deduct_* 欄位、不可結案原因）為機密，最高決策者的「代理人」也不可見**——實作代理簽核時，`$canSeeDeduct` 判定（store_CAR_API.php get_detail/export_csv/get_stats_report）只認總經理職位本人（car_is_final_decider）與扣款判定人員，**不得**把代理人納入可視名單。扣款判定亦由總經理於「結案」時直接填入（未填=不扣款），代理人代簽結案時扣款欄位必須留空交由本人補判或由管理課指定人員處理。
- 公司全名來源：`customer_list` 中 `is_own_company=1` 之 `customer_full`（後端 `car_own_company_full()`，隨 get_detail 回傳 `own_company`）。
- 兩條分隔線端點以**弦長**計算貼齊外圓內緣（`chord(y)`），整顆印章一體、任意縮放比例固定。
- 簽章資料存 `car_signature`（signed_name 已處理「廠商責任→壓廠商名」邏輯，見 `car_reply_signer_name()`）。
- 顯示位置：各欄位右下角，「簽章：」字樣緊貼印章左側（`stampRow()`）。

## 二、未來功能：掃描實體印章上傳（已與使用者討論、尚未實作）

需求：使用者將實際蓋章掃描上傳，系統自動去背後作為簽章圖，**僅中間日期帶由程式動態壓上**。

### 設計方案（經 2026-07-08 討論定案的方向）
1. **上傳**：後台設定頁（建議放 CAR 頁「設定」下拉或人員設定），每人（或每課）上傳一張掃描印章（jpg/png）。存放建議沿用附件根路徑下 `\_stamps\<user_id>.png`，或新表 `car_stamp_asset(user_id, file_path, band_top, band_bottom, created_at)`。
2. **自動去背（紅色抽取）**：PHP GD（專案已用於浮水印，見 `src/common/attachment_lib.php`）逐像素判斷：保留 `R 通道明顯高於 G、B`（如 R>150 且 R-G>40 且 R-B>40）的像素，其餘設為透明，輸出 PNG-24（imagesavealpha）。
3. **日期帶對位**：每顆實體印章的中間日期帶位置略異 → 上傳時提供**拖拉框選日期帶**的 UI，把框選的上下邊界（相對百分比）存入 DB（band_top/band_bottom）。
4. **組合顯示**：前端 SVG 改為 `<image href="去背PNG">` 當底圖；程式在 band 區域先鋪白色矩形遮罩（蓋掉掃描件上的舊日期），再疊動態日期文字（沿用現行 FONT_NUM 襯線數字）。人名與公司名都來自掃描件本身，程式不再繪製。
5. **回退機制**：使用者沒上傳掃描章時，沿用現行純 SVG 產生器（carStamp）。

### 注意事項
- 掃描件建議 300dpi、日期帶留空（或接受被白色遮罩蓋掉）。
- 去背品質差的（陰影、藍黑墨）給重新上傳提示；不要嘗試自動修復。
- 列印時 SVG/PNG 都可直接進列印流；PDF 匯出若走 TCPDF 需將 SVG 轉點陣（imagick）或改以 PNG 底圖為主。
- 預估工作量：0.5~1 天。

## 三、相關檔案
- `resource/js/eg_stamp.js`：共用圖章產生器（EGStamp.stamp/.row/FONT 定義），CAR 與報價單皆引用此檔
- `views/QA/correction_order.php`：CAR 頁面呼叫端（esc()/fmtDT() 留在本檔，圖章邏輯已搬至 eg_stamp.js）
- `src/common/car_lib.php`：car_own_company_full()、car_reply_signer_name()、car_sign_date_label()（格式 2026.07.08）
- `src/store/store_CAR_API.php`：sign_section / primary_sign / final_decide（簽章寫入點）
- 資料表：car_signature（section: desc/cause/correction/prevention/primary/final）
- **報價單簽核（2026-07-13起，僅test版）**：`views/Sales/quotation_list_test.php` buildPrintHtml() 呼叫端；`src/common/approval_lib.php`（共用簽核紀錄表 approval_record）、`src/common/quotation_approval.php`（報價單專屬業務規則/通知）；詳見記憶檔 [[quotation-approval]]
