# EGsystem 公告推播通知　設定說明

讓你在**關閉網頁、甚至關閉瀏覽器**後，電腦或手機仍能收到公司公告 / 通知。

---

## 📌 開始前必看（三個重點）

1. **一定要用這個網址**（有 `https`、有 `s`）：
   `https://192.168.2.128/EGsystem`
   （用 `http://`、或用 `localhost` 都收不到通知）

2. **要先登入系統**：通知會綁定你的登入帳號，系統才知道要推給誰。

3. **收通知 vs 看內容，網路需求不同**：
   - **收到通知橫幅**：只要手機有網路（**公司 Wi‑Fi 或 4G 都可以**），因為通知是從 Apple / Google 雲端送到你手機。
   - **點通知打開公告內容**：頁面在公司內網，需連**公司 Wi‑Fi 或 VPN** 才打得開（4G 看得到通知、但點進去要在公司網路）。

---

## 🔐 第一步（所有裝置共通）：安裝信任憑證

因為是公司內部網站，要先讓你的裝置「信任」它，之後才不會出現「不安全」警告、通知也才會生效。

**憑證下載網址**（用瀏覽器開）：
`http://192.168.2.128/EGsystem/egsystem-ca.crt`

> 檔名：`egsystem-ca.crt`。下載後依你的裝置照下面步驟安裝。

---

## 💻 一、電腦（Windows　Chrome / Edge）

### A. 安裝憑證
1. 下載 `egsystem-ca.crt`（上面的網址）。
2. **雙擊**該檔 → 按「**安裝憑證**」。
3. 存放位置選「**本機電腦**」→ 下一步。
4. 選「**將所有憑證放入以下的存放區**」→「瀏覽」→ 選「**受信任的根憑證授權單位**」→ 確定 → 完成。
5. **完全關閉瀏覽器再重開**。

### B. 開啟通知
1. 開 `https://192.168.2.128/EGsystem` 並**登入**（網址列應顯示鎖頭、無警告）。
2. 進「公告 / 通知」頁，點右上角「**🔔 開啟通知**」。
3. 瀏覽器跳出詢問 → 按「**允許**」。按鈕變「通知已開啟」即完成。

> 沒反應時：點網址列左邊鎖頭 →「網站設定」→ 把「通知」改成「允許」，重新整理再點一次。

---

## 📱 二、iPhone / iPad（限 iOS 16.4 以上，須用 Safari）

⚠️ iPhone 有兩個硬性規定：**必須用 Safari**、而且**必須「加入主畫面」當 App 開**，否則不會有通知功能。

### A. 安裝並「信任」憑證（這步 iPhone 最容易漏）
1. 用 **Safari** 開 `http://192.168.2.128/EGsystem/egsystem-ca.crt` → 允許下載描述檔。
2. 設定 →「**一般**」→「**VPN 與裝置管理**」→ 點「**EG System Internal Root CA**」→ 右上「**安裝**」。
3. 設定 →「一般」→「**關於本機**」→ 拉到最下面「**憑證信任設定**」→ 把「**EG System Internal Root CA**」的開關**打開**。

### B. 登入並加入主畫面
4. 用 **Safari** 開 `https://192.168.2.128/EGsystem` 並**登入你的帳號**（確認網址列沒有憑證警告）。
5. 點下方「**分享**」圖示 → 「**加入主畫面**」→ 加入。

### C. 從主畫面 App 開啟通知
6. 從**主畫面的圖示**開啟本 App（一定要這樣開，Safari 分頁裡不會有通知功能）。App 會開到**手機公告頁**。
   - ※ 若 App 開起來跳到登入畫面：直接在 App 裡登入，登入後會**自動回到公告頁**。
7. 點右上角的 🔔「**開啟通知**」→ 系統詢問時按「**允許**」。
8. 之後有新公告，點通知就會直接開到那筆公告的內容。

> 💡 **請保留主畫面的 App 圖示**，刪掉＝取消通知。
> 💡 開啟通知的按鈕在手機公告頁**右上角的 🔔**（不是公告管理頁的按鈕）。

---

## 🤖 三、Android 手機（Chrome）

### A. 安裝憑證
1. 用 Chrome 開 `http://192.168.2.128/EGsystem/egsystem-ca.crt` 下載。
2. 設定 → 搜尋「**憑證**」→「**安裝憑證 / 從儲存空間安裝**」→ 選「**CA 憑證**」→ 選剛下載的 `egsystem-ca.crt`。
   （各廠牌路徑略有不同，多在「設定 → 安全性 → 更多安全性設定 → 加密與憑證 → 安裝憑證」）

### B. 開啟通知
3. 用 Chrome 開 `https://192.168.2.128/EGsystem` 並**登入**（應顯示鎖頭、無警告）。
4. （建議）右上「⋮」→「**加到主畫面**」，日後從主畫面開更穩定。
5. 進「公告 / 通知」頁，點右上「**🔔 開啟通知**」→「允許」。

---

## ✅ 測試是否成功

設定完成後，請系統管理員**發布一則測試公告**（對象包含你），確認你的裝置有跳出通知。
收到＝設定成功；收不到請看下方排除。

---

## 🛠 常見問題排除

| 狀況 | 處理方式 |
|------|----------|
| 網址列顯示「不安全」 | 憑證沒裝好。電腦要裝到「**本機電腦 → 受信任的根憑證授權單位**」；iPhone 要記得開「**憑證信任設定**」開關 |
| iPhone 在綁定頁沒有「開始綁定」可按 / 按了沒反應 | ①iOS 需 16.4 以上 ②一定要從**主畫面 App 圖示**開 `bind.php`，不能在 Safari 分頁開 |
| iPhone App 一開就顯示登入畫面 | 正常，App 是獨立登入。直接在 App 裡登入，會自動回到綁定頁 |
| 找不到「憑證信任設定」 | 表示 iPhone 的描述檔還沒安裝成功，回到「設定 → 一般 → VPN 與裝置管理」重裝一次 |
| 按了允許卻沒收到 | 確認①用的是 `https://192.168.2.128`②已登入③手機的系統通知權限有開給瀏覽器 / 本 App |
| 出差用 4G 收到通知但點不開 | 正常。通知內容在公司內網，需連公司 Wi‑Fi 或 VPN 才打得開頁面 |
| 換手機 / 換帳號 | 在新裝置 / 新帳號重做一次「開啟通知」即可，系統會自動改綁 |

---

Android「未取得通知權限」排除步驟
① Android 系統層級：Chrome 的通知是否開啟（最常見，Android 13 以上很容易卡這）

設定 → 應用程式 → Chrome → 通知 → 打開（允許通知）。
若這裡是關的，網頁按「允許」也沒用、甚至不會跳詢問。
② 這個網站的通知之前被「封鎖」或被你關掉跳窗（被封鎖後 Chrome 不會再問）

在 Chrome 開著該頁時，點網址列的鎖頭圖示（或右上 ⋮ →「網站設定」）→ 權限 / 通知 → 改成「允許」。
然後重新整理頁面，再按一次「開始綁定手機通知」。
③ 按綁定時，跳出詢問要選「允許」

別點旁邊空白或「封鎖」。若跳窗一閃而過或沒出現，多半是第①點沒開。
④ Chrome 全域通知設定

網址列輸入 chrome://settings/content/notifications → 確認不是「網站無法傳送通知」，且該網站不在「封鎖」清單。
改好的提示現在會告訴你到底是「被封鎖」還是「沒按允許」。請你重開該頁、重按綁定，看黑色日誌框第 2 步的訊息：

顯示「通知已被封鎖」→ 做上面第 ②（再加第①）。
顯示「尚未允許通知 / 沒跳出詢問」→ 做上面第 ①（Chrome 系統通知開啟）後重試。
> 隱私說明：通知內容經**端對端加密**傳送，Apple / Google 只負責轉送、看不到內容。
