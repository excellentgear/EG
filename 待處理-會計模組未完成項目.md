# 待處理：會計模組未完成項目（交接給下一位 AI）

> 建立日期：2026-07-30（2026-07-30 更新：原本兩件，現剩下一件）
> 建立者：Claude（會計模組主要開發者）
> **本檔用途**：把會計模組剩下的一件未完成工作交接出去。
> **⚠ 全部項目完成並驗證通過後，請刪除本檔**（`git rm 待處理-會計模組未完成項目.md` 後 commit＋`git pushall`）。
> 若只完成一部分，請把完成的那節整節刪掉、保留未完成的，不要留「已完成」字樣在檔內。

---

## 0. 動工前必讀（不讀會踩坑，這些都是實際踩過的）

1. **先讀 `CLAUDE.md`**，特別是鐵律 2（巨檔保護）、3（不猜要問）、4（不破壞既有功能）、6（收尾四件事）。
2. **收尾一定要跑這兩支健檢**，兩支都必須「全部通過」才算完成：
   ```
   & C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\check_sidebar.php
   & C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\check_input_rules.php
   ```
3. **新頁面必須載入輸入規則共用檔**（雙擊清空、Enter 跳欄等），放在 `custom.min.js` 之後：
   ```html
   <script src="../../resource/js/eg_input_rules.js?v=<?= @filemtime(__DIR__.'/../../resource/js/eg_input_rules.js') ?: time() ?>"></script>
   ```
   不要自己手刻這些行為——共用檔走 document 事件委派，AJAX 動態產生的欄位也涵蓋。
4. **側欄陷阱**：CSS 若抄了 `#sidebar-menu{visibility:hidden;}`，就必須連 `$(document).ready` 裡的
   `$('#sidebar-menu').css('visibility','visible')` 一起抄，否則左側欄整片消失。照抄 `views/ACC/reconcile.php` 底部。
5. **測試紀律**：這是**正式資料庫**。測試若寫入資料，必須先快照原值、測完依 `lastInsertId` 或主鍵精準還原，
   並在最後印出「殘留 0 筆」證明乾淨。禁止按值刪除（`DELETE ... WHERE Note='測試'` 這種）。
6. **配色**：一律暖色系，禁冷暖混雜。應收＝琥珀橘 `#F0A24B`，應付＝暖棕赭 `#A2703A`／`#8A5A2B`，
   已對到＝暖淺綠 `#E4EDD4`。顏色不可是唯一資訊，要搭配文字標籤。

---

## 1. 現況：已經完成的部分（請勿重做，直接沿用）

### 1.1 檔案

| 檔案 | 行數 | 內容 |
|---|---|---|
| `src/common/acc_lib.php` | 3081 | 會計模組全部共用邏輯（62 個 `acc_*` 函式） |
| `src/store/Acc_API.php` | 1136 | 唯一 API 端點（51 個 action），CSRF＋RBAC fail-closed |
| `views/ACC/customer_invoice_data.php` | 703 | 客戶發票資料維護（統編檢查碼、CSV 匯入精靈） |
| `views/ACC/ar_statement.php` | 571 | 應收對帳單（客戶×帳款月份） |
| `views/ACC/invoice_export.php` | 971 | 發票開立與轉出、回填發票號、折讓單 |
| `views/ACC/receipt.php` | 624 | 收款與沖帳 |
| `views/ACC/ap_statement.php` | 537 | 應付對帳單（廠商加工費） |
| `views/ACC/reconcile.php` | 968 | 對帳作業（勾選/拖移/加總/拆分/計算機/暫存/鎖帳），可用 `?side=ap&party_id=X&bm=YYYY-MM` 直接開某一份 |
| `views/ACC/recon_overview.php` | 505 | 對帳單總覽（誰對完了／誰卡著／哪幾份有差額，應收應付同頁） |
| `views/ACC/_acc_tools.php` | 418 | 共用元件：單據快搜＋帳款月份調整（由 ar/ap_statement include） |

以上 8 頁都已登記進選單（`system_module_pages`，group_id=1 測試功能）。

### 1.2 資料表（都已建立，由 `acc_ensure_schema()` 在頁面載入時確保存在）

| 表 | 用途 |
|---|---|
| `acc_invoice` | 發票／折讓單主檔（`doc_type` 區分 INVOICE/ALLOWANCE） |
| `acc_invoice_item` | 發票明細，`UNIQUE(src_type, src_id)` 防同一張出貨開兩次發票 |
| `acc_receipt` / `acc_receipt_alloc` | 收款單／沖帳明細（支援部分沖帳、一筆對多張） |
| `acc_recon` | 對帳狀態（pending/ok/issue），獨立表不動來源表 |
| `acc_recon_sheet` | 對帳工作底稿，`UNIQUE(side, party_id, billing_month)` |
| `acc_recon_line` | 底稿明細（排序/勾選/加總組 group_no/拆分 split_parent/調整值 adj_*） |

**稽核不另建表**，沿用全站既有的 `audit_log`，`action_type` 用 `ACC_EDIT`／`ACC_MONTH`／`ACC_RECON`，
`changes` 存 JSON `{changes:{欄位:{old,new}}, reason:"..."}`。查詢用 `acc_audit_search()`。

### 1.3 RBAC（module = `accounting`，已建立於 `roles` 表）

| role_code | 名稱 | 權限 |
|---|---|---|
| `acc_view` | 會計檢閱 | 只能查詢與匯出 |
| `acc_edit` | 會計登錄 | 檢閱＋開票／沖帳／匯入／兩側都可對帳 |
| `acc_admin` | 會計管理員 | 登錄＋作廢發票、刪收款單、**退回已鎖帳的對帳單** |
| `acc_ar_recon` | 應收對帳(業務) | **只能對應收**，不可開票沖帳 |
| `acc_ap_recon` | 應付對帳(生管) | **只能對應付**，不可付款 |

`acc_perms()` 回傳的旗標：`isAdmin / canAdmin / canEdit / canView / reconAr / reconAp / canReconAr / canReconAp`。
**側別隔離已實作**：`acc_recon_guard($perms, $srcType)`，`TLOG`→驗 canReconAp、`IS`/`IR`→驗 canReconAr。
批次操作要**逐筆驗權**（避免夾帶另一側的單據繞過），`recon_set_bulk` 已這樣做，可參照。

### 1.4 重要口徑決定（改動前請先理解，不要無意間推翻）

- **帳款月份**：客戶／廠商 `settlement_mode`=FIXED 用其 `settlement_day`、EOM 為月底（不跨月）、
  VARIABLE 或未設定退回全域 `system_settings.billing_cutoff_day`（=25）。
  `is_list`/`ir_track` 的 `billing_month_override` 一律優先。函式：`acc_cutoff_for()` / `acc_billing_month()`。
- **應收不可用 `is_sale_type.is_count` 篩選**：那是「本業業績統計」旗標，機台買賣／刀具／砂輪／非本業
  `is_count=0` 但都是真的要開發票收錢的。應收計入所有金額不為 0 的出貨列。
- **應付月份**優先用資料本身的 `bom_ing_transfer_log.invoice_ym`（廠商發票年月），
  沒填才退回用 `transfer_date` 所在月份。
- **加總／拆分只存底稿，永不寫回來源憑證**。`orig_*` 永久保留原值供稽核比對。
- **有子列的拆分父列不計入合計**（金額由子列代表），前端不需也不該把父列歸零。
  拆分守恆：子列合計必須等於父列有效金額，否則 `acc_sheet_save()` 會擋下。
- **兩層鎖帳強度不同**（使用者已拍板）：
  - **對帳鎖**（`acc_recon_sheet.status='confirmed'`）→ 僅會計管理員可 `acc_sheet_reopen()` 退回，須填原因。
  - **發票鎖**（憑證已在未作廢的 `acc_invoice` 上）→ **不提供解鎖**，只能作廢／折讓／補開。
    理由：帳面必須與國稅局申報一致。**不要加後門**。

### 1.5 可直接沿用的 API action（共 52 個，以下列與本次工作相關的）

```
meta                 進頁初始化（回 perms + csrf）
sheet_list           對帳底稿清單（支援 side/status/bm 區間/kw/only_diff/only_open/sort/dir/分頁；
                     排序與統計對全部符合條件的資料算完才分頁；per_page 未給＝不分頁）
sheet_list_export    對帳單總覽 CSV（總覽層級，一列一份底稿）
sheet_load           載入底稿（有暫存回暫存，否則從憑證即時組出）
sheet_save           暫存底稿
sheet_confirm        確認鎖帳
sheet_reopen         退回重對（僅 canAdmin）
sheet_export         匯出單份底稿 CSV（含原始值與調整值對照）
recon_parties        某月份有帳的客戶／廠商清單
audit_search         稽核紀錄查詢
audit_export         稽核紀錄匯出
ar_summary/ar_detail 應收彙總／明細
ap_summary/ap_detail 應付彙總／明細
```

---

## 2. 【項目 B】應付帳款接入「材料／其他採購」

### 現況與為什麼還沒做
應付目前**只涵蓋廠商加工費**（來源 `bom_ing_transfer_log`，43,616/43,622 筆已帶
`invoice_date`/`invoice_ym`/`tax_amount`，資料完整度 99.99%）。

材料與其他採購的應付還沒接，原因有二，**動工前請先確認這兩點是否已解除**：
1. `purchase_request` 表當時**只有 0 筆資料**，接了也無法驗證。
2. 該模組當時正由另一位 AI 開發中，直接接會衝突。

先查一下：
```
& C:\MAMP\bin\php\php8.3.1\php.exe C:\MAMP\htdocs\EGsystem\ai-rules\tools\sql.php "SELECT COUNT(*) FROM purchase_request"
```
若仍是 0 筆或很少，**建議先不要做**，回報使用者等有實際單據再說。

**2026-07-30 覆查結果**：仍只有 **1 筆**（`PR-20260730-001` 常備品補貨-原子筆，
status=submitted、無廠商、金額 0、pay_status=unpaid），且採購模組當日仍在被另一位 AI 修改
（`views/pages/purchase_request.php`、`src/store/Purchase_API.php` 有未提交的變更）。
**兩個阻礙都還在，所以這次沒做**，等現場開始跑真實採購單、且採購模組穩定後再接。

### 使用者已拍板的設計方向（混合分層，不要改成別的）
> 不合併頁面，但合併資料主幹。`purchase_request` 是**唯一的採購憑證**，會計端不另建採購應付單。
> 會計只加一層「付款沖帳」，因為 `purchase_request.pay_status`／`pay_date` 是單頭單一欄位，
> 做不到「月結廠商一次匯款付掉五張採購單」或「先付一半」。

### 要做的事
1. **`acc_ap_summary()` 加來源切換**：現在只讀 `bom_ing_transfer_log`，
   要能同時彙總 `purchase_request`（已有 `vendor_id`/`vendor_name`/`tax_type`/`subtotal`/
   `tax_amount`/`grand_total`/`invoice_no`/`invoice_date`/`pay_status`/`pay_date`/`pay_method`）。
   建議在回傳的每一列加 `src`（`TLOG`／`PURC`）欄位，畫面上加來源篩選。
2. **`views/ACC/ap_statement.php` 加來源切換 UI**（加工費／採購／全部）。
3. **付款沖帳層**：比照應收的 `acc_receipt` + `acc_receipt_alloc`，新增
   `acc_payment` + `acc_payment_alloc`（一筆付款可沖多張應付單、支援部分付款）。
   `acc_ensure_schema()` 內加建表即可，寫法照抄同檔的 `acc_receipt` 段落。
4. **`acc_recon_line.src_type` 要能接受 `PURC`**：目前 `acc_src_table()` 與
   `acc_editable_fields()` 只支援 `IS`/`IR`/`TLOG`，要加 `PURC` 才能對採購單對帳。
5. **協調點**：採購頁的 `pay_status` 顯示要改成讀會計的沖帳結果（或由會計寫入時回寫），
   避免兩邊各判各的。**這點動工前要先跟使用者確認採用哪一種**。

### 用途歸屬欄位（採購模組那邊；2026-07-30 覆查：DB 欄位已由採購模組建好）
使用者要求 `purchase_request` 的「申請原因」改成**用途歸屬**，方便統計成本。
覆查 `SHOW COLUMNS FROM purchase_request LIKE 'purpose%'`，實際已存在這些欄位
（型別與當初規格略有出入，會計端要用時以**實際欄位**為準）：

| 欄位 | 實際型別 | 說明 |
|---|---|---|
| `purpose_type` | VARCHAR(10)（不是 ENUM，實測有 `STOCK` 值） | 用途類別 |
| `purpose_order_id` | INT NULL（有索引） | → `order_track.Order_id` |
| `purpose_bom` | VARCHAR(30) NULL（有索引） | → `bom.bom` |
| `purpose_d_id` | INT NULL（有索引） | → `d_setting.d_id`（料號主鍵） |
| `purpose_note` | VARCHAR(200) NULL | 其他原因自由文字 |
| `purpose_label` | VARCHAR(150) NULL（規格外新增） | 顯示用文字 |

**關鍵：綁訂單一定要存 `Order_id` 不能存訂單號字串。** `order_track` 有 8,641 列但只有 4,768 個
不同 `Order_oo`，一個訂單號最多對到 25 列（多料號訂單），存字串會歸錯成本。料號同理走
`d_setting.d_id`（料號字串有 159 個重複）。`reason` 保留當補充說明選填，不要刪。

會計端做成本歸屬統計時直接吃這些欄位即可，不要自己另建一套。
UI 那一半是否已完成請自行到採購頁確認，本檔不再重述。

---

## 3. 已知的資料缺口（不是 bug，是主檔缺資料，會影響驗收判斷）

動工時若看到以下狀況，**那是既有資料問題不是你改壞的**：

- **客戶統編只有 15/925 家有**，近一年有出貨的 175 家中只有 5 家有。
  發票轉出會因此被擋下（這是刻意的守門），要補請走 `customer_invoice_data.php` 的 CSV 匯入。
- **恆輝(CC005) 的統編 `1288257` 只有 7 碼**，補前導零或補尾碼都不合檢查碼，是真的填錯，待人工更正。
- **`is_list.Order_id` 42,071 筆只有 1 筆有值**（歷史資料 ERP 匯入未帶訂單號）。
  已做回填工具在 `views/Sales/Shipping_Quick.php`（管理員限定），但尚未大量套用。
- **應收客戶以簡稱歸戶**（`is_list.Client_id` 近期多為 NULL），約 17% 簡稱對不到 `customer_list.customer`，
  這些會顯示「未對應主檔」，金額仍計入但無法開發票。
- **`is_list.Unit_price` 與 `ir_track.Unit_price` 是 `int`**，應收單價不能有小數，
  `acc_edit_doc()` 會明白擋下並提示（不是靜默截斷）。應付 `process_amount` 是 `decimal(10,2)` 可以有小數。

---

## 4. 完成後要做的事

1. 每個改過的檔跑 `php -l`
2. 兩支健檢（`check_sidebar.php`、`check_input_rules.php`）全部通過
3. 新頁面登記進 `system_module_pages`（group_id=1、sort_order=MAX+1）
4. 寫 `page_change_log`（範本見 CLAUDE.md）
5. `git add` → `git commit` → **`git pushall`**（雙 remote，只 `git push` 會漏備份庫）
6. **刪除本檔**：
   ```
   git rm 待處理-會計模組未完成項目.md
   git commit -m "會計模組未完成項目已完成，移除交接文件"
   git pushall
   ```
   本檔現在只剩項目 B 一件；做完項目 B（或使用者裁示不做了）就整檔刪掉，不要在檔內留「已完成」字樣。
