# 表單簽核設計器(視覺化框選簽核流程)— 規格文件

> 本文件為交接規格,提供給負責實作的 AI 使用。文件末尾列出「待確認事項」,實作前請先向使用者詢問並補齊,不要自行假設。

---

## 1. 功能目標

在 egsystem 內新增一個「表單簽核設計器」功能:
管理員上傳表單原始檔(圖片或多頁 PDF),在畫面上框選出多個區塊並標記角色,設定簽核階段與順序,之後使用者可依此樣板送出實際案件,系統依序通知各關卡簽核人,簽核人以「圖章模板」蓋章回應,回覆內容顯示在對應框選區域,最終形成一份已簽核完成的合成文件。

---

## 2. 核心資料模型(概念層,實作時需比對現有 schema 命名慣例)

### 2.1 表單樣板(Form Template)
- 樣板基本資料:名稱、原始檔案(圖片或多頁 PDF)、狀態(啟用/停用)
- 連動 AS 表單名稱:樣板需可設定對應的 AS 表單名稱,作為列印標題使用(**列印規則請參照另一份 MD 檔,見待確認事項**)

### 2.2 框選區塊(Field Box)
- 所屬頁碼(多頁 PDF 需記錄框框在第幾頁)
- 座標與尺寸(x, y, width, height,以原始檔案像素或比例為基準)
- 類型:
  - 圖章框(綁定圖章模板,對應某個簽核階段)
  - 回覆內容框(顯示某階段/某人的文字回覆)
- 圖章框尺寸限制需比對圖章實際規格(**圖章大小規定請參照另一份 MD 檔,見待確認事項**),避免框選範圍小於圖章最小顯示尺寸

### 2.3 簽核階段(Stage)
每個表單樣板有一組有序的階段陣列,階段分兩種類型:

- **意見階段(諮詢型 / 並簽)**
  - 該階段可有多位簽核人,同時收到通知
  - 每人各自回應「同意 / 不同意」+ 可附文字回覆(對應回覆內容框)
  - 沒有「駁回」動作,不互相卡關
  - 判定「該階段完成」條件:全員已回應(或依設定的時限)

- **決策階段(決定型 / 線性)**
  - 該階段為 1~2 位最終決策者
  - 決策者可看到前面意見階段所有人的同意/不同意與回覆內容
  - 決策者的動作才是真正推動流程前進或終止的判斷

階段之間依序執行(前一階段完成才進下一階段),同一份表單樣板內意見階段與決策階段可混合出現多次。

### 2.4 案件(Case / Submission)
- 對應某個表單樣板 + 申請人 + 目前進度(第幾階段)
- 每個階段的每個簽核人回應記錄:回應內容(同意/不同意 或 決策結果)、蓋章的圖章模板 ID、蓋章時間、簽核人

### 2.5 圖章模板串接
- 直接使用既有「圖章管理」模組的圖章模板,不做手寫簽名輸入
- 蓋章動作記錄:誰、何時、用哪個圖章模板(後台需可查詢完整記錄)

---

## 3. 頁面需求

### 3.1 表單樣板設計頁(管理員用)
- 上傳表單檔案(圖片 或 多頁 PDF)
- 多頁 PDF 顯示方式:**預設雙頁並列顯示,並提供上下捲動**(非單頁切換)
- 框選介面:**採類似修圖軟體圖層標籤的做法**——畫面側邊列出所有「需要框選的範圍」標籤清單(依已設定的階段與框框類型自動產生),使用者將標籤拖曳到頁面對應位置完成框選,而非在頁面上憑空畫框後才回頭指定類型
- 框選圖章框時系統需檢查框選尺寸是否符合圖章最小顯示規格(**待確認,見第 5 節**)
- 設定連動的 AS 表單名稱(供列印標題使用)
- 設定階段:新增/排序意見階段與決策階段,指定各階段簽核人(角色或個人)

### 3.2 角色設定
- 本功能**不建立獨立的權限系統**,角色設定需串接至既有「權限管理」頁面
- 於權限管理頁面新增人員時,需可對應設定其在本功能中的角色(例如:某表單樣板的意見階段成員、決策者等)

### 3.3 頁面權限顯示
- 使用者進入相關頁面時,畫面需顯示:
  - 目前登入使用者的角色
  - 該角色對應的權限說明(可執行哪些操作)

### 3.4 案件簽核頁(簽核人用)
- 顯示表單原始檔案為背景,疊加圖章(已蓋章的階段)與回覆內容(對應框選位置)
- 意見階段:提供同意/不同意 + 文字回覆輸入
- 決策階段:顯示前面所有意見階段的回應彙總,決策者做出最終判斷

### 3.5 案件列表頁
- 列表需顯示每筆案件**目前的簽核狀態**(進度到第幾階段、該階段是否有人尚未回應)

---

## 4. 既有系統可參考/需串接的部分

- **圖章管理模組**:表單樣板的圖章框直接綁定此模組既有的圖章模板,不重新設計簽名機制
- **權限管理頁面**:角色設定需在此頁面內擴充,而非另建系統
- **既有簽核流程頁面**:公司內已有另一個具備簽核流程的頁面,實作前應先參考其資料表結構、狀態機設計、通知機制,盡量沿用一致的命名與架構慣例(**待確認,見第 5 節**)

---

## 5. 待確認事項(實作前請先向使用者詢問,不要自行假設)

1. **既有簽核流程頁面的參考資料**:請使用者提供該頁面的程式碼、資料表結構,或至少功能說明/畫面截圖,以便沿用一致的架構慣例。
2. **AS 表單文件之列印規則 MD 檔**:請使用者提供,內容應說明列印標題如何與 AS 表單名稱連動,以及其他列印相關規則。
3. **圖章大小規定 MD 檔**:請使用者提供,內容應包含圖章實際顯示尺寸規格,作為框選圖章框時的最小尺寸限制依據。
4. 意見階段的「時限」機制是否需要(逾期未回應如何處理:視為棄權?自動略過?),目前規格未定義。
5. 通知機制(是否沿用既有的 4G Push / Telegram 整合)未在本次對話中確認,建議依既有簽核流程頁面的作法沿用。

